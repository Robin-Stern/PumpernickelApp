---
phase: quick-260519-jo4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - androidApp/src/androidMain/kotlin/com/pumpernickel/android/ui/screens/SettingsSheet.kt
autonomous: true
requirements:
  - QUICK-260519-jo4-01
must_haves:
  truths:
    - "Opening the Settings bottom sheet on Android no longer crashes"
    - "Accent color picker still shows all 8 presets in a 4x2 grid"
    - "Tapping an accent preset still selects it (border + shadow + check icon visible)"
    - "Visual spacing remains identical (48.dp circles, 16.dp horizontal + vertical spacing)"
  artifacts:
    - path: "androidApp/src/androidMain/kotlin/com/pumpernickel/android/ui/screens/SettingsSheet.kt"
      provides: "SettingsSheet composable without LazyVerticalGrid"
      contains: "accentPresets.chunked(4)"
  key_links:
    - from: "SettingsSheet.kt accent color section"
      to: "accentPresets list (com.pumpernickel.android.ui.theme.accentPresets)"
      via: "accentPresets.chunked(4).forEach { row -> Row(...) }"
      pattern: "accentPresets\\.chunked\\(4\\)"
---

<objective>
Fix the `IllegalStateException: Vertically scrollable component was measured with an infinity maximum height constraints` crash that occurs whenever the Settings bottom sheet is opened on Android.

Root cause: `LazyVerticalGrid` (line 129) is nested inside a `Column` that uses `.verticalScroll(rememberScrollState())` (line 86). Compose forbids lazy layouts inside unbounded scrollable parents because the lazy container receives infinite height constraints.

Fix: Replace the `LazyVerticalGrid` with a plain `Column` of `Row`s built from `accentPresets.chunked(4)`. The list has exactly 8 items, so two rows of 4 reproduce the existing 4-column grid 1:1 — no behavior change, no visual change, no lazy layout needed.

Purpose: Unblock the Settings sheet on Android, which is currently 100% broken on open.
Output: Patched `SettingsSheet.kt` with the lazy grid replaced by chunked rows and unused lazy-grid imports removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

# Source file with the bug
@androidApp/src/androidMain/kotlin/com/pumpernickel/android/ui/screens/SettingsSheet.kt

<interfaces>
<!-- accentPresets is the only external symbol the accent section depends on. -->
<!-- From androidApp/src/androidMain/kotlin/com/pumpernickel/android/ui/theme/ (com.pumpernickel.android.ui.theme.accentPresets) -->

Each preset element exposes the following fields used in the accent picker:
  - preset.key:   String  (compared against accentColorKey for selection)
  - preset.color: Color   (background + shadow color)
  - preset.name:  String  (content description for the check icon)

`accentPresets` always has exactly 8 entries — `chunked(4)` produces exactly 2 rows of 4.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace LazyVerticalGrid with chunked Rows in SettingsSheet.kt</name>
  <files>androidApp/src/androidMain/kotlin/com/pumpernickel/android/ui/screens/SettingsSheet.kt</files>
  <action>
In `androidApp/src/androidMain/kotlin/com/pumpernickel/android/ui/screens/SettingsSheet.kt`:

1. Replace the `LazyVerticalGrid(...) { items(accentPresets) { preset -> ... } }` block (lines 129–161) with a non-lazy equivalent:

```kotlin
Column(
    modifier = Modifier.fillMaxWidth(),
    verticalArrangement = Arrangement.spacedBy(16.dp)
) {
    accentPresets.chunked(4).forEach { row ->
        Row(
            modifier = Modifier.fillMaxWidth(),
            horizontalArrangement = Arrangement.spacedBy(16.dp)
        ) {
            row.forEach { preset ->
                val isSelected = accentColorKey == preset.key
                Box(
                    contentAlignment = Alignment.Center,
                    modifier = Modifier
                        .size(48.dp)
                        .clip(CircleShape)
                        .background(preset.color, CircleShape)
                        .then(
                            if (isSelected) Modifier
                                .border(3.dp, Color.White, CircleShape)
                                .shadow(6.dp, CircleShape, ambientColor = preset.color, spotColor = preset.color)
                            else Modifier
                        )
                        .clickable { viewModel.setAccentColor(preset.key) }
                ) {
                    if (isSelected) {
                        Icon(
                            imageVector = Icons.Filled.Check,
                            contentDescription = preset.name,
                            tint = Color.White,
                            modifier = Modifier.size(20.dp)
                        )
                    }
                }
            }
        }
    }
}
```

Notes:
- The inner `Box(...)` block (selection, click, check icon) is byte-for-byte identical to the existing `items(accentPresets) { preset -> ... }` body. Copy it verbatim — do NOT change the selected border, shadow, or check icon logic.
- `Arrangement.spacedBy(16.dp)` on both the outer `Column` (vertical) and inner `Row` (horizontal) preserves the lazy grid's 16.dp gap exactly.
- Do NOT add `.weight(1f)` or any fill modifier to the inner `Box` — it must stay 48.dp fixed-size, matching the original.
- Do NOT pad rows to 4 cells if the last chunk is short; `accentPresets` always has exactly 8 entries so both rows are full. (If a future change makes the count not divisible by 4, the last row will be left-aligned, which is the standard non-lazy behavior — acceptable.)

2. Remove the three now-unused imports (lines 17–19):
   - `import androidx.compose.foundation.lazy.grid.GridCells`
   - `import androidx.compose.foundation.lazy.grid.LazyVerticalGrid`
   - `import androidx.compose.foundation.lazy.grid.items`

3. Do NOT remove any other imports. In particular, `androidx.compose.foundation.layout.Arrangement`, `Row`, `Column`, `Box` are still in use and must stay.

4. Do NOT touch any other section of the file (theme, weight unit, tutorial, gamification, training, debug, KI). Scope is the accent color picker only.
  </action>
  <verify>
    <automated>cd /Users/olli/Studium/semester_6/mobile_app_dev/PumpernickelApp &amp;&amp; ./gradlew :androidApp:assembleDebug 2&gt;&amp;1 | tail -40</automated>
    Additionally, grep-guard that the lazy grid is gone and the chunked replacement is present:
    - `grep -n "LazyVerticalGrid\|GridCells\|lazy.grid" androidApp/src/androidMain/kotlin/com/pumpernickel/android/ui/screens/SettingsSheet.kt` MUST return zero matches.
    - `grep -n "accentPresets.chunked(4)" androidApp/src/androidMain/kotlin/com/pumpernickel/android/ui/screens/SettingsSheet.kt` MUST return exactly one match.
  </verify>
  <done>
    - `:androidApp:assembleDebug` returns BUILD SUCCESSFUL.
    - No `LazyVerticalGrid`, `GridCells`, or `androidx.compose.foundation.lazy.grid.*` references remain in SettingsSheet.kt.
    - `accentPresets.chunked(4)` is used to build the accent picker.
    - All 8 presets render in a 4x2 layout with 16.dp horizontal + vertical spacing and 48.dp circles.
    - No other section of SettingsSheet.kt is modified.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
SettingsSheet accent color picker rebuilt without LazyVerticalGrid. The crash that occurred on every Settings sheet open should be gone. Visual layout should be identical to before the crash existed.
  </what-built>
  <how-to-verify>
1. Launch the Android app (debug build) on emulator or device.
2. Open the Settings bottom sheet (gear / settings entry point from the app).
3. Verify it opens WITHOUT crashing — no `IllegalStateException` in Logcat.
4. Scroll within the sheet — confirm vertical scrolling still works for the long settings list.
5. Find the "Akzentfarbe" / accent color section. Confirm:
   - All 8 color circles are visible.
   - They are arranged in 2 rows of 4 (4-column grid look).
   - Spacing between circles looks correct (16.dp gap, identical to before).
   - Tapping a circle selects it: white border + colored shadow + check icon appear.
   - Tapping a different circle moves the selection.
6. Close and re-open the sheet — selection persists, no crash.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any visual/behavioral regression.</resume-signal>
</task>

</tasks>

<verification>
- `./gradlew :androidApp:assembleDebug` succeeds.
- `grep` confirms no lazy-grid symbols remain in SettingsSheet.kt.
- Human verification confirms Settings sheet opens without crash and accent picker is visually unchanged.
</verification>

<success_criteria>
- Settings bottom sheet opens on Android without throwing `IllegalStateException`.
- Accent color picker preserves its prior look (4x2 grid, 48.dp circles, 16.dp gaps, selection visuals).
- All 8 accent presets remain selectable.
- No other Settings sections regress.
- No leftover dead imports.
</success_criteria>

<output>
After completion, create `.planning/quick/260519-jo4-fix-crash-lazyverticalgrid-inside-vertic/260519-jo4-SUMMARY.md`
</output>
