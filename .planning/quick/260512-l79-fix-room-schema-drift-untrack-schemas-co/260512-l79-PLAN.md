---
phase: 260512-l79-quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - shared/schemas/com.pumpernickel.data.db.AppDatabase/2.json
  - shared/schemas/com.pumpernickel.data.db.AppDatabase/3.json
  - shared/schemas/com.pumpernickel.data.db.AppDatabase/4.json
  - shared/schemas/com.pumpernickel.data.db.AppDatabase/5.json
  - shared/schemas/com.pumpernickel.data.db.AppDatabase/6.json
  - shared/schemas/com.pumpernickel.data.db.AppDatabase/7.json
  - shared/schemas/com.pumpernickel.data.db.AppDatabase/8.json
  - shared/schemas/com.pumpernickel.data.db.AppDatabase/9.json
  - shared/schemas/com.pumpernickel.data.db.AppDatabase/10.json
autonomous: true
requirements:
  - QUICK-260512-l79
must_haves:
  truths:
    - ".gitignore no longer ignores shared/schemas/"
    - "All 9 Room schema JSONs (v2-v10) are tracked by git"
    - "The fix lands as two atomic commits in correct order (gitignore fix BEFORE schema add)"
    - "Schema JSON file contents are unchanged (only their tracking status changes)"
    - "AppDatabase.kt and shared/build.gradle.kts are untouched"
  artifacts:
    - path: ".gitignore"
      provides: "Updated ignore rules without shared/schemas/ entry"
      contains: "no 'shared/schemas' lines"
    - path: "shared/schemas/com.pumpernickel.data.db.AppDatabase/"
      provides: "Tracked Room schema history for AutoMigration build input"
      contains: "2.json, 3.json, 4.json, 5.json, 6.json, 7.json, 8.json, 9.json, 10.json"
  key_links:
    - from: ".gitignore"
      to: "git add shared/schemas/"
      via: "ignore rule removal must precede git add (no -f flag)"
      pattern: "shared/schemas not present in .gitignore at time of add"
---

<objective>
Fix the Room schema drift config bug: `.gitignore` currently ignores `shared/schemas/`, but Room requires those JSONs as compile-time build inputs for AutoMigration generation (version=10 with AutoMigrations 6→7, 7→8, 8→9, 9→10 declared in `AppDatabase.kt`). The 9 schema JSONs (v2 through v10) exist locally but are untracked, meaning a fresh checkout or CI build would fail.

Purpose: Restore correct Room-KMP standard practice — schemas committed to VCS — so AutoMigration build works on any clone/CI/worktree.

Output: Two atomic commits — first removing the ignore rule, then adding the 9 schema JSONs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md
@.gitignore
@shared/build.gradle.kts
@shared/src/commonMain/kotlin/com/pumpernickel/data/db/AppDatabase.kt

<environment_constraint>
This plan MUST execute in the main working copy of the repo, NOT a worktree.
The schema JSONs are currently gitignored on `main`, so a fresh worktree checkout would not contain them. The local working copy at `/Users/olli/Studium/semester_6/mobile_app_dev/PumpernickelApp` has all 9 JSONs present on disk (verified). Do NOT switch branches or use a worktree.
</environment_constraint>

<current_state>
Verified before planning:
- `.gitignore` lines 26-27 contain:
  ```
  # Room schemas (generated)
  shared/schemas/
  ```
- `shared/schemas/com.pumpernickel.data.db.AppDatabase/` contains 9 files: `2.json` through `10.json`
- `git ls-files shared/schemas/` returns 0 entries (all untracked)
- `shared/build.gradle.kts:57-59` configures `room { schemaDirectory("$projectDir/schemas") }`
- `AppDatabase.kt:28-34` declares `version = 10` with AutoMigrations 6→7, 7→8, 8→9, 9→10
</current_state>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove shared/schemas from .gitignore</name>
  <files>.gitignore</files>
  <action>
Edit `.gitignore` to remove the two lines that ignore Room schemas:

```
# Room schemas (generated)
shared/schemas/
```

These are currently at lines 26-27. Also remove exactly ONE adjacent blank line to avoid leaving a double blank line gap in the file — prefer removing the blank line AFTER the deleted block (i.e. between the deleted block and the next `# Agent/CLI` comment), so the remaining structure stays clean and single-blank-line separated.

Do NOT touch any other `.gitignore` rules. Do NOT add new rules. Do NOT reorder anything else.

Rationale: The `(generated)` comment is misleading — Room writes these JSONs once per version bump, but they then become read-only build inputs for the NEXT build that has AutoMigrations. Standard Room-KMP practice is to commit them. This change must land BEFORE the `git add` in Task 2; otherwise the ignore rule would silently no-op the add (and we explicitly do NOT want to use `git add -f`).

After editing, stage and commit ONLY `.gitignore`:
- `git add .gitignore`
- `git commit -m "chore: stop ignoring shared/schemas — Room needs them for AutoMigration build"`
  </action>
  <verify>
    <automated>grep -c "shared/schemas" .gitignore | grep -q "^0$" &amp;&amp; git log -1 --pretty=%s | grep -q "stop ignoring shared/schemas"</automated>
  </verify>
  <done>
- `.gitignore` contains zero occurrences of `shared/schemas`
- `.gitignore` has no double-blank-line artifacts where the block was removed
- A single commit exists with message starting `chore: stop ignoring shared/schemas`
- No other files staged or committed in this step
  </done>
</task>

<task type="auto">
  <name>Task 2: Commit Room schema JSONs v2-v10</name>
  <files>
shared/schemas/com.pumpernickel.data.db.AppDatabase/2.json,
shared/schemas/com.pumpernickel.data.db.AppDatabase/3.json,
shared/schemas/com.pumpernickel.data.db.AppDatabase/4.json,
shared/schemas/com.pumpernickel.data.db.AppDatabase/5.json,
shared/schemas/com.pumpernickel.data.db.AppDatabase/6.json,
shared/schemas/com.pumpernickel.data.db.AppDatabase/7.json,
shared/schemas/com.pumpernickel.data.db.AppDatabase/8.json,
shared/schemas/com.pumpernickel.data.db.AppDatabase/9.json,
shared/schemas/com.pumpernickel.data.db.AppDatabase/10.json
  </files>
  <action>
Depends on Task 1 being committed (the gitignore rule MUST be gone first).

Pre-check (run, do not modify): confirm 9 JSONs exist on disk under `shared/schemas/com.pumpernickel.data.db.AppDatabase/` — `ls shared/schemas/com.pumpernickel.data.db.AppDatabase/*.json | wc -l` should output `9`. If not 9, STOP and report — do not proceed.

Then stage the entire schemas directory:
- `git add shared/schemas/`

This must be a plain `git add` (NOT `git add -f`). With Task 1's commit landed, the ignore rule is gone and the add will pick up all 9 untracked JSONs. If for any reason `git status --short shared/schemas/` shows fewer than 9 staged additions, STOP and report — do NOT fall back to `-f`.

Then commit:
- `git commit -m "chore(room): commit AppDatabase schemas v2-v10 for AutoMigration build"`

Do NOT modify the content of any JSON. Do NOT touch `AppDatabase.kt`, `shared/build.gradle.kts`, or any other file. This commit must contain ONLY the 9 schema JSONs.
  </action>
  <verify>
    <automated>test "$(git ls-files shared/schemas/com.pumpernickel.data.db.AppDatabase/ | wc -l | tr -d ' ')" = "9" &amp;&amp; test "$(ls shared/schemas/com.pumpernickel.data.db.AppDatabase/*.json | wc -l | tr -d ' ')" = "9" &amp;&amp; git log -1 --pretty=%s | grep -q "commit AppDatabase schemas v2-v10" &amp;&amp; git log -1 --name-only --pretty=format: | grep -v '^$' | grep -vc '^shared/schemas/' | grep -q '^0$'</automated>
  </verify>
  <done>
- `git ls-files shared/schemas/com.pumpernickel.data.db.AppDatabase/` lists exactly 9 entries (2.json through 10.json)
- Filesystem still has exactly 9 JSONs on disk (unchanged count)
- Latest commit message starts `chore(room): commit AppDatabase schemas v2-v10`
- Latest commit touches ONLY files under `shared/schemas/` (no other files leaked in)
- The previous commit (HEAD~1) is the Task 1 gitignore-fix commit (correct order preserved)
  </done>
</task>

</tasks>

<verification>
Final phase-wide checks (run all):

1. `grep -c "shared/schemas" .gitignore` → `0`
2. `git ls-files shared/schemas/com.pumpernickel.data.db.AppDatabase/ | wc -l` → `9`
3. `ls shared/schemas/com.pumpernickel.data.db.AppDatabase/*.json | wc -l` → `9` (filesystem unchanged)
4. `git log --oneline -2` shows exactly two new commits, in order:
   - HEAD:   `chore(room): commit AppDatabase schemas v2-v10 for AutoMigration build`
   - HEAD~1: `chore: stop ignoring shared/schemas — Room needs them for AutoMigration build`
5. `git diff HEAD~2 HEAD -- shared/src shared/build.gradle.kts` → empty (no leakage into source or build files)
6. `git status --short` → clean (no leftover staged/unstaged changes from this work)
</verification>

<success_criteria>
- Two atomic commits land in the correct order (gitignore fix BEFORE schema add).
- `shared/schemas/` no longer ignored.
- All 9 Room schema JSONs (v2-v10) tracked by git.
- No content changes to JSONs, `AppDatabase.kt`, or `shared/build.gradle.kts`.
- No use of `git add -f` anywhere.
- Working tree clean after both commits.
</success_criteria>

<output>
After completion, create `.planning/quick/260512-l79-fix-room-schema-drift-untrack-schemas-co/260512-l79-01-SUMMARY.md` documenting:
- Both commit SHAs
- Confirmation of all 6 verification checks above
- Note that Room AutoMigration build inputs are now version-controlled and a fresh clone / worktree will compile.
</output>
