---
phase: 260512-l79-quick
plan: 01
subsystem: build-config / room-persistence
tags: [room, kmp, gitignore, automigration, build-inputs]
requirements: [QUICK-260512-l79]
dependency_graph:
  requires: []
  provides:
    - "Tracked Room schema history (v2-v10) for AutoMigration build inputs"
    - ".gitignore no longer blocks shared/schemas/"
  affects: []
tech_stack:
  added: []
  patterns:
    - "Standard Room-KMP practice: schema JSONs committed to VCS as build inputs"
key_files:
  created:
    - shared/schemas/com.pumpernickel.data.db.AppDatabase/2.json
    - shared/schemas/com.pumpernickel.data.db.AppDatabase/3.json
    - shared/schemas/com.pumpernickel.data.db.AppDatabase/4.json
    - shared/schemas/com.pumpernickel.data.db.AppDatabase/5.json
    - shared/schemas/com.pumpernickel.data.db.AppDatabase/6.json
    - shared/schemas/com.pumpernickel.data.db.AppDatabase/7.json
    - shared/schemas/com.pumpernickel.data.db.AppDatabase/8.json
    - shared/schemas/com.pumpernickel.data.db.AppDatabase/9.json
    - shared/schemas/com.pumpernickel.data.db.AppDatabase/10.json
  modified:
    - .gitignore
decisions:
  - "Removed `shared/schemas/` from .gitignore (and its `# Room schemas (generated)` comment) rather than using `git add -f`. Room writes these JSONs once per version bump, then they become read-only build inputs for the next AutoMigration build — they belong in VCS."
  - "Removed the trailing blank line of the deleted block (not the leading one) so single-blank-line section separation is preserved between `# Xcode user state` and `# Agent/CLI`."
metrics:
  duration: "~1 minute"
  completed: "2026-05-12"
---

# Quick Plan 260512-l79: Fix Room Schema Drift (Untrack Schemas Commit) Summary

One-liner: Fixed the Room AutoMigration build-input drift by removing `shared/schemas/` from `.gitignore` and committing the 9 existing schema JSONs (v2-v10) so AutoMigration declarations in `AppDatabase.kt` compile on any clone / CI / worktree.

## What Changed

Two atomic commits, in strict order:

| Order  | SHA       | Full SHA                                   | Message                                                                               |
| ------ | --------- | ------------------------------------------ | ------------------------------------------------------------------------------------- |
| HEAD~1 | `981b5fb` | `981b5fbdae56b8dbf8f2a81d26c94e29f68bb0ff` | `chore: stop ignoring shared/schemas — Room needs them for AutoMigration build`       |
| HEAD   | `99f52bd` | `99f52bdc5d9ad95dc72b26a29ba03024fd7bec9e` | `chore(room): commit AppDatabase schemas v2-v10 for AutoMigration build`              |

### Task 1 — `.gitignore` lines removed

The following 3-line block was removed from `.gitignore` (previously at lines 26-28):

```
# Room schemas (generated)
shared/schemas/

```

(The trailing blank line after `shared/schemas/` was the one removed, leaving a single blank line between `# Xcode user state` and `# Agent/CLI`.)

Net: 1 file changed, 3 deletions(+).

### Task 2 — 9 schema JSONs added (content unchanged, only tracking status)

| File                                                            | Size (bytes) |
| --------------------------------------------------------------- | -----------: |
| `shared/schemas/com.pumpernickel.data.db.AppDatabase/2.json`    |        6,523 |
| `shared/schemas/com.pumpernickel.data.db.AppDatabase/3.json`    |       16,462 |
| `shared/schemas/com.pumpernickel.data.db.AppDatabase/4.json`    |       16,708 |
| `shared/schemas/com.pumpernickel.data.db.AppDatabase/5.json`    |       17,037 |
| `shared/schemas/com.pumpernickel.data.db.AppDatabase/6.json`    |       24,048 |
| `shared/schemas/com.pumpernickel.data.db.AppDatabase/7.json`    |       24,488 |
| `shared/schemas/com.pumpernickel.data.db.AppDatabase/8.json`    |       29,118 |
| `shared/schemas/com.pumpernickel.data.db.AppDatabase/9.json`    |       31,162 |
| `shared/schemas/com.pumpernickel.data.db.AppDatabase/10.json`   |       31,731 |

Net: 9 files changed, 6,379 insertions(+). Plain `git add shared/schemas/` succeeded — `-f` was not needed because Task 1's gitignore commit had already landed.

## Verification — All 6 Phase-Wide Checks Passed

| # | Check                                                                                                | Expected      | Actual        | Result |
| - | ---------------------------------------------------------------------------------------------------- | ------------- | ------------- | ------ |
| 1 | `grep -c "shared/schemas" .gitignore`                                                                | `0`           | `0`           | PASS   |
| 2 | `git ls-files shared/schemas/com.pumpernickel.data.db.AppDatabase/ \| wc -l`                         | `9`           | `9`           | PASS   |
| 3 | `ls shared/schemas/com.pumpernickel.data.db.AppDatabase/*.json \| wc -l`                             | `9`           | `9`           | PASS   |
| 4 | `git log --oneline -2` order (HEAD=schemas commit, HEAD~1=gitignore commit)                          | correct order | correct order | PASS   |
| 5 | `git diff HEAD~2 HEAD -- shared/src shared/build.gradle.kts \| wc -l`                                | `0`           | `0`           | PASS   |
| 6 | `git status --short` (excluding the orchestrator-managed `.planning/quick/...` dir)                  | clean         | clean         | PASS   |

Additionally:
- `git log -1 --name-only` for HEAD shows only `shared/schemas/...` entries (0 non-schema files in the schema commit).
- No `git add -f` used.

## Deviations from Plan

None — plan executed exactly as written.

## Effect

Room AutoMigration build inputs (declared 6→7, 7→8, 8→9, 9→10 in `AppDatabase.kt:28-34`, with `room { schemaDirectory("$projectDir/schemas") }` in `shared/build.gradle.kts:57-59`) are now version-controlled. A fresh clone, CI checkout, or worktree will now have the 9 schema JSONs on disk and will compile the shared module successfully.

## Self-Check: PASSED

- `.gitignore` exists, contains 0 occurrences of `shared/schemas` ✓
- Commit `981b5fbdae56b8dbf8f2a81d26c94e29f68bb0ff` found in `git log --all` ✓
- Commit `99f52bdc5d9ad95dc72b26a29ba03024fd7bec9e` found in `git log --all` ✓
- All 9 schema JSONs tracked under `shared/schemas/com.pumpernickel.data.db.AppDatabase/` ✓
- All 9 schema JSONs present on disk ✓
