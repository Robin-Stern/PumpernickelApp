---
phase: 260511-117-b-2-code-recipe-prompt-mit-worked-exampl
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/src/commonMain/resources/recipe-system-prompt.md
autonomous: true
requirements:
  - B.2-worked-example
must_haves:
  truths:
    - "Recipe-system-prompt contains a `## Worked example — hitting the macro target` section"
    - "The section sits between `## Composition guidance` and `## Refusal` (no other section was overwritten or moved)"
    - "The example demonstrates first-draft sum, ±10% tolerance check, one concrete iteration, and final re-check using ingredients from the catalog"
    - "The section is procedural (always-applies tone), not error-flagged (does not name 'the small-kcal case' or 'the case where macros are off')"
  artifacts:
    - path: "shared/src/commonMain/resources/recipe-system-prompt.md"
      provides: "Updated system prompt with worked example for macro arithmetic"
      contains: "## Worked example — hitting the macro target"
  key_links:
    - from: "## Composition guidance"
      to: "## Worked example — hitting the macro target"
      via: "section ordering"
      pattern: "## Worked example"
    - from: "## Worked example — hitting the macro target"
      to: "## Refusal"
      via: "section ordering"
      pattern: "## Refusal"
---

<objective>
Insert a single `## Worked example — hitting the macro target` section into `recipe-system-prompt.md` that demonstrates macro arithmetic with one explicit iteration cycle. The example uses `remaining = { kcal: 1100, protein: 55g }` and walks through first draft → sum → ±10% check → adjust → re-sum → final check using catalog ingredients (Hähnchenbrust, Reis, Brokkoli, Olivenöl).

Purpose: Address two eval failure modes — Llama-3.3 emits valid recipes with macros off by >15% (no iteration), and Qwen mode-collapses to identical quantities across different `remaining` values (no quantity adaptation). A procedural worked example shows iteration as a normal always-applies step, not an error-handling branch.

Output: One modified markdown file with a new section between the existing `## Composition guidance` (line 129) and `## Refusal` (line 131) sections. No code changes, no eval runs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md

# Current state of the file being edited (143 lines, 6 sections)
@shared/src/commonMain/resources/recipe-system-prompt.md

# Eval evidence motivating the change (read the "Recommended prompt changes"
# item #2, failure mode patterns #3 (Qwen mode collapse) and #4 (Llama-3.3
# macro arithmetic), and the B.1 delta section explaining why naming the
# danger case backfired).
@evals/MULTI-MODEL-RESULTS.md
</context>

<interfaces>
<!-- File structure being edited. The insertion point is the blank line
     after line 129 ("Aim for 4–8 ingredients...") and before line 131
     ("## Refusal"). All other sections stay verbatim. -->

Section ordering (before edit):
1. # Recipe Generation v2                 (line 1)
2. ## Response shape                      (line 9)
3. ## Hard rules                          (line 73)
4. ## Ingredient catalog                  (line 88)
5. ## Composition guidance                (line 122, ends line 129)
6. ## Refusal                             (line 131)
7. ## Final reminder                      (line 139)

Section ordering (after edit):
1. # Recipe Generation v2
2. ## Response shape
3. ## Hard rules
4. ## Ingredient catalog
5. ## Composition guidance
6. ## Worked example — hitting the macro target   ← NEW
7. ## Refusal
8. ## Final reminder

Catalog per-100g values available for the example (from lines 92, 99, 105, 112):
- Hähnchenbrust: 165 kcal, 31g protein
- Reis (gekocht): 130 kcal, 2.7g protein
- Brokkoli: 34 kcal, 2.8g protein
- Olivenöl: 884 kcal, 0g protein
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Insert worked-example section between Composition guidance and Refusal</name>
  <files>shared/src/commonMain/resources/recipe-system-prompt.md</files>
  <action>
Open `shared/src/commonMain/resources/recipe-system-prompt.md` and use Edit to insert a new section. The insertion point is immediately before the `## Refusal` heading (currently line 131).

The new section MUST:

1. Start with the heading `## Worked example — hitting the macro target` (exactly this text, em-dash `—` not hyphen).

2. Set up the scenario: target `remaining = { kcal: 1100, protein: 55g }`.

3. **First draft** — list four catalog ingredients with concrete `amountGrams`:
   - Hähnchenbrust 200g
   - Reis (gekocht) 200g
   - Brokkoli 150g
   - Olivenöl 10g

4. **Sum with intermediate arithmetic** (show the multiplications, not just totals). For each ingredient compute `per-100g × amountGrams / 100`:
   - Hähnchenbrust 200g: `165 × 2 = 330 kcal`, `31 × 2 = 62g protein`
   - Reis 200g: `130 × 2 = 260 kcal`, `2.7 × 2 = 5.4g protein`
   - Brokkoli 150g: `34 × 1.5 = 51 kcal`, `2.8 × 1.5 = 4.2g protein`
   - Olivenöl 10g: `884 × 0.1 = 88.4 kcal`, `0g protein`
   - **Totals: ≈ 729 kcal, ≈ 71.6g protein**

5. **Tolerance check against ±10% of target (1100 kcal, 55g protein)**:
   - kcal window: 990–1210. Got 729 → **UNDER by 24%** → needs adjustment.
   - protein window: 49.5–60.5. Got 71.6 → **OVER by 30%** → also off.
   - State explicitly: "Both are outside ±10%. Adjust."

6. **One iteration** — show a concrete adjustment with arithmetic:
   - Reduce Hähnchenbrust 200g → 130g: protein delta `31 × 1.3 - 31 × 2 = -21.7g` → protein now ≈ 49.9g
   - Increase Reis 200g → 450g: kcal delta `130 × 4.5 - 130 × 2 = +325 kcal` → kcal now ≈ 1054
   - (or equivalent adjustment — the point is showing the delta math, not the exact final numbers)

7. **Re-sum and final tolerance check**:
   - New totals: ≈ 1054 kcal, ≈ 51.4g protein (or whatever follows from the chosen adjustment).
   - kcal 1054 ∈ [990, 1210] ✓
   - protein 51.4 ∈ [49.5, 60.5] ✓
   - "Both inside ±10%. Ship it."

8. **End with one normative sentence (verbatim)**:
   > Calculate, check, adjust until both kcal AND protein land inside ±10%. Don't settle for "close enough."

Style + tone constraints:
- Length: 15–25 lines of markdown, **between 100 and 350 words** (word count is verified — stay in band).
- DO NOT emit JSON in this section. The schema example already lives in `## Response shape` above. This is a reasoning trace, not output.
- DO NOT name "the small-kcal case", "the case where macros are off", "if macros are wrong", or any error-flagged framing. Anti-pattern lesson from B.1: naming the danger case salience-primes reasoning models toward the dangerous branch. Frame the iteration as **a normal procedure that always applies**, not as a corrective branch.
- DO show intermediate arithmetic in the `165 × 2 = 330` style (not `≈ 330` or "around 330"). Models copy the form they see.
- Use a mix of bullet lists and inline math. A small table is fine ONLY if it makes the per-ingredient math clearer than bullets; otherwise stick to bullets to stay under the line budget.
- Keep the existing section "## Refusal" untouched — do not modify, do not move, do not delete its heading or its body.

After editing, the file should have 8 second-level headings (`##`) — the original 6 plus the new one, with `## Refusal` and `## Final reminder` unchanged.
  </action>
  <verify>
    <automated>
cd "$(git rev-parse --show-toplevel)" && \
FILE="shared/src/commonMain/resources/recipe-system-prompt.md" && \
test "$(grep -c '^## Worked example' "$FILE")" = "1" && \
test "$(grep -c '^## Refusal' "$FILE")" = "1" && \
test "$(grep -c '^## Composition guidance' "$FILE")" = "1" && \
test "$(grep -c '^## Final reminder' "$FILE")" = "1" && \
awk '/^## Worked example/{f=1;next} /^## /{f=0} f' "$FILE" > /tmp/worked-example-section.txt && \
WORDS=$(wc -w < /tmp/worked-example-section.txt | tr -d ' ') && \
echo "word count: $WORDS" && \
test "$WORDS" -ge 100 && test "$WORDS" -le 350 && \
grep -E '1100|±10%|kcal' /tmp/worked-example-section.txt > /dev/null && \
grep -E '165|130|×|x' /tmp/worked-example-section.txt > /dev/null && \
! grep -iE 'small.{0,5}kcal case|macros? are off|if macros? (are )?wrong|when macros fail' /tmp/worked-example-section.txt && \
echo "OK: section exists, neighbors intact, word count $WORDS in [100,350], has arithmetic, no error-flagged phrasing"
    </automated>
  </verify>
  <done>
- `grep -c '^## Worked example' recipe-system-prompt.md` returns 1
- `grep -c '^## Refusal' recipe-system-prompt.md` returns 1 (section preserved)
- `grep -c '^## Composition guidance' recipe-system-prompt.md` returns 1 (section preserved)
- `grep -c '^## Final reminder' recipe-system-prompt.md` returns 1 (section preserved)
- Word count of the new section's body (between `## Worked example` and the next `##`) is in [100, 350]
- Section body contains `1100`, `±10%`, `kcal`, and at least one of `165`, `130`, `×` (concrete arithmetic visible)
- Section body does NOT contain error-flagged phrasing (`small kcal case`, `macros are off`, `if macros are wrong`, etc.)
- File is otherwise unchanged from line 1 through end of `## Composition guidance` and from `## Refusal` through end of file
  </done>
</task>

</tasks>

<verification>
The verify step in Task 1 is the full plan verification. There is no eval run in this plan — the orchestrator handles B.2.eval separately to save tokens. The executor MUST NOT re-run `prepare.mjs` or any eval script; the prompt resource is read by the eval harness from this file directly on the next invocation.

Manual spot-check (optional, post-execution):
- Open the file and visually confirm section ordering: `## Composition guidance` → `## Worked example — hitting the macro target` → `## Refusal` → `## Final reminder`.
- Read the new section aloud — it should feel like "here's how you do it" not "here's what to do when something goes wrong."
</verification>

<success_criteria>
- Single atomic commit modifies exactly one file: `shared/src/commonMain/resources/recipe-system-prompt.md`.
- The file gains one new `## Worked example — hitting the macro target` section between the existing Composition guidance and Refusal sections.
- All four other `##` sections (Response shape, Hard rules, Ingredient catalog, Composition guidance, Refusal, Final reminder) remain byte-identical to the pre-edit version.
- Section length is 100–350 words.
- Section contains concrete arithmetic referencing `1100 kcal`, `±10%`, and at least one per-100g catalog value (165 for Hähnchenbrust, 130 for Reis, etc.) multiplied by a scalar.
- Section does NOT use error-flagged phrasing that names the failure modes directly (anti-pattern from B.1).
- No eval ran. No prepare.mjs ran. No other files modified.
</success_criteria>

<output>
After completion, create `.planning/quick/260511-117-b-2-code-recipe-prompt-mit-worked-exampl/260511-117-01-SUMMARY.md` summarizing:
- The exact word count of the inserted section
- The final adjustment chosen in the worked example (which ingredient went up/down by how much)
- Confirmation that all four pre-existing `##` neighbor headings are byte-identical to before
- Any deviation from the planned arithmetic (if the executor picked different final numbers, note them)
</output>
