---
phase: 260512-mr1-diagnose-logging-f-r-off-suche
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/src/commonMain/kotlin/com/pumpernickel/data/api/OpenFoodFactsApi.kt
  - shared/src/commonMain/kotlin/com/pumpernickel/domain/nutrition/SearchFoodsRemoteUseCase.kt
  - shared/src/commonMain/kotlin/com/pumpernickel/presentation/nutrition/FoodEntryViewModel.kt
autonomous: true
requirements:
  - DIAG-OFF-SEARCH
must_haves:
  truths:
    - "OpenFoodFactsApi.searchByName prints 4 [OFF] markers covering entry, response, HTML-detection, and pre-decode"
    - "SearchFoodsRemoteUseCase.invoke prints 4 [SearchUC] markers covering entry, api-return, mapping result, and catch"
    - "FoodEntryViewModel debounce-collect prints 4 [FoodVM] markers covering debounce-fire and each Result case (Success/Empty/Error)"
    - "iOS simulator framework still links cleanly after the println additions (no compilation regression)"
    - "All changes land in a single atomic commit with the specified message"
  artifacts:
    - path: "shared/src/commonMain/kotlin/com/pumpernickel/data/api/OpenFoodFactsApi.kt"
      provides: "[OFF] diagnostic markers at 4 strategic points in searchByName"
      contains: "[OFF]"
    - path: "shared/src/commonMain/kotlin/com/pumpernickel/domain/nutrition/SearchFoodsRemoteUseCase.kt"
      provides: "[SearchUC] diagnostic markers at 4 strategic points in invoke"
      contains: "[SearchUC]"
    - path: "shared/src/commonMain/kotlin/com/pumpernickel/presentation/nutrition/FoodEntryViewModel.kt"
      provides: "[FoodVM] diagnostic markers in the debounce-collect lambda"
      contains: "[FoodVM]"
  key_links:
    - from: "FoodEntryViewModel debounce-collect"
      to: "SearchFoodsRemoteUseCase.invoke"
      via: "useCase(query) call wrapped by [FoodVM] -> [SearchUC] entry print"
      pattern: "\\[FoodVM\\] debounce fired.*\\[SearchUC\\] invoke"
    - from: "SearchFoodsRemoteUseCase.invoke"
      to: "OpenFoodFactsApi.searchByName"
      via: "api.searchByName(query) call wrapped by [SearchUC] -> [OFF] entry print"
      pattern: "\\[SearchUC\\] invoke.*\\[OFF\\] searchByName"
---

<objective>
Insert temporary println diagnostic markers at strategic points across the
OpenFoodFacts search pipeline so the next manual run on iOS Simulator (or
Native) reveals exactly WHERE the chain dies — debounce, network call,
response receipt, JSON decode, mapNotNull filtering, or UseCase exception
caught silently.

Purpose: User reports "0 Ergebnisse, es passiert nichts" on both Native and
Simulator after a clean rebuild. Code has been live-verified (cgi/search.pl
returns HTTP 200, JSON is clean, DTOs match, framework freshly built), so
the failure is somewhere in the runtime flow we cannot observe without
instrumentation. These prints will be removed in a follow-up commit once
the root cause is identified.

Output: 12 println statements total (4 per file) in a single atomic commit.
No logic changes, no DTO changes, no UI changes, no Android files touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

@shared/src/commonMain/kotlin/com/pumpernickel/data/api/OpenFoodFactsApi.kt
@shared/src/commonMain/kotlin/com/pumpernickel/domain/nutrition/SearchFoodsRemoteUseCase.kt
@shared/src/commonMain/kotlin/com/pumpernickel/presentation/nutrition/FoodEntryViewModel.kt
</context>

<tasks>

<task type="auto" worktree="false">
  <name>Task 1: Insert println diagnostic markers across OFF search pipeline (3 files, 1 commit)</name>
  <files>
    shared/src/commonMain/kotlin/com/pumpernickel/data/api/OpenFoodFactsApi.kt,
    shared/src/commonMain/kotlin/com/pumpernickel/domain/nutrition/SearchFoodsRemoteUseCase.kt,
    shared/src/commonMain/kotlin/com/pumpernickel/presentation/nutrition/FoodEntryViewModel.kt
  </files>
  <action>
Add exactly 12 println statements across 3 files. NO other changes — do not refactor, rename, reformat, or "improve" surrounding code. Match the existing file's indentation style.

**File 1 — `shared/src/commonMain/kotlin/com/pumpernickel/data/api/OpenFoodFactsApi.kt`**

In the `searchByName(query, pageSize)` function, insert 4 prints:

1. At the very start of the function body, BEFORE the `client.get(...)` call:
   ```kotlin
   println("[OFF] searchByName query='$query' pageSize=$pageSize")
   ```

2. Immediately AFTER the `.bodyAsText()` call that assigns `responseText`:
   ```kotlin
   println("[OFF] response received bytes=${responseText.length} preview='${responseText.take(120)}'")
   ```

3. Inside the existing HTML-detection `if`-block, BEFORE the `throw IllegalStateException(...)`:
   ```kotlin
   println("[OFF] HTML detected — throwing IllegalStateException")
   ```

4. Immediately BEFORE the `return json.decodeFromString(responseText)` line:
   ```kotlin
   println("[OFF] decoding JSON…")
   ```

**File 2 — `shared/src/commonMain/kotlin/com/pumpernickel/domain/nutrition/SearchFoodsRemoteUseCase.kt`**

In the `invoke(query)` function, insert 4 prints:

1. At the very start of the function body, BEFORE the `try`:
   ```kotlin
   println("[SearchUC] invoke query='$query'")
   ```

2. Inside the `try`-block, immediately AFTER `val response = api.searchByName(query)`:
   ```kotlin
   println("[SearchUC] api returned count=${response.count} products=${response.products.size}")
   ```

3. Immediately AFTER `val results = response.products.mapNotNull { … }` (whichever variable name the file uses; if the variable is named differently, print the resulting list's size):
   ```kotlin
   println("[SearchUC] mapped results=${results.size}")
   ```

4. Inside the `catch`-block, as the FIRST line BEFORE any message inspection:
   ```kotlin
   println("[SearchUC] caught ${e::class.simpleName}: ${e.message}")
   ```

**File 3 — `shared/src/commonMain/kotlin/com/pumpernickel/presentation/nutrition/FoodEntryViewModel.kt`**

In the `init` block's debounce-collect chain, inside the `.collect { query -> … }` lambda, insert 4 prints:

1. As the FIRST line of the lambda body, BEFORE the `if (query.length >= 3)` check:
   ```kotlin
   println("[FoodVM] debounce fired query='$query' length=${query.length}")
   ```

2. Inside the `if (query.length >= 3)` branch, AFTER the `when` (one print per Result case, placed immediately BEFORE the `_uiState.update { … }` call inside each branch):
   - Success branch (case yields `result.foods` — whatever the existing variable name is):
     ```kotlin
     println("[FoodVM] -> Success size=${result.foods.size}")
     ```
   - Empty branch:
     ```kotlin
     println("[FoodVM] -> Empty")
     ```
   - Error branch:
     ```kotlin
     println("[FoodVM] -> Error: ${result.message}")
     ```

**If the existing variable names in any file differ from the placeholders above** (e.g. `responseText` is called `body`, `results` is `mapped`, `result.foods` is `result.items`, `result.message` is `result.error`), use the ACTUAL identifier the file already uses — do not introduce new variable names. The point is to instrument what is there, not refactor.

**After all 12 inserts:**

1. Run the grep verifications (see `<automated>` below) to confirm each marker count is 4.
2. Run the iOS simulator framework link to confirm no compile regression.
3. Stage exactly the 3 files and commit with the message:
   ```
   chore(diag): add println markers to OFF search pipeline (temporary)
   ```

**Strict NO-list (per planning_context constraints):**
- Do NOT touch DTOs (`OpenFoodFactsResponse`, `OpenFoodFactsProduct`, etc.)
- Do NOT touch the `OnSearchQueryChanged` event handler or any other UI/event logic
- Do NOT touch View-Logic, Ktor setup, or any Android files (`androidMain/`, `androidApp/`)
- Do NOT create a worktree (gradle needs the warm cache in the repo root)
- Do NOT touch `evals/`
- Do NOT remove or alter any existing prints/logs
- iOS UI (`iosApp/`) stays untouched
  </action>
  <verify>
    <automated>cd /Users/olli/Studium/semester_6/mobile_app_dev/PumpernickelApp &amp;&amp; \
      OFF=$(grep -c "\[OFF\]" shared/src/commonMain/kotlin/com/pumpernickel/data/api/OpenFoodFactsApi.kt) &amp;&amp; \
      UC=$(grep -c "\[SearchUC\]" shared/src/commonMain/kotlin/com/pumpernickel/domain/nutrition/SearchFoodsRemoteUseCase.kt) &amp;&amp; \
      VM=$(grep -c "\[FoodVM\]" shared/src/commonMain/kotlin/com/pumpernickel/presentation/nutrition/FoodEntryViewModel.kt) &amp;&amp; \
      echo "OFF=$OFF UC=$UC VM=$VM" &amp;&amp; \
      [ "$OFF" = "4" ] &amp;&amp; [ "$UC" = "4" ] &amp;&amp; [ "$VM" = "4" ] &amp;&amp; \
      ./gradlew :shared:linkDebugFrameworkIosSimulatorArm64 --quiet</automated>
  </verify>
  <done>
    - `grep -c "\[OFF\]" shared/src/commonMain/kotlin/com/pumpernickel/data/api/OpenFoodFactsApi.kt` == 4
    - `grep -c "\[SearchUC\]" shared/src/commonMain/kotlin/com/pumpernickel/domain/nutrition/SearchFoodsRemoteUseCase.kt` == 4
    - `grep -c "\[FoodVM\]" shared/src/commonMain/kotlin/com/pumpernickel/presentation/nutrition/FoodEntryViewModel.kt` == 4
    - `./gradlew :shared:linkDebugFrameworkIosSimulatorArm64 --quiet` exits 0 (within 900s timeout)
    - Exactly one new commit on HEAD with message `chore(diag): add println markers to OFF search pipeline (temporary)`
    - `git diff HEAD~1 HEAD --name-only` lists exactly the 3 files above, no others
    - No other source files (DTOs, UI, Android, evals, iosApp) modified
  </done>
</task>

</tasks>

<verification>
After execution:

1. **Marker counts**: All three `grep -c` checks return 4.
2. **Build health**: `:shared:linkDebugFrameworkIosSimulatorArm64` builds cleanly — no Kotlin compilation error introduced by stray `$` or unterminated string templates.
3. **Commit hygiene**: `git log -1 --oneline` shows the exact message; `git show --stat HEAD` lists only the 3 expected files.
4. **No drift**: `git status` clean after commit.
</verification>

<success_criteria>
- 4 `[OFF]` markers in `OpenFoodFactsApi.kt`
- 4 `[SearchUC]` markers in `SearchFoodsRemoteUseCase.kt`
- 4 `[FoodVM]` markers in `FoodEntryViewModel.kt`
- iOS simulator framework still links (no compile regression)
- Exactly one atomic commit touching only those 3 files, with the exact specified message
- No changes to DTOs, UI layers, Ktor setup, Android, evals, or iosApp/
</success_criteria>

<output>
After completion, create `.planning/quick/260512-mr1-diagnose-logging-f-r-off-suche/260512-mr1-SUMMARY.md` summarizing:
- Where each marker landed (line numbers per file)
- Whether any variable-name placeholder was substituted (and to what)
- Build result (`linkDebugFrameworkIosSimulatorArm64` pass/fail)
- Commit SHA and confirmation that exactly 3 files changed
- Reminder: prints are TEMPORARY — schedule removal once root cause identified
</output>
