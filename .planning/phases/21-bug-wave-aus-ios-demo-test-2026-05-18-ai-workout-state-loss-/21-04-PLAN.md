---
phase: 21-bug-wave-aus-ios-demo-test-2026-05-18-ai-workout-state-loss-
plan: 04
type: execute
wave: 3
depends_on: [21-03]
files_modified:
  - shared/src/commonMain/kotlin/com/pumpernickel/presentation/ai/WorkoutAiViewModel.kt
  - shared/src/commonMain/kotlin/com/pumpernickel/di/AiModule.kt
  - iosApp/iosApp/Views/AI/AIWorkoutGenView.swift
  - shared/src/iosMain/kotlin/com/pumpernickel/di/WorkoutAiKoinHelper.kt
autonomous: false
requirements: []
must_haves:
  truths:
    - "After AI workout generation finishes in the background (notification fires), navigating BACK into the AI workout screen shows the generated workout, not the empty Idle state"
    - "Root cause identified out of the three D-21-02 hypotheses (a/b/c) and documented in code as a `D-21-02 root-cause:` comment"
    - "Fix touches only the schicht in which the root cause lives — no shotgun edits across multiple layers"
  artifacts:
    - path: "shared/src/commonMain/kotlin/com/pumpernickel/presentation/ai/WorkoutAiViewModel.kt"
      provides: "Hardened state retention across screen re-entry"
      contains: "D-21-02"
    - path: "iosApp/iosApp/Views/AI/AIWorkoutGenView.swift"
      provides: "View no longer resets ViewModel state on re-mount"
      contains: "D-21-02"
  key_links:
    - from: "AIWorkoutGenView.swift `.task` or `.onAppear`"
      to: "WorkoutAiViewModel state"
      via: "guard: only re-trigger initial load if state == Idle"
      pattern: "D-21-02"
---

<objective>
B1 — User generiert ein AI-Workout, App pusht "ist bereit"-Notification, User tippt drauf bzw. navigiert zurück in den Screen, das generierte Workout ist weg, der State steht auf Idle. Drei Root-Cause-Hypothesen (D-21-02):
- (a) Koin: VM ist nicht Singleton, jeder Re-Entry erzeugt neue Instanz, State weg
- (b) `viewModelScope` cancel bei Screen-Unmount, Stream gecancelt, State landet auf Idle
- (c) SwiftUI `.task {}` re-runs bei View-Recreation und überschreibt State mit Initial

Per D-21-02 zuerst Investigation + Logging, dann Fix in der bestätigten Schicht.

Purpose: AI-Workout-Generation überlebt das Background-Generation-Pattern, User kommt zurück und sieht sein Workout (B1 in CONTEXT.md `<domain>`; locked by D-21-02). Voraussetzung für Demo-Run.
Output: Identifizierter Root-Cause + Fix in der entsprechenden Schicht; kein Regression-Test (per D-21-09).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

Honor D-21-02 (investigation-first, dann Fix), D-21-08 (commonMain wo möglich, VM-State ist in commonMain), D-21-09 (kein Regression-Test). Quick-Fix 260518-eny hat das Pattern für Hypothese (c) bereits etabliert (one-shot `savedEvent: SharedFlow<Unit>` + `reset()` in `onAppear`), bei Hypothese (c) Erweiterung dieses Patterns, nicht Neu-Erfindung.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-bug-wave-aus-ios-demo-test-2026-05-18-ai-workout-state-loss-/21-CONTEXT.md
@.planning/phases/21-bug-wave-aus-ios-demo-test-2026-05-18-ai-workout-state-loss-/21-03-PLAN.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/quick/260518-eny-ios-ai-workout-generation-state-reset-na/

<interfaces>
From shared/src/commonMain/kotlin/com/pumpernickel/presentation/ai/WorkoutAiViewModel.kt:
- 277 lines, read in full before editing
- Exposes: `state: StateFlow<WorkoutAiState>`
- States: Idle / Generating / Streaming / Saved / Error
- Quick-Fix 260518-eny already added: `savedEvent: SharedFlow<Unit>`, `reset()`

From shared/src/commonMain/kotlin/com/pumpernickel/di/AiModule.kt:
- Current binding (verify by reading): likely `viewModel { WorkoutAiViewModel(...) }`
- Hypothese (a) fix: replace with `single { }` so the VM survives screen-recreation

From iosApp/iosApp/Views/AI/AIWorkoutGenView.swift:
- Current pattern (verify): KoinHelper.shared.getWorkoutAiViewModel() at View init
- `.task { ... }` may re-run on view re-creation
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reproduce and add diagnostic logging</name>
  <files>shared/src/commonMain/kotlin/com/pumpernickel/presentation/ai/WorkoutAiViewModel.kt, iosApp/iosApp/Views/AI/AIWorkoutGenView.swift</files>
  <read_first>
    - shared/src/commonMain/kotlin/com/pumpernickel/presentation/ai/WorkoutAiViewModel.kt (entire 277 lines)
    - shared/src/commonMain/kotlin/com/pumpernickel/di/AiModule.kt — is the binding `viewModel { }`, `single { }`, or `factory { }` for WorkoutAiViewModel?
    - shared/src/iosMain/kotlin/com/pumpernickel/di/WorkoutAiKoinHelper.kt — does it cache the instance or call `KoinComponent.get()` fresh every time?
    - iosApp/iosApp/Views/AI/AIWorkoutGenView.swift — focus on init, `.task`, `.onAppear`, and any state-init code
    - shared/src/commonMain/kotlin/com/pumpernickel/domain/ai/AiGenerationManager.kt — does it own its own scope/Job? Crucial for hypothesis (b)
    - .planning/quick/260518-eny-ios-ai-workout-generation-state-reset-na/ — read the SUMMARY to understand the existing `savedEvent` + `reset()` pattern
  </read_first>
  <action>
    1. Add `println("[AiVM] ...")` log lines (CONVENTIONS.md convention: `println` with `[Tag]` prefix) at every state-transition point in WorkoutAiViewModel:
       - In `init { }`: `println("[AiVM] init instance=${this.hashCode()}")`
       - Before each `_state.value = ...` assignment: `println("[AiVM] state=${...next state class name...} instance=${this.hashCode()}")`
       - In `onCleared()` (override if not present): `println("[AiVM] onCleared instance=${this.hashCode()}")`
       Lets us tell at runtime whether the same instance survives re-entry (rules out hypothesis a) and whether `onCleared` is being called between Generating → screen-leave → screen-return (confirms hypothesis b).
    2. In AIWorkoutGenView.swift, add a print at View init and inside `.task` / `.onAppear`: `print("[AiView] onAppear vm=\(ObjectIdentifier(viewModel).hashValue)")`. Lets us tell whether SwiftUI is re-using the View or creating a new one (relevant for hypothesis c).
    3. Reproduce on iOS simulator:
       a. Build + launch on simulator
       b. Open AI workout gen, fill form, tap Generieren
       c. While in Generating/Streaming, switch to a different tab
       d. Wait for the "ist bereit"-notification to fire
       e. Navigate back to the AI workout gen screen
       f. Read Xcode console for `[AiVM]` and `[AiView]` log lines
    4. Diagnosis decision tree:
       - If `[AiVM] init` fires twice with different `instance=` hashes → hypothesis (a), Koin recreates the VM. Fix in Task 2.
       - If `[AiVM] onCleared` fires when leaving the screen and the streaming Job dies → hypothesis (b), viewModelScope cancellation. Fix in Task 2.
       - If same instance survives (hash matches) but the View re-renders to Idle → hypothesis (c), `.task` re-runs and overwrites. Fix in Task 2.
    5. Record the verdict as a comment at the top of WorkoutAiViewModel:
       `// D-21-02 root-cause: <a/b/c> — <one-line explanation, including the log evidence>`
    6. Leave the diagnostic prints in for now, they will be cleaned up after the fix is verified.
  </action>
  <verify>
    <automated>grep -c 'D-21-02 root-cause:' shared/src/commonMain/kotlin/com/pumpernickel/presentation/ai/WorkoutAiViewModel.kt</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '\[AiVM\]' shared/src/commonMain/kotlin/com/pumpernickel/presentation/ai/WorkoutAiViewModel.kt` returns at least 3
    - `grep -c 'D-21-02 root-cause:' shared/src/commonMain/kotlin/com/pumpernickel/presentation/ai/WorkoutAiViewModel.kt` returns 1
    - Recorded root-cause is one of: `(a)`, `(b)`, `(c)` with a specific log-line citation
    - iOS build green
    - Manual reproduction confirms both the bug symptom AND that the log lines pinpoint the layer
  </acceptance_criteria>
  <done>Root cause confirmed and documented in code; logs prove which schicht to fix.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the root-cause fix</name>
  <files>shared/src/commonMain/kotlin/com/pumpernickel/presentation/ai/WorkoutAiViewModel.kt, shared/src/commonMain/kotlin/com/pumpernickel/di/AiModule.kt, iosApp/iosApp/Views/AI/AIWorkoutGenView.swift, shared/src/iosMain/kotlin/com/pumpernickel/di/WorkoutAiKoinHelper.kt</files>
  <read_first>
    - shared/src/commonMain/kotlin/com/pumpernickel/presentation/ai/WorkoutAiViewModel.kt (with the Task-1 logs + root-cause comment)
    - shared/src/commonMain/kotlin/com/pumpernickel/di/AiModule.kt
    - shared/src/commonMain/kotlin/com/pumpernickel/di/SharedModule.kt (the `includes(...)` line for AiModule and the application-scope CoroutineScope, if any)
    - iosApp/iosApp/Views/AI/AIWorkoutGenView.swift
  </read_first>
  <action>
    Apply ONE of these three fix strategies based on Task 1's verdict.

    Variant (a) — Koin recreates the VM:
    1. In AiModule.kt, change the binding for WorkoutAiViewModel from `viewModel { ... }` (factory by default) to `single { WorkoutAiViewModel(get(), get(), ...) }` with the same constructor args.
    2. In WorkoutAiKoinHelper.kt ensure the helper returns the cached single (default behavior — verify it is not calling `getKoin().get()` with a new scope each time).
    3. Document with: `// D-21-02 fix (a) — single-scope so the VM survives screen re-entry while a background generation is running`.
    Risk: a `single` VM lives forever — ensure `reset()` is called on Save/Error completion (already true per Quick-Fix 260518-eny).

    Variant (b) — viewModelScope cancelled when screen unmounts:
    1. Move the streaming job from `viewModelScope` to an injected `appScope: CoroutineScope` or to `AiGenerationManager`'s own scope. The AiGenerationManager already lives application-scoped on iOS (see AiBgTaskRegistrar.kt), verify the scope and reuse it.
    2. The VM observes the manager's StateFlow and forwards to `_state`. When the VM dies, the manager keeps generating; when a new VM (or the same one) appears, it picks up the current manager state.
    3. Document with: `// D-21-02 fix (b) — generation runs in appScope/AiGenerationManager, not viewModelScope`.

    Variant (c) — `.task` re-runs and overwrites state:
    1. In AIWorkoutGenView.swift, guard the initial state-load: only call `viewModel.start(...)` / state-init inside `.task` if state is Idle. Pattern from Quick-Fix 260518-eny:
       ```
       .task {
           // D-21-02 fix (c) — don't re-init if a generation is already running/completed
           if case .idle = state { /* initial load only */ }
       }
       ```
    2. If a `reset()` is fired on view-disappear, remove it — let the savedEvent-driven path handle clearing.
    3. Document with: `// D-21-02 fix (c) — .task guarded so background-running state survives view re-creation`.

    For ALL three: after the fix is verified by manual UAT, remove the diagnostic `println` lines added in Task 1. Leave the root-cause comment.
  </action>
  <verify>
    <automated>./gradlew :shared:compileKotlinIosX64 :shared:compileKotlinAndroidMain && xcodebuild -project iosApp/iosApp.xcodeproj -scheme iosApp -configuration Debug -sdk iphonesimulator -destination 'generic/platform=iOS Simulator' build CODE_SIGNING_ALLOWED=NO | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'D-21-02 fix' shared/src/commonMain/kotlin/com/pumpernickel/presentation/ai/WorkoutAiViewModel.kt shared/src/commonMain/kotlin/com/pumpernickel/di/AiModule.kt iosApp/iosApp/Views/AI/AIWorkoutGenView.swift 2>/dev/null | grep -v ':0$' | wc -l` returns at least 1 (fix annotation exists in at least one file)
    - The fix is in exactly ONE schicht (Koin module OR VM scope OR View `.task`), not all three
    - `./gradlew :shared:compileKotlinIosX64 :shared:compileKotlinAndroidMain` green
    - xcodebuild Debug green
    - Diagnostic `println` lines from Task 1 are removed (`grep -c '\[AiVM\]' shared/src/commonMain/kotlin/com/pumpernickel/presentation/ai/WorkoutAiViewModel.kt` returns 0)
  </acceptance_criteria>
  <done>Root-cause-specific fix applied in the correct schicht; builds green; logs cleaned up.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual UAT — workout survives screen re-entry</name>
  <files>(no source file modified — human verification only; the executor pauses for user input)</files>
  <action>Human-verification checkpoint — the executor pauses here and waits for the user. See &lt;what-built&gt; / &lt;how-to-verify&gt; below for the exact verification steps. Do not proceed until the resume-signal is received.</action>
  <verify><automated>echo "Manual verification — executor pauses for user; see how-to-verify"</automated></verify>
  <done>Human verifier responds "approved" in the resume signal after exercising all verification steps; or describes which step failed and what was observed.</done>
  <what-built>The root cause of B1 is now mitigated in the schicht identified by Task 1 (Koin scope / appScope / .task guard).</what-built>
  <how-to-verify>
    1. Build + launch iOS simulator.
    2. Open AI workout gen, fill form, tap Generieren.
    3. While in Generating/Streaming, switch to a different tab and wait for the "ist bereit"-notification.
    4. Tap the notification (or manually navigate back to the AI workout gen tab).
    5. Expected: the generated workout is visible, the Save / Edit affordances are reachable, state is NOT Idle.
    6. Regression check: starting a NEW generation after this still works (no stale state from the previous run).
    7. Optional bonus: repeat the cycle 3 times in a row, no degradation.
  </how-to-verify>
  <resume-signal>Type "approved" if the workout survives re-entry; describe the failure mode if not.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Background generation lifecycle | Job lifetime is controlled by app/VM scope, not by external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-06 | D (DoS) | AI generation lifecycle | accept | Existing 10-min `requestTimeoutMillis` cap in OpenAICompatibleClient bounds the runaway-job risk. App-scope lifetime is bounded by app process lifetime. |
| T-21-07 | I (Info Disclosure) | Diagnostic logs | mitigate | Task 1 `println` lines log only instance hashes + state names, never the LLM payload or API key. Logs are removed after Task 2 verification. |
</threat_model>

<verification>
1. Build: `./gradlew :shared:compileKotlinIosX64 :shared:compileKotlinAndroidMain` green
2. Build: xcodebuild Debug green
3. Manual UAT (Task 3) confirms workout survives screen re-entry
4. Diagnostic prints removed after fix verified
</verification>

<success_criteria>
B1 ist gefixt: User kann während AI-Generation den Screen verlassen, die "ist bereit"-Notification empfangen, zurück-navigieren und sieht sein generiertes Workout. Root-cause schicht klar identifiziert, Fix targets nur diese Schicht.
</success_criteria>

<output>
After completion, create `.planning/phases/21-bug-wave-aus-ios-demo-test-2026-05-18-ai-workout-state-loss-/21-04-SUMMARY.md`.
</output>
