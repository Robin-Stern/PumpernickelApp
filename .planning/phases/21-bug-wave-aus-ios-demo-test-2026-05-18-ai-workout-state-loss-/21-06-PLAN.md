---
phase: 21-bug-wave-aus-ios-demo-test-2026-05-18-ai-workout-state-loss-
plan: 06
type: execute
wave: 4
depends_on: [21-04, 21-05]
files_modified:
  - shared/src/commonMain/kotlin/com/pumpernickel/domain/gamification/GamificationEngine.kt
  - shared/src/commonMain/kotlin/com/pumpernickel/domain/gamification/RankLadder.kt
  - shared/src/commonMain/kotlin/com/pumpernickel/presentation/overview/OverviewViewModel.kt
autonomous: false
requirements: []
must_haves:
  truths:
    - "Verdict recorded: B6 is either a real bug OR a UI/labeling issue (verify-first per D-21-07)"
    - "After a normal Workout-Save, XP is awarded AND the rank surface updates (either from Unranked to Bronze 1, or stays on current rank above Bronze 1)"
    - "If verdict is no-bug, the UI labeling around `Unranked` is updated to be unambiguous (no silent zeros, no misleading negative XP display)"
    - "If verdict is real bug, the engine/ladder path is patched so a normal workout produces a rank transition"
  artifacts:
    - path: "shared/src/commonMain/kotlin/com/pumpernickel/domain/gamification/GamificationEngine.kt"
      provides: "Workout-completion path traced and either verified or fixed"
      contains: "D-21-07"
  key_links:
    - from: "WorkoutSessionViewModel.saveReviewedWorkout"
      to: "GamificationEngine.applyWorkoutCompletion"
      via: "workout-save hook from Phase 15-06"
      pattern: "applyWorkoutCompletion"
    - from: "GamificationEngine.applyWorkoutCompletion"
      to: "RankLadder.rankForXp"
      via: "rank recompute on XP change"
      pattern: "rankForXp"
---

<objective>
B6 — User-Beobachtung im Demo-Test: nach normalem Workout-Save bekam der User nach eigenem Eindruck keine XP und stand auf "unranked", obwohl -50 XP-Penalty ihn unter Bronze1 gedrückt hatten. Per D-21-07 ist B6 ein verify-first Fall: kein klarer Bug, sondern eventuell ein Missverständnis oder ein Labeling-Issue. Diese Phase verifiziert die XP- und Rank-Logik end-to-end:
1. Trace `GamificationEngine.applyWorkoutCompletion` mit Logs, normale Workout-Save zeitlich
2. Trace `RankLadder.rankForXp(xp)` mit zero / negative / Bronze-Schwellwerten
3. Verdict: real bug vs. UI-Missverständnis
4. Wenn real bug -> Engine/Ladder Fix
5. Wenn kein Bug -> UI/Doku-Update (Overview soll "Bronze 1" zeigen statt "unranked", oder negative XP klar als Penalty kommunizieren)

Purpose: Sicherstellen dass XP-Bookkeeping korrekt arbeitet und User-Vertrauen ins Gamification-System hergestellt ist (B6 in CONTEXT.md `<domain>`; locked by D-21-07). Wave D, letzte Welle, hängt von gefixter Wave C ab damit XP-Werte nicht von B1/B2-Symptomen verfälscht werden.
Output: Entweder Doku-Update task ODER real fix task — entschieden durch Task 1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

Honor D-21-07 (verify-first, fix only if real bug), D-21-08 (commonMain — alle relevanten Files sind dort), D-21-09 (Test optional je nach Outcome).
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-bug-wave-aus-ios-demo-test-2026-05-18-ai-workout-state-loss-/21-CONTEXT.md
@.planning/phases/21-bug-wave-aus-ios-demo-test-2026-05-18-ai-workout-state-loss-/21-04-PLAN.md
@.planning/phases/21-bug-wave-aus-ios-demo-test-2026-05-18-ai-workout-state-loss-/21-05-PLAN.md
@.planning/codebase/ARCHITECTURE.md

<interfaces>
From shared/src/commonMain/kotlin/com/pumpernickel/domain/gamification/GamificationEngine.kt (499 lines):
- `applyWorkoutCompletion(...)` — main XP-award path from a saved workout
- Around line 363: `currentState = gamificationRepo.getRankStateSnapshot()`
- Around line 371: `if (currentState is RankState.Unranked && totalXp <= 0L) return` — relevant for "unranked stays unranked when xp drops below 0"
- Around line 373: `val newRank = RankLadder.rankForXp(totalXp)`
- Around line 387: `if (previousRank == targetRank && currentState !is RankState.Unranked) return`
- Around line 389: `gamificationRepo.setRankState(currentRank = targetRank, ...)`

From shared/src/commonMain/kotlin/com/pumpernickel/domain/gamification/RankLadder.kt:
- `rankForXp(xp: Long): Rank` at line 30 — maps an XP total to a Rank tier

From shared/src/commonMain/kotlin/com/pumpernickel/presentation/overview/OverviewViewModel.kt:
- exposes `rankState: StateFlow<RankState>` consumed by OverviewRankStrip on both Android and iOS

Phase-15 design reference (RankState.Unranked is a real state for users with 0 XP before their first workout, by design per the v1.5 Gamifikation phase).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify XP and rank dataflow end-to-end</name>
  <files>shared/src/commonMain/kotlin/com/pumpernickel/domain/gamification/GamificationEngine.kt, shared/src/commonMain/kotlin/com/pumpernickel/domain/gamification/RankLadder.kt</files>
  <read_first>
    - shared/src/commonMain/kotlin/com/pumpernickel/domain/gamification/GamificationEngine.kt — entire 499 lines, focus on applyWorkoutCompletion (around line 200-280) and the rank-recompute block (line 363-395)
    - shared/src/commonMain/kotlin/com/pumpernickel/domain/gamification/RankLadder.kt — rankForXp + the tier-threshold list
    - shared/src/commonMain/kotlin/com/pumpernickel/domain/gamification/Rank.kt — Rank enum + tier values
    - shared/src/commonMain/kotlin/com/pumpernickel/domain/gamification/RankState.kt — Unranked / Ranked variants
    - shared/src/commonMain/kotlin/com/pumpernickel/domain/gamification/XpFormula.kt — workout-completed XP formula + PENALTY constants (look for geofenceExitPenalty = -50 .. -200)
    - shared/src/commonMain/kotlin/com/pumpernickel/presentation/overview/OverviewViewModel.kt — how rankState flows into the UI
    - shared/src/commonMain/kotlin/com/pumpernickel/data/repository/GamificationRepository.kt (commit chain after Phase 20 — verify path) — getRankStateSnapshot / setRankState / xp_ledger query
  </read_first>
  <action>
    1. Document the expected behavior table from reading the code:
       - User starts at: RankState.Unranked, xp_ledger empty (totalXp = 0)
       - First normal workout completes: XpFormula.workoutCompletion gives +N XP (volume-scaled)
       - Engine writes ledger entry, recomputes totalXp = N
       - RankLadder.rankForXp(N) returns Bronze 1 (or higher if N happens to exceed thresholds)
       - currentState becomes RankState.Ranked(Bronze 1)
       - OverviewViewModel.rankState emits the new state, OverviewRankStrip re-renders
    2. Add `println("[B6] ...")` log lines at every transition in GamificationEngine.applyWorkoutCompletion:
       - Entry: `println("[B6] applyWorkoutCompletion start workoutId=${event.workoutId} volume=${volume}")`
       - After XP calculation: `println("[B6] xp awarded=${xpAmount} totalAfter=${totalXp}")`
       - After rank recompute: `println("[B6] rank previous=${previousRank} target=${targetRank} stateBefore=${currentState::class.simpleName}")`
       - After setRankState: `println("[B6] rank persisted=${targetRank}")`
    3. Add a focused `println("[B6 ladder]")` log inside RankLadder.rankForXp at the entry: `println("[B6 ladder] rankForXp xp=$xp -> ...")` — verify the threshold logic.
    4. Reproduce the demo-test scenario:
       a. Start from a state where the user is Unranked (or use a fresh install / wipe the gamification DataStore sentinel to force first-launch retroactive walker)
       b. (Optional, to recreate the -50 case from demo): apply a geofence-exit penalty before the workout, so total XP drops below zero. The penalty path is GamificationEngine.applyGeofenceExitPenalty (find the actual function name by grep). Verify whether penalty alone moves the user to Ranked(Bronze 1 minus) or keeps them Unranked.
       c. Save a normal workout in WorkoutSessionViewModel.saveReviewedWorkout
       d. Read the `[B6]` log output and check the rank surface in the Overview
    5. Verdict:
       - Logs show XP awarded > 0 AND rank moves to Bronze 1 AND OverviewRankStrip reflects the new rank → No bug, this is a UI labeling / user-perception issue. Continue with Task 2 (Doku/UI variant).
       - Logs show XP awarded = 0 (workoutCompletion returns 0) → real bug, formula misfire. Task 2 fix variant A.
       - Logs show XP awarded > 0 but rank stays Unranked (the line `if (currentState is RankState.Unranked && totalXp <= 0L) return` is too greedy, or the previousRank/targetRank shortcut at line 387 is wrong) → real bug, ladder shortcut bug. Task 2 fix variant B.
       - Logs show rank updates in the engine but UI does not reflect it → presentation-layer bug. Task 2 fix variant C.
    6. Record the verdict as a comment near `applyWorkoutCompletion`:
       `// D-21-07 verdict: <bug|no-bug> — <one-line explanation citing [B6] logs>`
  </action>
  <verify>
    <automated>grep -c 'D-21-07 verdict:' shared/src/commonMain/kotlin/com/pumpernickel/domain/gamification/GamificationEngine.kt</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '\[B6\]' shared/src/commonMain/kotlin/com/pumpernickel/domain/gamification/GamificationEngine.kt` returns at least 4 (entry, xp, rank, persist)
    - `grep -c '\[B6 ladder\]' shared/src/commonMain/kotlin/com/pumpernickel/domain/gamification/RankLadder.kt` returns at least 1
    - `grep -c 'D-21-07 verdict:' shared/src/commonMain/kotlin/com/pumpernickel/domain/gamification/GamificationEngine.kt` returns 1
    - Verdict is one of: `bug` (with specific variant A/B/C) or `no-bug` (with specific UI/perception cause)
    - Manual reproduction completed; logs cited in the verdict comment
  </acceptance_criteria>
  <done>Verdict recorded with log evidence; the schicht (or non-bug-cause) is unambiguous before Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Apply doc/UI update OR real engine fix per verdict</name>
  <files>shared/src/commonMain/kotlin/com/pumpernickel/domain/gamification/GamificationEngine.kt, shared/src/commonMain/kotlin/com/pumpernickel/domain/gamification/RankLadder.kt, shared/src/commonMain/kotlin/com/pumpernickel/presentation/overview/OverviewViewModel.kt</files>
  <read_first>
    - The verdict comment from Task 1
    - The schicht identified in the verdict
    - For UI variants: shared/src/commonMain/kotlin/com/pumpernickel/presentation/overview/OverviewViewModel.kt and the iOS/Android OverviewRankStrip files (for traceability only — UI rendering changes belong in this task only if verdict is a labeling issue)
  </read_first>
  <action>
    Apply ONE of these branches based on Task 1's verdict:

    Variant `no-bug` (UI labeling / perception issue):
    1. The system is correctly emitting RankState.Unranked or Ranked(Bronze 1) after a normal workout.
    2. The user-perceived issue was likely the label `Unranked` shown on the Overview when the first workout has not been replayed yet, OR the Overview not refreshing immediately after a -50 penalty pushed the user into a negative-XP state.
    3. Update OverviewViewModel.rankState consumer logic or the OverviewRankStrip rendering to:
       - Show "Bronze 1 — schließe dein erstes Workout für offizielles Ranking" as a first-launch hint (instead of bare "Unranked"), OR
       - When totalXp is negative but the user previously had a rank, show the rank floor with a "(-50 XP Penalty)" footnote instead of falling back to Unranked
    4. Document the UI change with `// D-21-07 UI hint — was previously perceived as B6 / no engine bug`.

    Variant `bug A` (XP awarded = 0):
    1. Trace XpFormula.workoutCompletion(volume, ...) — likely the formula returns 0 for low-volume workouts. Fix the formula or the volume calculation in the engine call site.
    2. Add a unit test in commonTest for the boundary (small workout still awards at least the minimum XP per Phase-15 design).

    Variant `bug B` (rank stays Unranked despite XP > 0):
    1. Fix the conditional in GamificationEngine around line 371-387 — likely the `currentState is RankState.Unranked && totalXp <= 0L` shortcut is firing when totalXp is positive but the snapshot is stale, OR the previousRank/targetRank early-return at 387 misfires for the Unranked → Ranked transition.
    2. Add a unit test that exercises the Unranked → Ranked transition with totalXp crossing the Bronze 1 threshold.

    Variant `bug C` (engine updates but UI does not reflect):
    1. Likely the OverviewViewModel rankState StateFlow does not re-subscribe to the GamificationRepository observable. Fix by ensuring `getRankState()` returns a Flow (not a snapshot) and OverviewViewModel collects it inside the viewModelScope with stateIn().
    2. No test needed; this is a wiring fix verifiable by UAT.

    For ALL variants: remove the `[B6]` and `[B6 ladder]` log lines from Task 1 after the UAT verifies the fix. Keep the verdict comment.
  </action>
  <verify>
    <automated>./gradlew :shared:compileKotlinIosX64 :shared:compileKotlinAndroidMain :shared:allTests</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'D-21-07' shared/src/commonMain/kotlin/com/pumpernickel/domain/gamification/GamificationEngine.kt shared/src/commonMain/kotlin/com/pumpernickel/presentation/overview/OverviewViewModel.kt 2>/dev/null | awk -F: '{s+=$2} END {print s}'` returns at least 2 (verdict + fix/UI comment)
    - Build green: `:shared:allTests`
    - If bug variant A or B: at least one new test exists in commonTest exercising the fixed path
    - `[B6]` `println` lines removed (`grep -rc '\[B6\]' shared/src/commonMain/kotlin/com/pumpernickel/` returns 0)
    - Fix lives in exactly one schicht matching the verdict
  </acceptance_criteria>
  <done>Verdict-appropriate change applied (doc/UI tweak or real engine fix); build + tests green; logs cleaned up.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual UAT — XP and rank update on workout save</name>
  <files>(no source file modified — human verification only; the executor pauses for user input)</files>
  <action>Human-verification checkpoint — the executor pauses here and waits for the user. See &lt;what-built&gt; / &lt;how-to-verify&gt; below for the exact verification steps. Do not proceed until the resume-signal is received.</action>
  <verify><automated>echo "Manual verification — executor pauses for user; see how-to-verify"</automated></verify>
  <done>Human verifier responds "approved" in the resume signal after exercising all verification steps; or describes which step failed and what was observed.</done>
  <what-built>XP-bookkeeping verified end-to-end; either a UI/doku tweak (no-bug variant) or a real engine fix (bug variant) is in place.</what-built>
  <how-to-verify>
    1. Build + launch (iOS or Android).
    2. From a known starting state (note the current rank + XP in Overview), save a normal workout.
    3. Expected after save:
       - The XP value in OverviewRankStrip increases by the expected workout-completion amount
       - The rank either stays the same (already at a tier whose threshold isn't crossed) or moves up to the next tier
       - If the user was Unranked before, the rank surface now shows Bronze 1 (or the verdict-specific hint)
    4. If verdict was `no-bug` (UI variant): confirm the new label/copy makes the previously-confusing state clear (e.g. "Bronze 1" with a first-workout hint instead of bare "Unranked").
    5. If verdict was a bug variant: replay the demo scenario (geofence-penalty -50 then normal workout) and confirm the user lands on the expected rank, not Unranked.
    6. Regression: existing rank transitions for users already above Bronze 1 still work.
  </how-to-verify>
  <resume-signal>Type "approved" when XP + rank surface match the expected behavior; describe the mismatch otherwise.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Workout-save -> Gamification engine | Internal handoff, no external input |
| Gamification engine -> rank persistence | Internal, bounded by Room transaction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-10 | T (Tampering) | XP ledger | accept | Local-only DataStore + Room; user can tamper with their own device's gamification data. No leaderboard or cross-user comparison exists. |
| T-21-11 | I (Info Disclosure) | Diagnostic logs | mitigate | `[B6]` logs print XP and rank class names (no PII); removed after Task 2 verification. |
</threat_model>

<verification>
1. Build green: `:shared:compileKotlinIosX64`, `:shared:compileKotlinAndroidMain`, `:shared:allTests`
2. If bug variant: new test exercises the fixed transition
3. Manual UAT (Task 3) — XP and rank surface match expected behavior post-workout
4. Diagnostic logs removed
</verification>

<success_criteria>
B6 ist verifiziert: entweder als kein-Bug bestätigt mit klarem UI-Hinweis-Update, oder als realer Bug in der entsprechenden Schicht gefixt. User-Vertrauen ins Gamification-System ist wiederhergestellt für den Demo-Run.
</success_criteria>

<output>
After completion, create `.planning/phases/21-bug-wave-aus-ios-demo-test-2026-05-18-ai-workout-state-loss-/21-06-SUMMARY.md`. Mark Phase 21 complete via ROADMAP update only after all 6 SUMMARYs exist and Phase 21 UAT artifacts are recorded.
</output>
