---
phase: 21-bug-wave-aus-ios-demo-test-2026-05-18-ai-workout-state-loss-
plan: 05
type: execute
wave: 3
depends_on: [21-03]
files_modified:
  - shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/nutrition/OpenFoodFactsAdapter.kt
  - shared/src/commonMain/kotlin/com/pumpernickel/domain/nutrition/LookupBarcodeUseCase.kt
  - shared/src/commonMain/kotlin/com/pumpernickel/presentation/nutrition/RecipeCreationViewModel.kt
  - shared/src/commonMain/kotlin/com/pumpernickel/data/repository/FoodRepositoryImpl.kt
autonomous: false
requirements: []
must_haves:
  truths:
    - "Barcode-Scan in Rezept-Edit returns a Food with non-zero macros when OFF has the data"
    - "If OFF has zero/missing macros for a scanned product, the UI shows a clear hint (not a silent zero-row)"
    - "Root cause identified out of the 3 D-21-03 candidates (API, Adapter mapping, ViewModel/Repository write-path) and documented in code as a `D-21-03 root-cause:` comment"
    - "DB-Snapshot of the persisted Food entity after a known-good-data scan shows non-zero kcal/protein/carbs/fat"
  artifacts:
    - path: "shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/nutrition/OpenFoodFactsAdapter.kt"
      provides: "Verified or fixed barcode-to-domain mapping"
      contains: "D-21-03"
    - path: "shared/src/commonMain/kotlin/com/pumpernickel/presentation/nutrition/RecipeCreationViewModel.kt"
      provides: "OnBarcodeScanned handler preserves macros when persisting the Food"
      contains: "D-21-03"
  key_links:
    - from: "RecipeCreationViewModel OnBarcodeScanned"
      to: "FoodRepositoryImpl save"
      via: "LookupBarcodeUseCase -> selectFood -> FoodRepository.save"
      pattern: "OnBarcodeScanned"
    - from: "OpenFoodFactsAdapter.lookupBarcode"
      to: "RemoteBarcodeProduct"
      via: "DTO -> domain mapping of nutriments"
      pattern: "lookupBarcode"
---

<objective>
B2 — User scannt Barcode beim Rezept-Edit, das gemappte Food zeigt alle Nährwerte = 0. Der Bug kann in 3 Stellen sitzen (D-21-03):
1. OpenFoodFactsApi.lookupBarcode returns 0-Werte (OFF-Datensatz tatsächlich leer)
2. OpenFoodFactsAdapter.lookupBarcode mapping verliert Werte
3. LookupBarcodeUseCase + RecipeCreationViewModel.OnBarcodeScanned -> Food persistiert in FoodRepository mit 0-Werten

Per D-21-03 zuerst Reproduce + Trace via Log-Statements, dann fix in der bestätigten Schicht.

Purpose: Barcode-Scan liefert verlässliche Nährwerte oder einen ehrlichen UI-Hinweis falls OFF die Daten nicht hat (B2 in CONTEXT.md `<domain>`; locked by D-21-03). Hängt von Wave B (B3, sauberer OFF-Baseline) ab.
Output: Identifizierter Root-Cause + Fix; ggf. UI-Hint bei OFF-Datenlücke.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

Honor D-21-03 (trace OFF -> Adapter -> UseCase -> ViewModel -> Repository -> DB, no assumption about root cause), D-21-08 (commonMain — alle relevanten Files sind dort), D-21-09 (kein automatisierter Regression-Test, Debug-Session).
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-bug-wave-aus-ios-demo-test-2026-05-18-ai-workout-state-loss-/21-CONTEXT.md
@.planning/phases/21-bug-wave-aus-ios-demo-test-2026-05-18-ai-workout-state-loss-/21-03-PLAN.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/INTEGRATIONS.md

<interfaces>
From shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/nutrition/OpenFoodFactsAdapter.kt:
- `override suspend fun lookupBarcode(barcode: String): RemoteBarcodeProduct?` at line ~57
- Maps `OpenFoodFactsResponse.product.nutriments` to domain shape

From shared/src/commonMain/kotlin/com/pumpernickel/domain/nutrition/LookupBarcodeUseCase.kt:
```
operator fun invoke(barcode): LookupBarcodeResult
sealed: Found(food: Food) | NotFound | Error
```

From shared/src/commonMain/kotlin/com/pumpernickel/presentation/nutrition/RecipeCreationViewModel.kt around line 198:
```
RecipeCreationEvent.OnBarcodeScanned -> {
    val result = lookupBarcode(event.barcode)
    // calls selectFood(food) which eventually persists via FoodRepository.save
}
```

From shared/src/commonMain/kotlin/com/pumpernickel/data/repository/FoodRepositoryImpl.kt:
- `suspend fun save(food: Food): Long` — final persistence step
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trace barcode dataflow with log statements</name>
  <files>shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/nutrition/OpenFoodFactsAdapter.kt, shared/src/commonMain/kotlin/com/pumpernickel/domain/nutrition/LookupBarcodeUseCase.kt, shared/src/commonMain/kotlin/com/pumpernickel/presentation/nutrition/RecipeCreationViewModel.kt, shared/src/commonMain/kotlin/com/pumpernickel/data/repository/FoodRepositoryImpl.kt</files>
  <read_first>
    - shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/nutrition/OpenFoodFactsAdapter.kt (entire 73 lines, especially lookupBarcode at line 57 and the nutriments mapping)
    - shared/src/commonMain/kotlin/com/pumpernickel/data/api/OpenFoodFactsApi.kt (line 14: lookupBarcode endpoint)
    - shared/src/commonMain/kotlin/com/pumpernickel/data/api/OpenFoodFactsDto.kt (Nutriments DTO shape — which fields are nullable, units of kcal vs kJ)
    - shared/src/commonMain/kotlin/com/pumpernickel/domain/nutrition/LookupBarcodeUseCase.kt
    - shared/src/commonMain/kotlin/com/pumpernickel/domain/nutrition/RemoteFoodSearchClient.kt (RemoteBarcodeProduct shape)
    - shared/src/commonMain/kotlin/com/pumpernickel/presentation/nutrition/RecipeCreationViewModel.kt lines 190-230 (OnBarcodeScanned handler + selectFood call chain)
    - shared/src/commonMain/kotlin/com/pumpernickel/data/repository/FoodRepositoryImpl.kt (save() function)
    - shared/src/commonMain/kotlin/com/pumpernickel/data/db/FoodEntity.kt (column shapes for kcal/protein/carbs/fat)
  </read_first>
  <action>
    Add `println("[B2] ...")` log lines at each handoff, recording every macro value as it passes through:

    1. In OpenFoodFactsAdapter.lookupBarcode, immediately after the API call returns:
       `println("[B2] adapter raw nutriments kcal=${response.product?.nutriments?.energyKcal100g} protein=${...} carbs=${...} fat=${...}")`
       And before returning the mapped RemoteBarcodeProduct:
       `println("[B2] adapter mapped barcode=$barcode kcal=${mapped.kcalPer100g} protein=${mapped.proteinPer100g} ...")`
    2. In LookupBarcodeUseCase.invoke (the line that constructs the domain Food from the RemoteBarcodeProduct):
       `println("[B2] usecase food kcal=${food.kcalPer100g} protein=${food.proteinPer100g} carbs=${food.carbsPer100g} fat=${food.fatPer100g}")`
    3. In RecipeCreationViewModel.OnBarcodeScanned branch, after the use case returns Found and before selectFood:
       `println("[B2] vm scanned food id=${food.id} kcal=${food.kcalPer100g} protein=${food.proteinPer100g}")`
    4. In FoodRepositoryImpl.save (or the equivalent insert/upsert method), immediately before the DAO call:
       `println("[B2] repo persist food kcal=${food.kcalPer100g} protein=${food.proteinPer100g} carbs=${food.carbsPer100g} fat=${food.fatPer100g}")`
       This logs what hits the DAO — combined with a manual sqlite3 dump (Android) or DB Browser (iOS), we know the DB state.

    Reproduction:
    1. Build + launch on iOS simulator (or Android — adapter path is commonMain so both converge)
    2. Open Rezept-Edit → "Zutat hinzufügen" → Barcode scan a product known to have nutriments on OFF (use the OFF website to confirm before scanning; a 1L Coca-Cola or a popular yogurt is a safe pick)
    3. Read the `[B2]` log lines and walk through the table:
       - Raw nutriments has the values? → API is good
       - Adapter mapped has the values? → adapter mapping is correct
       - Use case Food has the values? → use case maps cleanly
       - VM scanned has the values? → ViewModel preserves
       - Repo persist has the values? → repository writes correct
       - DB has the values? → manual DB inspection final check

    Record the verdict as a comment near OnBarcodeScanned in RecipeCreationViewModel.kt:
    `// D-21-03 root-cause: <layer> — <one-line explanation citing the [B2] log evidence>`

    Layer-attribution table:
    - API returns zero? → OFF dataset is genuinely empty. Fix becomes UI hint, not a code fix in the data path.
    - Adapter shows zero where API showed non-zero? → mapping bug. Fix in OpenFoodFactsAdapter.
    - Use case shows zero where adapter showed non-zero? → use case bug. Fix in LookupBarcodeUseCase.
    - VM shows zero where use case showed non-zero? → ViewModel bug.
    - Repo writes zero where VM had non-zero? → ViewModel-to-repo wiring bug (e.g. wrong arg, wrong copy).
  </action>
  <verify>
    <automated>grep -c 'D-21-03 root-cause:' shared/src/commonMain/kotlin/com/pumpernickel/presentation/nutrition/RecipeCreationViewModel.kt</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rcE '\[B2\]' shared/src/commonMain/kotlin/com/pumpernickel/ | awk -F: '{s+=$2} END {print s}'` returns at least 5 (one log per handoff)
    - `grep -c 'D-21-03 root-cause:' shared/src/commonMain/kotlin/com/pumpernickel/presentation/nutrition/RecipeCreationViewModel.kt` returns 1
    - Recorded root-cause identifies a specific layer: API, Adapter, UseCase, ViewModel, or Repository
    - Manual reproduction with a known-good barcode confirms the bug AND pinpoints the layer
  </acceptance_criteria>
  <done>Trace logs in place, manual reproduction completed, root cause documented in code.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the layer-specific fix (or UI hint)</name>
  <files>shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/nutrition/OpenFoodFactsAdapter.kt, shared/src/commonMain/kotlin/com/pumpernickel/domain/nutrition/LookupBarcodeUseCase.kt, shared/src/commonMain/kotlin/com/pumpernickel/presentation/nutrition/RecipeCreationViewModel.kt, shared/src/commonMain/kotlin/com/pumpernickel/data/repository/FoodRepositoryImpl.kt</files>
  <read_first>
    - The file identified as root cause in Task 1
    - Adjacent files in the data path (so the fix does not break the trace)
  </read_first>
  <action>
    Apply ONE of the following depending on Task 1's verdict:

    Variant API — OFF returns zero:
    1. Do NOT silently persist a zero-macro Food. In RecipeCreationViewModel.OnBarcodeScanned, when the macros are all zero (or below a sentinel like < 1 kcal), expose a UI state flag `barcodeHasNoMacros = true` and surface a hint in the UI: "OpenFoodFacts hat für dieses Produkt keine Nährwerte. Bitte manuell eintragen."
    2. The user can still save the Food (with name + barcode + kcal=0 fields editable in the manual-entry form). Pre-fill name from the OFF response, leave macros blank.
    3. Add D-21-03 comment near the new branch.

    Variant Adapter — mapping bug:
    1. Likely culprit: confusing energyKcal100g vs energy100g (kJ), or reading nutriments_grams when OFF returns nutriments_serving_size. Read the actual DTO field names from OpenFoodFactsDto.kt.
    2. Fix the field assignment in OpenFoodFactsAdapter.lookupBarcode mapping. Prefer `energy-kcal_100g` (German + International). If only `energy_100g` is available, divide by 4.184 to convert kJ -> kcal.
    3. Add D-21-03 fix comment.

    Variant UseCase / VM / Repo — copy bug:
    1. Identify the specific point where values become zero (e.g. a `.copy(kcalPer100g = 0)` somewhere, or a missing constructor arg, or `food.kcalPer100g` shadowed by a local `kcalPer100g = 0` variable).
    2. Fix the assignment.
    3. Add D-21-03 fix comment.

    For ALL variants: after the fix is verified by UAT, remove the `[B2]` `println` lines from Task 1. Leave the root-cause + fix comments.
  </action>
  <verify>
    <automated>./gradlew :shared:compileKotlinIosX64 :shared:compileKotlinAndroidMain :shared:allTests</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc 'D-21-03 fix' shared/src/commonMain/kotlin/com/pumpernickel/ | awk -F: '{s+=$2} END {print s}'` returns at least 1
    - The fix lives in ONE layer (matching the Task-1 verdict), not a shotgun edit
    - Build green: `:shared:compileKotlinIosX64`, `:shared:compileKotlinAndroidMain`, `:shared:allTests`
    - Diagnostic `[B2]` `println` lines from Task 1 are removed (`grep -rc '\[B2\]' shared/src/commonMain/kotlin/com/pumpernickel/` returns 0)
  </acceptance_criteria>
  <done>Layer-specific fix applied; logs cleaned up; build green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual UAT — barcode scan persists real macros</name>
  <files>(no source file modified — human verification only; the executor pauses for user input)</files>
  <action>Human-verification checkpoint — the executor pauses here and waits for the user. See &lt;what-built&gt; / &lt;how-to-verify&gt; below for the exact verification steps. Do not proceed until the resume-signal is received.</action>
  <verify><automated>echo "Manual verification — executor pauses for user; see how-to-verify"</automated></verify>
  <done>Human verifier responds "approved" in the resume signal after exercising all verification steps; or describes which step failed and what was observed.</done>
  <what-built>Barcode-scanned ingredients now either show real macros (when OFF has the data) or a clear UI hint (when OFF returns zero).</what-built>
  <how-to-verify>
    1. Build + launch the app on iOS simulator (or Android).
    2. Open Rezept-Edit → "Zutat hinzufügen" → Barcode-Scanner.
    3. Scan a product known to have OFF nutriments. Suggested test barcodes (verify on world.openfoodfacts.org first):
       - 4002971990126 (typical German Edeka/Rewe yoghurt, often has full nutriments)
       - Any 1L Coca-Cola barcode (very high data coverage)
    4. Expected: the resulting Food shows non-zero kcal/protein/carbs/fat. Persist and reopen the recipe to confirm DB persistence.
    5. Scan a product known to have NO OFF data (an obscure local product). Expected: clear UI hint "keine Nährwerte verfügbar" instead of silent zeros.
    6. Regression: name + barcode are still preserved correctly across both cases.
  </how-to-verify>
  <resume-signal>Type "approved" when both branches behave correctly; describe the failure otherwise.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OFF API -> Adapter | Untrusted JSON; existing HTML-shortcircuit handles malformed payloads |
| Adapter -> Repository | Untrusted-but-validated; macros bounded to non-negative by validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-08 | T (Tampering) | OFF nutriments response | accept | OFF can return wildly wrong macros; out of scope for B2. User can manually edit before persisting. |
| T-21-09 | I (Info Disclosure) | Diagnostic logs | mitigate | `[B2]` logs print macros (not PII); removed after Task 2 verification. |
</threat_model>

<verification>
1. Build green: `:shared:compileKotlinIosX64`, `:shared:compileKotlinAndroidMain`, `:shared:allTests`
2. Manual UAT (Task 3) — known-good barcode persists real macros; known-empty barcode shows hint
3. Diagnostic logs removed
</verification>

<success_criteria>
B2 ist gefixt: Barcode-Scan persistiert reale OFF-Macros, oder zeigt einen ehrlichen UI-Hinweis bei fehlenden Daten. Root-cause layer ist im Code dokumentiert.
</success_criteria>

<output>
After completion, create `.planning/phases/21-bug-wave-aus-ios-demo-test-2026-05-18-ai-workout-state-loss-/21-05-SUMMARY.md`.
</output>
