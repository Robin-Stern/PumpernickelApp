---
phase: 21-bug-wave-aus-ios-demo-test-2026-05-18-ai-workout-state-loss-
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - iosApp/iosApp/Views/Nutrition/NutritionFoodEntryView.swift
autonomous: false
requirements: []
must_haves:
  truths:
    - "Pressing Return on the iOS Daily-Log food-search field collapses the keyboard but DOES NOT clear `uiState.remoteSearchResults` and DOES NOT cancel the remote search job"
    - "Visible search results survive a `.onSubmit` event"
    - "No code path in NutritionFoodEntryView calls a clear/reset on the ViewModel as part of submit"
  artifacts:
    - path: "iosApp/iosApp/Views/Nutrition/NutritionFoodEntryView.swift"
      provides: "Daily-Log search TextField with neutral .onSubmit handler (collapse keyboard only)"
      contains: ".onSubmit"
  key_links:
    - from: "iosApp/iosApp/Views/Nutrition/NutritionFoodEntryView.swift TextField near line 271"
      to: "focusedField state"
      via: ".onSubmit { focusedField = false }"
      pattern: ".onSubmit"
---

<objective>
B4 — Tippt der User in der Daily-Log Lebensmittel-Suche Return, kollabieren die remote search results sofort zu "Keine Ergebnisse". Vermutlich triggert SwiftUI `.submitLabel(.search)` einen impliziten Action-Handler oder es existiert ein `.onSubmit`-Handler, der den ViewModel-State leert. Fix per D-21-05: `.onSubmit` neutralisieren — nur Tastatur einklappen via `focusedField = false`, keinen Job canceln, keinen State clearen.

Purpose: User kann nach Eingeben einer Query mit Return die Tastatur runter-collabsen ohne seine Suchresultate zu verlieren (B4 in CONTEXT.md `<domain>`; locked by D-21-05).
Output: Edit in `NutritionFoodEntryView.swift` (Single-File-Fix, iOS-only).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

Honor D-21-05 (iOS-only fix in NutritionFoodEntryView.swift; `.onSubmit` neutral, no clear, no job cancel). D-21-08 — B4 is the documented exception (UI-only iOS bug, kein commonMain-Anteil). D-21-09 — kein automatisierter Test (UI-Behavior, manuelle Verifikation).
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-bug-wave-aus-ios-demo-test-2026-05-18-ai-workout-state-loss-/21-CONTEXT.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- Current shape of the search TextField, line ~271-285 -->
From iosApp/iosApp/Views/Nutrition/NutritionFoodEntryView.swift:
```swift
TextField("Suchen", text: Binding(
    get: { uiState.searchQuery },
    set: { viewModel.onEvent(event: FoodEntryEventOnSearchQueryChanged(value: $0)) }
))
.textFieldStyle(.roundedBorder)
.submitLabel(.search)
.focused($focusedField)
```

Plus an existing top-level tap-handler at line 48:
```swift
.onTapGesture { focusedField = false }
```

And a `Fertig` toolbar button at line 62 that calls `focusedField = false`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Investigate which event collapses results</name>
  <files>iosApp/iosApp/Views/Nutrition/NutritionFoodEntryView.swift</files>
  <read_first>
    - iosApp/iosApp/Views/Nutrition/NutritionFoodEntryView.swift — read the entire file (444 lines)
    - Especially: lines 260-300 (search TextField block), lines 1-60 (state holders), and any `.onChange` / `.onSubmit` / `.task` modifiers attached to the search TextField or its enclosing VStack
    - Grep within the file: `grep -n "onSubmit\|searchQuery\|onEvent\|clear\|reset\|Remote" iosApp/iosApp/Views/Nutrition/NutritionFoodEntryView.swift`
  </read_first>
  <action>
    1. Identify the exact mechanism by which Return collapses results. Three likely culprits per D-21-05:
       (a) An explicit `.onSubmit { … }` on the TextField calls `onEvent(FoodEntryEventOnSearchQueryChanged(value: ""))` or a similar clear event.
       (b) `.onChange(of: focusedField)` triggers a clear when focus is lost.
       (c) `.submitLabel(.search)` combined with an enclosing form/section's default submit action clears state.
    2. Document the finding inline as a code comment in the file you will edit next, prefixed `// B4 root-cause:`. Format: `// B4 root-cause: <one-line description of which modifier triggers the clear>`.
    3. If the root cause is NOT obvious from the file (no `.onSubmit`, no relevant `.onChange`), expand the search to:
       - the `DailyLogViewModel` if delegated, and `FoodEntryViewModel.kt` (presentation layer): grep `RemoteSearchResults\|clearRemote\|searchJob` — any state-reset call wired to a submit event.
       - parent enclosing views (Form, NavigationStack) — submit propagation rules.
    4. Output (in the file, as the comment above the TextField block): the precise root-cause string for traceability before the fix lands in Task 2.
  </action>
  <verify>
    <automated>grep -c 'B4 root-cause:' iosApp/iosApp/Views/Nutrition/NutritionFoodEntryView.swift</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'B4 root-cause:' iosApp/iosApp/Views/Nutrition/NutritionFoodEntryView.swift` returns at least 1
    - The root-cause comment identifies a specific SwiftUI modifier or ViewModel event (not a vague "submit somehow")
    - File still compiles (no syntax errors introduced by the comment): `xcodebuild ... build` green
  </acceptance_criteria>
  <done>Root cause documented as inline comment near the TextField, traceable for the fix and any future regression.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the neutral submit handler fix</name>
  <files>iosApp/iosApp/Views/Nutrition/NutritionFoodEntryView.swift</files>
  <read_first>
    - iosApp/iosApp/Views/Nutrition/NutritionFoodEntryView.swift — same file, especially the now-annotated B4 root-cause block
  </read_first>
  <action>
    Apply the fix per D-21-05, based on Task 1's root-cause finding:

    **If root cause is an existing `.onSubmit { viewModel.onEvent(...) }` (variant a):**
    Replace its body with `focusedField = false`. No call to `viewModel.onEvent`, no `searchJob?.cancel()`.

    **If no `.onSubmit` modifier exists yet (variant c — implicit submit):**
    Add an explicit `.onSubmit { focusedField = false }` modifier directly after `.submitLabel(.search)` on the search TextField at line ~275. SwiftUI's default submit action falls through to the nearest enclosing form/section when `.onSubmit` is absent; the explicit no-op handler short-circuits it.

    **If root cause is `.onChange(of: focusedField) { … }` clearing state (variant b):**
    Remove the clear from that handler; if the handler also did something legit (e.g. tracking-only), preserve that part. Do not delete the `focusedField` state.

    Required final shape near line 275:
    ```swift
    TextField("Suchen", text: Binding(
        get: { uiState.searchQuery },
        set: { viewModel.onEvent(event: FoodEntryEventOnSearchQueryChanged(value: $0)) }
    ))
    .textFieldStyle(.roundedBorder)
    .submitLabel(.search)
    .focused($focusedField)
    .onSubmit {
        // D-21-05 / B4 — neutral submit: only collapse the keyboard,
        // never call onEvent or cancel remote-search jobs.
        focusedField = false
    }
    ```

    Do NOT change `.submitLabel(.search)` (the user wants a "search" button label visually). Do NOT remove other modifiers. Do NOT touch the Recipe-Edit `.searchable` block — out of scope per CONTEXT.md (deferred: separate verification).
  </action>
  <verify>
    <automated>xcodebuild -project iosApp/iosApp.xcodeproj -scheme iosApp -configuration Debug -sdk iphonesimulator -destination 'generic/platform=iOS Simulator' build CODE_SIGNING_ALLOWED=NO | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -A2 '.submitLabel(.search)' iosApp/iosApp/Views/Nutrition/NutritionFoodEntryView.swift` shows `.onSubmit` directly after, with body containing `focusedField = false` and `// D-21-05` comment
    - `grep -c 'onEvent(event: FoodEntryEventOnSearchQueryChanged' iosApp/iosApp/Views/Nutrition/NutritionFoodEntryView.swift` does NOT increase inside an .onSubmit block (only inside the TextField binding)
    - No new `.cancel()` / `clearRemote*` / `reset*` call introduced in the file
    - xcodebuild Debug green
  </acceptance_criteria>
  <done>`.onSubmit` is neutral; remote search results survive a Return press; iOS build green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual UAT — search results survive submit</name>
  <files>(no source file modified — human verification only; the executor pauses for user input)</files>
  <action>Human-verification checkpoint — the executor pauses here and waits for the user. See &lt;what-built&gt; / &lt;how-to-verify&gt; below for the exact verification steps. Do not proceed until the resume-signal is received.</action>
  <verify><automated>echo "Manual verification — executor pauses for user; see how-to-verify"</automated></verify>
  <done>Human verifier responds "approved" in the resume signal after exercising all verification steps; or describes which step failed and what was observed.</done>
  <what-built>iOS Daily-Log search TextField now has an explicit neutral `.onSubmit` handler that only collapses the keyboard, per D-21-05.</what-built>
  <how-to-verify>
    1. Run the iOS app on the simulator (Debug build).
    2. Navigate: Nutrition tab → Daily-Log → tap "Lebensmittel hinzufügen" → switch input mode to "Suchen".
    3. Type at least 3 characters of a known query (e.g. "Joghurt"). Wait until remote search results appear in the LazyVStack.
    4. Press the Return key on the simulator keyboard (Cmd+K to toggle hardware keyboard, or use the on-screen Search button).
    5. Expected: keyboard collapses, search results remain visible, search query stays in the field.
    6. Anti-expected (regression): "Keine Ergebnisse" appears OR the results disappear OR the query is cleared.
  </how-to-verify>
  <resume-signal>Type "approved" if results survive Return; describe the regression if they don't.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → ViewModel search event | Submit action on a search field; no untrusted data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-03 | D (DoS) | Daily-Log remote search | accept | Search query length is enforced ≥3 chars; OFF rate-limit / timeout already configured upstream. No new attack surface introduced by this fix. |
</threat_model>

<verification>
1. Build: `xcodebuild -project iosApp/iosApp.xcodeproj -scheme iosApp -configuration Debug -sdk iphonesimulator -destination 'generic/platform=iOS Simulator' build` green
2. Manual UAT (Task 3) confirms results survive Return
3. Cross-platform sanity (no Android change needed — UI is platform-specific; Android uses its own search component without a SwiftUI `.submitLabel`-style implicit reset)
</verification>

<success_criteria>
B4 ist gefixt: User kann die Tastatur im Daily-Log einklappen ohne seine Suchergebnisse zu verlieren. Bug-Symptom reproduzierbar verschwunden im UAT.
</success_criteria>

<output>
After completion, create `.planning/phases/21-bug-wave-aus-ios-demo-test-2026-05-18-ai-workout-state-loss-/21-02-SUMMARY.md`.
</output>
