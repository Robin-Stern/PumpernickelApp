---
phase: 21-bug-wave-aus-ios-demo-test-2026-05-18-ai-workout-state-loss-
plan: 03
type: execute
wave: 2
depends_on: [21-01, 21-02]
files_modified:
  - shared/src/commonMain/kotlin/com/pumpernickel/data/api/OpenFoodFactsApi.kt
  - shared/src/commonMain/kotlin/com/pumpernickel/data/api/OpenFoodFactsDto.kt
  - shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/nutrition/OpenFoodFactsAdapter.kt
  - shared/src/commonTest/kotlin/com/pumpernickel/infrastructure/nutrition/OpenFoodFactsAdapterBrandRankingTest.kt
autonomous: false
requirements: []
must_haves:
  truths:
    - "OFF v2 search request includes `generic_name` and `categories_tags` in the `fields` parameter"
    - "OFF v2 search request uses a relevance-friendly `sort_by` strategy (default server relevance OR `popularity_key` — picked after empirical comparison on the 3 test queries)"
    - "Adapter brand-match re-ranking promotes results whose `brands` field contains a query word above results that only match on name"
    - "Test query 'Chips ungarisch' returns 'Funny Frisch Chips Ungarisch' as top result (or at minimum within the top 3)"
    - "Test query 'Walnüsse gut und günstig' returns a 'Walnüsse'+'gut & günstig' product as top result, not 'Sprühsahne von gut & günstig'"
    - "Test query 'Joghurt' returns a recognised Joghurt product as top result"
    - "Adapter brand-ranking logic is unit-tested in commonTest using a sample-response fixture"
  artifacts:
    - path: "shared/src/commonMain/kotlin/com/pumpernickel/data/api/OpenFoodFactsApi.kt"
      provides: "Expanded `fields` parameter and revised `sort_by` for /search"
      contains: "generic_name"
    - path: "shared/src/commonMain/kotlin/com/pumpernickel/data/api/OpenFoodFactsDto.kt"
      provides: "DTO fields for generic_name + categories_tags so the adapter can read them"
      contains: "generic_name"
    - path: "shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/nutrition/OpenFoodFactsAdapter.kt"
      provides: "Brand-match re-ranking after API response"
      contains: "brand"
    - path: "shared/src/commonTest/kotlin/com/pumpernickel/infrastructure/nutrition/OpenFoodFactsAdapterBrandRankingTest.kt"
      provides: "Unit tests for adapter re-ranking logic using fixture responses"
      contains: "class OpenFoodFactsAdapterBrandRankingTest"
  key_links:
    - from: "OpenFoodFactsAdapter.searchByQuery"
      to: "OpenFoodFactsApi.searchByName"
      via: "API returns expanded fields + adapter re-ranks before returning to UseCase"
      pattern: "searchByName"
    - from: "OpenFoodFactsAdapter.searchByQuery"
      to: "brand-match scorer"
      via: "private fun scoreResult(result: SearchProductDto, queryTokens: List<String>): Int"
      pattern: "scoreResult"
---

<objective>
B3 — OFF v2 Suche liefert irrelevante Treffer ("Chips ungarisch" → "tue gut Form") und ignoriert Brand-Match ("Walnüsse gut und günstig" → "Sprühsahne von gut & günstig"). Per D-21-04 drei kombinierte Hebel:
1. `fields`-Parameter erweitern: `generic_name`, `categories_tags` dazu (Server matched die Query in mehr Feldern).
2. `sort_by` re-evaluieren — derzeit `unique_scans_n`, default Server-Relevanz oder `popularity_key` testen; finale Auswahl per empirischem Vergleich mit den 3 Test-Queries.
3. Lokales Brand-Re-Ranking im Adapter: Query-Wörter, die in `brands` eines Treffers vorkommen, geben Score-Bonus → Sortierung nach absteigendem Score.

Purpose: OFF-Suche liefert relevante Treffer für reale User-Queries auf Deutsch; bestätigt eine saubere Baseline bevor Wave C (B2 — Barcode-0-Macros) auf den Daten arbeitet (B3 in CONTEXT.md `<domain>`; locked by D-21-04).
Output: Edits in API/DTO/Adapter + commonTest für die Re-Ranking-Logik. Test-Queries dokumentiert.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

Honor D-21-04 (drei kombinierte Maßnahmen), D-21-08 (alles commonMain — OFF läuft cross-platform), D-21-09 (B3 bekommt commonTest-Tests für Re-Ranking-Logik).
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-bug-wave-aus-ios-demo-test-2026-05-18-ai-workout-state-loss-/21-CONTEXT.md
@.planning/phases/21-bug-wave-aus-ios-demo-test-2026-05-18-ai-workout-state-loss-/21-01-PLAN.md
@.planning/phases/21-bug-wave-aus-ios-demo-test-2026-05-18-ai-workout-state-loss-/21-02-PLAN.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/INTEGRATIONS.md

<interfaces>
<!-- Current API shape -->
From shared/src/commonMain/kotlin/com/pumpernickel/data/api/OpenFoodFactsApi.kt:
```kotlin
suspend fun searchByName(query: String, pageSize: Int = 20): OpenFoodFactsSearchResponse {
    // currently:
    parameter("fields", "product_name,brands,nutriments,nutrition_grade_fr")
    parameter("sort_by", "unique_scans_n")
}
```

From shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/nutrition/OpenFoodFactsAdapter.kt:
```kotlin
override suspend fun searchByQuery(query: String, pageSize: Int): List<RemoteFoodResult> {
    val response = api.searchByName(query, pageSize)
    // currently: maps response.products → RemoteFoodResult, no local re-ranking
}
```

From shared/src/commonMain/kotlin/com/pumpernickel/domain/nutrition/RemoteFoodSearchClient.kt:
```kotlin
interface RemoteFoodSearchClient {
    suspend fun searchByQuery(query: String, pageSize: Int = 20): List<RemoteFoodResult>
    suspend fun lookupBarcode(barcode: String): RemoteBarcodeProduct?
}
```

<!-- Test queries (from CONTEXT <specifics>) -->
- "Chips ungarisch" → expect "Funny Frisch Chips Ungarisch"
- "Walnüsse gut und günstig" → expect a "Walnüsse, gut & günstig" product (NOT "Sprühsahne von gut & günstig")
- "Joghurt" → expect a recognised Joghurt brand product
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand OFF `fields` + DTO; evaluate `sort_by` empirically</name>
  <files>shared/src/commonMain/kotlin/com/pumpernickel/data/api/OpenFoodFactsApi.kt, shared/src/commonMain/kotlin/com/pumpernickel/data/api/OpenFoodFactsDto.kt</files>
  <read_first>
    - shared/src/commonMain/kotlin/com/pumpernickel/data/api/OpenFoodFactsApi.kt (39 lines)
    - shared/src/commonMain/kotlin/com/pumpernickel/data/api/OpenFoodFactsDto.kt (entire file)
    - shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/nutrition/OpenFoodFactsAdapter.kt (73 lines, to verify which DTO fields the adapter currently reads)
  </read_first>
  <action>
    1. In `OpenFoodFactsApi.kt` (line 26): change the `fields` parameter from
       `"product_name,brands,nutriments,nutrition_grade_fr"`
       to
       `"product_name,generic_name,brands,categories_tags,nutriments,nutrition_grade_fr"`.
       (Per D-21-04 step 1 — add `generic_name` and `categories_tags`; keep all existing fields so the rest of the adapter is unaffected.)
    2. `sort_by` empirical decision — pick ONE of these and document the choice as a comment:
       - **Option A** (recommended starting point per D-21-04 step 2): remove the `sort_by` parameter entirely so OFF defaults to its server-side relevance ranking.
       - **Option B**: switch to `parameter("sort_by", "popularity_key")`.
       Run a hand-test with each of the 3 test queries (see `<interfaces>`) against the live API (curl is fine — base URL `https://world.openfoodfacts.org`). Pick whichever returns the most relevant top-3 for the 3 queries.
       Examples (paste into terminal to evaluate manually before editing):
       ```
       curl -A 'PumpernickelApp/1.0 (test)' 'https://world.openfoodfacts.org/cgi/search.pl?search_terms=Chips+ungarisch&json=1&page_size=10&fields=product_name,generic_name,brands,categories_tags,nutriments,nutrition_grade_fr' | head -c 2000
       curl -A 'PumpernickelApp/1.0 (test)' 'https://world.openfoodfacts.org/cgi/search.pl?search_terms=Walnüsse+gut+und+günstig&json=1&page_size=10&fields=...' | head -c 2000
       curl -A 'PumpernickelApp/1.0 (test)' 'https://world.openfoodfacts.org/cgi/search.pl?search_terms=Joghurt&json=1&page_size=10&fields=...' | head -c 2000
       ```
       Record the final choice in a comment above the parameter line:
       `// D-21-04 — sort_by: <chosen-strategy> (empirically beats <alternative> for queries Chips/Walnüsse/Joghurt)`.
    3. In `OpenFoodFactsDto.kt` extend `SearchProductDto` (or equivalent — confirm class name when reading the file):
       - add `val generic_name: String? = null`
       - add `val categories_tags: List<String>? = null`
       Keep all existing fields and the `@SerialName` conventions used by the rest of the DTOs. Mark optional + default-null so existing fixtures and historical responses continue to deserialize.
    4. Do NOT change the `lookupBarcode` endpoint. Do NOT change the User-Agent or auth-less header pattern.
  </action>
  <verify>
    <automated>./gradlew :shared:compileKotlinIosX64 :shared:compileKotlinAndroidMain</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'generic_name' shared/src/commonMain/kotlin/com/pumpernickel/data/api/OpenFoodFactsApi.kt` returns at least 1
    - `grep -c 'categories_tags' shared/src/commonMain/kotlin/com/pumpernickel/data/api/OpenFoodFactsApi.kt` returns at least 1
    - `grep -c 'generic_name' shared/src/commonMain/kotlin/com/pumpernickel/data/api/OpenFoodFactsDto.kt` returns at least 1
    - `grep -c 'categories_tags' shared/src/commonMain/kotlin/com/pumpernickel/data/api/OpenFoodFactsDto.kt` returns at least 1
    - `grep -c 'D-21-04' shared/src/commonMain/kotlin/com/pumpernickel/data/api/OpenFoodFactsApi.kt` returns at least 1 (sort_by decision comment)
    - `grep -c 'unique_scans_n' shared/src/commonMain/kotlin/com/pumpernickel/data/api/OpenFoodFactsApi.kt` returns 0 (old strategy removed; if Option B was chosen instead, this becomes 0 because popularity_key replaces it; if Option A was chosen, the line is removed entirely)
    - `./gradlew :shared:compileKotlinIosX64 :shared:compileKotlinAndroidMain` green
  </acceptance_criteria>
  <done>`fields` expanded + DTO carries the new fields + `sort_by` strategy documented with empirical rationale. Build green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Adapter-side brand-match re-ranking + unit test</name>
  <files>shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/nutrition/OpenFoodFactsAdapter.kt, shared/src/commonTest/kotlin/com/pumpernickel/infrastructure/nutrition/OpenFoodFactsAdapterBrandRankingTest.kt</files>
  <read_first>
    - shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/nutrition/OpenFoodFactsAdapter.kt (entire 73-line file; especially the `searchByQuery` mapping)
    - shared/src/commonMain/kotlin/com/pumpernickel/domain/nutrition/RemoteFoodSearchClient.kt (for `RemoteFoodResult` shape — what fields exist on the port-level result type)
    - shared/src/commonMain/kotlin/com/pumpernickel/data/api/OpenFoodFactsDto.kt (post Task 1 — to confirm `brands` access)
    - shared/src/commonTest/kotlin/com/pumpernickel/ — one existing test file to copy import conventions
  </read_first>
  <behavior>
    Adapter re-ranking — per D-21-04 step 3 + Claude's-Discretion note ("simpler Score (brand-match → +2, name-match → +1) reicht erstmal"):

    For each query-word (case-insensitive, after splitting on whitespace and removing tokens of length < 2):
    - +2 score if any token of `result.brands` (split on comma/whitespace) equals the query word
    - +1 score if `result.product_name` (case-insensitive) contains the query word
    - +1 score if `result.generic_name` (case-insensitive) contains the query word
    Multi-word queries: sum across all query words. Sort the result list in descending score order. Ties keep their server-side order (stable sort).

    Test cases (using inline `SearchProductDto` fixtures):
    1. Query "Chips ungarisch", products = [
         {product_name: "Tue Gut Form", brands: "Tue Gut"},  // server top, score 0
         {product_name: "Chips Ungarisch", brands: "Funny Frisch"}, // score 2 (chips+ungarisch in name)
       ] → expected order: [Chips Ungarisch, Tue Gut Form]
    2. Query "Walnüsse gut und günstig", products = [
         {product_name: "Sprühsahne", brands: "gut & günstig"},  // brand-match on "gut" + "günstig" → 4
         {product_name: "Walnüsse", brands: "gut & günstig"},    // brand-match + name-match on Walnüsse → 5
       ] → expected: [Walnüsse first, Sprühsahne second]
    3. Query "Joghurt", products = [
         {product_name: "Joghurt natur", brands: "Alpro"}  // score 1
       ] → expected: [Joghurt natur] (single result, trivially sorted)
    4. Brand-token boundary test: query "alpro", product {brands: "Alpro Soja"} → brand match (case-insensitive on tokenised brand-list, NOT substring match like "alproletariat" wouldn't match).
    5. Stable sort test: two products both score 0 → preserve server order.
  </behavior>
  <action>
    1. In `OpenFoodFactsAdapter.kt`:
       - Add a private function:
         ```kotlin
         // D-21-04 step 3 — local brand-match re-ranking
         internal fun scoreResult(productName: String?, genericName: String?, brands: String?, queryTokens: List<String>): Int {
             if (queryTokens.isEmpty()) return 0
             val nameLower = productName.orEmpty().lowercase()
             val genericLower = genericName.orEmpty().lowercase()
             val brandTokens = brands.orEmpty().split(',', ' ').map { it.trim().lowercase() }.filter { it.isNotEmpty() }
             var score = 0
             for (q in queryTokens) {
                 if (brandTokens.contains(q)) score += 2
                 if (nameLower.contains(q)) score += 1
                 if (genericLower.contains(q)) score += 1
             }
             return score
         }
         ```
         Mark `internal` (not private) so the commonTest can call it directly without reflection.
       - In `searchByQuery`, after mapping the DTO list to `RemoteFoodResult`, sort:
         ```kotlin
         val tokens = query.lowercase().split(Regex("\\s+")).filter { it.length >= 2 }
         val ranked = response.products.orEmpty()
             .mapIndexed { idx, dto -> Triple(idx, dto, scoreResult(dto.product_name, dto.generic_name, dto.brands, tokens)) }
             .sortedWith(compareByDescending<Triple<Int, SearchProductDto, Int>> { it.third }.thenBy { it.first })
             .map { (_, dto, _) -> /* existing dto → RemoteFoodResult mapping moved here */ }
         ```
         Keep the existing HTML-error-shortcircuit, the existing nutriments mapping, and the existing pageSize cap.
    2. Create test file `shared/src/commonTest/kotlin/com/pumpernickel/infrastructure/nutrition/OpenFoodFactsAdapterBrandRankingTest.kt`:
       - Test `scoreResult` directly with the 5 cases in `<behavior>`.
       - Additionally test the end-to-end ordering: construct an `OpenFoodFactsAdapter` with a stubbed `OpenFoodFactsApi` returning a canned `OpenFoodFactsSearchResponse`, call `adapter.searchByQuery("Chips ungarisch", pageSize = 10)`, assert top result is "Chips Ungarisch".
       - If `OpenFoodFactsApi` is an `open class` it can be subclassed; if it is `final`, extract a thin interface or expose the response via a constructor-injected lambda — keep the change small and document with `// D-21-04 — testability seam`.
    3. Do NOT change the `lookupBarcode` path. Do NOT change `RemoteFoodSearchClient` interface. Do NOT touch domain layer.
  </action>
  <verify>
    <automated>./gradlew :shared:compileKotlinIosX64 :shared:compileKotlinAndroidMain :shared:allTests --tests "com.pumpernickel.infrastructure.nutrition.OpenFoodFactsAdapterBrandRankingTest"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'fun scoreResult' shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/nutrition/OpenFoodFactsAdapter.kt` returns 1
    - `grep -c 'sortedWith\|sortedByDescending' shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/nutrition/OpenFoodFactsAdapter.kt` returns at least 1
    - `grep -c '@Test' shared/src/commonTest/kotlin/com/pumpernickel/infrastructure/nutrition/OpenFoodFactsAdapterBrandRankingTest.kt` returns at least 5 (one per case-group)
    - `grep -c 'Chips Ungarisch\|Walnüsse\|Joghurt' shared/src/commonTest/kotlin/com/pumpernickel/infrastructure/nutrition/OpenFoodFactsAdapterBrandRankingTest.kt` returns at least 3
    - `./gradlew :shared:allTests` green (all prior tests still pass)
  </acceptance_criteria>
  <done>Brand-match re-ranker implemented + tested; the three CONTEXT.md spec queries produce the expected top result in the unit tests.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual UAT — search relevance on real OFF responses</name>
  <files>(no source file modified — human verification only; the executor pauses for user input)</files>
  <action>Human-verification checkpoint — the executor pauses here and waits for the user. See &lt;what-built&gt; / &lt;how-to-verify&gt; below for the exact verification steps. Do not proceed until the resume-signal is received.</action>
  <verify><automated>echo "Manual verification — executor pauses for user; see how-to-verify"</automated></verify>
  <done>Human verifier responds "approved" in the resume signal after exercising all verification steps; or describes which step failed and what was observed.</done>
  <what-built>OFF v2 search request now includes `generic_name`+`categories_tags`, a relevance-optimised `sort_by`, and an adapter brand-match re-ranker.</what-built>
  <how-to-verify>
    1. Run the app (iOS or Android, your call — adapter is commonMain so both paths converge).
    2. Open Daily-Log → "Lebensmittel hinzufügen" → "Suchen" mode (iOS) OR equivalent on Android.
    3. Type each of these queries and observe the top result:
       - "Chips ungarisch" → expected top result contains "Chips" + "Ungarisch" (ideally "Funny Frisch Chips Ungarisch")
       - "Walnüsse gut und günstig" → expected top result contains "Walnüsse" AND has brand "gut & günstig"
       - "Joghurt" → expected top result is a recognised Joghurt product
    4. Acceptable: top-1 is exactly the expected product. Also acceptable: expected product is in top-3.
    5. Regression check: queries that worked before should still work (e.g. "Banane", "Apfel" should still return a banana/apple in the top result).
  </how-to-verify>
  <resume-signal>Type "approved" if all 3 spec queries return relevant top-3 results; describe the failure if not.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → OpenFoodFacts public API | Unauthenticated HTTPS endpoint, no PII transmitted beyond the user query |
| OFF API → Adapter | Untrusted JSON; existing HTML-error shortcircuit already catches HTML masquerading as JSON |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-04 | T (Tampering) | OFF API response | mitigate | Existing HTML-shortcircuit in `OpenFoodFactsApi.searchByName` stays; new optional DTO fields default to null so malformed JSON does not crash deserialization. |
| T-21-05 | I (Info Disclosure) | OFF query | accept | Search query is sent unencrypted-from-the-app-perspective via HTTPS to a public API; users implicitly consent by using the feature. No new exposure surface. |
</threat_model>

<verification>
1. Build: `./gradlew :shared:compileKotlinIosX64 :shared:compileKotlinAndroidMain :shared:allTests` green
2. Unit tests: new `OpenFoodFactsAdapterBrandRankingTest` cases all pass
3. Manual UAT (Task 3): all 3 spec queries produce relevant top-3 results, no regression on baseline queries
</verification>

<success_criteria>
B3 ist gefixt: OFF-Suche liefert für die 3 dokumentierten Queries einen sinnvollen Top-3-Treffer; Brand-Match wirkt; Adapter-Re-Ranking ist unit-tested; Phase-20-Dependency-Rule unverletzt (kein domain → data Import neu hinzu).
</success_criteria>

<output>
After completion, create `.planning/phases/21-bug-wave-aus-ios-demo-test-2026-05-18-ai-workout-state-loss-/21-03-SUMMARY.md`.
</output>
