---
phase: 20-clean-architecture-refactor-dependency-rule-fixen-repository
plan: 03
type: execute
wave: 3
depends_on: [20-02]
files_modified:
  - shared/src/commonMain/kotlin/com/pumpernickel/data/repository/TemplateRepository.kt
  - shared/src/commonMain/kotlin/com/pumpernickel/data/repository/TemplateRepositoryImpl.kt
  - shared/src/commonMain/kotlin/com/pumpernickel/domain/repository/TemplateRepository.kt
  - shared/src/commonMain/kotlin/com/pumpernickel/data/repository/ExerciseRepository.kt
  - shared/src/commonMain/kotlin/com/pumpernickel/data/repository/ExerciseRepositoryImpl.kt
  - shared/src/commonMain/kotlin/com/pumpernickel/domain/repository/ExerciseRepository.kt
  - shared/src/commonMain/kotlin/com/pumpernickel/data/repository/FoodRepository.kt
  - shared/src/commonMain/kotlin/com/pumpernickel/domain/repository/FoodRepository.kt
  - shared/src/commonMain/kotlin/com/pumpernickel/di/SharedModule.kt
autonomous: true
requirements: [D-20-01, D-20-08, D-20-09]
must_haves:
  truths:
    - "`TemplateRepository` Interface liegt in `domain/repository/`, Impl in eigener Datei `data/repository/TemplateRepositoryImpl.kt`."
    - "`ExerciseRepository` analog."
    - "`FoodRepository` Interface zieht nach `domain/repository/` (Impl bleibt unverändert in `data/repository/FoodRepositoryImpl.kt` — bereits gesplittet)."
    - "Alle Consumer-Imports zeigen auf `domain/repository/`."
    - "Koin-Bindings funktional."
  artifacts:
    - path: "shared/src/commonMain/kotlin/com/pumpernickel/domain/repository/TemplateRepository.kt"
      provides: "Interface TemplateRepository"
    - path: "shared/src/commonMain/kotlin/com/pumpernickel/domain/repository/ExerciseRepository.kt"
      provides: "Interface ExerciseRepository"
    - path: "shared/src/commonMain/kotlin/com/pumpernickel/domain/repository/FoodRepository.kt"
      provides: "Interface FoodRepository"
  key_links:
    - from: "shared/src/commonMain/kotlin/com/pumpernickel/di/SharedModule.kt"
      to: "drei neue domain-Interfaces"
      via: "single<...> { ...Impl(...) }"
      pattern: "single<(Template|Exercise|Food)Repository>"
---

# Plan 20-03: TemplateRepository + ExerciseRepository + FoodRepository → domain/repository

<objective>
Smell 1 für drei weitere Repos in einem Commit. `FoodRepository` ist trivial (Interface bereits in eigenem File — nur Move ins andere Package). `TemplateRepository` und `ExerciseRepository` brauchen Split + Move analog zu Plan 20-02.

Purpose: Mittlere Consumer-Volumen-Repos (~4-6 Konsumenten je) in einem semantisch-bündeligen Commit.

Output: 3 neue Interface-Files in `domain/repository/`, 2 neue Impl-Files in `data/repository/`, 3 alte Files gelöscht/refactored, Konsumenten-Imports aktualisiert.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-clean-architecture-refactor-dependency-rule-fixen-repository/20-CONTEXT.md
@shared/src/commonMain/kotlin/com/pumpernickel/data/repository/TemplateRepository.kt
@shared/src/commonMain/kotlin/com/pumpernickel/data/repository/ExerciseRepository.kt
@shared/src/commonMain/kotlin/com/pumpernickel/data/repository/FoodRepository.kt
@shared/src/commonMain/kotlin/com/pumpernickel/data/repository/FoodRepositoryImpl.kt
@shared/src/commonMain/kotlin/com/pumpernickel/di/SharedModule.kt
</context>

<files_modified>
- shared/src/commonMain/kotlin/com/pumpernickel/data/repository/TemplateRepository.kt  (DELETED nach Split)
- shared/src/commonMain/kotlin/com/pumpernickel/data/repository/TemplateRepositoryImpl.kt  (CREATED)
- shared/src/commonMain/kotlin/com/pumpernickel/domain/repository/TemplateRepository.kt  (CREATED)
- shared/src/commonMain/kotlin/com/pumpernickel/data/repository/ExerciseRepository.kt  (DELETED)
- shared/src/commonMain/kotlin/com/pumpernickel/data/repository/ExerciseRepositoryImpl.kt  (CREATED)
- shared/src/commonMain/kotlin/com/pumpernickel/domain/repository/ExerciseRepository.kt  (CREATED)
- shared/src/commonMain/kotlin/com/pumpernickel/data/repository/FoodRepository.kt  (DELETED — Interface zieht um)
- shared/src/commonMain/kotlin/com/pumpernickel/domain/repository/FoodRepository.kt  (CREATED — Move)
- shared/src/commonMain/kotlin/com/pumpernickel/di/SharedModule.kt  (MODIFIED — 3 Imports)
- N Consumer-Files (siehe Pre-flight grep)
</files_modified>

<files_created>
- shared/src/commonMain/kotlin/com/pumpernickel/domain/repository/TemplateRepository.kt
- shared/src/commonMain/kotlin/com/pumpernickel/data/repository/TemplateRepositoryImpl.kt
- shared/src/commonMain/kotlin/com/pumpernickel/domain/repository/ExerciseRepository.kt
- shared/src/commonMain/kotlin/com/pumpernickel/data/repository/ExerciseRepositoryImpl.kt
- shared/src/commonMain/kotlin/com/pumpernickel/domain/repository/FoodRepository.kt
</files_created>

<files_deleted_renamed>
- `data/repository/TemplateRepository.kt` → gesplittet
- `data/repository/ExerciseRepository.kt` → gesplittet
- `data/repository/FoodRepository.kt` → nur das Interface umgezogen (Datei wird gelöscht, da bereits ohne Impl); `FoodRepositoryImpl.kt` BLEIBT unverändert in `data/repository/`
</files_deleted_renamed>

<approach>
1. **Pre-flight Konsumenten-Inventar** für ALLE DREI Interfaces:
   ```
   grep -rln "import com.pumpernickel.data.repository.TemplateRepository\b" shared/ androidApp/
   grep -rln "import com.pumpernickel.data.repository.ExerciseRepository\b" shared/ androidApp/
   grep -rln "import com.pumpernickel.data.repository.FoodRepository\b" shared/ androidApp/
   ```
   Liste pro Repo aufschreiben, in `files_modified` ergänzen falls neue auftauchen.

2. **Für TemplateRepository + ExerciseRepository**: gleicher Prozess wie Plan 20-02:
   - Interface-Block isolieren → nach `domain/repository/{Name}.kt` mit Package `com.pumpernickel.domain.repository`
   - Impl-Block isolieren → nach `data/repository/{Name}Impl.kt` mit `import com.pumpernickel.domain.repository.{Name}`
   - Original-File löschen

3. **Für FoodRepository**: 
   - File `data/repository/FoodRepository.kt` ist bereits nur das Interface (per STRUCTURE.md `FoodRepository.kt:8`). Read das File, kopiere Inhalt nach `domain/repository/FoodRepository.kt`, ändere Package zu `com.pumpernickel.domain.repository`. Lösche das alte File.
   - `FoodRepositoryImpl.kt` bleibt unangetastet in `data/repository/`, nur eine neue Import-Zeile `import com.pumpernickel.domain.repository.FoodRepository` einfügen falls heute der lokale Package-Import implizit nutzt (Kotlin: gleicher Package = kein Import nötig; nach Move BRAUCHT es expliziten Import → Edit-Schritt).

4. **Consumer-Updates** (Edit-Tool pro File):
   - `import com.pumpernickel.data.repository.TemplateRepository` → `import com.pumpernickel.domain.repository.TemplateRepository`
   - dito für Exercise- und FoodRepository
   - **Wichtig:** Regex muss `\b` (word boundary) sein, damit `TemplateRepositoryImpl` und `FoodRepositoryImpl` nicht mit-getroffen werden.

5. **SharedModule.kt-Edits** (4 Bindings — Template, Exercise, Food + iOS RetroactiveWalker bleibt unverändert):
   - Add `import com.pumpernickel.data.repository.TemplateRepositoryImpl`
   - Add `import com.pumpernickel.data.repository.ExerciseRepositoryImpl`
   - `FoodRepositoryImpl` ist bereits importiert.
   - Drei `import com.pumpernickel.data.repository.{Name}` Zeilen → `import com.pumpernickel.domain.repository.{Name}`

6. **Compile-Verify:**
   - `:shared:compileCommonMainKotlinMetadata`
   - `:shared:compileDebugKotlinAndroid`
   - `:shared:compileKotlinIosX64`
   - `:androidApp:compileDebugKotlinAndroid`

7. **Test:** `:shared:allTests`.

8. **Atomic commit:** `refactor(20-03): move Template/Exercise/Food repository interfaces to domain/repository`.
</approach>

<tasks>

<task type="auto">
  <name>Task 1: TemplateRepository + ExerciseRepository splitten + Move</name>
  <files>
    shared/src/commonMain/kotlin/com/pumpernickel/domain/repository/TemplateRepository.kt,
    shared/src/commonMain/kotlin/com/pumpernickel/data/repository/TemplateRepositoryImpl.kt,
    shared/src/commonMain/kotlin/com/pumpernickel/domain/repository/ExerciseRepository.kt,
    shared/src/commonMain/kotlin/com/pumpernickel/data/repository/ExerciseRepositoryImpl.kt
  </files>
  <action>
    Pro Repo: Read das Original (156 LOC für Template, 93 LOC für Exercise). Trenne Interface- vs. Impl-Block. Erstelle Interface-File in `domain/repository/` mit Package `com.pumpernickel.domain.repository`. Erstelle Impl-File in `data/repository/` mit explizitem `import com.pumpernickel.domain.repository.{Name}`. Übernimm Imports passend (Interface: nur kotlinx-coroutines + domain-models; Impl: alles andere). Originale werden in Task 3 gelöscht.
    
    Sanity-Check: `grep -E "Entity|Dao" shared/src/commonMain/kotlin/com/pumpernickel/domain/repository/TemplateRepository.kt shared/src/commonMain/kotlin/com/pumpernickel/domain/repository/ExerciseRepository.kt` — sollte 0 Treffer haben. Falls Entity/Dao im Interface-Signature: dokumentieren im Commit, **nicht** in diesem Plan beheben.
  </action>
  <verify>
    <automated>./gradlew :shared:compileCommonMainKotlinMetadata --quiet 2>&amp;1 | tail -10</automated>
    Erwartung: BUILD SUCCESSFUL.
  </verify>
  <done>Vier neue Files vorhanden; Originale stehen noch (werden in Task 3 gelöscht).</done>
</task>

<task type="auto">
  <name>Task 2: FoodRepository-Interface umziehen + Consumer-Imports aller drei Repos updaten</name>
  <files>
    shared/src/commonMain/kotlin/com/pumpernickel/domain/repository/FoodRepository.kt,
    shared/src/commonMain/kotlin/com/pumpernickel/data/repository/FoodRepositoryImpl.kt,
    shared/src/commonMain/kotlin/com/pumpernickel/di/SharedModule.kt,
    [N Consumer-Files via grep]
  </files>
  <action>
    Phase A — FoodRepository-Move: Read `data/repository/FoodRepository.kt` (23 LOC, nur Interface). Erstelle `domain/repository/FoodRepository.kt` mit identischem Content, Package geändert auf `com.pumpernickel.domain.repository`. Adde in `FoodRepositoryImpl.kt` (154 LOC) eine `import com.pumpernickel.domain.repository.FoodRepository`-Zeile am Anfang.
    
    Phase B — Consumer-Updates für TemplateRepository, ExerciseRepository, FoodRepository: in JEDEM Consumer (via grep ermittelt) den Import-Pfad `data.repository` → `domain.repository`. Vergiss SharedModule.kt nicht: dort 3 Imports aktualisieren + 2 neue Impl-Imports (TemplateRepositoryImpl, ExerciseRepositoryImpl) hinzufügen.
  </action>
  <verify>
    <automated>./gradlew :shared:compileDebugKotlinAndroid :shared:compileKotlinIosX64 :androidApp:compileDebugKotlinAndroid --quiet 2>&amp;1 | tail -10</automated>
    Erwartung: BUILD SUCCESSFUL across all three. Zusätzlich:
    `! grep -rn "^import com.pumpernickel.data.repository\.\(Template\|Exercise\|Food\)Repository$" shared/src/ androidApp/src/` (sollte leer sein).
  </verify>
  <done>Alle Consumer auf domain/repository umgestellt. Cross-Platform-Compile grün.</done>
</task>

<task type="auto">
  <name>Task 3: Originale löschen + Final-Verify</name>
  <files>
    shared/src/commonMain/kotlin/com/pumpernickel/data/repository/TemplateRepository.kt,
    shared/src/commonMain/kotlin/com/pumpernickel/data/repository/ExerciseRepository.kt,
    shared/src/commonMain/kotlin/com/pumpernickel/data/repository/FoodRepository.kt
  </files>
  <action>
    `rm` der drei alten Files. Vollbuild + Tests. Commit.
  </action>
  <verify>
    <automated>./gradlew :shared:allTests :androidApp:compileDebugKotlinAndroid --quiet 2>&amp;1 | tail -10</automated>
    Erwartung: BUILD SUCCESSFUL, alle Tests grün. Verifikations-Grep:
    `for f in TemplateRepository ExerciseRepository FoodRepository; do test -f "shared/src/commonMain/kotlin/com/pumpernickel/domain/repository/$f.kt" || echo "MISSING $f in domain"; ! test -f "shared/src/commonMain/kotlin/com/pumpernickel/data/repository/$f.kt" || echo "STILL EXISTS in data: $f"; done`
    Erwartung: keine Output-Zeile.
  </verify>
  <done>Drei alte Files weg, alles compiled, alle Tests grün.</done>
</task>

</tasks>

<verification>
- Vollkompiler-Chain: `./gradlew :shared:compileCommonMainKotlinMetadata :shared:compileDebugKotlinAndroid :shared:compileKotlinIosX64 :androidApp:compileDebugKotlinAndroid` — BUILD SUCCESSFUL
- `./gradlew :shared:allTests` — grün
- Grep-Guard: `! grep -rn "^import com.pumpernickel.data.repository\.\(Template\|Exercise\|Food\)Repository$" shared/src/ androidApp/src/`
</verification>

<success_criteria>
- 3 Interfaces in `domain/repository/` vorhanden
- 2 neue Impl-Files in `data/repository/`, FoodRepositoryImpl unverändert
- Drei alte Files gelöscht
- Alle Konsumenten-Imports angepasst (inkl. SharedModule)
- Tests grün, Cross-Platform-Compile grün
</success_criteria>

<risks_pitfalls>
- **Word-Boundary in Grep/sed:** Beim Updaten der Imports `\b`-Pattern verwenden — sonst werden `FoodRepositoryImpl` und `TemplateRepositoryImpl` mit-gematcht und kaputt-replaced.
- **FoodRepositoryImpl-Import nach Package-Move:** Vor dem Move war Interface + Impl im selben Package (`data.repository`) — Kotlin braucht keinen expliziten Import. Nach dem Move auf `domain.repository` MUSS Impl explizit importen. Vergiss diese Zeile nicht.
- **Smell 1 + Smell 4 Konflikt bei AI-UseCases:** `LookupBarcodeUseCase` und `SearchFoodsRemoteUseCase` importieren beide `FoodRepository`. Beim Import-Update bleibt der Pfad ändert sich; der Smell-4-Fix (Trennung von `OpenFoodFactsApi`) kommt in Plan 20-06/20-07.
- **iOS Swift-Export:** `FoodRepository`/`TemplateRepository`/`ExerciseRepository` werden via KoinHelper.kt **nicht direkt** exposed (nur WorkoutRepository ist es). Sehr niedriges Swift-Risiko hier.
- **Atomic commit:** Drei Repos zusammen — semantisch gleicher Refactor-Typ, ein commit ok.
</risks_pitfalls>

<output>
After completion, create `.planning/phases/20-clean-architecture-refactor-dependency-rule-fixen-repository/20-03-SUMMARY.md`
</output>
