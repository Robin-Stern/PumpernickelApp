---
phase: 20-clean-architecture-refactor-dependency-rule-fixen-repository
plan: 02
type: execute
wave: 2
depends_on: []
files_modified:
  - shared/src/commonMain/kotlin/com/pumpernickel/data/repository/WorkoutRepository.kt
  - shared/src/commonMain/kotlin/com/pumpernickel/data/repository/WorkoutRepositoryImpl.kt
  - shared/src/commonMain/kotlin/com/pumpernickel/domain/repository/WorkoutRepository.kt
  - shared/src/commonMain/kotlin/com/pumpernickel/di/SharedModule.kt
  - shared/src/commonMain/kotlin/com/pumpernickel/presentation/workout/WorkoutSessionViewModel.kt
  - shared/src/commonMain/kotlin/com/pumpernickel/presentation/history/WorkoutHistoryViewModel.kt
  - shared/src/commonMain/kotlin/com/pumpernickel/presentation/overview/OverviewViewModel.kt
  - shared/src/commonMain/kotlin/com/pumpernickel/domain/workout/GetUndertrainedMusclesUseCase.kt
  - shared/src/commonMain/kotlin/com/pumpernickel/data/repository/RetroactiveWalker.kt
  - shared/src/commonMain/kotlin/com/pumpernickel/domain/ai/WorkoutAiUseCase.kt
  - shared/src/iosMain/kotlin/com/pumpernickel/di/KoinHelper.kt
  - androidApp/src/androidMain/kotlin/com/pumpernickel/android/ui/components/DebugGeofencePanel.kt
autonomous: true
requirements: [D-20-01, D-20-08, D-20-09]
must_haves:
  truths:
    - "`WorkoutRepository` Interface liegt in `domain/repository/`."
    - "`WorkoutRepositoryImpl` ist eine eigene Datei in `data/repository/`."
    - "Alle 7+ Consumer importieren das Interface aus `com.pumpernickel.domain.repository.WorkoutRepository`."
    - "Koin-Binding `single<WorkoutRepository> { WorkoutRepositoryImpl(...) }` bleibt funktional."
    - "iOS Swift-Build (Shared.framework) kompiliert unverändert; KoinHelper.kt exposed das Interface."
  artifacts:
    - path: "shared/src/commonMain/kotlin/com/pumpernickel/domain/repository/WorkoutRepository.kt"
      provides: "Interface WorkoutRepository (Member-Methoden 1:1 vom Status-quo übernommen)."
    - path: "shared/src/commonMain/kotlin/com/pumpernickel/data/repository/WorkoutRepositoryImpl.kt"
      provides: "Class WorkoutRepositoryImpl, implementiert das domain-Interface."
  key_links:
    - from: "shared/src/iosMain/kotlin/com/pumpernickel/di/KoinHelper.kt"
      to: "com.pumpernickel.domain.repository.WorkoutRepository"
      via: "Direktes Import + Return-Type von getWorkoutRepository()"
      pattern: "import com.pumpernickel.domain.repository.WorkoutRepository"
    - from: "shared/src/commonMain/kotlin/com/pumpernickel/di/SharedModule.kt"
      to: "domain-Interface + data-Impl"
      via: "single<WorkoutRepository> { WorkoutRepositoryImpl(...) }"
      pattern: "single<WorkoutRepository>"
---

# Plan 20-02: WorkoutRepository Interface → domain/repository, Impl → eigene Datei

<objective>
Smell 1 für `WorkoutRepository` (eines der zwei hochfrequentierten Repos): Interface aus `data/repository/WorkoutRepository.kt` nach `domain/repository/WorkoutRepository.kt` verschieben, Impl in eigene Datei `data/repository/WorkoutRepositoryImpl.kt` herauslösen. Alle Consumer-Imports updaten. Koin-Binding bleibt funktional (Typ-Argument ändert sich nur im Import-Pfad).

Purpose: Eigener Plan für `WorkoutRepository`, weil 7 Consumer (inkl. iOS `KoinHelper.kt:4,78` — kritisch für KMP-NativeCoroutines Swift-Export). Pro-Repo-Atomic-Commit-Strategie reduziert Blast-Radius bei Compile-Fehlern.

Output: 2 Datei-Moves (1 Datei wird gesplittet → 2 Dateien), 7 Consumer-Imports aktualisiert, 1 Koin-Binding-Import.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-clean-architecture-refactor-dependency-rule-fixen-repository/20-CONTEXT.md
@.planning/codebase/CONCERNS.md
@shared/src/commonMain/kotlin/com/pumpernickel/data/repository/WorkoutRepository.kt
@shared/src/commonMain/kotlin/com/pumpernickel/data/repository/FoodRepository.kt
@shared/src/commonMain/kotlin/com/pumpernickel/data/repository/FoodRepositoryImpl.kt
@shared/src/commonMain/kotlin/com/pumpernickel/di/SharedModule.kt
@shared/src/iosMain/kotlin/com/pumpernickel/di/KoinHelper.kt

<interfaces>
<!-- Ziel-Pattern (bereits etabliert für FoodRepository): -->
<!-- data/repository/FoodRepository.kt → reine Interface-Datei -->
<!-- data/repository/FoodRepositoryImpl.kt → reine Impl-Datei -->
<!-- ABER: Interface muss zusätzlich nach domain/repository/ wandern; FoodRepository wird in Plan 20-03 entsprechend umziehen. -->

<!-- Bekannte Consumer (grep -rln "import com.pumpernickel.data.repository.WorkoutRepository" shared/ androidApp/): -->
<!-- shared/src/iosMain/kotlin/com/pumpernickel/di/KoinHelper.kt -->
<!-- shared/src/commonMain/kotlin/com/pumpernickel/presentation/history/WorkoutHistoryViewModel.kt -->
<!-- shared/src/commonMain/kotlin/com/pumpernickel/presentation/workout/WorkoutSessionViewModel.kt -->
<!-- shared/src/commonMain/kotlin/com/pumpernickel/domain/workout/GetUndertrainedMusclesUseCase.kt -->
<!-- shared/src/commonMain/kotlin/com/pumpernickel/di/SharedModule.kt -->
<!-- shared/src/commonMain/kotlin/com/pumpernickel/presentation/overview/OverviewViewModel.kt -->
<!-- androidApp/src/androidMain/kotlin/com/pumpernickel/android/ui/components/DebugGeofencePanel.kt -->

<!-- Zusätzlich indirekt: data/repository/RetroactiveWalker.kt, domain/ai/WorkoutAiUseCase.kt — können WorkoutRepository nutzen. Pflicht vor Split: erneut greppen. -->
</interfaces>
</context>

<files_modified>
- shared/src/commonMain/kotlin/com/pumpernickel/data/repository/WorkoutRepository.kt  (DELETED nach Move/Split)
- shared/src/commonMain/kotlin/com/pumpernickel/data/repository/WorkoutRepositoryImpl.kt  (CREATED — Impl-Hälfte)
- shared/src/commonMain/kotlin/com/pumpernickel/domain/repository/WorkoutRepository.kt  (CREATED — Interface-Hälfte)
- shared/src/commonMain/kotlin/com/pumpernickel/di/SharedModule.kt  (MODIFIED — Import)
- shared/src/commonMain/kotlin/com/pumpernickel/presentation/workout/WorkoutSessionViewModel.kt  (MODIFIED — Import)
- shared/src/commonMain/kotlin/com/pumpernickel/presentation/history/WorkoutHistoryViewModel.kt  (MODIFIED — Import)
- shared/src/commonMain/kotlin/com/pumpernickel/presentation/overview/OverviewViewModel.kt  (MODIFIED — Import)
- shared/src/commonMain/kotlin/com/pumpernickel/domain/workout/GetUndertrainedMusclesUseCase.kt  (MODIFIED — Import)
- shared/src/commonMain/kotlin/com/pumpernickel/data/repository/RetroactiveWalker.kt  (CONDITIONAL — wenn es importiert)
- shared/src/commonMain/kotlin/com/pumpernickel/domain/ai/WorkoutAiUseCase.kt  (CONDITIONAL)
- shared/src/iosMain/kotlin/com/pumpernickel/di/KoinHelper.kt  (MODIFIED — Import + Return-Type)
- androidApp/src/androidMain/kotlin/com/pumpernickel/android/ui/components/DebugGeofencePanel.kt  (MODIFIED — Import)
</files_modified>

<files_created>
- shared/src/commonMain/kotlin/com/pumpernickel/data/repository/WorkoutRepositoryImpl.kt
- shared/src/commonMain/kotlin/com/pumpernickel/domain/repository/WorkoutRepository.kt
</files_created>

<files_deleted_renamed>
- `data/repository/WorkoutRepository.kt` → gesplittet in Interface-File (`domain/repository/WorkoutRepository.kt`) und Impl-File (`data/repository/WorkoutRepositoryImpl.kt`). Die alte Datei wird gelöscht.
</files_deleted_renamed>

<approach>
1. **Pre-flight (Konsumenten-Inventar):**
   ```
   grep -rln "import com.pumpernickel.data.repository.WorkoutRepository" shared/src/ androidApp/src/
   ```
   Erwartung: Liste der 7+ Stellen aus dem Plan-Frontmatter. Wenn weitere auftauchen (z.B. RetroactiveWalker, WorkoutAiUseCase), zur Modifikationsliste hinzufügen.

2. **Read** `data/repository/WorkoutRepository.kt:1-320`. Identifiziere:
   - Zeile X (Anfang `interface WorkoutRepository`) bis Zeile Y (`}`-Ende des Interfaces).
   - Zeile Y+1 bis EOF: `class WorkoutRepositoryImpl(...)`. Inkl. private Helper-Funktionen.
   - Top-of-file Imports: trenne, welche fürs Interface vs. für die Impl gebraucht werden.

3. **Create** `shared/src/commonMain/kotlin/com/pumpernickel/domain/repository/WorkoutRepository.kt`:
   - Package `com.pumpernickel.domain.repository`
   - Imports: nur das, was im Interface-Block referenziert wird (typisch: `kotlinx.coroutines.flow.Flow`, `com.pumpernickel.domain.model.*`, `com.pumpernickel.domain.workout.*`). KEINE `data.db.*`-Imports (falls eines erscheint, ist das ein Signal dass eine Methode noch nicht domain-rein ist — flag im Commit-Body und behebe via vorgezogenem Mapping; nicht im Rahmen dieses Plans neu refactoren).
   - Interface-Block 1:1 vom Original kopieren.

4. **Create** `shared/src/commonMain/kotlin/com/pumpernickel/data/repository/WorkoutRepositoryImpl.kt`:
   - Package `com.pumpernickel.data.repository`
   - `import com.pumpernickel.domain.repository.WorkoutRepository`
   - Restliche Imports aus dem Original (DAOs, AppDatabase-Konstanten, etc.).
   - Class-Block 1:1 vom Original kopieren. `class WorkoutRepositoryImpl(...) : WorkoutRepository`.

5. **Delete** `shared/src/commonMain/kotlin/com/pumpernickel/data/repository/WorkoutRepository.kt` (das Original, das jetzt redundant ist).

6. **Update Konsumenten-Imports** via gezielten `sed -i ''` (macOS) ODER manuell pro Datei via Edit-Tool. Beide tragbar — Edit-Tool sicherer, weil Whitespace exakt sein muss:
   - Pattern: `import com.pumpernickel.data.repository.WorkoutRepository` → `import com.pumpernickel.domain.repository.WorkoutRepository`
   - 7+ Files (Liste oben).
   - Spezialfall `iosMain/di/KoinHelper.kt:4`: Import-Zeile UND Zeile 78 `fun getWorkoutRepository(): WorkoutRepository` — der Return-Type ist nur `WorkoutRepository` ohne FQN, daher reicht der Import-Update. Trotzdem visuell verifizieren.

7. **SharedModule.kt:**
   - Import aktualisieren: `import com.pumpernickel.data.repository.WorkoutRepository` → `import com.pumpernickel.domain.repository.WorkoutRepository`
   - `import com.pumpernickel.data.repository.WorkoutRepositoryImpl` HINZUFÜGEN (vorher war Impl im selben File, kein separater Import nötig).
   - Binding-Zeile `single<WorkoutRepository> { WorkoutRepositoryImpl(...) }` bleibt unverändert in Syntax.

8. **Compile-Verify** sofort:
   - `./gradlew :shared:compileCommonMainKotlinMetadata`
   - `./gradlew :shared:compileDebugKotlinAndroid`
   - `./gradlew :shared:compileKotlinIosX64`
   - `./gradlew :androidApp:compileDebugKotlinAndroid`
   - Falls Fehler "unresolved reference: WorkoutRepository" auftauchen → grep finden, Import-Pfad fixen.

9. **Tests:**
   - `./gradlew :shared:allTests` — alle 7 commonTest grün (keine davon nutzt `WorkoutRepository`, aber dennoch Smoketest).
</approach>

<tasks>

<task type="auto">
  <name>Task 1: Interface + Impl in zwei neue Files splitten</name>
  <files>
    shared/src/commonMain/kotlin/com/pumpernickel/domain/repository/WorkoutRepository.kt,
    shared/src/commonMain/kotlin/com/pumpernickel/data/repository/WorkoutRepositoryImpl.kt
  </files>
  <action>
    Read das alte `data/repository/WorkoutRepository.kt` vollständig. Trenne Interface-Block (in das neue `domain/repository/WorkoutRepository.kt`-File mit Package `com.pumpernickel.domain.repository`) und Impl-Block (in das neue `data/repository/WorkoutRepositoryImpl.kt`-File). Impl-File `import com.pumpernickel.domain.repository.WorkoutRepository`. Imports an beiden Files nur so behalten, wie sie für ihren jeweiligen Block nötig sind — entferne tote Imports. **KRITISCH:** Falls das Interface heute eine Member-Methode mit Room-Entity-Return-Type hätte (z.B. `getActiveSessionEntity(): Flow<ActiveSessionEntity?>`), wäre das ein Smell-1-Restbleeding — die Methode bleibt im Interface, aber im Commit-Body als "Folge-Smell, separat zu adressieren" festhalten. (Spot check via `grep -E "Entity|Dao" shared/src/commonMain/kotlin/com/pumpernickel/domain/repository/WorkoutRepository.kt` — sollte idealerweise nur 0 Treffer haben.)
  </action>
  <verify>
    <automated>./gradlew :shared:compileCommonMainKotlinMetadata --quiet 2>&amp;1 | tail -10</automated>
    Erwartung: BUILD SUCCESSFUL. Beide neuen Files compilen. Zusätzlich:
    - `! grep -q "interface WorkoutRepository" shared/src/commonMain/kotlin/com/pumpernickel/data/repository/WorkoutRepositoryImpl.kt`
    - `grep -q "interface WorkoutRepository" shared/src/commonMain/kotlin/com/pumpernickel/domain/repository/WorkoutRepository.kt`
    - `grep -q "class WorkoutRepositoryImpl" shared/src/commonMain/kotlin/com/pumpernickel/data/repository/WorkoutRepositoryImpl.kt`
  </verify>
  <done>Zwei neue Files, alte Datei wird in Task 3 gelöscht. Compile-Layer-Metadata grün.</done>
</task>

<task type="auto">
  <name>Task 2: Consumer-Imports updaten</name>
  <files>
    shared/src/commonMain/kotlin/com/pumpernickel/di/SharedModule.kt,
    shared/src/commonMain/kotlin/com/pumpernickel/presentation/workout/WorkoutSessionViewModel.kt,
    shared/src/commonMain/kotlin/com/pumpernickel/presentation/history/WorkoutHistoryViewModel.kt,
    shared/src/commonMain/kotlin/com/pumpernickel/presentation/overview/OverviewViewModel.kt,
    shared/src/commonMain/kotlin/com/pumpernickel/domain/workout/GetUndertrainedMusclesUseCase.kt,
    shared/src/iosMain/kotlin/com/pumpernickel/di/KoinHelper.kt,
    androidApp/src/androidMain/kotlin/com/pumpernickel/android/ui/components/DebugGeofencePanel.kt
  </files>
  <action>
    In jedem Konsumenten-File die Zeile `import com.pumpernickel.data.repository.WorkoutRepository` durch `import com.pumpernickel.domain.repository.WorkoutRepository` ersetzen. In `SharedModule.kt` zusätzlich `import com.pumpernickel.data.repository.WorkoutRepositoryImpl` hinzufügen. Erneut `grep -rn "data.repository.WorkoutRepository" shared/ androidApp/` — sollte 0 Treffer in `import`-Zeilen liefern (nur Re-Exporte aus iOS-Framework-Generation könnten verbleiben — die sind erlaubt).
    
    Wenn weitere Konsumenten beim Greppen auftauchen (z.B. RetroactiveWalker.kt, WorkoutAiUseCase.kt), diese auch updaten und im Commit-Body listen.
  </action>
  <verify>
    <automated>./gradlew :shared:compileDebugKotlinAndroid :shared:compileKotlinIosX64 :androidApp:compileDebugKotlinAndroid --quiet 2>&amp;1 | tail -10</automated>
    Erwartung: BUILD SUCCESSFUL across all three. Zusätzlich:
    `! grep -rn "^import com.pumpernickel.data.repository.WorkoutRepository$" shared/src/ androidApp/src/` (sollte leer sein — nur Impl-Import erlaubt)
  </verify>
  <done>Alle Imports auf domain/repository umgestellt. Cross-Platform-Compile grün.</done>
</task>

<task type="auto">
  <name>Task 3: Alte data/repository/WorkoutRepository.kt löschen + Final-Verify</name>
  <files>shared/src/commonMain/kotlin/com/pumpernickel/data/repository/WorkoutRepository.kt</files>
  <action>
    `rm shared/src/commonMain/kotlin/com/pumpernickel/data/repository/WorkoutRepository.kt`. Vollständigen Build laufen lassen inkl. Tests. Bei Erfolg: commit `refactor(20-02): move WorkoutRepository interface to domain/repository, split impl into own file`.
  </action>
  <verify>
    <automated>./gradlew :shared:allTests :androidApp:compileDebugKotlinAndroid --quiet 2>&amp;1 | tail -10</automated>
    Erwartung: BUILD SUCCESSFUL, 7 Tests passed. Zusätzlich:
    - `! test -f shared/src/commonMain/kotlin/com/pumpernickel/data/repository/WorkoutRepository.kt`
    - `test -f shared/src/commonMain/kotlin/com/pumpernickel/domain/repository/WorkoutRepository.kt`
    - `test -f shared/src/commonMain/kotlin/com/pumpernickel/data/repository/WorkoutRepositoryImpl.kt`
  </verify>
  <done>Alte Datei weg, alles compiliert, alle Tests grün.</done>
</task>

</tasks>

<verification>
- `./gradlew :shared:compileCommonMainKotlinMetadata :shared:compileDebugKotlinAndroid :shared:compileKotlinIosX64 :androidApp:compileDebugKotlinAndroid` — BUILD SUCCESSFUL
- `./gradlew :shared:allTests` — alle Tests grün
- iOS Xcode-Build: `xcodebuild -workspace iosApp/iosApp.xcworkspace -scheme iosApp -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 15' build 2>&1 | tail -20` ODER **manueller Verify-Checkpoint** (Plan 20-13).
- Grep-Guard: `! grep -rn "^import com.pumpernickel.data.repository.WorkoutRepository$" shared/src/ androidApp/src/`
</verification>

<success_criteria>
- 2 neue Files vorhanden, 1 altes File weg
- 7+ Consumer importieren das Interface aus `domain/repository`
- iOS KoinHelper.kt:78 `getWorkoutRepository(): WorkoutRepository` resolved correctly (KMP-NativeCoroutines exportiert Interface-Typ statt Impl)
- Alle existierenden Tests grün
- Cross-Platform-Compile grün (Android + iOS X64)
</success_criteria>

<risks_pitfalls>
- **KMP-NativeCoroutines Swift-Export-Risiko (HOCH):** `iosMain/di/KoinHelper.kt:78 fun getWorkoutRepository(): WorkoutRepository` ist nach Swift exposed (siehe Phase 15 STATE.md Notes: KMP-Native exportiert Properties flat). Wenn das Swift-File `WorkoutEnforcementDetailView.swift` o.ä. `Shared.WorkoutRepository`-Type referenziert (was es jetzt indirekt via `KoinHelper.shared.getWorkoutRepository()` tut), bleibt der Swift-Type-Name flat `Shared.WorkoutRepository` UNVERÄNDERT — der Kotlin-Package-Pfad ist im Swift-Header irrelevant. **Aber:** Falls KMP-NativeCoroutines aufgrund des Package-Pfads das Symbol anders generiert, könnten ObjC-Mapping-Bugs erscheinen. Mitigation: Plan 20-13 (Final Verification) macht expliziten iOS-Build-Test. Wenn brennt, Symbol-Mismatch via `swift_name`-Annotation am Kotlin-Interface fixen.
- **Interface mit Room-Entity-Leak im Status-quo:** Wenn `WorkoutRepository`-Interface heute eine Method-Signature mit `Entity`-Return-Type hätte, würde das beim Move nach `domain/repository/` einen Domain→Data-Re-Import erzeugen. Pre-flight `grep -E "Entity|Dao" data/repository/WorkoutRepository.kt` — alle Treffer im Interface-Block (Zeile 18-83) prüfen. Erwartung: 0 Treffer. Falls Treffer: Methode behält Entity-Return-Type für jetzt, Smell als Folge-Concern in Commit-Body flaggen, **nicht** in diesem Plan refaktoren.
- **Import-Cycle:** Sollte nicht auftreten — `domain/repository/` darf nichts importieren, was Richtung `data/` zeigt. Falls cycle: bedeutet eine Interface-Methode nutzt `data.db.*Entity` direkt im Signaturpunkt, siehe oben.
- **Atomic commit:** Ein commit; Files 11+ — das ist ok weil semantisch ein-Ding (interface move + impl split).
- **Smell 7 (DebugGeofencePanel) wird HIER nicht gefixt:** Der Import-Update am DebugGeofencePanel.kt bleibt; der Composable importiert WorkoutRepository auch nach dem Refactor noch direkt (D-20-01 explizit out-of-scope).
</risks_pitfalls>

<output>
After completion, create `.planning/phases/20-clean-architecture-refactor-dependency-rule-fixen-repository/20-02-SUMMARY.md`
</output>
