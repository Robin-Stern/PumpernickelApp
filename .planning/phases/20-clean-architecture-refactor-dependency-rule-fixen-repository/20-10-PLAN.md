---
phase: 20-clean-architecture-refactor-dependency-rule-fixen-repository
plan: 10
type: execute
wave: 6
depends_on: []
files_modified:
  - shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/progresspic/BiometricGate.kt
  - shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/progresspic/PhotoCaptureLauncher.kt
  - shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/progresspic/PhotoVault.kt
  - shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/SecureKeyStore.kt
  - shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/notification/NotificationService.kt
  - shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/geofence/GeofenceProvider.kt
  - shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/location/LocationProvider.kt
  - shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/permissions/PermissionController.kt
  - shared/src/commonMain/kotlin/com/pumpernickel/domain/progresspic/BiometricGate.kt
  - shared/src/commonMain/kotlin/com/pumpernickel/domain/progresspic/PhotoCaptureLauncher.kt
  - shared/src/commonMain/kotlin/com/pumpernickel/domain/progresspic/PhotoVault.kt
  - shared/src/commonMain/kotlin/com/pumpernickel/domain/ai/SecureKeyStore.kt
  - shared/src/commonMain/kotlin/com/pumpernickel/domain/ai/NotificationService.kt
  - shared/src/commonMain/kotlin/com/pumpernickel/domain/geofence/GeofenceProvider.kt
  - shared/src/commonMain/kotlin/com/pumpernickel/domain/location/LocationProvider.kt
  - shared/src/commonMain/kotlin/com/pumpernickel/domain/permissions/PermissionController.kt
autonomous: true
requirements: [D-20-02, D-20-08]
must_haves:
  truths:
    - "`shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/` enthält die Sub-Pakete `ai/`, `progresspic/`, `geofence/`, `location/`, `permissions/`, `notification/`."
    - "expect-/interface-Declarations sind vom `domain/`-Layer in `infrastructure/` umgezogen — gleicher Class-Name, gleiche Signaturen."
    - "Alte `domain/`-Files existieren nicht mehr (nur in commonMain — actuals werden in Plan 20-11/20-12 nachgezogen)."
    - "**Bemerkung:** Build wird in DIESEM Plan brechen, weil die actuals in androidMain/iosMain noch auf `domain/`-Package zeigen. Plan 20-11 (Android) und 20-12 (iOS) ziehen synchron mit. Daher Verify in 20-10 ist NUR `:shared:compileCommonMainKotlinMetadata` (ohne Platform-Targets)."
  artifacts:
    - path: "shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/"
      provides: "Neues Top-Level-Layer für expect-/Port-Declarations, die OS-Infrastructure abstrahieren."
  key_links:
    - from: "commonMain/.../infrastructure/{ai,progresspic,...}/"
      to: "androidMain/iosMain actuals (Plan 20-11/12)"
      via: "Package-Pfad-Match"
      pattern: "package com.pumpernickel.infrastructure.<sub>"
---

# Plan 20-10: infrastructure/ commonMain — expect-/Port-Declarations aus domain/ extrahieren (Smell 12 / D-20-02)

<objective>
D-20-02 / Smell 12: Top-Level-Layer `com.pumpernickel.infrastructure` wird eingeführt. In `commonMain` wandern die `expect class`- und Provider-Interfaces, die heute fälschlich unter `com.pumpernickel.domain.{ai,progresspic,geofence,location,permissions}` liegen, in entsprechende `infrastructure/<sub>/`-Pakete:

| Heute | Künftig |
|-------|---------|
| `domain/ai/SecureKeyStore.kt` (expect) | `infrastructure/ai/SecureKeyStore.kt` |
| `domain/ai/NotificationService.kt` (expect) | `infrastructure/notification/NotificationService.kt` |
| `domain/progresspic/BiometricGate.kt` (expect) | `infrastructure/progresspic/BiometricGate.kt` |
| `domain/progresspic/PhotoVault.kt` (expect) | `infrastructure/progresspic/PhotoVault.kt` |
| `domain/progresspic/PhotoCaptureLauncher.kt` (expect) | `infrastructure/progresspic/PhotoCaptureLauncher.kt` |
| `domain/geofence/GeofenceProvider.kt` (interface) | `infrastructure/geofence/GeofenceProvider.kt` |
| `domain/location/LocationProvider.kt` (interface) | `infrastructure/location/LocationProvider.kt` |
| `domain/permissions/PermissionController.kt` (interface) | `infrastructure/permissions/PermissionController.kt` |

Domain-Files in `domain/{ai,progresspic,geofence,location,permissions}/` bleiben für **alle anderen** Inhalte (z.B. `domain/ai/AiPromptCatalog.kt`, `domain/ai/AiError.kt`, `domain/geofence/EarlyExitTracker.kt`, `domain/geofence/PendingGeofenceExit.kt`, `domain/permissions/LocationPermissionStatus.kt`, `domain/progresspic/ProgressGalleryTile.kt`, etc.). Nur die OS-Port-Files wandern.

**KRITISCH — Build wird zwischenzeitlich brechen:** Die platform-`actual`-Files in androidMain (`shared/src/androidMain/kotlin/com/pumpernickel/domain/ai/SecureKeyStore.android.kt` etc.) und iosMain (`shared/src/iosMain/kotlin/com/pumpernickel/domain/progresspic/BiometricGate.ios.kt` etc.) zeigen noch auf das alte `domain/`-Package. Plan 20-11 (Android-Side) und Plan 20-12 (iOS-Side) ziehen synchron mit. Beide laufen in Wave 7 NACH 20-10 und müssen für einen grünen Build BEIDE kompletten.

**Daher Verify in 20-10:** NUR `:shared:compileCommonMainKotlinMetadata` (compileCommonMainKotlinMetadata akzeptiert dass expect ohne actual ist, weil `-Xexpect-actual-classes` enabled — Schema ist gültig, aber jedes Platform-Compile (`compileDebugKotlinAndroid`/`compileKotlinIosX64`) bricht. Das wird in Plan 20-11/20-12/20-13 finalisiert.

Consumer-Imports in `commonMain` (die heute `com.pumpernickel.domain.progresspic.BiometricGate` etc. nutzen — siehe ViewModels, UseCases) müssen ebenfalls aktualisiert werden — siehe Approach Schritt 4.

Purpose: Klare Schicht-Trennung Domain (pure logic) vs. Infrastructure (OS-Ports). Vereinheitlicht mit androidMain/iosMain-Move (Plan 20-11/12).

Output: 8 neue Files im `infrastructure/`-Tree, 8 alte `domain/`-Files gelöscht, Konsumenten-Imports in commonMain aktualisiert.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-clean-architecture-refactor-dependency-rule-fixen-repository/20-CONTEXT.md
@.planning/codebase/STRUCTURE.md
@shared/src/commonMain/kotlin/com/pumpernickel/domain/ai/SecureKeyStore.kt
@shared/src/commonMain/kotlin/com/pumpernickel/domain/ai/NotificationService.kt
@shared/src/commonMain/kotlin/com/pumpernickel/domain/progresspic/BiometricGate.kt
@shared/src/commonMain/kotlin/com/pumpernickel/domain/progresspic/PhotoCaptureLauncher.kt
@shared/src/commonMain/kotlin/com/pumpernickel/domain/progresspic/PhotoVault.kt
@shared/src/commonMain/kotlin/com/pumpernickel/domain/geofence/GeofenceProvider.kt
@shared/src/commonMain/kotlin/com/pumpernickel/domain/location/LocationProvider.kt
@shared/src/commonMain/kotlin/com/pumpernickel/domain/permissions/PermissionController.kt

<interfaces>
<!-- Files die NICHT umziehen (bleiben in domain/<sub>/): -->
<!--   domain/ai/AiPromptCatalog.kt, AiError.kt, AiGenerationManager.kt, ApiKeyState.kt, -->
<!--   RecipeAi*.kt, WorkoutAi*.kt -->
<!--   domain/progresspic/ProgressGalleryTile.kt, ProgressPicture.kt, UnlockResult.kt -->
<!--   domain/geofence/EarlyExitTracker.kt, GeofenceEvent.kt, PendingGeofenceExit.kt, PendingGeofenceExitStore.kt -->
<!--   domain/location/GeoPoint.kt -->
<!--   domain/permissions/LocationPermissionStatus.kt -->

<!-- Diese Files importieren die zu verschiebenden expects/interfaces — Import-Pfad muss updaten: -->
<!--   shared/src/commonMain/kotlin/com/pumpernickel/presentation/*ViewModel.kt — viele (ai, progresspic, settings, …) -->
<!--   shared/src/commonMain/kotlin/com/pumpernickel/di/SharedModule.kt, AiModule.kt, etc. -->
<!--   shared/src/commonMain/kotlin/com/pumpernickel/domain/ai/AiGenerationManager.kt (nutzt NotificationService) -->
<!--   shared/src/commonMain/kotlin/com/pumpernickel/domain/geofence/EarlyExitTracker.kt (nutzt evtl. GeofenceProvider) -->
<!--   shared/src/commonMain/kotlin/com/pumpernickel/data/repository/SettingsRepository.kt (implements PendingGeofenceExitStore, nicht von Plan-Scope betroffen) -->
</interfaces>
</context>

<files_modified>
- shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/SecureKeyStore.kt  (CREATED — moved expect)
- shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/notification/NotificationService.kt  (CREATED — moved expect)
- shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/progresspic/BiometricGate.kt  (CREATED)
- shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/progresspic/PhotoCaptureLauncher.kt  (CREATED)
- shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/progresspic/PhotoVault.kt  (CREATED)
- shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/geofence/GeofenceProvider.kt  (CREATED)
- shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/location/LocationProvider.kt  (CREATED)
- shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/permissions/PermissionController.kt  (CREATED)
- shared/src/commonMain/kotlin/com/pumpernickel/domain/ai/SecureKeyStore.kt  (DELETED)
- shared/src/commonMain/kotlin/com/pumpernickel/domain/ai/NotificationService.kt  (DELETED)
- shared/src/commonMain/kotlin/com/pumpernickel/domain/progresspic/BiometricGate.kt  (DELETED)
- shared/src/commonMain/kotlin/com/pumpernickel/domain/progresspic/PhotoCaptureLauncher.kt  (DELETED)
- shared/src/commonMain/kotlin/com/pumpernickel/domain/progresspic/PhotoVault.kt  (DELETED)
- shared/src/commonMain/kotlin/com/pumpernickel/domain/geofence/GeofenceProvider.kt  (DELETED)
- shared/src/commonMain/kotlin/com/pumpernickel/domain/location/LocationProvider.kt  (DELETED)
- shared/src/commonMain/kotlin/com/pumpernickel/domain/permissions/PermissionController.kt  (DELETED)
- Konsumenten in `commonMain/` (ViewModels, UseCases, di-Module) — Import-Pfade aktualisiert (Approach Schritt 4)
</files_modified>

<approach>
1. **Pre-flight: Inventar der Konsumenten in commonMain:**
   ```
   for f in SecureKeyStore NotificationService BiometricGate PhotoCaptureLauncher PhotoVault GeofenceProvider LocationProvider PermissionController; do
     echo "=== $f ==="
     grep -rln "com\.pumpernickel\.domain\.\(ai\|progresspic\|geofence\|location\|permissions\)\.$f" shared/src/commonMain/
   done
   ```
   Liste alle Files. Diese müssen in Schritt 4 ihre Imports updaten.

2. **Move (8 Files):** Pro File:
   - Read das alte File komplett (es ist klein — typisch 10–40 Zeilen).
   - Erstelle das neue File im Ziel-Package mit identischem Body, nur Package-Zeile geändert (`package com.pumpernickel.infrastructure.<sub>`).
   - Lösche das alte File: `rm shared/src/commonMain/kotlin/com/pumpernickel/domain/<sub>/<name>.kt`

3. **Sub-Package-Map (D-20-02):**
   - `SecureKeyStore` → `infrastructure/ai/` (bleibt in `ai/`-Sub-Paket, gehört konzeptuell zu AI-Vertikale)
   - `NotificationService` → `infrastructure/notification/` (neues Sub-Paket — heute irreführend unter `domain/ai/`, ist generischer)
   - `BiometricGate`, `PhotoCaptureLauncher`, `PhotoVault` → `infrastructure/progresspic/`
   - `GeofenceProvider` → `infrastructure/geofence/`
   - `LocationProvider` → `infrastructure/location/`
   - `PermissionController` → `infrastructure/permissions/`

4. **Konsumenten-Imports updaten:**
   Pro File aus dem Inventar (Schritt 1):
   - `com.pumpernickel.domain.ai.SecureKeyStore` → `com.pumpernickel.infrastructure.ai.SecureKeyStore`
   - `com.pumpernickel.domain.ai.NotificationService` → `com.pumpernickel.infrastructure.notification.NotificationService`
   - `com.pumpernickel.domain.progresspic.BiometricGate` → `com.pumpernickel.infrastructure.progresspic.BiometricGate`
   - … (analog für die anderen 5)

   Erwartete betroffene Files (Schätzung):
   - `presentation/ai/AiSettingsViewModel.kt`
   - `presentation/progresspic/ProgressGalleryViewModel.kt`, `ProgressPicturePromptViewModel.kt`, `ProgressViewerViewModel.kt`
   - `domain/ai/AiGenerationManager.kt` (nutzt `NotificationService` + `ApiKeyState`)
   - `di/SharedModule.kt`, `di/AiModule.kt`, `di/PlatformModule.android.kt`, `di/PlatformModule.ios.kt`
   - `di/ProgressGalleryModule.kt`, `AchievementGalleryModule.kt`
   - Eventuell `WorkoutSessionViewModel.kt` (`PermissionController`?)

5. **Verify (in dieser Phase nur commonMain):**
   - `./gradlew :shared:compileCommonMainKotlinMetadata --quiet`
   - Erwartung: BUILD SUCCESSFUL (expect-Declarations ohne actuals sind OK weil `-Xexpect-actual-classes` set in shared/build.gradle.kts)
   - `! find shared/src/commonMain/kotlin/com/pumpernickel/domain/ai -name SecureKeyStore.kt`
   - `find shared/src/commonMain/kotlin/com/pumpernickel/infrastructure -type f | wc -l` ≥ 8
   - `! grep -rn "import com\.pumpernickel\.domain\.\(ai\)\.SecureKeyStore" shared/src/commonMain/`
   - Analog für alle 8 Klassen
   - **`:shared:compileDebugKotlinAndroid` und `:shared:compileKotlinIosX64` werden brechen** — das ist OK in diesem Plan, fixt Plan 20-11/20-12.

**Important — Atomicity-Caveat:** Dieser Plan zerstört absichtlich Platform-Compile zwischen-zustand. Wenn der Tagessstand committed werden soll, dann ist die Reihenfolge im Repo: **20-10 + 20-11 + 20-12 müssen sehr eng aufeinander folgen** (idealerweise in einer Session). Wenn ein Commit-Boundary dazwischen muss, dann splitte das nicht — sequence ohne Push.

Alternative für strenge Reviewer: Plan 20-10/20-11/20-12 als ein gemeinsamer Plan zusammenführen. Aber das ist 30+ Files in einem Commit — Plan-Sizing bevorzugt drei separate.

**Empfehlung Executor:** Plan 20-10, 20-11, 20-12 sequenziell, alle drei Commits vor dem Push. CI darf Zwischenstände sehen, lokaler Dev nicht.
</approach>

<tasks>

<task type="auto">
  <name>Task 1: 8 expect/interface-Files nach infrastructure/&lt;sub&gt;/ verschieben</name>
  <files>
    shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/SecureKeyStore.kt,
    shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/notification/NotificationService.kt,
    shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/progresspic/BiometricGate.kt,
    shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/progresspic/PhotoCaptureLauncher.kt,
    shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/progresspic/PhotoVault.kt,
    shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/geofence/GeofenceProvider.kt,
    shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/location/LocationProvider.kt,
    shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/permissions/PermissionController.kt
  </files>
  <action>
    Pro File: Read alten Inhalt, Create new File mit identischem Body und neuem Package, Delete alten File. Mapping: SecureKeyStore→ai, NotificationService→notification, BiometricGate/PhotoCaptureLauncher/PhotoVault→progresspic, GeofenceProvider→geofence, LocationProvider→location, PermissionController→permissions. **Keine Body-Änderungen** — nur Package-Zeile. Imports innerhalb der Files (z.B. `import com.pumpernickel.domain.geofence.GeofenceEvent` in GeofenceProvider) bleiben — die Domain-DTOs ziehen NICHT mit um.
  </action>
  <verify>
    <automated>./gradlew :shared:compileCommonMainKotlinMetadata --quiet 2>&amp;1 | tail -10</automated>
    Erwartung: BUILD SUCCESSFUL für commonMain. Zusätzlich:
    - `find shared/src/commonMain/kotlin/com/pumpernickel/infrastructure -name '*.kt' | wc -l` ≥ 8
    - `! test -f shared/src/commonMain/kotlin/com/pumpernickel/domain/ai/SecureKeyStore.kt`
    - `! test -f shared/src/commonMain/kotlin/com/pumpernickel/domain/progresspic/BiometricGate.kt`
    - `! test -f shared/src/commonMain/kotlin/com/pumpernickel/domain/permissions/PermissionController.kt`
  </verify>
  <done>8 Files verschoben, alte Files weg. commonMain-Compile grün (expect ohne actual = OK).</done>
</task>

<task type="auto">
  <name>Task 2: Konsumenten-Imports in commonMain aktualisieren</name>
  <files>
    shared/src/commonMain/kotlin/com/pumpernickel/presentation/ai/AiSettingsViewModel.kt,
    shared/src/commonMain/kotlin/com/pumpernickel/presentation/progresspic/ProgressGalleryViewModel.kt,
    shared/src/commonMain/kotlin/com/pumpernickel/presentation/progresspic/ProgressPicturePromptViewModel.kt,
    shared/src/commonMain/kotlin/com/pumpernickel/presentation/progresspic/ProgressViewerViewModel.kt,
    shared/src/commonMain/kotlin/com/pumpernickel/domain/ai/AiGenerationManager.kt,
    shared/src/commonMain/kotlin/com/pumpernickel/di/SharedModule.kt,
    shared/src/commonMain/kotlin/com/pumpernickel/di/AiModule.kt
  </files>
  <action>
    Erst `grep -rln "com\.pumpernickel\.domain\.\(ai\|progresspic\|geofence\|location\|permissions\)\.\(SecureKeyStore\|NotificationService\|BiometricGate\|PhotoCaptureLauncher\|PhotoVault\|GeofenceProvider\|LocationProvider\|PermissionController\)" shared/src/commonMain/` um die EXAKTE Liste der Konsumenten zu bekommen — diese ergänzt die hier vorab geschätzte. Dann pro File: jede betroffene `import …`-Zeile auf das neue `infrastructure/<sub>/`-Package umstellen. ViewModels die nur den Class-Namen referenzieren (kein FQN inline) brauchen nur Import-Update. Constructor-Param-Typen bleiben gleich (Class-Name unverändert).
  </action>
  <verify>
    <automated>./gradlew :shared:compileCommonMainKotlinMetadata --quiet 2>&amp;1 | tail -10</automated>
    Erwartung: BUILD SUCCESSFUL.
    Zusätzlich:
    - `! grep -rE "^import com\.pumpernickel\.domain\.(ai\.SecureKeyStore|ai\.NotificationService|progresspic\.BiometricGate|progresspic\.PhotoCaptureLauncher|progresspic\.PhotoVault|geofence\.GeofenceProvider|location\.LocationProvider|permissions\.PermissionController)$" shared/src/commonMain/`
  </verify>
  <done>Alle commonMain-Konsumenten importieren aus `com.pumpernickel.infrastructure.*`. commonMain-Compile grün.</done>
</task>

</tasks>

<verification>
- `./gradlew :shared:compileCommonMainKotlinMetadata` — BUILD SUCCESSFUL
- **`:shared:compileDebugKotlinAndroid` und `:shared:compileKotlinIosX64` werden brechen** — wird in Plan 20-11/20-12 gefixt.
- `find shared/src/commonMain/kotlin/com/pumpernickel/infrastructure -type f` ≥ 8 Files
- `find shared/src/commonMain/kotlin/com/pumpernickel/domain/ai -name 'SecureKeyStore.kt' -o -name 'NotificationService.kt'` — leer
- `find shared/src/commonMain/kotlin/com/pumpernickel/domain/progresspic -name 'BiometricGate.kt' -o -name 'PhotoVault.kt' -o -name 'PhotoCaptureLauncher.kt'` — leer
- `find shared/src/commonMain/kotlin/com/pumpernickel/domain/geofence -name 'GeofenceProvider.kt'` — leer (aber `EarlyExitTracker.kt`, `GeofenceEvent.kt`, `PendingGeofenceExit.kt`, `PendingGeofenceExitStore.kt` bleiben)
- `find shared/src/commonMain/kotlin/com/pumpernickel/domain/location -name 'LocationProvider.kt'` — leer (aber `GeoPoint.kt` bleibt)
- `find shared/src/commonMain/kotlin/com/pumpernickel/domain/permissions -name 'PermissionController.kt'` — leer (aber `LocationPermissionStatus.kt` bleibt)
</verification>

<success_criteria>
- 8 neue Files unter `infrastructure/` mit korrekten Sub-Paketen
- 8 alte Files unter `domain/` gelöscht
- Konsumenten-Imports in commonMain umgestellt
- `:shared:compileCommonMainKotlinMetadata` grün
- **Platform-Compile (Android/iOS) ist erwartet BROKEN** — Plan 20-11/12 fixt das. KEIN Commit-Push zwischen 20-10 und 20-11/12.
</success_criteria>

<risks_pitfalls>
- **Pitfall (KRITISCH): Intermediate broken state.** Zwischen Plan 20-10-Commit und Plan 20-11/12-Commit ist `:shared:compileDebugKotlinAndroid` und `:shared:compileKotlinIosX64` **broken**. Das ist unvermeidlich beim expect/actual-Move. **Mitigation:** Plan 20-10/11/12 in einer einzigen Execution-Session ablaufen lassen, alle drei Commits NACHEINANDER, dann Push. Bei abgebrochener Session: `git revert` Plan 20-10-Commit oder Forwards-Fix.
- **Pitfall: Doppelte Package-Imports.** Falls Konsumenten BOTH `com.pumpernickel.domain.ai.SecureKeyStore` UND `com.pumpernickel.infrastructure.ai.SecureKeyStore` importieren → Compile-Fehler "ambiguous import". Mitigation: greppe nach allen Resten und fix.
- **Pitfall: Import in den verschobenen Files SELBST.** GeofenceProvider.kt importiert evtl. `com.pumpernickel.domain.geofence.GeofenceEvent` — das bleibt, weil GeofenceEvent ein Domain-DTO ist, kein expect. Verifiziere dass der Import noch resolved.
- **KMP-NativeCoroutines:** Expects werden in KoinHelper.kt (`iosMain/di/KoinHelper.kt`) als Return-Types verwendet (z.B. `getSecureKeyStore(): SecureKeyStore`). Wenn die KoinHelper-Imports nicht in Plan 20-12 nachgezogen werden, Swift-Bridge brennt. Plan 20-12 muss das adressieren.
- **Atomic commit:** Ein commit für Plan 20-10 (8 Move + Consumer-Updates ~7 Files = ~15 Files). Großer Commit aber semantisch ein-Ding.
</risks_pitfalls>

<output>
After completion, create `.planning/phases/20-clean-architecture-refactor-dependency-rule-fixen-repository/20-10-SUMMARY.md`
</output>
</content>
</invoke>