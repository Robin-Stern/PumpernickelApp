---
phase: 20-clean-architecture-refactor-dependency-rule-fixen-repository
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/src/commonMain/kotlin/com/pumpernickel/domain/repository/EarlyExitBudgetStore.kt
  - shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/AiClient.kt
autonomous: true
requirements: [D-20-02, D-20-05, D-20-08]
must_haves:
  truths:
    - "Narrow Domain-Port `EarlyExitBudgetStore` existiert in `domain/repository/`."
    - "Generisches `AiClient`-Port-Interface existiert in `infrastructure/ai/`."
    - "Beide Files compilen ohne Imports aus `data/*`."
  artifacts:
    - path: "shared/src/commonMain/kotlin/com/pumpernickel/domain/repository/EarlyExitBudgetStore.kt"
      provides: "Narrow Port (Smell 13) — `earlyExitsRemaining: Flow<Int>` + `decrementEarlyExitBudget()`-Methoden, abgeleitet vom Status-quo in SettingsRepository."
    - path: "shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/AiClient.kt"
      provides: "Generischer AI-Port — `suspend fun complete(systemPrompt: String, userPrompt: String, schema: JsonSchemaSpec): String` (rohe Response, Domain-DTO-Parsing in den Use-Cases später). Schema-Typ wird ebenfalls als Pure-Kotlin Datenklasse in diesem File deklariert (kein Ktor-Import)."
  key_links:
    - from: "shared/src/commonMain/kotlin/com/pumpernickel/domain/repository/EarlyExitBudgetStore.kt"
      to: "kotlinx.coroutines.flow"
      via: "Flow<Int>-Return-Type"
      pattern: "import kotlinx.coroutines.flow.Flow"
    - from: "shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/AiClient.kt"
      to: "(no imports)"
      via: "Pure-Kotlin (Strings, suspend)"
      pattern: "no data.api imports"
---

# Plan 20-01: Foundation — Narrow Ports + AiClient Interface

<objective>
Wave-1-Fundament: zwei neue Port-Files anlegen, die spätere Plans als Abstraktion verwenden. Reine Datei-Erstellung, kein Move, keine Edits an Bestandscode. Keine DI-Bindings (kommen in den jeweiligen Implementierungs-Plänen). Compile-only-Verify, keine Verhaltensänderung.

Purpose: Spätere Plans (Plan 20-04 `EarlyExitTracker`-Umstellung, Plan 20-06 AI-UseCase-Entkopplung) brauchen diese Interfaces; mit dem Foundation-Wave werden sie parallelisierbar.

Output: 2 neue .kt-Files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-clean-architecture-refactor-dependency-rule-fixen-repository/20-CONTEXT.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/STRUCTURE.md
@shared/src/commonMain/kotlin/com/pumpernickel/domain/geofence/PendingGeofenceExitStore.kt
@shared/src/commonMain/kotlin/com/pumpernickel/data/repository/SettingsRepository.kt
@shared/src/commonMain/kotlin/com/pumpernickel/data/api/AiChatDto.kt
@shared/src/commonMain/kotlin/com/pumpernickel/domain/ai/WorkoutAiUseCase.kt
@shared/src/commonMain/kotlin/com/pumpernickel/domain/ai/RecipeAiUseCase.kt

<interfaces>
<!-- Vorbild für Narrow Port (existierender Status quo): -->
<!-- shared/src/commonMain/kotlin/com/pumpernickel/domain/geofence/PendingGeofenceExitStore.kt -->
<!-- ```kotlin
<!-- package com.pumpernickel.domain.geofence
<!-- import kotlinx.coroutines.flow.Flow
<!-- interface PendingGeofenceExitStore {
<!--   val pendingExit: Flow<PendingGeofenceExit?>
<!--   suspend fun setPendingExit(exit: PendingGeofenceExit?)
<!-- }
<!-- ``` -->

<!-- AiClient muss schemagetrieben sein, weil sowohl WorkoutAiUseCase als auch RecipeAiUseCase
     OpenAICompatibleClient.requestJson(...) mit unterschiedlichen JsonSchemaSpecs aufrufen. -->
</interfaces>
</context>

<files_modified>
- shared/src/commonMain/kotlin/com/pumpernickel/domain/repository/EarlyExitBudgetStore.kt  (CREATED)
- shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/AiClient.kt  (CREATED)
</files_modified>

<files_created>
- shared/src/commonMain/kotlin/com/pumpernickel/domain/repository/EarlyExitBudgetStore.kt
- shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/AiClient.kt
</files_created>

<files_deleted_renamed>
(none)
</files_deleted_renamed>

<approach>
1. **Read** `domain/geofence/PendingGeofenceExitStore.kt` als Pattern-Vorbild — Stil und Imports übernehmen.
2. **Read** `data/repository/SettingsRepository.kt` und identifiziere genau die Felder/Methoden, die `EarlyExitTracker` heute auf `SettingsRepository` aufruft (typischerweise: ein `Flow<Int>` für aktuelles Budget + eine `suspend fun` zum Dekrementieren). Spiegele exakt diese Oberfläche im neuen Port, nichts mehr. Belege via `grep -n "settingsRepository\." shared/src/commonMain/kotlin/com/pumpernickel/domain/geofence/EarlyExitTracker.kt` welche Aufrufe das sind.
3. **Create** `shared/src/commonMain/kotlin/com/pumpernickel/domain/repository/EarlyExitBudgetStore.kt`:
   - Package `com.pumpernickel.domain.repository`
   - `interface EarlyExitBudgetStore { val earlyExitsRemaining: Flow<Int>; suspend fun consumeEarlyExit() }` (Methoden-Namen an Real-API in SettingsRepo anpassen, falls Schritt 2 abweichende Signaturen ergibt)
   - Keine Imports außer `kotlinx.coroutines.flow.Flow`
4. **Read** `data/api/AiChatDto.kt`, suche `JsonSchemaSpec` / `ResponseFormat` und replizier die minimale schemagetriebene Oberfläche als Pure-Kotlin in `infrastructure/ai/AiClient.kt` (Strings + Data Classes, KEIN Ktor-Import, KEINE kotlinx-serialization-`@Serializable`-Annotation — das bleibt im Adapter).
5. **Create** `shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/AiClient.kt`:
   - Package `com.pumpernickel.infrastructure.ai`
   - `data class AiJsonSchema(val name: String, val schemaJson: String)` (oder semantisch äquivalent — Schema als String, weil Use-Cases heute auch via JSON-String-Schema reden)
   - `interface AiClient { suspend fun completeJson(systemPrompt: String, userPrompt: String, schema: AiJsonSchema, model: String?, baseUrl: String, apiKey: String): String }` (Argumente an aktuellen `OpenAICompatibleClient.requestJson`-Aufruf-Signaturen anpassen)
   - Falls die heute existierende API kein einheitliches Schema-Argument trägt, dann zwei `suspend fun completeWorkout` / `completeRecipe` als Pragmatiklösung — aber generisch ist bevorzugt (D-20 Specifics, Default-Empfehlung).
6. **Compile-Verify** nur `:shared:compileCommonMainKotlinMetadata` — beide neue Files sind self-contained.

**Important:** Keine Bindings in Koin-Modules dieser Plan, keine Impls. Die Wires kommen in Plan 20-04 (EarlyExitTracker-Umstellung) bzw. Plan 20-06 (AiClientImpl als Adapter).

**Important:** Falls beim Lesen von `WorkoutAiUseCase.kt`/`RecipeAiUseCase.kt` klar wird, dass eine generische `completeJson(...)` nicht ohne Verlust beide Use-Cases bedienen kann (z.B. weil RecipeAi multi-message chat history hält), dann zwei spezialisierte Methoden in einem `AiClient`-Interface ist akzeptabel. Entscheidung dokumentieren in Commit-Message.
</approach>

<tasks>

<task type="auto">
  <name>Task 1: EarlyExitBudgetStore-Port anlegen</name>
  <files>shared/src/commonMain/kotlin/com/pumpernickel/domain/repository/EarlyExitBudgetStore.kt</files>
  <action>
    Erstelle das File mit Package `com.pumpernickel.domain.repository`. Interface deklariert die minimale Oberfläche, die `EarlyExitTracker` aktuell auf `SettingsRepository` aufruft. Vorbild-Style: `domain/geofence/PendingGeofenceExitStore.kt`. Belege durch vorherigen `grep -n "settingsRepository\." shared/src/commonMain/kotlin/com/pumpernickel/domain/geofence/EarlyExitTracker.kt`, welche genauen Methoden migriert werden — implementiere nur diese.
  </action>
  <verify>
    <automated>./gradlew :shared:compileCommonMainKotlinMetadata --quiet 2>&amp;1 | tail -5</automated>
    Erwartung: BUILD SUCCESSFUL, keine neuen Warnings. `! grep -E "com.pumpernickel.data" shared/src/commonMain/kotlin/com/pumpernickel/domain/repository/EarlyExitBudgetStore.kt` (sollte leer sein).
  </verify>
  <done>File existiert, compiled, importiert NICHTS aus `com.pumpernickel.data.*`.</done>
</task>

<task type="auto">
  <name>Task 2: AiClient-Port + AiJsonSchema-Datenklasse anlegen</name>
  <files>shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/AiClient.kt</files>
  <action>
    Erstelle das File mit Package `com.pumpernickel.infrastructure.ai`. Enthält `data class AiJsonSchema(...)` als Pure-Kotlin-Repräsentation des Status-quo-`JsonSchemaSpec` aus `data/api/AiChatDto.kt` (aber OHNE `@Serializable`-Annotation; das Mapping zur Ktor-DTO ist Aufgabe des Adapters in Plan 20-06). Interface `AiClient` mit einer schemagetriebenen `suspend fun completeJson(...)`-Methode. Argumente: pragmatisch — schaue welche Aufruf-Stelle in `WorkoutAiUseCase` und `RecipeAiUseCase` heute `OpenAICompatibleClient` nutzt, leite davon die Signatur ab. KEINE Ktor-/kotlinx-serialization-Imports.
  </action>
  <verify>
    <automated>./gradlew :shared:compileCommonMainKotlinMetadata --quiet 2>&amp;1 | tail -5</automated>
    Erwartung: BUILD SUCCESSFUL. `! grep -E "io.ktor|kotlinx.serialization" shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/AiClient.kt` (sollte leer sein).
  </verify>
  <done>File existiert, compiled, keine Framework-Imports. AiJsonSchema-Datenklasse ist deklariert.</done>
</task>

</tasks>

<verification>
- `./gradlew :shared:compileCommonMainKotlinMetadata --quiet` — BUILD SUCCESSFUL
- `./gradlew :shared:compileDebugKotlinAndroid --quiet` — BUILD SUCCESSFUL (Smoketest dass nichts existierendes bricht)
- `find shared/src/commonMain/kotlin/com/pumpernickel/infrastructure -type f` — listet das eine neue AiClient.kt File, infrastructure-Tree existiert
- `find shared/src/commonMain/kotlin/com/pumpernickel/domain/repository -type f` — listet das eine neue EarlyExitBudgetStore.kt File
</verification>

<success_criteria>
- 2 neue .kt-Files unter `domain/repository/` und `infrastructure/ai/`
- Beide compilen
- Beide importieren NICHTS aus `com.pumpernickel.data.*`, `io.ktor.*`, `kotlinx.serialization.*`
- Tests in `commonTest` weiterhin grün (`./gradlew :shared:allTests --quiet`)
- iOS-Compile als Sanity (`./gradlew :shared:compileKotlinIosX64 --quiet`)
</success_criteria>

<risks_pitfalls>
- **Pitfall:** Die schema-getriebene `AiClient`-Oberfläche zu eng/zu generisch zuschneiden. Schaue dir VORHER beide Use-Cases (`WorkoutAiUseCase.kt`, `RecipeAiUseCase.kt`) an, sammle alle Aufrufe gegen `OpenAICompatibleClient` ein, leite davon die Signatur ab. Bei Unsicherheit: zwei spezialisierte Methoden im selben Interface (akzeptabel, dokumentiere im Commit).
- **Pitfall:** Versehentlich `@Serializable` aus `kotlinx.serialization` auf `AiJsonSchema` setzen — domain/infrastructure ist frei davon. Das Mapping zu Ktor-DTO macht der Adapter (Plan 20-06).
- **Pitfall:** Imports aus `com.pumpernickel.data.api.*` einschmuggeln (z.B. `JsonSchemaSpec` direkt referenzieren statt zu replizieren). Strikte Trennung — `infrastructure/ai/` hängt nicht von `data/api/`.
- **KMP-NativeCoroutines:** Diese Pläne erzeugen keine `Flow`/`StateFlow`-Properties an Swift-exponierten Typen. KEIN Swift-Compile-Risiko in diesem Plan.
- **Atomic commit:** Eine commit für beide Files (`refactor(20-01): add EarlyExitBudgetStore + AiClient port files`).
</risks_pitfalls>

<output>
After completion, create `.planning/phases/20-clean-architecture-refactor-dependency-rule-fixen-repository/20-01-SUMMARY.md`
</output>
