---
phase: 20-clean-architecture-refactor-dependency-rule-fixen-repository
plan: 13
type: execute
wave: 8
depends_on: [20-02, 20-03, 20-04, 20-05, 20-06, 20-07, 20-08, 20-09, 20-10, 20-11, 20-12]
files_modified: []
autonomous: false
requirements: [D-20-01, D-20-02, D-20-03, D-20-04, D-20-05, D-20-06, D-20-07, D-20-08, D-20-09]
must_haves:
  truths:
    - "Dependency-Rule wirklich hält: kein File in `shared/src/commonMain/kotlin/com/pumpernickel/domain/` importiert irgendetwas aus `com.pumpernickel.data.*`."
    - "Android-Build (`./gradlew :androidApp:assembleDebug`) grün; App installierbar; Smoketest (Workout starten, Set loggen, abbrechen) funktional."
    - "iOS-Build (Xcode) grün; App installierbar; gleicher Smoketest funktional."
    - "Alle 7 commonTest grün."
    - "Manueller Geofence-Smoketest (User-side, Phase-19-Funktionalität): Region setzen → simulieren Enter/Exit → korrekte Grace-Period / Early-Exit-Dialog Verhalten."
  artifacts:
    - path: ".planning/phases/20-clean-architecture-refactor-dependency-rule-fixen-repository/20-13-SUMMARY.md"
      provides: "Verifikations-Report + Phase-20-Abschluss-Note."
  key_links: []
---

# Plan 20-13: Final Verification + iOS UAT + Phase-20-Abschluss

<objective>
Letzter Plan der Phase 20. **Keine Code-Edits** — nur Verifikation, dass alle bisherigen Pläne sauber zusammenspielen. Enthält:
1. Automatisierter Dependency-Rule-Guard (grep-Tests)
2. Cross-Platform-Build (Common + Android + iOS) für alle Targets
3. Test-Suite (commonTest + ggf. Smoketests)
4. Manueller Checkpoint: Android-Smoketest (Workout-Flow + Geofence) → User confirms
5. Manueller Checkpoint: iOS-Smoketest (gleicher Flow, Xcode-Build + Simulator) → User confirms

Wenn etwas brennt → bugfix in einem Sub-Plan oder Hot-Fix-Quick.

Purpose: Phase-20-Closing — sicherstellen dass die Refactor-Welle keine Regressionen versteckt hat. User-side Confirmation für iOS (UI-Flow).

Output: SUMMARY mit Verifikations-Report; Phase 20 als komplett markieren in ROADMAP.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-clean-architecture-refactor-dependency-rule-fixen-repository/20-CONTEXT.md
@.planning/codebase/CONCERNS.md
@.planning/phases/20-clean-architecture-refactor-dependency-rule-fixen-repository/20-01-SUMMARY.md
@.planning/phases/20-clean-architecture-refactor-dependency-rule-fixen-repository/20-02-SUMMARY.md
@.planning/phases/20-clean-architecture-refactor-dependency-rule-fixen-repository/20-03-SUMMARY.md
@.planning/phases/20-clean-architecture-refactor-dependency-rule-fixen-repository/20-04-SUMMARY.md
@.planning/phases/20-clean-architecture-refactor-dependency-rule-fixen-repository/20-05-SUMMARY.md
@.planning/phases/20-clean-architecture-refactor-dependency-rule-fixen-repository/20-06-SUMMARY.md
@.planning/phases/20-clean-architecture-refactor-dependency-rule-fixen-repository/20-07-SUMMARY.md
@.planning/phases/20-clean-architecture-refactor-dependency-rule-fixen-repository/20-08-SUMMARY.md
@.planning/phases/20-clean-architecture-refactor-dependency-rule-fixen-repository/20-09-SUMMARY.md
@.planning/phases/20-clean-architecture-refactor-dependency-rule-fixen-repository/20-10-SUMMARY.md
@.planning/phases/20-clean-architecture-refactor-dependency-rule-fixen-repository/20-11-SUMMARY.md
@.planning/phases/20-clean-architecture-refactor-dependency-rule-fixen-repository/20-12-SUMMARY.md
</context>

<approach>
1. **Automated Dependency-Rule-Guard (grep-Sweep):**
   Die Hauptaussage von Phase 20 ist: Domain importiert KEIN data.*. Das wird mit einem strikten grep verifiziert. Erwartung: 0 Treffer.
   
   ```bash
   # Hauptverbot: domain/ darf NICHT aus data/ importieren
   grep -rn "^import com\.pumpernickel\.data\." shared/src/commonMain/kotlin/com/pumpernickel/domain/ | grep -v '^#' | wc -l
   # Erwartung: 0
   
   # Nebenverbot: domain/ darf NICHT aus io.ktor importieren (außer in spezifischen Adaptern, aber die sind in infrastructure/)
   grep -rn "^import io\.ktor\." shared/src/commonMain/kotlin/com/pumpernickel/domain/ | wc -l
   # Erwartung: 0
   
   # Verbot: domain/model/ Files haben keine data.db-Imports
   grep -rn "^import com\.pumpernickel\.data\." shared/src/commonMain/kotlin/com/pumpernickel/domain/model/ | wc -l
   # Erwartung: 0
   
   # Smell-12-Verbot: domain/ enthält keine actuals (expects in infrastructure/)
   find shared/src/androidMain/kotlin/com/pumpernickel/domain -name '*.android.kt' | wc -l
   find shared/src/iosMain/kotlin/com/pumpernickel/domain -name '*.ios.kt' | wc -l
   # Erwartung: 0 / 0 (alle actuals leben jetzt in infrastructure/)
   
   # D-20-03: Android platform/ existiert nicht mehr
   test ! -d shared/src/androidMain/kotlin/com/pumpernickel/platform
   test ! -d shared/src/androidMain/kotlin/com/pumpernickel/feature
   
   # iOS data/{geofence,location,permissions} existiert nicht mehr
   test ! -d shared/src/iosMain/kotlin/com/pumpernickel/data/geofence
   test ! -d shared/src/iosMain/kotlin/com/pumpernickel/data/location
   test ! -d shared/src/iosMain/kotlin/com/pumpernickel/data/permissions
   ```
   
   Jeder failed Check ist ein Phase-20-Restleak → bugfix in Sub-Plan.

2. **Cross-Platform Compile (alle Targets):**
   ```
   ./gradlew clean :shared:compileCommonMainKotlinMetadata :shared:compileDebugKotlinAndroid :shared:compileKotlinIosX64 :shared:compileKotlinIosArm64 :shared:compileKotlinIosSimulatorArm64 :shared:linkDebugFrameworkIosSimulatorArm64 :androidApp:assembleDebug --quiet
   ```
   Erwartung: BUILD SUCCESSFUL across all targets. Falls Linkage fail (Swift-Symbol-Mismatch nach Plan 20-12) → fix per `@ObjCName`-Annotation.

3. **Test-Suite:**
   ```
   ./gradlew :shared:allTests
   ```
   Erwartung: 7/7 commonTest grün (`XpFormulaTest`, `RankLadderTest`, `StreakCalculatorTest`, `AchievementCatalogTest`, `AchievementRulesTest`, `NutritionGoalDayPolicyTest` (mit ConsumptionEntry-Signatur nach Plan 20-08), `TdeeCalculatorTest`).

4. **Koin-DI-Graph-Validierung (Android Runtime):**
   App installieren via `./gradlew :androidApp:installDebug`. App starten — Koin muss alle Bindings resolven. Falls `KoinDefinitionFoundException` oder `DefinitionAlreadyExistsException`:
   - Check `SharedModule.kt`, `AiModule.kt`, `GamificationEngineModule.kt`, `PlatformModule.android.kt` für doppelte/fehlende Bindings.
   - Häufige Ursache nach Phase 20: Repository-Bindings nutzen jetzt Interface-Typen — wenn ein Modul zweimal `single<WorkoutRepository>` bindet, brennt es.

5. **Android-UI-Smoketest (Manual Verify-Checkpoint):**
   - App starten → MainScreen erscheint, Tabs Workout/Overview/Nutrition sichtbar
   - Workout-Tab → "Start Workout" → Template wählen → Set loggen → Workout speichern
   - Overview-Tab → letzter Workout taucht in History auf
   - Nutrition-Tab → Food hinzufügen, ConsumptionEntry persistiert
   - Geofence-Pfad (Phase-19-Smoketest): Region setzen, exit simulieren (Debug-Panel) → Early-Exit-Dialog erscheint, Penalty wird vergeben
   - AI-Tab (sofern API-Key gesetzt): Workout generieren → Preview erscheint → Akzeptieren → Template gespeichert
   
6. **iOS-UI-Smoketest (Manual Verify-Checkpoint):**
   - Xcode öffnen, iosApp.xcworkspace
   - Build for Simulator → erfolgreich
   - App starten → MainTabView erscheint
   - Workout-Flow analog Android — User testet
   - Photo-Vault (`PhotoVault`-actual nach `infrastructure/progresspic/`) — Progress-Picture aufnehmen → korrekte Bio-Auth-Prompt → speichern → in Gallery sichtbar
   - SecureKeyStore (`infrastructure/ai/`) — API-Key setzen → AI-Settings testen → Workout generieren

7. **ROADMAP-Update:**
   - `.planning/ROADMAP.md` Phase-20-Block: `**Plans:** 13 plans`, Plan-Liste fertig, Status auf `[x]`.
   - Goal-Feld füllen mit dem Klartext aus D-20-CONTEXT.md §"Phase Boundary".

8. **STATE.md-Update:**
   - "Roadmap Evolution: Phase 20 completed (YYYY-MM-DD)" eintragen.
   - Decisions D-20-01 … D-20-10 als implemented markieren.

9. **SUMMARY.md schreiben:**
   - `.planning/phases/20-clean-architecture-refactor-dependency-rule-fixen-repository/20-13-SUMMARY.md` mit Verifikations-Report (Welche grep-Checks bestanden, welche Tests grün, welche manuellen Checks abgehakt).

**Atomicity:** Plan 20-13 hat keine code-edits außer ROADMAP/STATE-Markup. Falls bugfixes nötig: in einem Sub-Plan (20-13a etc.) oder einem Hot-Fix-Quick.
</approach>

<tasks>

<task type="auto">
  <name>Task 1: Dependency-Rule Grep-Sweep + Cross-Platform-Build + Tests</name>
  <files>(read-only; nur Verifikation)</files>
  <action>
    Führe die 6 grep-Guard-Checks aus Approach-Schritt 1 aus, sammle die Counts. Sind alle 0/fehlt? Wenn nicht: liste Restleaks im SUMMARY-Draft + entscheide Bugfix-Strategie (kleiner inline-Fix oder neuer Sub-Plan). Führe `./gradlew clean :shared:compileCommonMainKotlinMetadata :shared:compileDebugKotlinAndroid :shared:compileKotlinIosSimulatorArm64 :shared:linkDebugFrameworkIosSimulatorArm64 :androidApp:assembleDebug :shared:allTests` aus. Erwartung: BUILD SUCCESSFUL, 7/7 Tests passed.
  </action>
  <verify>
    <automated>./gradlew clean :shared:compileCommonMainKotlinMetadata :shared:compileDebugKotlinAndroid :shared:compileKotlinIosSimulatorArm64 :shared:linkDebugFrameworkIosSimulatorArm64 :androidApp:assembleDebug :shared:allTests 2>&amp;1 | tail -25</automated>
    Erwartung: BUILD SUCCESSFUL across all targets, 7 commonTest passed.
    Zusätzlich grep-Guards:
    - `[ $(grep -rn "^import com\.pumpernickel\.data\." shared/src/commonMain/kotlin/com/pumpernickel/domain/ | wc -l | tr -d ' ') -eq 0 ]`
    - `[ $(find shared/src/androidMain/kotlin/com/pumpernickel/domain -name '*.android.kt' 2>/dev/null | wc -l | tr -d ' ') -eq 0 ]`
    - `[ $(find shared/src/iosMain/kotlin/com/pumpernickel/domain -name '*.ios.kt' 2>/dev/null | wc -l | tr -d ' ') -eq 0 ]`
    - `! test -d shared/src/androidMain/kotlin/com/pumpernickel/platform`
    - `! test -d shared/src/androidMain/kotlin/com/pumpernickel/feature`
  </verify>
  <done>Alle Build-Targets grün, alle Tests grün, alle grep-Guards bestanden.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Android-UI Smoketest — User verifiziert manuell</name>
  <what-built>
    Phase 20 (Clean Architecture Refactor) ist code-seitig abgeschlossen. Alle Repos sind über Interfaces gebunden, Domain-Layer ist data-frei, `infrastructure/`-Layer existiert, Android-`feature/`+`platform/`-Folders sind weg, iOS-`data/{geofence,location,permissions}` ist weg.
  </what-built>
  <how-to-verify>
    1. `./gradlew :androidApp:installDebug` — App installiert sich
    2. App starten — MainScreen lädt ohne Crash, Bottom-Nav zeigt 3 Tabs (Workout, Overview, Nutrition)
    3. **Workout-Flow:** Tab "Workout" → "Start Workout" → ein Template wählen → mindestens 1 Set loggen (Reps + Gewicht) → "Finish Workout" → speichern
    4. Tab "Overview" → der eben gespeicherte Workout taucht in der History auf, ggf. mit XP-Toast
    5. **Geofence-Smoketest (Phase 19):** Settings → Workout Enforcement → Region setzen (eigener Standort) → Workout starten → Debug-Panel öffnen → "Simulate Exit" → Grace-Period-Toast erscheint → 5 Min warten ODER Test verkürzen → Early-Exit-Dialog erscheint → "Continue Anyway" ODER "Save Workout"
    6. **Nutrition-Flow:** Tab "Nutrition" → Daily Log → Food hinzufügen (manuell oder per OpenFoodFacts-Suche, wenn Internet)
    7. **AI-Flow (sofern API-Key gesetzt):** Settings → AI Settings → API-Key prüfen → Workout-Gen-Screen → Form ausfüllen → Generate → Preview erscheint → Akzeptieren → Template-Liste zeigt das neue Template
    
    Erwartung: kein Crash, keine "Koin Definition not found"-Exception, keine "Class not found"-Errors.
  </how-to-verify>
  <resume-signal>Type "android-ok" oder beschreibe Issues.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: iOS-UI Smoketest — User verifiziert manuell (KMP-NativeCoroutines + Swift)</name>
  <what-built>
    iOS Shared.framework neu gelinkt nach `infrastructure/`-Move. Swift-Code in `iosApp/` sollte UNVERÄNDERT funktionieren, weil KMP-NativeCoroutines Class-Namen flat exportiert (kein Kotlin-Package im Swift-Namespace). KoinHelper-Files haben aktualisierte Imports, aber Method-Signaturen (z.B. `getSecureKeyStore(): SecureKeyStore`) sind unverändert.
  </what-built>
  <how-to-verify>
    1. **Xcode öffnen:** `open iosApp/iosApp.xcworkspace`
    2. **Clean Build:** Cmd-Shift-K
    3. **Build for Simulator (iPhone 15):** Cmd-B → Erwartung: BUILD SUCCESSFUL ohne "Symbol not found"-Linker-Errors oder Swift "Cannot find type 'SecureKeyStore'"
    4. **Run im Simulator:** Cmd-R → MainTabView erscheint
    5. **Workout-Flow (analog Android Task 2):** Workout-Tab → Template wählen → Set loggen → speichern → Overview zeigt History
    6. **Photo-Vault-Smoketest:** Settings → Progress Pictures → "Take Photo" → Biometric-Prompt (Face/Touch ID) → Foto aufnehmen → Speichern → in Gallery sichtbar
    7. **SecureKeyStore-Smoketest:** Settings → AI Settings → API-Key eingeben/sehen → AI-Workout-Gen testen
    8. **Geofence-Smoketest:** Workout Enforcement Settings → Region setzen → Workout starten → Standort im Simulator außerhalb der Region setzen ("Features → Location → Custom Location") → Early-Exit-Dialog sollte erscheinen
    
    Erwartung: kein App-Crash, kein "Koin Definition not found", kein Swift-Class-Not-Found. KMP-NativeCoroutines exponiert alle Typen korrekt.
    
    **WICHTIG (User-Profil): User ist iOS-fokussiert.** Wenn iOS-spezifische UI-Issues (z.B. `@State` reagiert nicht auf VM-State-Change), User-Issue → ggf. Re-Plan.
  </how-to-verify>
  <resume-signal>Type "ios-ok" oder beschreibe Issues.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: ROADMAP + STATE updaten + Phase-20-SUMMARY schreiben</name>
  <files>
    .planning/ROADMAP.md,
    .planning/STATE.md,
    .planning/phases/20-clean-architecture-refactor-dependency-rule-fixen-repository/20-13-SUMMARY.md
  </files>
  <action>
    1. `.planning/ROADMAP.md`: Phase-20-Block aktualisieren — Goal ausfüllen (aus CONTEXT.md §"Phase Boundary": "Pure Strukturarbeit am Package-Layout, damit die Dependency-Rule (presentation → domain ← data) wirklich hält. Keine neuen Features, keine Schema-Migrationen, keine Verhaltensänderungen. Acht Title-Smells gefixt: 1, 2, 3, 4, 5, 11, 12, 13."), Plans-Count auf `**Plans:** 13 plans`, Plan-Liste mit allen 13 Plänen und `[x]`-Status.
    2. `.planning/STATE.md`: Eintrag "Roadmap Evolution: Phase 20 completed (Datum)" mit Auflistung der 13 Pläne + impl. Decisions D-20-01 … D-20-10. Restleaks (falls in Task 1 gefunden) als Folge-Concerns für separate Quick-Tasks/SEEDs notieren.
    3. `20-13-SUMMARY.md` schreiben: Test-Results, grep-Guard-Counts, manuelle UAT-Status, bekannte Restthemen (z.B. out-of-scope Smells 6/7/8/9/10/14). Closing-Note: "Phase 20 closed. Domain-Layer ist data-frei. Out-of-scope-Smells in deferred Capture (Quick-Tasks oder eigene Phasen)."
  </action>
  <verify>
    <automated>grep -A 3 "Phase 20:" .planning/ROADMAP.md | head -10 &amp;&amp; test -f .planning/phases/20-clean-architecture-refactor-dependency-rule-fixen-repository/20-13-SUMMARY.md</automated>
    Erwartung: ROADMAP zeigt Phase 20 mit gefülltem Goal und 13 Plänen. SUMMARY-File existiert.
  </verify>
  <done>ROADMAP + STATE aktualisiert, SUMMARY committed.</done>
</task>

</tasks>

<verification>
- Alle Tasks dieses Plans abgeschlossen
- Android-UAT bestanden (User-Confirmation)
- iOS-UAT bestanden (User-Confirmation)
- ROADMAP zeigt Phase 20 als 13 plans, alle abgehakt
- STATE.md hat Phase-20-completed-Eintrag
- SUMMARY listet Verifikations-Status
</verification>

<success_criteria>
- 0 `import com.pumpernickel.data.*` in `shared/src/commonMain/kotlin/com/pumpernickel/domain/`
- 0 `*.android.kt` / `*.ios.kt` Files in `shared/src/{android,ios}Main/kotlin/com/pumpernickel/domain/`
- `androidMain/feature/` und `androidMain/platform/`-Folders existieren nicht mehr
- `iosMain/data/{geofence,location,permissions}/`-Folders existieren nicht mehr
- Cross-Platform-Build grün
- 7/7 commonTest grün
- Android- und iOS-Smoketests durch User bestätigt
- ROADMAP + STATE aktualisiert
- Phase 20 closed
</success_criteria>

<risks_pitfalls>
- **Pitfall: User-UAT findet UI-Regression.** Z.B. ein Composable, das nach Mapper-Move (Plan 20-06) eine Property nicht mehr aus dem Domain-Modell bekommt. Mitigation: Bei UAT-Fehler → bug minimal reproduzieren → Sub-Plan / Quick-Task starten — Phase 20 bleibt open bis fix.
- **Pitfall: Koin DefinitionAlreadyExistsException zur Runtime.** Wenn Plan 20-05 (Settings + Narrow-Ports) zwei Bindings für `SettingsRepository` erzeugt hat ODER Plan 20-07 (AiClient) ein bestehendes Binding doppelt: App crasht beim Start. Mitigation: `.planning/codebase/CONCERNS.md` aktualisieren um Reproduzier-Notiz, dann fix.
- **Pitfall: KMP-NativeCoroutines Swift-Symbol-Drift.** Wenn nach Plan 20-12 Swift `SecureKeyStore` nicht findet, kann das gefixt werden via `@ObjCName(name = "SecureKeyStore", swift_name = "SecureKeyStore")` auf der Kotlin-Class. **Falls dieser Fix nötig wird → Quick-Task `20-13-fix-objc-names` oder direkt im Plan 20-13-Sub.**
- **iOS-Geofence-Test (Phase 19) ist tricky im Simulator.** User kann das via "Features → Location → Custom Location" + Geofence-Region-Trigger machen, aber nicht alle iOS-Versionen reagieren gleich. Wenn der Test im Simulator nicht klappt, akzeptiere Hardware-Test oder Vertrau-auf-Phase-19-Suite (UAT-Result von Plan 19-06).
- **Atomic commit:** Task 4 macht 1 commit (ROADMAP+STATE+SUMMARY). Task 1-3 sind read-only. Bei UAT-Bugfixes: separate commits.
</risks_pitfalls>

<output>
After completion, create `.planning/phases/20-clean-architecture-refactor-dependency-rule-fixen-repository/20-13-SUMMARY.md`
</output>
</content>
</invoke>