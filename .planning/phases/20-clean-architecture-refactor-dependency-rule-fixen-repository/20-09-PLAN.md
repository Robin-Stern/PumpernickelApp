---
phase: 20-clean-architecture-refactor-dependency-rule-fixen-repository
plan: 09
type: execute
wave: 7
depends_on: [20-08]
files_modified:
  - shared/src/commonMain/kotlin/com/pumpernickel/domain/gamification/ApplyRetroactiveGamificationUseCase.kt
  - shared/src/commonMain/kotlin/com/pumpernickel/data/repository/RetroactiveWalker.kt
  - shared/src/commonMain/kotlin/com/pumpernickel/di/GamificationEngineModule.kt
  - shared/src/iosMain/kotlin/com/pumpernickel/di/GamificationStartupIos.kt
  - shared/src/commonMain/kotlin/com/pumpernickel/di/GamificationStartup.kt
autonomous: true
requirements: [D-20-01, D-20-06, D-20-08]
must_haves:
  truths:
    - "`RetroactiveWalker` ist nach `domain/gamification/ApplyRetroactiveGamificationUseCase` umgezogen — Class-Rename + Package-Move + File-Rename."
    - "Datei `data/repository/RetroactiveWalker.kt` existiert nicht mehr."
    - "Koin-Binding in `GamificationEngineModule.kt` referenziert den neuen Type."
    - "Konsumenten-Imports (`GamificationStartup`, `GamificationStartupIos`) aktualisiert."
    - "Existing-Verhalten 1:1: applyIfNeeded() inkl. Sentinel-Check + replay() + setRetroactiveApplied(true)."
  artifacts:
    - path: "shared/src/commonMain/kotlin/com/pumpernickel/domain/gamification/ApplyRetroactiveGamificationUseCase.kt"
      provides: "Class `ApplyRetroactiveGamificationUseCase` — ehemaliger RetroactiveWalker."
  key_links:
    - from: "shared/src/commonMain/kotlin/com/pumpernickel/di/GamificationEngineModule.kt"
      to: "ApplyRetroactiveGamificationUseCase"
      via: "single { ApplyRetroactiveGamificationUseCase(get(), get(), get(), get()) }"
      pattern: "single \\{ ApplyRetroactiveGamificationUseCase"
---

# Plan 20-09: RetroactiveWalker → ApplyRetroactiveGamificationUseCase (Smell 11)

<objective>
Smell 11 + D-20-06: `data/repository/RetroactiveWalker.kt` ist eine Domain-Orchestration (kein Repository) — wandert nach `domain/gamification/ApplyRetroactiveGamificationUseCase.kt`. Class-Rename + Package-Move + File-Rename in einem atomic commit. Koin-Binding-Name updaten. Konsumenten-Imports updaten.

Voraussetzung: Plan 20-08 hat den Walker bereits modifiziert (Entity→Domain-Mapper für ConsumptionEntry) und die Engine ist DAO-frei. Plan 20-09 ist daher rein mechanisch — kein Logic-Refactor.

Purpose: Saubere Domain-Layer-Repräsentation einer Use-Case-Sematik. Datei lebt dort, wo sie konzeptuell hingehört.

Output: 1 neue Datei in `domain/gamification/`, 1 alte Datei gelöscht, 3 Konsumenten-Imports aktualisiert, 1 Koin-Binding aktualisiert.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-clean-architecture-refactor-dependency-rule-fixen-repository/20-CONTEXT.md
@shared/src/commonMain/kotlin/com/pumpernickel/data/repository/RetroactiveWalker.kt
@shared/src/commonMain/kotlin/com/pumpernickel/di/GamificationEngineModule.kt
@shared/src/commonMain/kotlin/com/pumpernickel/di/GamificationStartup.kt
@shared/src/iosMain/kotlin/com/pumpernickel/di/GamificationStartupIos.kt

<interfaces>
<!-- Heutige Klasse (data/repository/RetroactiveWalker.kt:29): -->
<!--   class RetroactiveWalker( -->
<!--     private val engine: GamificationEngine, -->
<!--     private val settingsRepo: SettingsRepository, -->
<!--     private val completedWorkoutDao: CompletedWorkoutDao, -->
<!--     private val nutritionDao: NutritionDao -->
<!--   ) -->
<!-- ACHTUNG: Konstruktor hat noch DAO-Params. Plan 20-08 hatte Engine entkoppelt, aber Walker selbst NICHT — er ist in data/-Layer, da darf er DAOs nutzen. -->
<!-- Nach Move nach domain/gamification/ sind DAO-Params NICHT MEHR erlaubt. -->
<!-- Daher: Walker-Move braucht zusätzlich DAO → Repository-Substitution für seine eigenen DAO-Calls. -->

<!-- Konsumenten (grep -rln "RetroactiveWalker" shared/): -->
<!--   di/GamificationEngineModule.kt:3 (import), :30 (binding) -->
<!--   di/GamificationStartup.kt (commonMain) -->
<!--   iosMain/di/GamificationStartupIos.kt -->
-->
</interfaces>
</context>

<files_modified>
- shared/src/commonMain/kotlin/com/pumpernickel/domain/gamification/ApplyRetroactiveGamificationUseCase.kt  (CREATED)
- shared/src/commonMain/kotlin/com/pumpernickel/data/repository/RetroactiveWalker.kt  (DELETED)
- shared/src/commonMain/kotlin/com/pumpernickel/di/GamificationEngineModule.kt  (MODIFIED — Import + Binding)
- shared/src/commonMain/kotlin/com/pumpernickel/di/GamificationStartup.kt  (MODIFIED — Import + ggf. Konstruktor-Param-Typ)
- shared/src/iosMain/kotlin/com/pumpernickel/di/GamificationStartupIos.kt  (MODIFIED — Import + Konstruktor-Param-Typ)
- shared/src/commonMain/kotlin/com/pumpernickel/domain/repository/CompletedWorkoutHistoryQuery.kt  (CONDITIONAL — falls neuer Narrow-Port nötig)
</files_modified>

<files_created>
- shared/src/commonMain/kotlin/com/pumpernickel/domain/gamification/ApplyRetroactiveGamificationUseCase.kt
</files_created>

<files_deleted_renamed>
- `data/repository/RetroactiveWalker.kt` → `domain/gamification/ApplyRetroactiveGamificationUseCase.kt` (move + rename)
</files_deleted_renamed>

<approach>
1. **Pre-flight: Konsumenten-Inventur:**
   ```
   grep -rln "RetroactiveWalker" shared/ androidApp/
   ```
   Erwartung: Module-Bindings (di/GamificationEngineModule.kt, di/GamificationStartup.kt, iosMain/di/GamificationStartupIos.kt). Keine ViewModels (Walker ist Startup-Only).

2. **DAO → Repository-Substitution (analog Plan 20-08):**
   Der Walker hat heute `completedWorkoutDao: CompletedWorkoutDao` und `nutritionDao: NutritionDao` als Konstruktor-Param. Da er ins `domain/`-Layer wandert, dürfen diese nicht mehr im Konstruktor stehen.
   - `completedWorkoutDao.getAllWorkouts()` → ersetze durch existierende Repository-Methode (z.B. `gamificationRepo.getAllCompletedWorkouts()` falls vorhanden, oder lege eine neue Methode auf das passende Repo an).
   - `nutritionDao.<…>` → analog. Plan 20-08 hat ggf. schon einen `ConsumptionHistoryQuery`-Port oder eine Methode auf `FoodRepository` ergänzt — wiederverwenden.
   - Falls Plan 20-08 die Walker-DAO-Calls NICHT antastet (was wahrscheinlich ist, da 08 nur die Engine refactor): hier nachholen.

3. **Klasse umbenennen + verschieben:**
   - Erstelle `shared/src/commonMain/kotlin/com/pumpernickel/domain/gamification/ApplyRetroactiveGamificationUseCase.kt`
   - Package `com.pumpernickel.domain.gamification`
   - `class ApplyRetroactiveGamificationUseCase(...)` (Konstruktor-Params: alle DAO-Refs durch Repository-Refs ersetzt)
   - Body 1:1 vom alten Walker übertragen, mit angepassten Method-Calls (Repo statt DAO)
   - **Konvention:** Methode `applyIfNeeded()` bleibt — User-Code (Startup) ruft das.

4. **Alten File löschen:**
   ```
   rm shared/src/commonMain/kotlin/com/pumpernickel/data/repository/RetroactiveWalker.kt
   ```

5. **GamificationEngineModule.kt:**
   - Import: `com.pumpernickel.data.repository.RetroactiveWalker` → `com.pumpernickel.domain.gamification.ApplyRetroactiveGamificationUseCase`
   - Binding (Zeile ~30): `single { RetroactiveWalker(get(), get(), get(), get()) }` → `single { ApplyRetroactiveGamificationUseCase(get(), get(), get(), get()) }` (Argumentliste an neuen Konstruktor anpassen — DAO-Gets durch Repo-Gets ersetzen)

6. **GamificationStartup.kt (commonMain):**
   - Falls die Klasse einen Konstruktor-Param vom Typ `RetroactiveWalker` hat: Type-Rename auf `ApplyRetroactiveGamificationUseCase`.
   - Import-Update.
   - `walker.applyIfNeeded()`-Call bleibt syntaktisch identisch.

7. **GamificationStartupIos.kt:**
   - Analog Schritt 6 für die iOS-spezifische Startup-Variante.

8. **Cross-Platform Build + Tests:**
   ```
   ./gradlew :shared:compileCommonMainKotlinMetadata :shared:compileDebugKotlinAndroid :shared:compileKotlinIosX64 :shared:allTests --quiet
   ```
   Erwartung: BUILD SUCCESSFUL, alle 7 Tests grün.

**KMP-NativeCoroutines:** `ApplyRetroactiveGamificationUseCase` selbst wird nicht direkt nach Swift exposed (Startup macht das intern). `GamificationStartupIos.kt:23` ruft `walker.applyIfNeeded()` — Method-Name bleibt. Kein Swift-Bridge-Risiko in diesem Plan.
</approach>

<tasks>

<task type="auto">
  <name>Task 1: Class-Move + DAO→Repository-Substitution für Walker</name>
  <files>
    shared/src/commonMain/kotlin/com/pumpernickel/domain/gamification/ApplyRetroactiveGamificationUseCase.kt,
    shared/src/commonMain/kotlin/com/pumpernickel/data/repository/RetroactiveWalker.kt
  </files>
  <action>
    Read den alten Walker komplett. Identifiziere DAO-Calls (`completedWorkoutDao.*`, `nutritionDao.*`). Für jeden: ersetze durch passenden Repository-Call (`gamificationRepo` oder `foodRepo`). Falls Repository-Methode fehlt: ergänze in `domain/repository/*.kt` Interface + Impl (analog Plan 20-08-Schritt 2). Erstelle neuen File `ApplyRetroactiveGamificationUseCase.kt` mit Package `com.pumpernickel.domain.gamification`, Klassen-Body 1:1 vom Walker übertragen mit substituierten Calls. Konstruktor-Params: `engine: GamificationEngine`, `settingsRepo: SettingsRepository` (Interface aus domain/repository/ ab Plan 20-05), plus die Repos die jetzt die DAOs ersetzen. Anschließend alten File löschen: `rm shared/src/commonMain/kotlin/com/pumpernickel/data/repository/RetroactiveWalker.kt`.
  </action>
  <verify>
    <automated>./gradlew :shared:compileCommonMainKotlinMetadata --quiet 2>&amp;1 | tail -10</automated>
    Erwartung: Compile-Fehler in Konsumenten (Module, Startup) — das ist erwartet und wird in Task 2 gefixt.
    Zusätzlich:
    - `test -f shared/src/commonMain/kotlin/com/pumpernickel/domain/gamification/ApplyRetroactiveGamificationUseCase.kt`
    - `! test -f shared/src/commonMain/kotlin/com/pumpernickel/data/repository/RetroactiveWalker.kt`
    - `grep -c "class ApplyRetroactiveGamificationUseCase" shared/src/commonMain/kotlin/com/pumpernickel/domain/gamification/ApplyRetroactiveGamificationUseCase.kt` == 1
    - `! grep -E "Dao(\)|completedWorkoutDao|nutritionDao" shared/src/commonMain/kotlin/com/pumpernickel/domain/gamification/ApplyRetroactiveGamificationUseCase.kt`
  </verify>
  <done>Datei wurde umbenannt, verschoben, DAO-frei. Bestehende Konsumenten brechen — Task 2 fixt sie.</done>
</task>

<task type="auto">
  <name>Task 2: Konsumenten-Updates (GamificationEngineModule + Startup x2) + Final-Verify</name>
  <files>
    shared/src/commonMain/kotlin/com/pumpernickel/di/GamificationEngineModule.kt,
    shared/src/commonMain/kotlin/com/pumpernickel/di/GamificationStartup.kt,
    shared/src/iosMain/kotlin/com/pumpernickel/di/GamificationStartupIos.kt
  </files>
  <action>
    In allen drei Files: `RetroactiveWalker` → `ApplyRetroactiveGamificationUseCase`. Import-Pfade von `com.pumpernickel.data.repository.RetroactiveWalker` → `com.pumpernickel.domain.gamification.ApplyRetroactiveGamificationUseCase`. Falls Konstruktor-Param-Typen sich geändert haben (DAO→Repo): in `GamificationEngineModule.kt` die `single { ApplyRetroactiveGamificationUseCase(get(), get(), get(), get()) }`-Binding-Arity an neuen Konstruktor anpassen. In Startup.kt + StartupIos.kt: Property-/Param-Typ-Rename, `walker.applyIfNeeded()`-Call bleibt (oder umbenennen auf `applyRetroactive.applyIfNeeded()` falls sich der Variable-Name spürbar liest).
  </action>
  <verify>
    <automated>./gradlew :shared:compileCommonMainKotlinMetadata :shared:compileDebugKotlinAndroid :shared:compileKotlinIosX64 :androidApp:compileDebugKotlinAndroid :shared:allTests --quiet 2>&amp;1 | tail -15</automated>
    Erwartung: BUILD SUCCESSFUL across all targets, alle Tests grün.
    Zusätzlich:
    - `! grep -rn "RetroactiveWalker" shared/ androidApp/` (sollte 0 Treffer haben außer evtl. Kommentar-Referenzen)
    - `grep -c "ApplyRetroactiveGamificationUseCase" shared/src/commonMain/kotlin/com/pumpernickel/di/GamificationEngineModule.kt` ≥ 1
  </verify>
  <done>Alle Konsumenten umgestellt, Cross-Platform-Compile grün, Tests grün.</done>
</task>

</tasks>

<verification>
- `./gradlew :shared:compileCommonMainKotlinMetadata :shared:compileDebugKotlinAndroid :shared:compileKotlinIosX64 :androidApp:compileDebugKotlinAndroid :shared:allTests` — BUILD SUCCESSFUL
- Grep-Guard: `! grep -rn "RetroactiveWalker" shared/src/ androidApp/src/` (0 Treffer)
- Grep-Guard: `test -f shared/src/commonMain/kotlin/com/pumpernickel/domain/gamification/ApplyRetroactiveGamificationUseCase.kt`
- Grep-Guard: `! test -f shared/src/commonMain/kotlin/com/pumpernickel/data/repository/RetroactiveWalker.kt`
</verification>

<success_criteria>
- Walker-Klasse umbenannt + verschoben nach `domain/gamification/`
- DAO-frei (Konstruktor + Body)
- Koin-Binding + Konsumenten-Imports aktualisiert
- Cross-Platform-Compile + alle Tests grün
- Verhalten 1:1: `applyIfNeeded()` macht Sentinel-Check + Replay + Sentinel-Set
</success_criteria>

<risks_pitfalls>
- **Pitfall: Walker DAO-Calls die in keinem Repo existieren.** Wenn Plan 20-08 schon einen Domain-Port `ConsumptionHistoryQuery` o.ä. eingeführt hat, hier wiederverwenden. Sonst neu anlegen (CONDITIONAL files_modified-Eintrag).
- **Pitfall: GamificationStartupIos.kt nutzt GlobalScope (Known Bug 2 in CONCERNS.md).** Das bleibt — nicht in Phase-20-Scope. Nur Klassen-Type renamen.
- **Atomic commit:** Ein commit. ~5 Files.
- **KMP-NativeCoroutines:** `ApplyRetroactiveGamificationUseCase` hat nur `suspend fun applyIfNeeded()` — wird nicht direkt nach Swift exposed (Startup ist interner Konsument). Kein Swift-Risiko.
- **Plan 20-09 muss NACH Plan 20-08:** weil 20-08 ggf. neue Repository-Methoden auf `GamificationRepository` oder `FoodRepository` einführt, die Walker hier wiederverwendet.
</risks_pitfalls>

<output>
After completion, create `.planning/phases/20-clean-architecture-refactor-dependency-rule-fixen-repository/20-09-SUMMARY.md`
</output>
</content>
</invoke>