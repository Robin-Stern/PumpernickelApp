---
phase: 20-clean-architecture-refactor-dependency-rule-fixen-repository
plan: 11
type: execute
wave: 7
depends_on: [20-10]
files_modified:
  - shared/src/androidMain/kotlin/com/pumpernickel/infrastructure/ai/SecureKeyStore.android.kt
  - shared/src/androidMain/kotlin/com/pumpernickel/infrastructure/notification/NotificationService.android.kt
  - shared/src/androidMain/kotlin/com/pumpernickel/infrastructure/progresspic/BiometricGate.android.kt
  - shared/src/androidMain/kotlin/com/pumpernickel/infrastructure/progresspic/PhotoCaptureLauncher.android.kt
  - shared/src/androidMain/kotlin/com/pumpernickel/infrastructure/progresspic/PhotoVault.android.kt
  - shared/src/androidMain/kotlin/com/pumpernickel/infrastructure/progresspic/PhotoCaptureLauncherHost.kt
  - shared/src/androidMain/kotlin/com/pumpernickel/infrastructure/progresspic/BiometricGateActivityHolder.kt
  - shared/src/androidMain/kotlin/com/pumpernickel/infrastructure/geofence/AndroidGeofenceProvider.kt
  - shared/src/androidMain/kotlin/com/pumpernickel/infrastructure/geofence/GeofenceBroadcastReceiver.kt
  - shared/src/androidMain/kotlin/com/pumpernickel/infrastructure/location/AndroidLocationProvider.kt
  - shared/src/androidMain/kotlin/com/pumpernickel/infrastructure/permissions/AndroidPermissionController.kt
  - shared/src/androidMain/kotlin/com/pumpernickel/infrastructure/permissions/PermissionActivityHolder.kt
  - shared/src/androidMain/kotlin/com/pumpernickel/data/db/Database.android.kt
  - shared/src/androidMain/kotlin/com/pumpernickel/data/preferences/createDataStore.android.kt
  - shared/src/androidMain/kotlin/com/pumpernickel/di/PlatformModule.android.kt
  - androidApp/src/androidMain/kotlin/com/pumpernickel/android/MainActivity.kt
  - androidApp/src/androidMain/kotlin/com/pumpernickel/android/AiGenerationService.kt
  - androidApp/src/androidMain/kotlin/com/pumpernickel/android/notifications/GeofenceNotifications.kt
autonomous: true
requirements: [D-20-02, D-20-03, D-20-08]
must_haves:
  truths:
    - "Android-actuals leben in `androidMain/.../infrastructure/<sub>/`-Pakete entsprechend Plan 20-10."
    - "Alter `androidMain/.../feature/`-Folder existiert nicht mehr."
    - "Alter `androidMain/.../platform/`-Folder existiert nicht mehr (Inhalte nach `data/db/` und `data/preferences/` verschoben — D-20-03)."
    - "Alte `androidMain/.../domain/{ai,progresspic}/`-Files (actuals) sind weg."
    - "`PlatformModule.android.kt` Imports zeigen auf `infrastructure/`- und `data/`-Pakete."
    - "`./gradlew :shared:compileDebugKotlinAndroid` und `:androidApp:compileDebugKotlinAndroid` grün."
  artifacts:
    - path: "shared/src/androidMain/kotlin/com/pumpernickel/infrastructure/"
      provides: "Vereinheitlichter Android-Platform-Tree."
    - path: "shared/src/androidMain/kotlin/com/pumpernickel/data/db/Database.android.kt"
      provides: "Room-Builder actual aus dem alten `platform/`-Folder."
  key_links:
    - from: "shared/src/androidMain/kotlin/com/pumpernickel/di/PlatformModule.android.kt"
      to: "Android-actuals in infrastructure/ und data/"
      via: "Koin Single-Bindings mit aktualisierten Imports"
      pattern: "import com.pumpernickel.infrastructure"
---

# Plan 20-11: androidMain Move — feature/ + platform/ + domain/{ai,progresspic} → infrastructure/ + data/ (D-20-02, D-20-03)

<objective>
Android-Side des Layer-Refactors. Drei sibling-Folders im `androidMain` werden konsolidiert:

| Heute | Künftig |
|-------|---------|
| `androidMain/.../feature/biometric/BiometricGateActivityHolder.kt` | `androidMain/.../infrastructure/progresspic/BiometricGateActivityHolder.kt` |
| `androidMain/.../feature/geofence/AndroidGeofenceProvider.kt` | `androidMain/.../infrastructure/geofence/AndroidGeofenceProvider.kt` |
| `androidMain/.../feature/geofence/GeofenceBroadcastReceiver.kt` | `androidMain/.../infrastructure/geofence/GeofenceBroadcastReceiver.kt` |
| `androidMain/.../feature/location/AndroidLocationProvider.kt` | `androidMain/.../infrastructure/location/AndroidLocationProvider.kt` |
| `androidMain/.../feature/permissions/AndroidPermissionController.kt` | `androidMain/.../infrastructure/permissions/AndroidPermissionController.kt` |
| `androidMain/.../feature/permissions/PermissionActivityHolder.kt` | `androidMain/.../infrastructure/permissions/PermissionActivityHolder.kt` |
| `androidMain/.../feature/photo/PhotoCaptureLauncherHost.kt` | `androidMain/.../infrastructure/progresspic/PhotoCaptureLauncherHost.kt` |
| `androidMain/.../domain/ai/SecureKeyStore.android.kt` | `androidMain/.../infrastructure/ai/SecureKeyStore.android.kt` |
| `androidMain/.../domain/ai/NotificationService.android.kt` | `androidMain/.../infrastructure/notification/NotificationService.android.kt` |
| `androidMain/.../domain/progresspic/BiometricGate.android.kt` | `androidMain/.../infrastructure/progresspic/BiometricGate.android.kt` |
| `androidMain/.../domain/progresspic/PhotoCaptureLauncher.android.kt` | `androidMain/.../infrastructure/progresspic/PhotoCaptureLauncher.android.kt` |
| `androidMain/.../domain/progresspic/PhotoVault.android.kt` | `androidMain/.../infrastructure/progresspic/PhotoVault.android.kt` |
| `androidMain/.../platform/Database.android.kt` | `androidMain/.../data/db/Database.android.kt` (D-20-03) |
| `androidMain/.../platform/createDataStore.android.kt` | `androidMain/.../data/preferences/createDataStore.android.kt` (D-20-03) |

Konsumenten in `androidMain/di/PlatformModule.android.kt` + `androidApp/.../MainActivity.kt` + `AiGenerationService.kt` + `GeofenceNotifications.kt` müssen Import-Pfade aktualisieren.

Voraussetzung: Plan 20-10 hat commonMain expects nach `infrastructure/` verschoben. Wenn 20-10 nicht durchgelaufen ist: `actual class SecureKeyStore` in `androidMain/infrastructure/ai/SecureKeyStore.android.kt` zeigt auf gar keinen `expect` → Compile-Fehler.

Purpose: Symmetrie Common↔Android↔iOS Layer-Struktur. Android-`feature/` und `platform/`-Folders verschwinden komplett.

Output: ~15 Android-Files verschoben, 14 alte Files gelöscht, 4 Konsumenten-Files mit aktualisierten Imports.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-clean-architecture-refactor-dependency-rule-fixen-repository/20-CONTEXT.md
@.planning/codebase/STRUCTURE.md
@shared/src/androidMain/kotlin/com/pumpernickel/di/PlatformModule.android.kt
@shared/src/androidMain/kotlin/com/pumpernickel/feature/geofence/AndroidGeofenceProvider.kt
@shared/src/androidMain/kotlin/com/pumpernickel/platform/Database.android.kt

<interfaces>
<!-- Erwartete Konsumenten-Imports die aktualisiert werden müssen (grep zur Sicherheit): -->
<!--   PlatformModule.android.kt: bindet AndroidGeofenceProvider, AndroidLocationProvider, AndroidPermissionController, -->
<!--                              PhotoCaptureLauncherHost, BiometricGateActivityHolder, PermissionActivityHolder, -->
<!--                              Database (Room), DataStore -->
<!--   MainActivity.kt: holt evtl. BiometricGateActivityHolder, PermissionActivityHolder, PhotoCaptureLauncherHost -->
<!--                    (Activity-Lifecycle-Bindings) -->
<!--   AiGenerationService.kt: nutzt NotificationService.android.kt (vielleicht) -->
<!--   GeofenceNotifications.kt: nutzt NotificationService.android.kt -->

<!-- DAOs etc. werden in PlatformModule.android.kt aus AppDatabase geholt. -->
<!-- Database.android.kt liefert die Room-Database-Instance. -->
</interfaces>
</context>

<files_modified>
- (Move 1: feature/ → infrastructure/) — 7 Files (siehe oben)
- (Move 2: domain/{ai,progresspic} → infrastructure/{ai,notification,progresspic}) — 5 actuals
- (Move 3: platform/ → data/{db,preferences}) — 2 Files (D-20-03)
- shared/src/androidMain/kotlin/com/pumpernickel/di/PlatformModule.android.kt  (MODIFIED — Imports)
- androidApp/src/androidMain/kotlin/com/pumpernickel/android/MainActivity.kt  (MODIFIED — Imports falls referenziert)
- androidApp/src/androidMain/kotlin/com/pumpernickel/android/AiGenerationService.kt  (MODIFIED — falls relevant)
- androidApp/src/androidMain/kotlin/com/pumpernickel/android/notifications/GeofenceNotifications.kt  (MODIFIED — Imports)
</files_modified>

<files_deleted_renamed>
- `shared/src/androidMain/kotlin/com/pumpernickel/feature/` (komplett) — Folder verschwindet
- `shared/src/androidMain/kotlin/com/pumpernickel/platform/` (komplett) — Folder verschwindet (D-20-03)
- `shared/src/androidMain/kotlin/com/pumpernickel/domain/ai/SecureKeyStore.android.kt` — moved
- `shared/src/androidMain/kotlin/com/pumpernickel/domain/ai/NotificationService.android.kt` — moved
- `shared/src/androidMain/kotlin/com/pumpernickel/domain/progresspic/BiometricGate.android.kt` — moved
- `shared/src/androidMain/kotlin/com/pumpernickel/domain/progresspic/PhotoCaptureLauncher.android.kt` — moved
- `shared/src/androidMain/kotlin/com/pumpernickel/domain/progresspic/PhotoVault.android.kt` — moved
</files_deleted_renamed>

<approach>
1. **Konsumenten-Inventur:**
   ```
   grep -rln "com\.pumpernickel\.feature\." shared/src/androidMain/ androidApp/src/
   grep -rln "com\.pumpernickel\.platform\." shared/src/androidMain/ androidApp/src/
   grep -rln "com\.pumpernickel\.domain\.ai\.\(SecureKeyStore\|NotificationService\)" shared/src/androidMain/
   grep -rln "com\.pumpernickel\.domain\.progresspic\.\(BiometricGate\|PhotoCaptureLauncher\|PhotoVault\)" shared/src/androidMain/
   ```
   Liste alle Konsumenten.

2. **Move feature/ → infrastructure/ (7 Files):**
   - `feature/biometric/BiometricGateActivityHolder.kt` → `infrastructure/progresspic/BiometricGateActivityHolder.kt` (Helper für BiometricGate, semantisch zu `progresspic` gehörend)
   - `feature/geofence/AndroidGeofenceProvider.kt` → `infrastructure/geofence/AndroidGeofenceProvider.kt`
   - `feature/geofence/GeofenceBroadcastReceiver.kt` → `infrastructure/geofence/GeofenceBroadcastReceiver.kt`
   - `feature/location/AndroidLocationProvider.kt` → `infrastructure/location/AndroidLocationProvider.kt`
   - `feature/permissions/AndroidPermissionController.kt` → `infrastructure/permissions/AndroidPermissionController.kt`
   - `feature/permissions/PermissionActivityHolder.kt` → `infrastructure/permissions/PermissionActivityHolder.kt`
   - `feature/photo/PhotoCaptureLauncherHost.kt` → `infrastructure/progresspic/PhotoCaptureLauncherHost.kt`

   Pro File: Read content. Create new File mit gleichem Body, **Package-Zeile geändert** (`package com.pumpernickel.infrastructure.<sub>`). Delete altes File. Falls das alte File `internal` referenced (z.B. `import com.pumpernickel.feature.permissions.LocationPermissionStatus`), prüfen ob das mit umzieht — typisch ja.

3. **Move domain/{ai,progresspic} actuals → infrastructure/ (5 Files):**
   - `domain/ai/SecureKeyStore.android.kt` → `infrastructure/ai/SecureKeyStore.android.kt`
   - `domain/ai/NotificationService.android.kt` → `infrastructure/notification/NotificationService.android.kt`
   - `domain/progresspic/BiometricGate.android.kt` → `infrastructure/progresspic/BiometricGate.android.kt`
   - `domain/progresspic/PhotoCaptureLauncher.android.kt` → `infrastructure/progresspic/PhotoCaptureLauncher.android.kt`
   - `domain/progresspic/PhotoVault.android.kt` → `infrastructure/progresspic/PhotoVault.android.kt`

   **WICHTIG:** Diese sind `actual class …`-Files. Package muss EXAKT matchen mit dem `expect class`-Package aus Plan 20-10 — sonst Compile-Fehler "no expect for actual class". D.h.:
   - `actual class SecureKeyStore` → Package `com.pumpernickel.infrastructure.ai`
   - `actual class NotificationService` → Package `com.pumpernickel.infrastructure.notification`
   - `actual class BiometricGate` → Package `com.pumpernickel.infrastructure.progresspic`
   - etc.

4. **Move platform/ → data/ (D-20-03, 2 Files):**
   - `platform/Database.android.kt` → `data/db/Database.android.kt` (Package `com.pumpernickel.data.db`)
   - `platform/createDataStore.android.kt` → `data/preferences/createDataStore.android.kt` (Package `com.pumpernickel.data.preferences`)

   Diese sind `actual fun`-Files für expects in `commonMain/.../data/db/` und `data/preferences/`. iOS-Side hat das bereits in `iosMain/.../data/db/Database.ios.kt` und `iosMain/.../data/preferences/createDataStore.ios.kt` — Android zieht symmetrisch nach.

5. **PlatformModule.android.kt Imports updaten:**
   - `com.pumpernickel.feature.geofence.AndroidGeofenceProvider` → `com.pumpernickel.infrastructure.geofence.AndroidGeofenceProvider`
   - `com.pumpernickel.feature.location.AndroidLocationProvider` → `com.pumpernickel.infrastructure.location.AndroidLocationProvider`
   - `com.pumpernickel.feature.permissions.AndroidPermissionController` → `com.pumpernickel.infrastructure.permissions.AndroidPermissionController`
   - `com.pumpernickel.feature.permissions.PermissionActivityHolder` → `com.pumpernickel.infrastructure.permissions.PermissionActivityHolder`
   - `com.pumpernickel.feature.biometric.BiometricGateActivityHolder` → `com.pumpernickel.infrastructure.progresspic.BiometricGateActivityHolder`
   - `com.pumpernickel.feature.photo.PhotoCaptureLauncherHost` → `com.pumpernickel.infrastructure.progresspic.PhotoCaptureLauncherHost`
   - **D-20-09:** Datei bleibt in `di/` — nur Imports ändern, kein Move.

6. **MainActivity.kt / AiGenerationService.kt / GeofenceNotifications.kt:**
   - Pro File die `feature/`- oder `platform/`-Imports auf `infrastructure/`- oder `data/`-Imports umstellen.
   - GeofenceNotifications.kt nutzt evtl. `NotificationService.android.kt` (vielleicht über `infrastructure/notification/`-Path nach Move) — Import-Update.

7. **Verify (komplett):**
   ```
   ./gradlew :shared:compileCommonMainKotlinMetadata :shared:compileDebugKotlinAndroid :androidApp:compileDebugKotlinAndroid --quiet
   ```
   Erwartung: BUILD SUCCESSFUL.
   - `! test -d shared/src/androidMain/kotlin/com/pumpernickel/feature` (Folder weg)
   - `! test -d shared/src/androidMain/kotlin/com/pumpernickel/platform` (Folder weg)
   - `! test -d shared/src/androidMain/kotlin/com/pumpernickel/domain/ai` (Folder weg, da nur actuals drin waren)
   - `! test -d shared/src/androidMain/kotlin/com/pumpernickel/domain/progresspic` (Folder weg)
   - `find shared/src/androidMain/kotlin/com/pumpernickel/infrastructure -type f` listet ≥ 12 .kt-Files

**iOS-Compile bleibt erwartet broken** bis Plan 20-12 fertig ist.

**Hinweis (User-Profil):** User ist iOS-fokussiert, nicht Android-Dev. Wenn Subtilitäten im Android-Move auftauchen (z.B. `AndroidManifest`-Reference auf `GeofenceBroadcastReceiver` mit FQN-Package-Pfad), Executor entscheidet autonom — keine Decision-Checkpoint nötig.

**AndroidManifest.xml Caveat:** `GeofenceBroadcastReceiver` ist im AndroidManifest registriert (`androidApp/src/androidMain/AndroidManifest.xml`). Wenn dort der vollqualifizierte Klassenname steht (`<receiver android:name="com.pumpernickel.feature.geofence.GeofenceBroadcastReceiver"/>`), muss das ebenfalls aktualisiert werden auf `com.pumpernickel.infrastructure.geofence.GeofenceBroadcastReceiver`. Pre-check via `grep -n "feature.geofence" androidApp/src/androidMain/AndroidManifest.xml`. Falls Treffer: Manifest-File mit in files_modified aufnehmen.
</approach>

<tasks>

<task type="auto">
  <name>Task 1: feature/ + platform/ + domain/{ai,progresspic} actuals umziehen</name>
  <files>
    shared/src/androidMain/kotlin/com/pumpernickel/infrastructure/ai/SecureKeyStore.android.kt,
    shared/src/androidMain/kotlin/com/pumpernickel/infrastructure/notification/NotificationService.android.kt,
    shared/src/androidMain/kotlin/com/pumpernickel/infrastructure/progresspic/BiometricGate.android.kt,
    shared/src/androidMain/kotlin/com/pumpernickel/infrastructure/progresspic/PhotoCaptureLauncher.android.kt,
    shared/src/androidMain/kotlin/com/pumpernickel/infrastructure/progresspic/PhotoVault.android.kt,
    shared/src/androidMain/kotlin/com/pumpernickel/infrastructure/progresspic/PhotoCaptureLauncherHost.kt,
    shared/src/androidMain/kotlin/com/pumpernickel/infrastructure/progresspic/BiometricGateActivityHolder.kt,
    shared/src/androidMain/kotlin/com/pumpernickel/infrastructure/geofence/AndroidGeofenceProvider.kt,
    shared/src/androidMain/kotlin/com/pumpernickel/infrastructure/geofence/GeofenceBroadcastReceiver.kt,
    shared/src/androidMain/kotlin/com/pumpernickel/infrastructure/location/AndroidLocationProvider.kt,
    shared/src/androidMain/kotlin/com/pumpernickel/infrastructure/permissions/AndroidPermissionController.kt,
    shared/src/androidMain/kotlin/com/pumpernickel/infrastructure/permissions/PermissionActivityHolder.kt,
    shared/src/androidMain/kotlin/com/pumpernickel/data/db/Database.android.kt,
    shared/src/androidMain/kotlin/com/pumpernickel/data/preferences/createDataStore.android.kt
  </files>
  <action>
    Pro File aus dem Mapping (Approach Schritte 2/3/4): Read alten Inhalt, Create new File im Ziel-Package mit identischem Body und korrekter Package-Zeile, Delete alten File. **Wichtig:** `actual`-Class-Files müssen exakt das Package der `expect`-Files aus Plan 20-10 matchen — sonst Compile-Fehler. Internal `import com.pumpernickel.feature.<sub>.<helper>`-Referenzen zwischen den verschobenen Files entsprechend umstellen. Alle leer-werdenden Folders (`feature/`, `platform/`, `domain/ai/`, `domain/progresspic/`) entfernen (`rmdir` oder `find -empty -delete`).
  </action>
  <verify>
    <automated>./gradlew :shared:compileDebugKotlinAndroid --quiet 2>&amp;1 | tail -15</automated>
    Erwartung: BUILD SUCCESSFUL für Android-shared-Modul.
    Zusätzlich:
    - `find shared/src/androidMain/kotlin/com/pumpernickel/infrastructure -type f | wc -l` ≥ 12
    - `! test -d shared/src/androidMain/kotlin/com/pumpernickel/feature`
    - `! test -d shared/src/androidMain/kotlin/com/pumpernickel/platform`
    - `test -f shared/src/androidMain/kotlin/com/pumpernickel/data/db/Database.android.kt`
    - `test -f shared/src/androidMain/kotlin/com/pumpernickel/data/preferences/createDataStore.android.kt`
  </verify>
  <done>Alle 14 Files an neuen Plätzen, alte Folders weg, `:shared:compileDebugKotlinAndroid` grün.</done>
</task>

<task type="auto">
  <name>Task 2: Konsumenten-Imports updaten (PlatformModule + MainActivity + Service + Manifest)</name>
  <files>
    shared/src/androidMain/kotlin/com/pumpernickel/di/PlatformModule.android.kt,
    androidApp/src/androidMain/kotlin/com/pumpernickel/android/MainActivity.kt,
    androidApp/src/androidMain/kotlin/com/pumpernickel/android/AiGenerationService.kt,
    androidApp/src/androidMain/kotlin/com/pumpernickel/android/notifications/GeofenceNotifications.kt,
    androidApp/src/androidMain/AndroidManifest.xml
  </files>
  <action>
    Pre-check: `grep -rn "com\.pumpernickel\.\(feature\|platform\)\." shared/src/androidMain/ androidApp/src/` und `grep -n "feature\.geofence" androidApp/src/androidMain/AndroidManifest.xml`. Pro Treffer Import auf neues Package umstellen (`feature/<x>` → `infrastructure/<sub>`, `platform/` → `data/<sub>`). AndroidManifest.xml: `android:name=` auf neuen FQN aktualisieren. Doppelte Imports vermeiden — strikter Such-und-Ersetz pro Datei. PlatformModule.android.kt bleibt strukturell unverändert; nur Imports ändern (D-20-09).
  </action>
  <verify>
    <automated>./gradlew :shared:compileDebugKotlinAndroid :androidApp:compileDebugKotlinAndroid --quiet 2>&amp;1 | tail -15</automated>
    Erwartung: BUILD SUCCESSFUL.
    Zusätzlich:
    - `! grep -rn "^import com\.pumpernickel\.\(feature\|platform\)\." shared/src/androidMain/ androidApp/src/`
    - `! grep -n "com.pumpernickel.feature" androidApp/src/androidMain/AndroidManifest.xml`
    - `! grep -n "com.pumpernickel.platform" androidApp/src/androidMain/AndroidManifest.xml`
  </verify>
  <done>Alle Imports aktualisiert, Cross-Module-Compile grün, AndroidManifest sauber.</done>
</task>

</tasks>

<verification>
- `./gradlew :shared:compileCommonMainKotlinMetadata :shared:compileDebugKotlinAndroid :androidApp:compileDebugKotlinAndroid :shared:allTests` — BUILD SUCCESSFUL
- Grep-Guards:
  - `! grep -rn "com\.pumpernickel\.feature" shared/src/androidMain/ androidApp/src/`
  - `! grep -rn "com\.pumpernickel\.platform" shared/src/androidMain/ androidApp/src/`
  - `! grep -rn "com\.pumpernickel\.domain\.ai\.\(SecureKeyStore\|NotificationService\)" shared/src/androidMain/`
  - `! test -d shared/src/androidMain/kotlin/com/pumpernickel/feature`
  - `! test -d shared/src/androidMain/kotlin/com/pumpernickel/platform`
- `find shared/src/androidMain/kotlin/com/pumpernickel/infrastructure -type f | wc -l` ≥ 12
</verification>

<success_criteria>
- 14 Android-Files an neuen Plätzen (12 in infrastructure/, 2 in data/)
- `feature/` und `platform/`-Folders existieren nicht mehr
- `domain/ai/` und `domain/progresspic/` in androidMain existieren nicht mehr (waren leer nach Move)
- PlatformModule.android.kt + MainActivity + AiGenerationService + GeofenceNotifications haben aktualisierte Imports
- AndroidManifest.xml hat korrekten FQN für GeofenceBroadcastReceiver
- Android-Build (shared + androidApp) grün
- iOS-Build erwartet broken — Plan 20-12 fixt das
</success_criteria>

<risks_pitfalls>
- **Pitfall (HOCH): AndroidManifest BroadcastReceiver FQN.** `GeofenceBroadcastReceiver` ist über `<receiver android:name="…"/>` registriert. Wenn der FQN nach dem Move nicht aktualisiert wird, brennt zur Laufzeit der Geofence (Phase 19!). Mitigation: Pre-check via grep, Manifest mit in files_modified.
- **Pitfall: Cross-File-Imports in feature/.** `AndroidGeofenceProvider.kt` importiert evtl. `PermissionActivityHolder` aus dem Schwesterordner. Nach Move auf `infrastructure/permissions/PermissionActivityHolder` zeigen. Verify durch Compile.
- **Pitfall: expect/actual-Package-Mismatch.** Wenn Plan 20-10 ein Common-expect z.B. nach `infrastructure/notification/` legt, aber dieser Plan den actual nach `infrastructure/ai/` legt → Compile-Fehler "no actual for expect". Strikte Symmetrie check.
- **Pitfall: D-20-09 verletzen.** PlatformModule.android.kt bleibt in `di/`. NICHT in `infrastructure/di/` o.ä. Nur Imports updaten.
- **Pitfall (User-spezifisch): User ist iOS-fokussiert.** Wenn Android-spezifische Subtilitäten (z.B. AndroidGeofenceProvider's `PendingIntent`-Setup) durch den Move-Side-Effekte ändern, Executor MUSS commit message darauf hinweisen + Plan 20-13 (Android UAT) muss explizit Geofence-Smoketest fordern.
- **Atomic commit:** Ein commit für den ganzen Move + Konsumenten-Updates (~18 Files). Großer Commit, semantisch ein-Ding.
- **KMP-NativeCoroutines:** Android-Side hat keine direkten Swift-Bridges. Kein Swift-Risiko.
</risks_pitfalls>

<output>
After completion, create `.planning/phases/20-clean-architecture-refactor-dependency-rule-fixen-repository/20-11-SUMMARY.md`
</output>
</content>
</invoke>