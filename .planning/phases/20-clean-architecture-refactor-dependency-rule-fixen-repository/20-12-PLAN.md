---
phase: 20-clean-architecture-refactor-dependency-rule-fixen-repository
plan: 12
type: execute
wave: 7
depends_on: [20-10]
files_modified:
  - shared/src/iosMain/kotlin/com/pumpernickel/infrastructure/ai/SecureKeyStore.ios.kt
  - shared/src/iosMain/kotlin/com/pumpernickel/infrastructure/notification/NotificationService.ios.kt
  - shared/src/iosMain/kotlin/com/pumpernickel/infrastructure/progresspic/BiometricGate.ios.kt
  - shared/src/iosMain/kotlin/com/pumpernickel/infrastructure/progresspic/PhotoCaptureLauncher.ios.kt
  - shared/src/iosMain/kotlin/com/pumpernickel/infrastructure/progresspic/PhotoVault.ios.kt
  - shared/src/iosMain/kotlin/com/pumpernickel/infrastructure/geofence/IosGeofenceProvider.kt
  - shared/src/iosMain/kotlin/com/pumpernickel/infrastructure/location/IosLocationProvider.kt
  - shared/src/iosMain/kotlin/com/pumpernickel/infrastructure/permissions/IosPermissionController.kt
  - shared/src/iosMain/kotlin/com/pumpernickel/di/PlatformModule.ios.kt
  - shared/src/iosMain/kotlin/com/pumpernickel/di/KoinHelper.kt
  - shared/src/iosMain/kotlin/com/pumpernickel/di/PhotoVaultKoinHelper.kt
  - shared/src/iosMain/kotlin/com/pumpernickel/di/ProgressGalleryKoinHelper.kt
  - shared/src/iosMain/kotlin/com/pumpernickel/di/ProgressPicturePromptKoinHelper.kt
  - shared/src/iosMain/kotlin/com/pumpernickel/di/ProgressViewerKoinHelper.kt
  - shared/src/iosMain/kotlin/com/pumpernickel/di/AiSettingsKoinHelper.kt
  - shared/src/iosMain/kotlin/com/pumpernickel/domain/ai/AiBgTaskRegistrar.kt
autonomous: true
requirements: [D-20-02, D-20-08]
must_haves:
  truths:
    - "iOS-actuals leben in `iosMain/.../infrastructure/<sub>/`-Pakete entsprechend Plan 20-10."
    - "Alter `iosMain/.../data/{geofence,location,permissions}/`-Tree existiert nicht mehr (Inhalte verschoben — D-20-02)."
    - "Alte `iosMain/.../domain/{ai,progresspic}/`-Files (actuals) sind weg — die nicht-actual Files (z.B. AiBgTaskRegistrar) bleiben oder werden konsultativ wo passend untergebracht."
    - "iOS-spezifische KoinHelper.kt-Files haben aktualisierte Imports."
    - "`./gradlew :shared:compileKotlinIosX64 :shared:linkPodReleaseFrameworkIosX64` grün."
  artifacts:
    - path: "shared/src/iosMain/kotlin/com/pumpernickel/infrastructure/"
      provides: "Vereinheitlichter iOS-Platform-Tree."
  key_links:
    - from: "shared/src/iosMain/kotlin/com/pumpernickel/di/KoinHelper.kt"
      to: "Import-Pfade zu infrastructure/* statt domain/*"
      via: "import com.pumpernickel.infrastructure.<sub>.<class>"
      pattern: "import com.pumpernickel.infrastructure"
---

# Plan 20-12: iosMain Move — data/{geofence,location,permissions} + domain/{ai,progresspic} → infrastructure/ (D-20-02)

<objective>
iOS-Side des Layer-Refactors (analog Plan 20-11 für Android). 8 actuals + 3 provider impls werden konsolidiert:

| Heute | Künftig |
|-------|---------|
| `iosMain/.../data/geofence/IosGeofenceProvider.kt` | `iosMain/.../infrastructure/geofence/IosGeofenceProvider.kt` |
| `iosMain/.../data/location/IosLocationProvider.kt` | `iosMain/.../infrastructure/location/IosLocationProvider.kt` |
| `iosMain/.../data/permissions/IosPermissionController.kt` | `iosMain/.../infrastructure/permissions/IosPermissionController.kt` |
| `iosMain/.../domain/ai/SecureKeyStore.ios.kt` | `iosMain/.../infrastructure/ai/SecureKeyStore.ios.kt` |
| `iosMain/.../domain/ai/NotificationService.ios.kt` | `iosMain/.../infrastructure/notification/NotificationService.ios.kt` |
| `iosMain/.../domain/progresspic/BiometricGate.ios.kt` | `iosMain/.../infrastructure/progresspic/BiometricGate.ios.kt` |
| `iosMain/.../domain/progresspic/PhotoCaptureLauncher.ios.kt` | `iosMain/.../infrastructure/progresspic/PhotoCaptureLauncher.ios.kt` |
| `iosMain/.../domain/progresspic/PhotoVault.ios.kt` | `iosMain/.../infrastructure/progresspic/PhotoVault.ios.kt` |

**`iosMain/.../domain/ai/AiBgTaskRegistrar.kt`** ist KEIN actual eines expects — es ist iOS-spezifischer Code für BGTaskScheduler-Registration. Bleibt erstmal in `iosMain/.../domain/ai/`. Wenn der Folder dadurch nur diese eine Datei enthält → akzeptabel; alternativ Move nach `iosMain/.../infrastructure/ai/AiBgTaskRegistrar.kt` (sauberere Symmetrie). **Empfehlung: Mitnehmen nach `infrastructure/ai/`.** Damit verschwindet `domain/ai/` in iosMain komplett.

**`iosMain/.../data/db/Database.ios.kt`** und **`iosMain/.../data/preferences/createDataStore.ios.kt`** sind D-20-03-Cousins und bleiben unverändert in `data/` (iOS hat das schon symmetrisch — Android zieht in 20-11 nach).

Konsumenten (12+ KoinHelpers + PlatformModule.ios.kt):
- KoinHelper.kt referenziert evtl. `getSecureKeyStore(): SecureKeyStore` etc. — Imports updaten
- PhotoVaultKoinHelper.kt, ProgressGalleryKoinHelper.kt, ProgressPicturePromptKoinHelper.kt, ProgressViewerKoinHelper.kt, AiSettingsKoinHelper.kt — diese exponieren progresspic-/ai-Typen nach Swift → Import-Updates kritisch
- PlatformModule.ios.kt bindet die Provider — Import-Updates

Voraussetzung: Plan 20-10 hat commonMain expects nach `infrastructure/` verschoben.

Purpose: Symmetrie mit Android (Plan 20-11) und commonMain (Plan 20-10). Domain-Layer ist nach 20-10/11/12 von OS-Infra-Klassen befreit, plattformübergreifend.

Output: 8 iOS-actuals + AiBgTaskRegistrar an neuen Plätzen, alte Folders weg, 7+ KoinHelper-Files mit aktualisierten Imports.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-clean-architecture-refactor-dependency-rule-fixen-repository/20-CONTEXT.md
@.planning/codebase/STRUCTURE.md
@shared/src/iosMain/kotlin/com/pumpernickel/di/KoinHelper.kt
@shared/src/iosMain/kotlin/com/pumpernickel/di/PhotoVaultKoinHelper.kt
@shared/src/iosMain/kotlin/com/pumpernickel/di/PlatformModule.ios.kt
@shared/src/iosMain/kotlin/com/pumpernickel/data/geofence/IosGeofenceProvider.kt
@shared/src/iosMain/kotlin/com/pumpernickel/domain/ai/SecureKeyStore.ios.kt

<interfaces>
<!-- KoinHelper.kt — KMP-NativeCoroutines exponiert getXxx() nach Swift. -->
<!-- Wenn der Return-Type des Getters ein `infrastructure/`-Class ist, dann ist die generierte Swift-Class -->
<!-- Shared.SecureKeyStore (flat — kein Package im Swift-Namespace). -->
<!-- Swift-Code wird also NICHT brechen durch den Package-Rename in Kotlin, weil Swift den Klassennamen flat sieht. -->
<!-- ABER: KMP-NativeCoroutines könnte den Symbol-Namen unterschiedlich generieren je nach Package-Pfad. -->
<!-- Verify-Strategie: Plan 20-13 baut iOS-Framework und prüft Swift-Compile. -->

<!-- iOS KoinHelpers (12+) und welche Typen sie exponieren — Inventar via grep im Approach. -->
</interfaces>
</context>

<files_modified>
- (Move 1: data/{geofence,location,permissions} → infrastructure/) — 3 Files
- (Move 2: domain/{ai,progresspic} → infrastructure/) — 6 Files (5 actuals + AiBgTaskRegistrar)
- shared/src/iosMain/kotlin/com/pumpernickel/di/PlatformModule.ios.kt  (MODIFIED — Imports)
- shared/src/iosMain/kotlin/com/pumpernickel/di/KoinHelper.kt  (MODIFIED — Imports)
- shared/src/iosMain/kotlin/com/pumpernickel/di/PhotoVaultKoinHelper.kt  (MODIFIED)
- shared/src/iosMain/kotlin/com/pumpernickel/di/ProgressGalleryKoinHelper.kt  (MODIFIED)
- shared/src/iosMain/kotlin/com/pumpernickel/di/ProgressPicturePromptKoinHelper.kt  (MODIFIED)
- shared/src/iosMain/kotlin/com/pumpernickel/di/ProgressViewerKoinHelper.kt  (MODIFIED)
- shared/src/iosMain/kotlin/com/pumpernickel/di/AiSettingsKoinHelper.kt  (MODIFIED)
- shared/src/iosMain/kotlin/com/pumpernickel/di/GamificationStartupIos.kt  (CONDITIONAL — falls Imports referenziert)
</files_modified>

<files_deleted_renamed>
- `shared/src/iosMain/kotlin/com/pumpernickel/data/geofence/IosGeofenceProvider.kt` — moved
- `shared/src/iosMain/kotlin/com/pumpernickel/data/location/IosLocationProvider.kt` — moved
- `shared/src/iosMain/kotlin/com/pumpernickel/data/permissions/IosPermissionController.kt` — moved
- `shared/src/iosMain/kotlin/com/pumpernickel/domain/ai/SecureKeyStore.ios.kt` — moved
- `shared/src/iosMain/kotlin/com/pumpernickel/domain/ai/NotificationService.ios.kt` — moved
- `shared/src/iosMain/kotlin/com/pumpernickel/domain/ai/AiBgTaskRegistrar.kt` — moved (zu infrastructure/ai/)
- `shared/src/iosMain/kotlin/com/pumpernickel/domain/progresspic/BiometricGate.ios.kt` — moved
- `shared/src/iosMain/kotlin/com/pumpernickel/domain/progresspic/PhotoCaptureLauncher.ios.kt` — moved
- `shared/src/iosMain/kotlin/com/pumpernickel/domain/progresspic/PhotoVault.ios.kt` — moved
- `shared/src/iosMain/kotlin/com/pumpernickel/data/geofence/` (Folder leer → entfernen)
- `shared/src/iosMain/kotlin/com/pumpernickel/data/location/` (Folder leer → entfernen)
- `shared/src/iosMain/kotlin/com/pumpernickel/data/permissions/` (Folder leer → entfernen)
- `shared/src/iosMain/kotlin/com/pumpernickel/domain/ai/` (Folder leer → entfernen)
- `shared/src/iosMain/kotlin/com/pumpernickel/domain/progresspic/` (Folder leer → entfernen)
</files_deleted_renamed>

<approach>
1. **Konsumenten-Inventur:**
   ```
   grep -rln "com\.pumpernickel\.data\.\(geofence\|location\|permissions\)\." shared/src/iosMain/ iosApp/
   grep -rln "com\.pumpernickel\.domain\.\(ai\|progresspic\)\.\(SecureKeyStore\|NotificationService\|BiometricGate\|PhotoCaptureLauncher\|PhotoVault\|AiBgTaskRegistrar\)" shared/src/iosMain/
   ```
   Erwartung: 7+ KoinHelpers, PlatformModule.ios.kt, evtl. GamificationStartupIos.kt, AiBgTaskRegistrar selbst.
   
   iosApp/ Swift-Code referenziert Kotlin via KoinHelper → Swift-Imports sind `import shared` — Klassen-Namen werden flat exposed. **Swift-Code muss NICHT angefasst werden** (vorausgesetzt KMP-NativeCoroutines erzeugt symmetrisch-named Symbols).

2. **Move data/{geofence,location,permissions} → infrastructure/ (3 Files):**
   - `data/geofence/IosGeofenceProvider.kt` → `infrastructure/geofence/IosGeofenceProvider.kt` (Package `com.pumpernickel.infrastructure.geofence`)
   - `data/location/IosLocationProvider.kt` → `infrastructure/location/IosLocationProvider.kt`
   - `data/permissions/IosPermissionController.kt` → `infrastructure/permissions/IosPermissionController.kt`

   Diese sind `class Xxx : GeofenceProvider`-Files — implementieren das Interface aus `infrastructure/<sub>/` (nach Plan 20-10). Import des Interfaces auf neuen Pfad updaten (war `com.pumpernickel.domain.geofence.GeofenceProvider` → wird `com.pumpernickel.infrastructure.geofence.GeofenceProvider`).

3. **Move domain/{ai,progresspic} actuals → infrastructure/ (6 Files inkl. AiBgTaskRegistrar):**
   - `domain/ai/SecureKeyStore.ios.kt` → `infrastructure/ai/SecureKeyStore.ios.kt` (Package match zu commonMain expect-Package nach Plan 20-10)
   - `domain/ai/NotificationService.ios.kt` → `infrastructure/notification/NotificationService.ios.kt`
   - `domain/ai/AiBgTaskRegistrar.kt` → `infrastructure/ai/AiBgTaskRegistrar.kt` (kein actual, nur Move für Konsistenz)
   - `domain/progresspic/BiometricGate.ios.kt` → `infrastructure/progresspic/BiometricGate.ios.kt`
   - `domain/progresspic/PhotoCaptureLauncher.ios.kt` → `infrastructure/progresspic/PhotoCaptureLauncher.ios.kt`
   - `domain/progresspic/PhotoVault.ios.kt` → `infrastructure/progresspic/PhotoVault.ios.kt`

4. **PlatformModule.ios.kt Imports updaten:**
   - `com.pumpernickel.data.geofence.IosGeofenceProvider` → `com.pumpernickel.infrastructure.geofence.IosGeofenceProvider`
   - `com.pumpernickel.data.location.IosLocationProvider` → `com.pumpernickel.infrastructure.location.IosLocationProvider`
   - `com.pumpernickel.data.permissions.IosPermissionController` → `com.pumpernickel.infrastructure.permissions.IosPermissionController`
   - Bindings (`single<GeofenceProvider> { IosGeofenceProvider(...) }`) bleiben in Syntax, aber das `GeofenceProvider`-Interface kommt jetzt aus `infrastructure/geofence/`.
   - **D-20-09:** PlatformModule.ios.kt bleibt in `di/`. Nur Imports.

5. **KoinHelper.kt + Feature-KoinHelpers updaten:**
   - Pro Helper: `grep -n "import com.pumpernickel.domain.\(ai\|progresspic\)\.\|import com.pumpernickel.data.\(geofence\|location\|permissions\)\." <file>`
   - Imports umstellen.
   - Beispiel `KoinHelper.kt`: heute evtl. `import com.pumpernickel.domain.geofence.GeofenceProvider` und `fun getGeofenceProvider(): GeofenceProvider`. Nach Refactor: `import com.pumpernickel.infrastructure.geofence.GeofenceProvider`.
   - **Swift-Surface bleibt flat:** Swift code in `iosApp/iosApp/Views/Settings/DebugGeofencePanel.swift` o.ä. ruft `KoinHelper.shared.getGeofenceProvider()` und bekommt `Shared.GeofenceProvider`-Type. Der Kotlin-Package-Pfad steckt nicht im generierten Swift-Class-Namen — kein Swift-Change nötig.

6. **AiBgTaskRegistrar-internal Imports:**
   - Falls AiBgTaskRegistrar `NotificationService` o.ä. importiert: Pfad-Update.

7. **Cross-Platform-Verify (Final für commonMain + Android + iOS):**
   ```
   ./gradlew :shared:compileCommonMainKotlinMetadata :shared:compileDebugKotlinAndroid :shared:compileKotlinIosX64 :shared:linkDebugFrameworkIosSimulatorArm64 :androidApp:compileDebugKotlinAndroid :shared:allTests
   ```
   Erwartung: BUILD SUCCESSFUL across all targets, 7 Tests grün.

8. **Swift-Compile Sanity (optional, idealerweise Plan 20-13):**
   ```
   xcodebuild -workspace iosApp/iosApp.xcworkspace -scheme iosApp -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 15' build
   ```

**Wichtig:** Nach Plan 20-12 SOLLTE der gesamte Refactor compileable und testbar sein. Plan 20-13 ist die finale Verifikations-Schleife inkl. iOS UAT.

**KMP-NativeCoroutines-Risiko:** Wenn KMP-NativeCoroutines die Swift-Class-Namen auf Basis des Kotlin-Package-Pfads generiert (z.B. `Shared.InfrastructureAiSecureKeyStore` statt `Shared.SecureKeyStore`), brennt Swift. Mitigation: Plan 20-13 baut die Framework + Xcode → wenn Swift bricht, dann ist die Lösung typischerweise `@ObjCName(name = "SecureKeyStore", swift_name = "SecureKeyStore")`-Annotation am Kotlin-Class. Aber: laut Phase-19-Lernen exportiert KMP-Native Class-Namen flat per default — Risk is LOW.
</approach>

<tasks>

<task type="auto">
  <name>Task 1: 9 iOS-Files (3 data → infrastructure, 6 domain → infrastructure) umziehen</name>
  <files>
    shared/src/iosMain/kotlin/com/pumpernickel/infrastructure/geofence/IosGeofenceProvider.kt,
    shared/src/iosMain/kotlin/com/pumpernickel/infrastructure/location/IosLocationProvider.kt,
    shared/src/iosMain/kotlin/com/pumpernickel/infrastructure/permissions/IosPermissionController.kt,
    shared/src/iosMain/kotlin/com/pumpernickel/infrastructure/ai/SecureKeyStore.ios.kt,
    shared/src/iosMain/kotlin/com/pumpernickel/infrastructure/notification/NotificationService.ios.kt,
    shared/src/iosMain/kotlin/com/pumpernickel/infrastructure/ai/AiBgTaskRegistrar.kt,
    shared/src/iosMain/kotlin/com/pumpernickel/infrastructure/progresspic/BiometricGate.ios.kt,
    shared/src/iosMain/kotlin/com/pumpernickel/infrastructure/progresspic/PhotoCaptureLauncher.ios.kt,
    shared/src/iosMain/kotlin/com/pumpernickel/infrastructure/progresspic/PhotoVault.ios.kt
  </files>
  <action>
    Pro File: Read alten Inhalt, Create new File im Ziel-Package mit identischem Body + neuer Package-Zeile. Für `actual class …`: Package muss commonMain-expect-Package matchen (siehe Plan 20-10). Für plain `class …` (z.B. IosGeofenceProvider implements GeofenceProvider): Interface-Import auf `infrastructure/<sub>/`-Pfad anpassen. Delete alten File. Leere Folders entfernen (`find … -empty -type d -delete`).
  </action>
  <verify>
    <automated>./gradlew :shared:compileKotlinIosX64 --quiet 2>&amp;1 | tail -15</automated>
    Erwartung: BUILD SUCCESSFUL für iOS.
    Zusätzlich:
    - `find shared/src/iosMain/kotlin/com/pumpernickel/infrastructure -type f | wc -l` ≥ 8
    - `! test -d shared/src/iosMain/kotlin/com/pumpernickel/data/geofence`
    - `! test -d shared/src/iosMain/kotlin/com/pumpernickel/data/location`
    - `! test -d shared/src/iosMain/kotlin/com/pumpernickel/data/permissions`
    - `! test -d shared/src/iosMain/kotlin/com/pumpernickel/domain/ai`
    - `! test -d shared/src/iosMain/kotlin/com/pumpernickel/domain/progresspic`
  </verify>
  <done>9 iOS-Files an neuen Plätzen, alte Folders weg, `:shared:compileKotlinIosX64` grün.</done>
</task>

<task type="auto">
  <name>Task 2: PlatformModule.ios.kt + 6 KoinHelpers updaten + Cross-Platform-Final-Verify</name>
  <files>
    shared/src/iosMain/kotlin/com/pumpernickel/di/PlatformModule.ios.kt,
    shared/src/iosMain/kotlin/com/pumpernickel/di/KoinHelper.kt,
    shared/src/iosMain/kotlin/com/pumpernickel/di/PhotoVaultKoinHelper.kt,
    shared/src/iosMain/kotlin/com/pumpernickel/di/ProgressGalleryKoinHelper.kt,
    shared/src/iosMain/kotlin/com/pumpernickel/di/ProgressPicturePromptKoinHelper.kt,
    shared/src/iosMain/kotlin/com/pumpernickel/di/ProgressViewerKoinHelper.kt,
    shared/src/iosMain/kotlin/com/pumpernickel/di/AiSettingsKoinHelper.kt
  </files>
  <action>
    Pre-grep: `grep -rln "com\.pumpernickel\.\(data\.\(geofence\|location\|permissions\)\|domain\.\(ai\|progresspic\)\)" shared/src/iosMain/di/`. Pro File die alten Imports auf `infrastructure/<sub>/`-Pfade umstellen. Bei `PhotoVaultKoinHelper.kt` etc. besonders auf Class-Namen achten (Return-Types von `getPhotoVault(): PhotoVault` brauchen den neuen Import). PlatformModule.ios.kt bleibt strukturell unverändert; nur Imports + Class-References. Falls `GamificationStartupIos.kt` einen umgezogenen Typ referenziert: zusätzliches File zur Liste hinzu.
  </action>
  <verify>
    <automated>./gradlew :shared:compileCommonMainKotlinMetadata :shared:compileDebugKotlinAndroid :shared:compileKotlinIosX64 :shared:linkDebugFrameworkIosSimulatorArm64 :androidApp:compileDebugKotlinAndroid :shared:allTests --quiet 2>&amp;1 | tail -20</automated>
    Erwartung: BUILD SUCCESSFUL across all targets, 7 Tests grün.
    Zusätzlich:
    - `! grep -rn "^import com\.pumpernickel\.data\.\(geofence\|location\|permissions\)\." shared/src/iosMain/`
    - `! grep -rn "^import com\.pumpernickel\.domain\.\(ai\|progresspic\)\." shared/src/iosMain/` (Domain-DTOs wie `domain.ai.AiError` sind erlaubt — sicherstellen via greppen nach den 8 spezifischen Klassen)
  </verify>
  <done>iOS-Side Imports aktualisiert, Cross-Platform-Final-Compile grün, alle Tests grün.</done>
</task>

</tasks>

<verification>
- `./gradlew :shared:compileCommonMainKotlinMetadata :shared:compileDebugKotlinAndroid :shared:compileKotlinIosX64 :shared:linkDebugFrameworkIosSimulatorArm64 :androidApp:compileDebugKotlinAndroid :shared:allTests` — BUILD SUCCESSFUL
- Grep-Guards:
  - `! grep -rln "com\.pumpernickel\.data\.\(geofence\|location\|permissions\)" shared/src/iosMain/`
  - `! grep -rln "com\.pumpernickel\.domain\.ai\.\(SecureKeyStore\|NotificationService\|AiBgTaskRegistrar\)" shared/src/iosMain/`
  - `! grep -rln "com\.pumpernickel\.domain\.progresspic\.\(BiometricGate\|PhotoCaptureLauncher\|PhotoVault\)" shared/src/iosMain/`
  - `find shared/src/iosMain/kotlin/com/pumpernickel/infrastructure -type f | wc -l` ≥ 8
  - `! test -d shared/src/iosMain/kotlin/com/pumpernickel/data/geofence`
  - `! test -d shared/src/iosMain/kotlin/com/pumpernickel/domain/progresspic`
</verification>

<success_criteria>
- 9 iOS-Files an neuen `infrastructure/`-Plätzen
- `iosMain/data/{geofence,location,permissions}/` + `iosMain/domain/{ai,progresspic}/`-Folders sind weg
- 7+ KoinHelper-Files + PlatformModule.ios.kt + ggf. GamificationStartupIos.kt mit aktualisierten Imports
- Cross-Platform-Compile (Common + Android + iOS X64 + iosSimulatorArm64-Framework) grün
- Alle Tests grün
- **Plan 20-13 macht das iOS-UAT (Swift-Build + Smoketest).**
</success_criteria>

<risks_pitfalls>
- **Pitfall (HOCH): KMP-NativeCoroutines Swift-Symbol-Mismatch.** Wenn Swift den umbenannten Kotlin-Typ nicht findet (z.B. `Shared.SecureKeyStore` vs. `Shared.InfrastructureAiSecureKeyStore`), Swift-Build bricht. **Mitigation:** Plan 20-13 hat dedizierten iOS-Build-Step. Lösung wenn nötig: `@ObjCName(name = "...", swift_name = "...")`-Annotation am Kotlin-Class. **Risk-Estimate LOW** weil KMP-Native default-Verhalten flat exportiert.
- **Pitfall: `data/geofence/IosGeofenceProvider.kt` implementiert `GeofenceProvider`-Interface.** Nach Plan 20-10 lebt das Interface in `infrastructure/geofence/`. Wenn der Provider-File den Interface-Import nicht updatet → "GeofenceProvider not found". Pflicht-Check beim Move.
- **Pitfall: AiBgTaskRegistrar Move ist nicht zwingend.** Wenn das einen Konflikt mit existing Konsumenten erzeugt, kann es im `domain/ai/` bleiben — aber dann sieht der Tree inkonsistent aus. **Recommendation: Move nach `infrastructure/ai/`.**
- **Pitfall: Swift-Code in `iosApp/`.** Swift importiert `Shared.SecureKeyStore` flat — KEIN Kotlin-Package im Pfad. Wenn KMP-Native das hält, ist Swift-Code unangetastet. Sicherheits-Verify in Plan 20-13.
- **Atomic commit:** Ein commit für den Move + Konsumenten-Updates (~16 Files).
- **GamificationStartupIos.kt Known Bug:** GlobalScope-Issue (CONCERNS.md) ist out of scope für Phase 20. Beim Import-Update nichts anderes anfassen.
- **D-20-09:** PlatformModule.ios.kt bleibt in `di/`. Nicht in `infrastructure/di/` o.ä.
</risks_pitfalls>

<output>
After completion, create `.planning/phases/20-clean-architecture-refactor-dependency-rule-fixen-repository/20-12-SUMMARY.md`
</output>
</content>
</invoke>