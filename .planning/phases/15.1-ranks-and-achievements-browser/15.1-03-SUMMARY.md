---
phase: "15.1"
plan: "03"
subsystem: "gamification-android-navigation"
tags: [compose, navigation, android, material3, routes, overview-tab, achievement-gallery]
dependency_graph:
  requires:
    - "androidApp/src/androidMain/kotlin/com/pumpernickel/android/ui/screens/RankLadderScreen.kt (Plan 02)"
    - "shared/src/commonMain/kotlin/com/pumpernickel/presentation/gamification/RanksAndAchievementsViewModel.kt (Plan 01)"
    - "androidApp/src/androidMain/kotlin/com/pumpernickel/android/ui/navigation/Routes.kt (pre-existing)"
    - "androidApp/src/androidMain/kotlin/com/pumpernickel/android/ui/navigation/MainScreen.kt (pre-existing)"
    - "androidApp/src/androidMain/kotlin/com/pumpernickel/android/ui/screens/OverviewScreen.kt (pre-existing)"
    - "androidApp/src/androidMain/kotlin/com/pumpernickel/android/ui/screens/OverviewRankStrip.kt (pre-existing)"
    - "androidApp/src/androidMain/kotlin/com/pumpernickel/android/ui/screens/AchievementGalleryScreen.kt (pre-existing)"
  provides:
    - "OverviewRootRoute + RanksAndAchievementsRoute (new @Serializable data object routes)"
    - "Overview-tab NavHost (startDestination=OverviewRootRoute, 3 registered routes)"
    - "Tappable OverviewRankStrip (Card(onClick=onTap) + navController threading)"
    - "Reward-XP label on every AchievementCard (D-151-19)"
  affects:
    - "androidApp/src/androidMain/kotlin/com/pumpernickel/android/ui/navigation/Routes.kt"
    - "androidApp/src/androidMain/kotlin/com/pumpernickel/android/ui/navigation/MainScreen.kt"
    - "androidApp/src/androidMain/kotlin/com/pumpernickel/android/ui/screens/OverviewScreen.kt"
    - "androidApp/src/androidMain/kotlin/com/pumpernickel/android/ui/screens/OverviewRankStrip.kt"
    - "androidApp/src/androidMain/kotlin/com/pumpernickel/android/ui/screens/AchievementGalleryScreen.kt"
tech_stack:
  added: []
  patterns:
    - "Always-composed + alpha-gated NavHost per tab (mirrors workout-tab pattern)"
    - "Card(onClick = ...) Material 3 native clickable-card overload for ripple semantics"
    - "navController as first parameter convention (matches TemplateListScreen, AchievementGalleryScreen)"
    - "private fun helper co-located with composable for tier-derived values"
key_files:
  created: []
  modified:
    - "androidApp/src/androidMain/kotlin/com/pumpernickel/android/ui/navigation/Routes.kt"
    - "androidApp/src/androidMain/kotlin/com/pumpernickel/android/ui/navigation/MainScreen.kt"
    - "androidApp/src/androidMain/kotlin/com/pumpernickel/android/ui/screens/OverviewScreen.kt"
    - "androidApp/src/androidMain/kotlin/com/pumpernickel/android/ui/screens/OverviewRankStrip.kt"
    - "androidApp/src/androidMain/kotlin/com/pumpernickel/android/ui/screens/AchievementGalleryScreen.kt"
decisions:
  - "Duplicate composable<AchievementGalleryRoute> in both workout-tab and overview-tab NavHosts for back-stack separation (D-151-15)"
  - "onTap: () -> Unit is non-optional in OverviewRankStrip — every caller must supply a tap handler; intent is explicit"
  - "rewardXp returns Int (25/75/200), not Long — string template coerces automatically"
metrics:
  duration: "~180s"
  completed_date: "2026-04-23"
  tasks_completed: 4
  files_created: 0
  files_modified: 5
---

# Phase 15.1 Plan 03: Navigation Wiring + Tappable Rank Strip — Summary

Wired the rank ladder screen (Plan 02) into Android navigation: Overview tab gains its own NavHost (startDestination = OverviewRootRoute), the OverviewRankStrip becomes a tappable Card that navigates to RanksAndAchievementsRoute, and every AchievementCard now shows a "Reward: +{25|75|200} XP" label per D-151-19.

## What Was Built

### Files Modified

**`androidApp/.../navigation/Routes.kt`** (+4 lines)

Added two new `@Serializable data object` route declarations after `AchievementGalleryRoute`:
```kotlin
// Overview tab — Ranks & Achievements Browser (Phase 15.1)
@Serializable data object OverviewRootRoute
@Serializable data object RanksAndAchievementsRoute
```
File now has 14 `@Serializable data object` declarations (was 12).

**`androidApp/.../navigation/MainScreen.kt`** (+19 lines, -2 lines)

Three edits:
1. Added `import com.pumpernickel.android.ui.screens.RankLadderScreen`
2. Added `val overviewNavController: NavHostController = rememberNavController()` next to `workoutNavController`
3. Replaced the bare `OverviewScreen()` call at lines 161-164 with a NavHost (always-composed + alpha-gated):
   ```kotlin
   NavHost(navController = overviewNavController, startDestination = OverviewRootRoute) {
       composable<OverviewRootRoute> { OverviewScreen(navController = overviewNavController) }
       composable<RanksAndAchievementsRoute> { RankLadderScreen(navController = overviewNavController) }
       composable<AchievementGalleryRoute> { AchievementGalleryScreen(navController = overviewNavController) }
   }
   ```

`composable<AchievementGalleryRoute>` registration locations:
- **Workout tab** (~line 155): `AchievementGalleryScreen(navController = workoutNavController)` — UNCHANGED (Phase 15-09 path preserved)
- **Overview tab** (new): `AchievementGalleryScreen(navController = overviewNavController)` — for Overview back stack (D-151-15)

**`androidApp/.../screens/OverviewScreen.kt`** (+5 lines)

Three sub-edits:
1. Added `import androidx.navigation.NavHostController`
2. Added `import com.pumpernickel.android.ui.navigation.RanksAndAchievementsRoute`
3. Extended signature: `navController: NavHostController` added as FIRST parameter (before VM defaults)
4. Updated OverviewRankStrip call at **line 114** (post-edit numbering):
   ```kotlin
   OverviewRankStrip(
       rankState = rankState,
       onTap = { navController.navigate(RanksAndAchievementsRoute) }
   )
   ```

**`androidApp/.../screens/OverviewRankStrip.kt`** (+2 lines)

- Added `onTap: () -> Unit` parameter between `rankState` and `modifier`
- Changed `Card(modifier = ...)` to `Card(onClick = onTap, modifier = ...)` — Material 3 native clickable-card, no extra `Modifier.clickable` needed (per D-151-14)
- D-11 literal `"Unranked — complete a workout to unlock Silver"` at original line 73 is **byte-for-byte unchanged** (D-151-23)

**`androidApp/.../screens/AchievementGalleryScreen.kt`** (+14 lines)

- Inserted new Text composable in `AchievementCard` between tierLabel and flavourCopy:
  ```kotlin
  Text(
      text = "Reward: +${rewardXp(tile.tier)} XP",
      style = MaterialTheme.typography.labelSmall,
      color = MaterialTheme.colorScheme.onSurfaceVariant
  )
  ```
- Added `private fun rewardXp(tier: Tier) = when (tier)` helper after `tierLabel`:
  - `Tier.BRONZE -> 25`
  - `Tier.SILVER -> 75`
  - `Tier.GOLD -> 200`
- All four file-scope helpers exist: `tierLabel`, `tierColor`, `rewardXp`, `formatUnlockDate`

## Navigation Architecture After This Plan

| Tab | NavHost controller | Start destination | Routes registered |
|-----|--------------------|-------------------|-------------------|
| Workout (tab 0) | `workoutNavController` | `TemplateListRoute` | ...all workout routes... + `AchievementGalleryRoute` (Phase 15-09) |
| Overview (tab 1) | `overviewNavController` | `OverviewRootRoute` | `OverviewRootRoute`, `RanksAndAchievementsRoute`, `AchievementGalleryRoute` |
| Nutrition (tab 2) | `nutritionNavController` | `NutritionDailyLogRoute` | ...all nutrition routes... |

## D-11 Literal Confirmation (D-151-23)

`grep -F "Unranked — complete a workout to unlock Silver"` returns matches in BOTH:
- `OverviewRankStrip.kt` (existing, unchanged)
- `RankLadderScreen.kt` (Plan 02, unchanged)

Character-for-character match including em-dash U+2014 confirmed.

## Compile Verification

`./gradlew :androidApp:compileDebugKotlin` exits 0. Warnings are pre-existing from unrelated files (`NutritionDailyLogScreen.kt`, `WorkoutSessionViewModel.kt`, `MainScreen.kt` StringRes annotation). No warnings from any file modified in this plan.

Note: Worktree was missing Room schema files 2.json–7.json (these are untracked in the main working tree and weren't included in the worktree checkout). They were copied from the main working tree to unblock compilation. This is a pre-existing worktree limitation, not caused by this plan's changes.

## Commits

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Add OverviewRootRoute + RanksAndAchievementsRoute to Routes.kt | e5d539a | `Routes.kt` |
| 2 | Introduce Overview-tab NavHost in MainScreen.kt | ea04f12 | `MainScreen.kt` |
| 3 | Make OverviewRankStrip tappable, thread navController through OverviewScreen | 75a99f6 | `OverviewScreen.kt`, `OverviewRankStrip.kt` |
| 4 | Add D-151-19 reward-XP label to AchievementCard | 633a463 | `AchievementGalleryScreen.kt` |

## Notes for Downstream Plans

**Plan 04 (iOS handoff):** On Android, the Overview tab NavHost uses `overviewNavController` with start destination `OverviewRootRoute`. The strip's `onTap` lambda navigates to `RanksAndAchievementsRoute`. The iOS mirror of this pattern is: a `Button` (or `onTapGesture`) wrapping the rank strip view, pushing to a `RankLadderView` via `NavigationStack`. No equivalent of "OverviewRootRoute" needed on iOS — SwiftUI's `NavigationStack` provides back-stack separation natively. The strip navigation lambda on iOS will call into the `RanksAndAchievementsKoinHelper` (from Plan 01) to obtain the ViewModel.

## Deviations from Plan

**[Rule 3 - Blocking] Missing Room schema files in worktree**
- **Found during:** Task 4 gradle compile
- **Issue:** Worktree was created without the schema files 2.json–7.json that exist as untracked files in the main working tree. KSP's Room AutoMigration processor requires all intermediate schemas to be present.
- **Fix:** Copied the 6 missing schema files from the main working tree into the worktree's schema directory. No code changes required.
- **Files modified:** None (schema JSON files, not committed — they are already present on the main branch)

## Known Stubs

None — all navigation wiring is fully connected. The Overview tab NavHost registers `AchievementGalleryRoute` for future use; it is reachable from the Overview back stack should a "View all achievements" link be added to RankLadderScreen in a future iteration. No placeholder data or hardcoded strings introduced.

## Threat Flags

None — this plan introduces no new network endpoints, auth paths, file access patterns, or schema changes. All changes are pure Compose navigation wiring and UI text.

## Self-Check: PASSED

- FOUND: `androidApp/src/androidMain/kotlin/com/pumpernickel/android/ui/navigation/Routes.kt` contains `OverviewRootRoute` + `RanksAndAchievementsRoute`
- FOUND: `androidApp/src/androidMain/kotlin/com/pumpernickel/android/ui/navigation/MainScreen.kt` contains `overviewNavController` + `composable<RanksAndAchievementsRoute>` + two `composable<AchievementGalleryRoute>` registrations
- FOUND: `androidApp/src/androidMain/kotlin/com/pumpernickel/android/ui/screens/OverviewScreen.kt` contains `navController: NavHostController` + `onTap = { navController.navigate(RanksAndAchievementsRoute) }`
- FOUND: `androidApp/src/androidMain/kotlin/com/pumpernickel/android/ui/screens/OverviewRankStrip.kt` contains `onTap: () -> Unit` + `onClick = onTap` + D-11 literal unchanged
- FOUND: `androidApp/src/androidMain/kotlin/com/pumpernickel/android/ui/screens/AchievementGalleryScreen.kt` contains reward XP text + `rewardXp` helper with BRONZE=25, SILVER=75, GOLD=200
- FOUND commit: e5d539a (Task 1)
- FOUND commit: ea04f12 (Task 2)
- FOUND commit: 75a99f6 (Task 3)
- FOUND commit: 633a463 (Task 4)
- `./gradlew :androidApp:compileDebugKotlin` exits 0
