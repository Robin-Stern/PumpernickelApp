---
phase: 15.1-ranks-and-achievements-browser
verified: 2026-04-23T18:00:00Z
status: human_needed
score: 9/9 must-haves verified (all automated checks pass; 4 human checks required for iOS side)
overrides_applied: 0
human_verification:
  - test: "iOS RankLadderView.swift — build and run on iOS simulator"
    expected: "App builds without errors after creating RankLadderView.swift from handoff template; ladder shows all 10 tiers; Unranked state shows header copy and 'First workout unlocks' on SILVER"
    why_human: "Swift files not created by GSD (D-151-17 scope boundary); user writes them per handoff doc"
  - test: "iOS RanksAndAchievementsView.swift — navigation from Overview rank strip"
    expected: "Tapping OverviewRankStrip in iOS pushes RanksAndAchievementsView; 'View all achievements' NavigationLink within it reaches AchievementGalleryView"
    why_human: "Swift file not created by GSD; requires OverviewView.swift edit by user per Edit 3 in handoff doc"
  - test: "iOS AchievementGalleryView.swift — reward-XP label on tiles"
    expected: "Each tile in AchievementGalleryView shows 'Reward: +25 XP' / '+75 XP' / '+200 XP' between tier label and flavour copy"
    why_human: "Swift edit not performed by GSD; user applies Edit 4 from handoff doc"
  - test: "Android end-to-end smoke test — Overview tab -> rank strip tap -> Ranks screen -> back"
    expected: "Fresh install: rank strip tappable, navigates to RankLadderScreen with 10 rows, Unranked header, 'First workout unlocks' on SILVER; back returns to Overview tab with back stack preserved"
    why_human: "Visual + navigation flow requires running the Android app on device/emulator"
---

# Phase 15.1: Ranks & Achievements Browser — Verification Report

**Phase Goal:** Surface the rank ladder and achievement catalog as browsable UI — make the existing gamification state (rank tiers, achievement tiers + rewards) transparent to the user. Pure presentation over existing GamificationRepository / RankLadder / AchievementCatalog — no new domain logic, no schema changes, no new XP sources. Closes the Phase 15 UAT transparency gap (GAME-01 follow-up).
**Verified:** 2026-04-23T18:00:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

All automated must-haves are fully verified. The Kotlin/Android side of the phase is complete and correct. The iOS side is intentionally deferred to user implementation per D-151-17 — the handoff document is the deliverable.

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | RanksAndAchievementsViewModel exposes @NativeCoroutinesState rankLadderState backed by combine(rankState, totalXp) | ✓ VERIFIED | File line 68-77: `@NativeCoroutinesState val rankLadderState: StateFlow<RankLadderUiState> = combine(gamificationRepository.rankState, gamificationRepository.totalXp) { ... }.stateIn(...)` |
| 2 | RankLadderUiState + RankRow + RankRowStatus with correct fields declared in same file | ✓ VERIFIED | Lines 24-47 of RanksAndAchievementsViewModel.kt: all three types present with correct fields; enum has exactly PASSED, CURRENT, LOCKED |
| 3 | D-151-07 derivation rules: Unranked→all LOCKED, SILVER xpToReach=0; Ranked→PASSED/CURRENT/LOCKED by ordinal, rows ordered by Rank.values() | ✓ VERIFIED | Lines 88-138: both when-branches present; `val allRanks = Rank.values().toList()` at line 89; SILVER xpToReach=0L at line 94; coerceAtLeast(0L) for LOCKED rows |
| 4 | VM registered in existing GamificationUiModule (not a new module) with arity-1 constructor | ✓ VERIFIED | GamificationUiModule.kt line 15: `viewModel { RanksAndAchievementsViewModel(get()) }` alongside existing GamificationViewModel binding |
| 5 | iOS KoinHelper is a class exposing getRanksAndAchievementsViewModel() via KoinPlatform | ✓ VERIFIED | RanksAndAchievementsKoinHelper.kt: `class RanksAndAchievementsKoinHelper { fun getRanksAndAchievementsViewModel(): RanksAndAchievementsViewModel = KoinPlatform.getKoin().get() }` |
| 6 | Android RankLadderScreen renders 10 tiers with PASSED/CURRENT/LOCKED, D-11 literal, First workout unlocks, locale-formatted XP, ranked dimming | ✓ VERIFIED | RankLadderScreen.kt (270 lines): all three RankRowStatus branches, `alpha(if (isLocked) 0.45f else 1f)`, `%,d XP`.format, `%,d XP to unlock`.format, D-11 literal character-for-character |
| 7 | Overview-tab NavHost with 3 routes; two composable<AchievementGalleryRoute> registrations; OverviewRankStrip tappable via Card(onClick=onTap) | ✓ VERIFIED | MainScreen.kt: `val overviewNavController`, NavHost with OverviewRootRoute/RanksAndAchievementsRoute/AchievementGalleryRoute; `composable<AchievementGalleryRoute>` count=2; OverviewRankStrip.kt line 43-44: `Card(onClick = onTap, ...)` |
| 8 | AchievementCard gains Reward:+25/75/200 XP label derived from tier (D-151-19) | ✓ VERIFIED | AchievementGalleryScreen.kt lines 170-176: `text = "Reward: +${rewardXp(tile.tier)} XP"` + `private fun rewardXp` with BRONZE=25, SILVER=75, GOLD=200 |
| 9 | iOS handoff doc (15.1-IOS-HANDOFF.md) exists with all required sections and does not prescribe pbxproj edits | ✓ VERIFIED | File exists (457 lines); contains RankLadderView.swift + RanksAndAchievementsView.swift paths, VM retrieval call, asyncSequence(for: rankLadderStateFlow), NESTED typealias comment, D-11 literal, First workout unlocks, Reward:+ label, D-151-17 pbxproj disclaimer |

**Score:** 9/9 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `shared/src/commonMain/kotlin/com/pumpernickel/presentation/gamification/RanksAndAchievementsViewModel.kt` | VM + RankLadderUiState + RankRow + RankRowStatus | ✓ VERIFIED | 139 lines; all four types declared; combine wiring present |
| `shared/src/commonMain/kotlin/com/pumpernickel/di/GamificationUiModule.kt` | Koin registration alongside GamificationViewModel | ✓ VERIFIED | Both viewModel bindings present; arity-1 for new VM |
| `shared/src/iosMain/kotlin/com/pumpernickel/di/RanksAndAchievementsKoinHelper.kt` | iOS factory class | ✓ VERIFIED | 16 lines; class (not object); one getter; KoinPlatform.getKoin().get() |
| `androidApp/src/androidMain/kotlin/com/pumpernickel/android/ui/screens/RankLadderScreen.kt` | Compose screen with full ladder rendering | ✓ VERIFIED | 270 lines; all acceptance criteria confirmed |
| `androidApp/src/androidMain/kotlin/com/pumpernickel/android/ui/navigation/Routes.kt` | OverviewRootRoute + RanksAndAchievementsRoute | ✓ VERIFIED | Both `@Serializable data object` declarations present on lines 31-32 |
| `androidApp/src/androidMain/kotlin/com/pumpernickel/android/ui/navigation/MainScreen.kt` | Overview-tab NavHost with 3 routes, overviewNavController | ✓ VERIFIED | overviewNavController declared; NavHost with OverviewRootRoute startDestination; 2x composable<AchievementGalleryRoute> |
| `androidApp/src/androidMain/kotlin/com/pumpernickel/android/ui/screens/OverviewScreen.kt` | navController param + onTap lambda wired to RanksAndAchievementsRoute | ✓ VERIFIED | First param `navController: NavHostController`; onTap = { navController.navigate(RanksAndAchievementsRoute) } at line 117 |
| `androidApp/src/androidMain/kotlin/com/pumpernickel/android/ui/screens/OverviewRankStrip.kt` | onTap param + Card(onClick=onTap); D-11 literal preserved | ✓ VERIFIED | `onTap: () -> Unit` param present; `Card(onClick = onTap, ...)` at line 43-44; D-11 literal byte-for-byte unchanged |
| `androidApp/src/androidMain/kotlin/com/pumpernickel/android/ui/screens/AchievementGalleryScreen.kt` | rewardXp helper + Reward:+ label | ✓ VERIFIED | Text label at line 173; rewardXp helper at line 211-215; BRONZE=25, SILVER=75, GOLD=200 |
| `.planning/phases/15.1-ranks-and-achievements-browser/15.1-IOS-HANDOFF.md` | iOS handoff specification | ✓ VERIFIED | 457 lines; all 13 required grep assertions pass per Plan 04 acceptance criteria |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| RanksAndAchievementsViewModel.rankLadderState | GamificationRepository.rankState + .totalXp | combine(...) { ... }.stateIn(...) | ✓ WIRED | Lines 68-77 of VM file; both repo flows combined |
| GamificationUiModule | RanksAndAchievementsViewModel | viewModel { RanksAndAchievementsViewModel(get()) } | ✓ WIRED | GamificationUiModule.kt line 15 |
| RanksAndAchievementsKoinHelper | RanksAndAchievementsViewModel (via Koin) | KoinPlatform.getKoin().get() | ✓ WIRED | KoinHelper.kt line 14-15 |
| RankLadderScreen | RanksAndAchievementsViewModel.rankLadderState | koinViewModel() + collectAsState() | ✓ WIRED | RankLadderScreen.kt lines 68-70 |
| RankLadderScreen unranked header | D-11 literal (char-for-char match with OverviewRankStrip) | identical string constant | ✓ WIRED | Both files: `"Unranked — complete a workout to unlock Silver"` with U+2014 em-dash confirmed via Python character comparison |
| OverviewScreen.OverviewRankStrip onTap | overviewNavController.navigate(RanksAndAchievementsRoute) | lambda in OverviewScreen | ✓ WIRED | OverviewScreen.kt lines 115-118 |
| MainScreen Overview-tab NavHost | RankLadderScreen | composable<RanksAndAchievementsRoute> | ✓ WIRED | MainScreen.kt line 172-174 |
| MainScreen Overview-tab NavHost | AchievementGalleryScreen (Overview back stack) | duplicate composable<AchievementGalleryRoute> | ✓ WIRED | MainScreen.kt lines 175-180; count=2 confirmed |
| AchievementCard tile | Reward: +{rewardXp} XP label | Text composable calling rewardXp() helper | ✓ WIRED | AchievementGalleryScreen.kt lines 170-176; rewardXp helper at lines 211-215 |
| iOS handoff doc | asyncSequence(for: viewModel.rankLadderStateFlow) | observation pattern documented | ✓ WIRED | Handoff doc line 134; mirrors OverviewView.swift pattern |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| RanksAndAchievementsViewModel | rankLadderState | combine(GamificationRepository.rankState, GamificationRepository.totalXp) | Yes — both flows come from DB-backed repository established in Phase 15 | ✓ FLOWING |
| RankLadderScreen | uiState (via collectAsState) | RanksAndAchievementsViewModel.rankLadderState | Yes — all 10 rows rendered from real VM state | ✓ FLOWING |
| AchievementGalleryScreen | rewardXp(tile.tier) | private fun rewardXp(): pure derivation from tier enum | Yes — 25/75/200 derived from Tier.BRONZE/SILVER/GOLD | ✓ FLOWING |

### Behavioral Spot-Checks

Step 7b: SKIPPED (no runnable server entry points testable without device/emulator). Compilation checks substitute:

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| :shared:compileKotlinMetadata succeeds | Reported in Plan 01 SUMMARY | exits 0 per SUMMARY | ✓ PASS |
| :androidApp:compileDebugKotlin succeeds | Reported in Plan 03 SUMMARY | exits 0 per SUMMARY; no new warnings from phase files | ✓ PASS |
| No Swift/pbxproj files modified by GSD | git log --stat on phase commits | Only .kt, .md files in commits | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| GAME-01 | Plans 01, 02, 03, 04 | Gamification — XP, achievements, ranks (F4); follow-up: make rank ladder and achievement catalog browsable | ✓ SATISFIED | Full rank ladder screen (Android); tappable rank strip; reward-XP on tiles; iOS handoff for user implementation; no domain changes |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| OverviewRankStrip.kt | 91 | `TODO(polish): swap to per-rank icon/badge asset` | ℹ️ Info | Pre-existing from Phase 15-08; not introduced by Phase 15.1; does not block phase goal |
| OverviewRankStrip.kt | 129 | `TODO(polish): per-rank gradient colours` | ℹ️ Info | Pre-existing from Phase 15-08; not introduced by Phase 15.1; tier-banded tint is sufficient for this phase |

No blockers or warnings from files introduced or modified by Phase 15.1.

### Human Verification Required

#### 1. iOS RankLadderView.swift Implementation

**Test:** Create `iosApp/iosApp/Views/Overview/RankLadderView.swift` from the template in the handoff doc (File 1 section). Build the iOS target in Xcode.
**Expected:** Build succeeds; launching the app on the Overview tab shows the tappable rank strip; tapping opens a Ranks screen with all 10 tiers listed SILVER → GLOBAL_ELITE; Unranked state shows the header copy ("Unranked — complete a workout to unlock Silver" with em-dash) and SILVER row shows "First workout unlocks"; other LOCKED rows show "{X,XXX} XP to unlock" with locale-formatted thousands separator.
**Why human:** Swift files are not created by GSD per D-151-17 scope boundary (user hand-writes SwiftUI per MEMORY.md). The Kotlin KoinHelper is shipped; the Swift-side factory call and observation pattern are specified in the handoff doc.

#### 2. iOS RanksAndAchievementsView.swift + OverviewView.swift Edit

**Test:** Create `iosApp/iosApp/Views/Overview/RanksAndAchievementsView.swift` and apply Edit 3 to `OverviewView.swift` (wrap `OverviewRankStrip(rankState: rankState)` in a `NavigationLink { RanksAndAchievementsView() }` with `.buttonStyle(.plain)`).
**Expected:** Tapping the rank strip on the Overview tab pushes `RanksAndAchievementsView`; the container shows the rank ladder on top; scrolling to the bottom shows a "View all achievements" NavigationLink; pressing back returns to Overview. The existing Settings → Achievements path still works independently.
**Why human:** Swift edits to existing and new files; requires Xcode build + device/simulator run to verify visual push navigation and back-stack behavior.

#### 3. iOS AchievementGalleryView.swift Reward-XP Label

**Test:** Apply Edit 4 from the handoff doc to `iosApp/iosApp/Views/Gamification/AchievementGalleryView.swift` — add `rewardXp()` helper and insert `Text("Reward: +\(rewardXp(tile.tier)) XP")` in `AchievementTileView`.
**Expected:** Every achievement tile (locked and unlocked) shows a small secondary "Reward: +25 XP" / "+75 XP" / "+200 XP" line between the tier label and the flavour copy text. BRONZE=+25, SILVER=+75, GOLD=+200.
**Why human:** Swift edit to existing file; requires visual inspection on device/simulator to confirm correct placement and values.

#### 4. Android End-to-End Smoke Test

**Test:** Install the Android app (debug build), open Overview tab, tap the rank strip, verify navigation and content.
**Expected:** (1) Fresh install: Unranked header at top of Ranks screen, 10 rows all LOCKED, SILVER="First workout unlocks", others show "{threshold:,} XP to unlock"; (2) After completing a workout: SILVER row = CURRENT (star icon), higher ranks LOCKED with correct XP deltas; (3) Back button returns to Overview tab; (4) Workout tab → Settings → Achievements still works (Phase 15-09 path unregressed).
**Why human:** Requires running the app; the visual flow, ripple on Card tap, navigation animation, and live state updates from GamificationRepository cannot be verified via static analysis.

### Gaps Summary

No gaps found. All automated must-haves pass. The phase's Kotlin/Android deliverables are complete and correct. The iOS Swift implementation is intentionally human-gated per D-151-17 — this is by design and tracked under human verification, not as a gap.

---

## Detailed Verification Notes

### D-11 Literal Check
The load-bearing string `"Unranked — complete a workout to unlock Silver"` was verified character-by-character via Python `ord()` comparison across both Android call sites:
- `OverviewRankStrip.kt` line 75 (text = property) — matches exactly, U+2014 em-dash confirmed
- `RankLadderScreen.kt` line 139 (text = property) — matches exactly, U+2014 em-dash confirmed

### D-151-07 Derivation Rules
- `Rank.values().toList()` at VM line 89 ensures SILVER→GLOBAL_ELITE order
- SILVER xpToReach=0L in Unranked branch at line 94 (`if (rank == Rank.SILVER) 0L else threshold`)
- LOCKED rows in Ranked branch use `(threshold - totalXp).coerceAtLeast(0L)` at line 120
- PASSED/CURRENT/LOCKED by ordinal comparison at lines 114-117

### D-151-22 No Domain Changes
Git log over all phase commits (5eba5d8..fc1f32e) shows zero changes to:
- `GamificationEngine.kt`
- `GamificationRepository.kt`
- `RankLadder.kt`
- `AchievementCatalog.kt`
- Any `schema/*.json` migration file

### Two composable<AchievementGalleryRoute> Registrations
`grep -c "composable<AchievementGalleryRoute>"` returns exactly 2 in MainScreen.kt:
- Line 157: `AchievementGalleryScreen(navController = workoutNavController)` — workout tab (Phase 15-09 unchanged)
- Line 175: `AchievementGalleryScreen(navController = overviewNavController)` — overview tab (new, D-151-15)

### iOS Handoff No pbxproj Edits
The handoff document explicitly states on line 5: "this phase does NOT modify `iosApp/iosApp.xcodeproj/project.pbxproj`" and again on line 453: "Xcode pbxproj modifications — per D-151-17 this is a follow-up action the user performs in Xcode". Git commit fc1f32e shows only the handoff .md file was created — no Swift or pbxproj modifications.

---

_Verified: 2026-04-23T18:00:00Z_
_Verifier: Claude (gsd-verifier)_
