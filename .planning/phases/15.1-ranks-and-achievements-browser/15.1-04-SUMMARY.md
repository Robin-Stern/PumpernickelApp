---
phase: "15.1"
plan: "04"
subsystem: "ios-handoff-documentation"
tags: [ios, handoff, documentation, swiftui, rank-ladder, achievements]
dependency_graph:
  requires:
    - "15.1-01-SUMMARY.md (RanksAndAchievementsViewModel + RanksAndAchievementsKoinHelper shipped)"
  provides:
    - "15.1-IOS-HANDOFF.md — complete SwiftUI implementation spec for the iOS side of Phase 15.1"
  affects:
    - "iosApp/iosApp/Views/Overview/RankLadderView.swift (user will create per spec)"
    - "iosApp/iosApp/Views/Overview/RanksAndAchievementsView.swift (user will create per spec)"
    - "iosApp/iosApp/Views/Overview/OverviewView.swift (user will modify per spec)"
    - "iosApp/iosApp/Views/Gamification/AchievementGalleryView.swift (user will modify per spec)"
tech_stack:
  added: []
  patterns:
    - "iOS handoff doc pattern — documentation-only plan, no Swift/Kotlin/pbxproj touches"
    - "NavigationLink + .buttonStyle(.plain) for card-tap navigation (D-151-14 iOS mirror)"
    - "asyncSequence(for: viewModel.rankLadderStateFlow) observation pattern"
    - "RanksAndAchievementsKoinHelper().getRanksAndAchievementsViewModel() class-style factory call"
key_files:
  created:
    - ".planning/phases/15.1-ranks-and-achievements-browser/15.1-IOS-HANDOFF.md"
  modified: []
decisions:
  - "Document-only plan per D-151-16/17 — GSD stops at Kotlin/Koin boundary; user writes SwiftUI"
  - "Handoff covers 4 surfaces: 2 new Swift files + 2 existing-file edits"
  - "Typealias block uses NESTED Shared.RankState.Unranked / Shared.RankState.Ranked form per Phase 15 discovery"
  - "D-11 copy 'Unranked — complete a workout to unlock Silver' preserved verbatim (em dash U+2014)"
metrics:
  duration: "130s"
  completed_date: "2026-04-23"
  tasks_completed: 1
  files_created: 1
  files_modified: 0
---

# Phase 15.1 Plan 04: iOS Handoff — Ranks & Achievements Browser Summary

iOS implementation contract for Phase 15.1: a 457-line Markdown specification that tells the user exactly what SwiftUI files to create, what existing files to edit, and how to wire `RanksAndAchievementsKoinHelper().getRanksAndAchievementsViewModel()` with the KMP-NativeCoroutines `asyncSequence(for: viewModel.rankLadderStateFlow)` observation pattern.

## What Was Built

### Files Created

**`.planning/phases/15.1-ranks-and-achievements-browser/15.1-IOS-HANDOFF.md`** (457 lines)

A complete, self-contained iOS handoff document structured around four implementation surfaces:

| Surface | Section |
|---|---|
| NEW `RankLadderView.swift` | File 1 — full SwiftUI template, typealias block, 5 gotchas |
| NEW `RanksAndAchievementsView.swift` | File 2 — container template, NavigationLink to gallery, 4 gotchas |
| MODIFY `OverviewView.swift` | Edit 3 — NavigationLink wrap of `OverviewRankStrip` with `.buttonStyle(.plain)` |
| MODIFY `AchievementGalleryView.swift` | Edit 4 — `rewardXp()` helper + "Reward: +N XP" label (D-151-19) |

Document also includes:
- Kotlin contract excerpt (types, enum cases, VM retrieval idiom)
- `asyncSequence(for: viewModel.rankLadderStateFlow)` observation pattern (mirrors OverviewView.swift lines 73-81)
- Nested typealias block warning (`Shared.RankState.Unranked` NOT flat `SharedRankStateUnranked`)
- Settings entry preservation note (D-151-02 / D-21)
- 8-step verification checklist
- Out-of-scope section (deferred ideas from CONTEXT.md)

### Files Modified

None — documentation-only plan.

## Key Decisions Documented

- **D-151-17 honoured:** No Swift, Kotlin, or pbxproj files touched. The handoff doc explicitly states the user must add new Swift files via Xcode's "Add Files to…" dialog — not a GSD edit.
- **D-11 copy preserved:** `"Unranked — complete a workout to unlock Silver"` appears verbatim in the `unrankedHeader` template (same em dash U+2014 as OverviewRankStrip.swift line 45).
- **D-151-07 edge case documented:** SILVER row when unranked shows `"First workout unlocks"` rather than `"0 XP to unlock"` — the D-11 contract surfaced at row granularity.
- **D-151-18 locale formatting:** `NumberFormatter.localizedString(from:number:)` specified for XP thresholds (not string interpolation).
- **D-151-19 reward label:** `"Reward: +\(rewardXp(tile.tier)) XP"` insert between displayName and flavourCopy in AchievementTileView, with `rewardXp()` helper deriving 25/75/200 from tier.

## Verification Results

All 13 automated grep assertions from Task 1 `<verify>` block pass:

| Check | Result |
|---|---|
| File exists | PASS |
| `iosApp/iosApp/Views/Overview/RankLadderView.swift` path | PASS |
| `iosApp/iosApp/Views/Overview/RanksAndAchievementsView.swift` path | PASS |
| `RanksAndAchievementsKoinHelper().getRanksAndAchievementsViewModel()` | PASS |
| `asyncSequence(for: viewModel.rankLadderStateFlow)` | PASS |
| `Unranked — complete a workout to unlock Silver` (em dash) | PASS |
| `First workout unlocks` | PASS |
| `Reward: +` | PASS |
| `AchievementGalleryView.swift` | PASS |
| `OverviewView.swift` | PASS |
| `.buttonStyle(.plain)` | PASS |
| `D-151-17` | PASS |
| `NumberFormatter.localizedString` | PASS |

No Swift files, Kotlin files, or pbxproj files touched (verified via `git diff`).

## Note to User

Implement the four iOS-side changes described in `15.1-IOS-HANDOFF.md`:

1. Create `iosApp/iosApp/Views/Overview/RankLadderView.swift` from the template in File 1
2. Create `iosApp/iosApp/Views/Overview/RanksAndAchievementsView.swift` from the template in File 2
3. Edit `iosApp/iosApp/Views/Overview/OverviewView.swift` — wrap `OverviewRankStrip(rankState: rankState)` in a `NavigationLink { RanksAndAchievementsView() }` with `.buttonStyle(.plain)` (Edit 3)
4. Edit `iosApp/iosApp/Views/Gamification/AchievementGalleryView.swift` — add the `rewardXp()` helper and `"Reward: +\(rewardXp(tile.tier)) XP"` Text in `AchievementTileView` (Edit 4)

After completing all four changes, run the 8-step verification checklist at the bottom of the handoff doc to confirm the implementation.

Remember to add the two new Swift files to the Xcode project (drag them in or use File → Add Files to "iosApp"…) — per D-151-17 this step is outside GSD plan execution.

## Deviations from Plan

None — plan executed exactly as written. The handoff document body matches the plan's `<action>` specification with two minor improvements applied inline:
- Edit 4 section updated the approximate line numbers to match the actual current state of `AchievementGalleryView.swift` (lines 112-193, `displayName` at ~129) based on reading the live file.
- Edit 4 `rewardXp()` helper placed after the existing `tierColor` / `tierLabel` computed properties per the actual file's structure (function-scope within `AchievementTileView`, not file-scope as written in plan body — both work; function-scope is cleaner given `SharedTier` is already in scope).

## Known Stubs

None — this is a documentation-only plan. The handoff document contains Swift code templates that the user will copy and implement; no stub data flows through production code paths.

## Threat Flags

None — documentation file only, no new network endpoints, auth paths, file access patterns, or schema changes.

## Commits

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Write 15.1-IOS-HANDOFF.md — complete SwiftUI handoff spec | fc1f32e | `.planning/phases/15.1-ranks-and-achievements-browser/15.1-IOS-HANDOFF.md` (created, 457 lines) |

## Self-Check: PASSED

- FOUND: `.planning/phases/15.1-ranks-and-achievements-browser/15.1-IOS-HANDOFF.md`
- FOUND commit: fc1f32e
- All 13 grep assertions pass
- No Swift/Kotlin/pbxproj files modified (git diff confirms)
