---
phase: 22-anthropic-ai-provider-3-provider-neben-openai-together-mit-o
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/src/commonMain/kotlin/com/pumpernickel/domain/repository/SettingsRepository.kt
  - shared/src/commonMain/kotlin/com/pumpernickel/data/repository/SettingsRepositoryImpl.kt
autonomous: true
requirements:
  - D-22-08
  - D-22-09
must_haves:
  truths:
    - "activeProvider als Flow<ProviderId> ist aus SettingsRepository observable"
    - "modelByProvider liefert für jeden Provider den persistierten Modell-Namen (mit Defaults)"
    - "baseUrlByProvider liefert für jeden Provider die Base-URL (Anthropic fixed, OpenAI/Together overridable)"
    - "migratedToMultiProvider-Sentinel macht die one-time Migration idempotent"
  artifacts:
    - path: shared/src/commonMain/kotlin/com/pumpernickel/domain/repository/SettingsRepository.kt
      provides: "Erweiterung um activeProvider, modelByProvider, baseUrlByProvider, migratedToMultiProvider"
    - path: shared/src/commonMain/kotlin/com/pumpernickel/data/repository/SettingsRepositoryImpl.kt
      provides: "DataStore-Persistenz für active_provider + model_<provider> + base_url_<provider> + migrated_to_multi_provider"
  key_links:
    - from: "SettingsRepositoryImpl.activeProvider"
      to: "DataStore key 'active_provider'"
      via: "stringPreferencesKey + ProviderId.fromWireNameOrNull mapping"
      pattern: "active_provider"
    - from: "SettingsRepositoryImpl.modelByProvider"
      to: "DataStore keys model_openai / model_together / model_anthropic"
      via: "combine over three stringPreferencesKey flows"
      pattern: "model_openai|model_together|model_anthropic"
---

<objective>
Erweitere `SettingsRepository` (interface + Impl) um Multi-Provider-Konfiguration: aktiver Provider, per-Provider-Modell und per-Provider-Base-URL plus einen `migratedToMultiProvider`-Sentinel für die one-time Migration in Plan 06.

Purpose: Settings-State als einzige Source-of-Truth für Provider-Switch. Der Koin-DI-Layer (Plan 07) resolved den AiClient basierend auf `activeProvider.first()`; die UI (Plan 08) edits `setActiveProvider()` und `setModel(provider, model)`. Bestehende `aiBaseUrl / aiModel / aiProviderPreset`-Flows bleiben backward-kompatibel als deprecated-View über die neuen Keys (Plan 07 entscheidet Lebensdauer).

Output: Neue Flows + Setter im Interface, DataStore-Keys + Combine-Impl im Repository. Defaults: OpenAI / `gpt-4o-mini` / `https://api.openai.com/v1` — Together / `google/gemma-4-31B-it` / `https://api.together.ai/v1` — Anthropic / `claude-opus-4-7` / `https://api.anthropic.com`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/22-anthropic-ai-provider-3-provider-neben-openai-together-mit-o/22-CONTEXT.md
@.planning/phases/22-anthropic-ai-provider-3-provider-neben-openai-together-mit-o/22-PATTERNS.md
@shared/src/commonMain/kotlin/com/pumpernickel/domain/repository/SettingsRepository.kt
@shared/src/commonMain/kotlin/com/pumpernickel/data/repository/SettingsRepositoryImpl.kt

<interfaces>
<!-- Existing AI section in SettingsRepository (must be EXTENDED, kept backward-compatible). -->
```kotlin
// AI config (D-18-06)
val aiProviderPreset: Flow<String>
val aiBaseUrl: Flow<String>
val aiModel: Flow<String>
suspend fun setAiProviderPreset(preset: String)
suspend fun setAiBaseUrl(url: String)
suspend fun setAiModel(model: String)
```

<!-- Pattern reference — same combine{} approach used for NutritionGoals. -->
```kotlin
override val nutritionGoals: Flow<NutritionGoals> = combine(
    dataStore.data.map { it[calorieGoalKey]?.toIntOrNull() ?: 2500 },
    dataStore.data.map { it[proteinGoalKey]?.toIntOrNull() ?: 150 },
    // ...
) { cal, pro, fat, carb, sugar -> NutritionGoals(cal, pro, fat, carb, sugar) }
```

<!-- ProviderId enum (Plan 01) — assumed to compile in commonMain by the time this plan runs. -->
```kotlin
enum class ProviderId(val wireName: String) { OpenAI("openai"), Together("together"), Anthropic("anthropic") }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SettingsRepository interface extension (commonMain)</name>
  <files>shared/src/commonMain/kotlin/com/pumpernickel/domain/repository/SettingsRepository.kt</files>
  <read_first>
    shared/src/commonMain/kotlin/com/pumpernickel/domain/repository/SettingsRepository.kt,
    shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/ProviderId.kt
  </read_first>
  <action>
Hänge nach der bestehenden "AI config (D-18-06)"-Sektion (Zeilen 65-71) eine neue "Multi-Provider AI config (D-22-09)"-Sektion an. NICHT die alten Flows entfernen — bleiben für Backward-Compat während der Migration.

Füge folgende Members in `interface SettingsRepository` ein (direkt nach `suspend fun setAiModel(model: String)`):

```kotlin
    // Multi-Provider AI config (D-22-09 / Phase 22) — supersedes the single
    // aiBaseUrl/aiModel/aiProviderPreset triple above. The legacy flows remain
    // available so SettingsMigration (Plan 22-06) and any in-flight reader can
    // migrate gracefully; new call sites must use the per-provider flows.

    /** Currently active provider for all AI generation. Default: OpenAI. */
    val activeProvider: Flow<com.pumpernickel.infrastructure.ai.ProviderId>
    suspend fun setActiveProvider(provider: com.pumpernickel.infrastructure.ai.ProviderId)

    /**
     * Per-provider model id snapshot. Defaults:
     * - OpenAI:    `gpt-4o-mini`
     * - Together:  `google/gemma-4-31B-it`
     * - Anthropic: `claude-opus-4-7` (D-22-06)
     */
    val modelByProvider: Flow<Map<com.pumpernickel.infrastructure.ai.ProviderId, String>>
    suspend fun setModel(provider: com.pumpernickel.infrastructure.ai.ProviderId, model: String)

    /**
     * Per-provider base URL. Defaults:
     * - OpenAI:    `https://api.openai.com/v1`
     * - Together:  `https://api.together.ai/v1`
     * - Anthropic: `https://api.anthropic.com` (fixed; user override has no effect)
     */
    val baseUrlByProvider: Flow<Map<com.pumpernickel.infrastructure.ai.ProviderId, String>>
    suspend fun setBaseUrl(provider: com.pumpernickel.infrastructure.ai.ProviderId, url: String)

    /** One-shot migration sentinel (D-22-08); true after SettingsMigration has run successfully. */
    val migratedToMultiProvider: Flow<Boolean>
    suspend fun setMigratedToMultiProvider(value: Boolean)
```

KEINE Imports am Anfang ergänzen — fully-qualified `com.pumpernickel.infrastructure.ai.ProviderId` lässt den File-Header unverändert und vermeidet Reihenfolge-Probleme im interface-Block. Die `Flow`-Imports sind bereits da.
  </action>
  <verify>
    <automated>grep -c "val activeProvider: Flow" shared/src/commonMain/kotlin/com/pumpernickel/domain/repository/SettingsRepository.kt | grep -q "^1$" && grep -q "val modelByProvider: Flow<Map<com.pumpernickel.infrastructure.ai.ProviderId, String>>" shared/src/commonMain/kotlin/com/pumpernickel/domain/repository/SettingsRepository.kt && grep -q "val baseUrlByProvider: Flow<Map<com.pumpernickel.infrastructure.ai.ProviderId, String>>" shared/src/commonMain/kotlin/com/pumpernickel/domain/repository/SettingsRepository.kt && grep -q "val migratedToMultiProvider: Flow<Boolean>" shared/src/commonMain/kotlin/com/pumpernickel/domain/repository/SettingsRepository.kt && grep -q "val aiBaseUrl: Flow<String>" shared/src/commonMain/kotlin/com/pumpernickel/domain/repository/SettingsRepository.kt</automated>
  </verify>
  <acceptance_criteria>
- activeProvider, modelByProvider, baseUrlByProvider, migratedToMultiProvider sind als Flow-properties im Interface deklariert
- Setter setActiveProvider / setModel / setBaseUrl / setMigratedToMultiProvider sind deklariert
- Bestehende aiProviderPreset / aiBaseUrl / aiModel / Setter sind UNBERÜHRT
- Build (interface-only check): `./gradlew :shared:compileCommonMainKotlinMetadata` läuft an — compile-broken solange Impl noch nicht override-ed (in Task 2 fixed)
  </acceptance_criteria>
  <done>Interface erweitert; nächste Task (Impl) macht commonMain wieder grün.</done>
</task>

<task type="auto">
  <name>Task 2: SettingsRepositoryImpl persistence + combine flows</name>
  <files>shared/src/commonMain/kotlin/com/pumpernickel/data/repository/SettingsRepositoryImpl.kt</files>
  <read_first>
    shared/src/commonMain/kotlin/com/pumpernickel/data/repository/SettingsRepositoryImpl.kt,
    shared/src/commonMain/kotlin/com/pumpernickel/domain/repository/SettingsRepository.kt,
    shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/ProviderId.kt
  </read_first>
  <action>
Erweitere `SettingsRepositoryImpl` (Datei-Ende, NACH der `D-19-04 — PendingGeofenceExitStore implementation`-Sektion). Aktiver Provider, per-Provider-Model/BaseUrl, Migration-Sentinel.

(1) Imports oben in der Datei ergänzen:
```kotlin
import com.pumpernickel.infrastructure.ai.ProviderId
```

(2) DataStore-Keys (im Companion- oder field-Bereich nahe den anderen AI-Keys ~Zeile 238):
```kotlin
    // Phase 22 — Multi-Provider AI config (D-22-09)
    private val activeProviderKey = stringPreferencesKey("active_provider")
    private val modelOpenAiKey = stringPreferencesKey("model_openai")
    private val modelTogetherKey = stringPreferencesKey("model_together")
    private val modelAnthropicKey = stringPreferencesKey("model_anthropic")
    private val baseUrlOpenAiKey = stringPreferencesKey("base_url_openai")
    private val baseUrlTogetherKey = stringPreferencesKey("base_url_together")
    private val baseUrlAnthropicKey = stringPreferencesKey("base_url_anthropic")
    private val migratedToMultiProviderKey = booleanPreferencesKey("migrated_to_multi_provider")
```

(3) Override-Implementations (am File-Ende, vor der schließenden `}`):
```kotlin
    // ============================================================
    // D-22-09 — Multi-Provider AI configuration
    // ============================================================

    override val activeProvider: Flow<ProviderId> = dataStore.data.map { prefs ->
        ProviderId.fromWireNameOrNull(prefs[activeProviderKey]) ?: ProviderId.OpenAI
    }

    override suspend fun setActiveProvider(provider: ProviderId) {
        dataStore.edit { prefs -> prefs[activeProviderKey] = provider.wireName }
    }

    override val modelByProvider: Flow<Map<ProviderId, String>> = dataStore.data.map { prefs ->
        mapOf(
            ProviderId.OpenAI to (prefs[modelOpenAiKey] ?: DEFAULT_MODEL_OPENAI),
            ProviderId.Together to (prefs[modelTogetherKey] ?: DEFAULT_MODEL_TOGETHER),
            ProviderId.Anthropic to (prefs[modelAnthropicKey] ?: DEFAULT_MODEL_ANTHROPIC)
        )
    }

    override suspend fun setModel(provider: ProviderId, model: String) {
        val key = when (provider) {
            ProviderId.OpenAI -> modelOpenAiKey
            ProviderId.Together -> modelTogetherKey
            ProviderId.Anthropic -> modelAnthropicKey
        }
        dataStore.edit { prefs -> prefs[key] = model }
    }

    override val baseUrlByProvider: Flow<Map<ProviderId, String>> = dataStore.data.map { prefs ->
        mapOf(
            ProviderId.OpenAI to (prefs[baseUrlOpenAiKey] ?: DEFAULT_BASE_URL_OPENAI),
            ProviderId.Together to (prefs[baseUrlTogetherKey] ?: DEFAULT_BASE_URL_TOGETHER),
            // D-22-01: Anthropic ist fixed — user override hat keinen Effekt (kein Setter-Path).
            ProviderId.Anthropic to DEFAULT_BASE_URL_ANTHROPIC
        )
    }

    override suspend fun setBaseUrl(provider: ProviderId, url: String) {
        val key = when (provider) {
            ProviderId.OpenAI -> baseUrlOpenAiKey
            ProviderId.Together -> baseUrlTogetherKey
            // D-22-01: Anthropic URL ist fixed — setBaseUrl(Anthropic, ...) ist ein No-Op.
            ProviderId.Anthropic -> return
        }
        dataStore.edit { prefs -> prefs[key] = url }
    }

    override val migratedToMultiProvider: Flow<Boolean> = dataStore.data.map { prefs ->
        prefs[migratedToMultiProviderKey] ?: false
    }

    override suspend fun setMigratedToMultiProvider(value: Boolean) {
        dataStore.edit { prefs -> prefs[migratedToMultiProviderKey] = value }
    }

    companion object {
        const val DEFAULT_MODEL_OPENAI = "gpt-4o-mini"
        const val DEFAULT_MODEL_TOGETHER = "google/gemma-4-31B-it"
        const val DEFAULT_MODEL_ANTHROPIC = "claude-opus-4-7"
        const val DEFAULT_BASE_URL_OPENAI = "https://api.openai.com/v1"
        const val DEFAULT_BASE_URL_TOGETHER = "https://api.together.ai/v1"
        const val DEFAULT_BASE_URL_ANTHROPIC = "https://api.anthropic.com"
    }
```

WICHTIG: Die existierende `companion object`-Konvention in der Datei ist nicht vorhanden — falls eine andere Klasse im File bereits einen `companion object` hat, MERGEN; sonst neu. Aktuell: `SettingsRepositoryImpl` hat keinen — neu anlegen am Class-Ende, vor schließender `}`.

BACKWARDS-COMPAT: Lasse `aiProviderPreset`/`aiBaseUrl`/`aiModel`/-Setter UNBERÜHRT (Zeilen 236-264). Die Migration in Plan 06 liest die alten Keys, schreibt die neuen, setzt das `migratedToMultiProviderKey`-Flag.
  </action>
  <verify>
    <automated>./gradlew :shared:compileCommonMainKotlinMetadata --quiet 2>&1 | tail -20 && grep -q "override val activeProvider: Flow<ProviderId>" shared/src/commonMain/kotlin/com/pumpernickel/data/repository/SettingsRepositoryImpl.kt && grep -q "DEFAULT_MODEL_ANTHROPIC = \"claude-opus-4-7\"" shared/src/commonMain/kotlin/com/pumpernickel/data/repository/SettingsRepositoryImpl.kt && grep -q "DEFAULT_BASE_URL_ANTHROPIC = \"https://api.anthropic.com\"" shared/src/commonMain/kotlin/com/pumpernickel/data/repository/SettingsRepositoryImpl.kt && grep -q "ProviderId.Anthropic -> return" shared/src/commonMain/kotlin/com/pumpernickel/data/repository/SettingsRepositoryImpl.kt</automated>
  </verify>
  <acceptance_criteria>
- Sechs neue stringPreferencesKey + ein booleanPreferencesKey für Multi-Provider-Settings
- activeProvider liefert ProviderId.OpenAI bei fehlendem Wert
- modelByProvider liefert für jeden Provider den Default wenn nichts persistiert
- baseUrlByProvider liefert Anthropic = `https://api.anthropic.com` IMMER (kein DataStore-Override)
- setBaseUrl(Anthropic, _) ist No-Op (early return)
- DEFAULT_MODEL_ANTHROPIC == "claude-opus-4-7" (D-22-06)
- migratedToMultiProvider Flow default false, setter persists true
- `:shared:compileCommonMainKotlinMetadata` BUILD SUCCESSFUL — commonMain wieder grün (alle override fulfilled)
  </acceptance_criteria>
  <done>Repository-Impl liefert alle Multi-Provider-Settings; commonMain compile grün; legacy aiBaseUrl/aiModel/aiProviderPreset bleiben unverändert für Migration-Kompatibilität.</done>
</task>

</tasks>

<verification>
- `grep -n "model_openai\|model_together\|model_anthropic" shared/src/commonMain/kotlin/com/pumpernickel/data/repository/SettingsRepositoryImpl.kt` zeigt drei Treffer (DataStore-Keys)
- `grep -n "claude-opus-4-7\|google/gemma-4-31B-it\|gpt-4o-mini" shared/src/commonMain/kotlin/com/pumpernickel/data/repository/SettingsRepositoryImpl.kt` zeigt die drei Default-Modelle
- commonMain kompiliert (interface + Impl beide grün) — Konsumenten in androidApp/iosApp können noch broken sein (werden in Plan 04 + 07 angepasst)
</verification>

<success_criteria>
- Multi-Provider-Settings persistieren in DataStore mit Defaults
- Anthropic-BaseURL ist fixed (kein User-Override möglich)
- Migration-Sentinel bereit für Plan 06
</success_criteria>

<output>
After completion, create `.planning/phases/22-anthropic-ai-provider-3-provider-neben-openai-together-mit-o/22-02-SUMMARY.md` documenting:
- Liste aller neuen DataStore-Keys (`active_provider`, `model_<provider>`, `base_url_<provider>`, `migrated_to_multi_provider`)
- Default-Werte pro Provider (Modell + BaseURL)
- Anthropic-BaseURL-Fixed-Constraint (kein DataStore-Override; Setter No-Op)
- Notiz: legacy `aiBaseUrl/aiModel/aiProviderPreset` flows bleiben — werden in Plan 06 von SettingsMigration konsumiert
</output>
