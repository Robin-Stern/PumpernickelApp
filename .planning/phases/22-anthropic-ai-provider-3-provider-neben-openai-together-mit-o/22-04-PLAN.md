---
phase: 22-anthropic-ai-provider-3-provider-neben-openai-together-mit-o
plan: 04
type: execute
wave: 2
depends_on: [1, 2, 3]
files_modified:
  - shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/AnthropicAiClient.kt
  - shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/DispatchingAiClient.kt
  - shared/src/commonMain/kotlin/com/pumpernickel/domain/ai/WorkoutAiUseCase.kt
  - shared/src/commonMain/kotlin/com/pumpernickel/domain/ai/RecipeAiUseCase.kt
autonomous: true
requirements:
  - D-22-03
  - D-22-04
  - D-22-10
must_haves:
  truths:
    - "AnthropicAiClient implementiert das bestehende AiClient-Interface"
    - "completeJsonSchema + completeJsonObject beider Methoden mappen auf top-level system + user-message-Format"
    - "DispatchingAiClient liest activeProvider per Aufruf und delegiert an die richtige Sub-Impl"
    - "Use-Cases lesen baseUrl per activeProvider aus baseUrlByProvider (statt aiBaseUrl)"
    - "Use-Cases lesen model per activeProvider aus modelByProvider (statt aiModel)"
  artifacts:
    - path: shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/AnthropicAiClient.kt
      provides: "AiClient-Adapter über AnthropicClient — bestehende Prompts (D-22-10) durchgereicht ans system-Field"
    - path: shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/DispatchingAiClient.kt
      provides: "AiClient-Facade — settings.activeProvider.first() pro Aufruf, delegiert an Sub-Impl"
    - path: shared/src/commonMain/kotlin/com/pumpernickel/domain/ai/WorkoutAiUseCase.kt
      provides: "Use-Case nutzt baseUrlByProvider/modelByProvider statt legacy aiBaseUrl/aiModel"
    - path: shared/src/commonMain/kotlin/com/pumpernickel/domain/ai/RecipeAiUseCase.kt
      provides: "Use-Case nutzt baseUrlByProvider/modelByProvider statt legacy aiBaseUrl/aiModel"
  key_links:
    - from: "DispatchingAiClient.completeJsonSchema"
      to: "settings.activeProvider.first() → openAiAdapter|anthropicAdapter"
      via: "when (activeProvider) {...} per Aufruf"
      pattern: "activeProvider.first\\(\\)"
    - from: "AnthropicAiClient.completeJsonSchema"
      to: "AnthropicClient.chatCompletionStreaming(MessagesRequest)"
      via: "systemPrompt → request.system; userPrompt → messages=[{role=user, content=userPrompt}]"
      pattern: "AnthropicMessagesRequest\\(.*system = systemPrompt"
---

<objective>
Vervollständige Wave 2 mit dem Port-Adapter `AnthropicAiClient` (implementiert das bestehende `AiClient`-Interface) und der Provider-Switch-Facade `DispatchingAiClient`. Passe `WorkoutAiUseCase` + `RecipeAiUseCase` so an, dass sie baseUrl/model aus den neuen Multi-Provider-Flows lesen (Plan 02 Output).

Purpose: Use-Cases bleiben unverändert in ihrer Logik — sie injecten weiterhin den `AiClient`-Port. Nur die Quelle von baseUrl/model wechselt von `aiBaseUrl/aiModel` auf `baseUrlByProvider[activeProvider]/modelByProvider[activeProvider]`. Das gibt Provider-Switching nahezu kostenlos: kein Use-Case kennt Anthropic vs OpenAI vs Together, der DispatchingAiClient resolved es zur Laufzeit.

Output: Zwei neue Adapter-Files in `infrastructure/ai/` + zwei modifizierte Use-Cases. Die existing `OpenAiCompatibleAiClient`-Implementierung bleibt UNBERÜHRT — nur die Wiring-Schicht in Plan 06 stellt fest, dass `DispatchingAiClient` jetzt der primäre `AiClient`-Binding ist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-anthropic-ai-provider-3-provider-neben-openai-together-mit-o/22-CONTEXT.md
@shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/AiClient.kt
@shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/OpenAiCompatibleAiClient.kt
@shared/src/commonMain/kotlin/com/pumpernickel/data/api/AnthropicClient.kt
@shared/src/commonMain/kotlin/com/pumpernickel/data/api/AnthropicMessagesDto.kt
@shared/src/commonMain/kotlin/com/pumpernickel/domain/ai/WorkoutAiUseCase.kt
@shared/src/commonMain/kotlin/com/pumpernickel/domain/ai/RecipeAiUseCase.kt

<interfaces>
<!-- AiClient port — UNCHANGED. AnthropicAiClient + DispatchingAiClient both implement it. -->
```kotlin
interface AiClient {
    suspend fun completeJsonSchema(
        baseUrl: String, model: String,
        systemPrompt: String, userPrompt: String,
        schema: AiJsonSchema,
        onProgress: (content: String, reasoning: String) -> Unit = { _, _ -> }
    ): String

    suspend fun completeJsonObject(
        baseUrl: String, model: String,
        systemPrompt: String, userPrompt: String,
        onProgress: (content: String, reasoning: String) -> Unit = { _, _ -> }
    ): String
}
```

<!-- OpenAiCompatibleAiClient — adapter pattern to mirror. -->
```kotlin
class OpenAiCompatibleAiClient(
    private val client: OpenAICompatibleClient,
    private val json: Json = Json { ignoreUnknownKeys = true; encodeDefaults = true }
) : AiClient {
    override suspend fun completeJsonSchema(...) {
        val request = ChatRequest(
            model = model,
            messages = listOf(ChatMessage(role="system", content=systemPrompt), ChatMessage(role="user", content=userPrompt)),
            responseFormat = ResponseFormat(type="json_schema", jsonSchema = ...),
            temperature = 0.7, maxTokens = 4096
        )
        return client.chatCompletionStreaming(baseUrl, request, onProgress)
    }
}
```

<!-- AnthropicClient (Plan 03) — note: chatCompletionStreaming takes AnthropicMessagesRequest, NOT a baseUrl. -->
```kotlin
class AnthropicClient(...) {
    suspend fun chatCompletionStreaming(
        request: AnthropicMessagesRequest,
        onProgress: (content: String, reasoning: String) -> Unit
    ): String
}
```

<!-- Use-Case context — legacy flow reads to be replaced. -->
```kotlin
// WorkoutAiUseCase.invoke(...):
val baseUrl = settingsRepository.aiBaseUrl.first()      // BEFORE
val model = settingsRepository.aiModel.first()          // BEFORE
// REPLACE WITH:
val activeProvider = settingsRepository.activeProvider.first()
val baseUrl = settingsRepository.baseUrlByProvider.first()[activeProvider] ?: error("no base URL for $activeProvider")
val model = settingsRepository.modelByProvider.first()[activeProvider] ?: error("no model for $activeProvider")
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AnthropicAiClient adapter (implements AiClient)</name>
  <files>shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/AnthropicAiClient.kt</files>
  <read_first>
    shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/AiClient.kt,
    shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/OpenAiCompatibleAiClient.kt,
    shared/src/commonMain/kotlin/com/pumpernickel/data/api/AnthropicClient.kt,
    shared/src/commonMain/kotlin/com/pumpernickel/data/api/AnthropicMessagesDto.kt
  </read_first>
  <action>
Erstelle `shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/AnthropicAiClient.kt`. Analog zu `OpenAiCompatibleAiClient`, aber:
- baseUrl wird IGNORIERT (Anthropic-Endpoint ist fixed im AnthropicClient — D-22-09)
- schema-Argument wird IGNORIERT in `completeJsonSchema` (Anthropic hat kein response_format; das System-Prompt enthält bereits das Schema per D-22-10)
- systemPrompt → top-level `AnthropicMessagesRequest.system`
- userPrompt → `messages = [AnthropicMessage(role="user", content=userPrompt)]` (KEINE role="system" message!)
- temperature 0.7 + maxTokens 4096 spiegelbildlich zu OpenAiCompatibleAiClient

```kotlin
package com.pumpernickel.infrastructure.ai

import com.pumpernickel.data.api.AnthropicClient
import com.pumpernickel.data.api.AnthropicMessage
import com.pumpernickel.data.api.AnthropicMessagesRequest

/**
 * D-22-03 / D-22-10 — concrete adapter implementing the [AiClient] port via
 * the Ktor-based [AnthropicClient] in `data/api/`. Symmetric with
 * [OpenAiCompatibleAiClient].
 *
 * Anthropic-specific deviations from the port contract:
 *  - **baseUrl is IGNORED** — Anthropic endpoint is fixed at
 *    `https://api.anthropic.com/v1/messages` (D-22-09). The port still
 *    accepts a baseUrl argument so [DispatchingAiClient] can pass-through the
 *    OpenAI/Together base URL; the Anthropic adapter just doesn't look at it.
 *  - **schema is IGNORED** in [completeJsonSchema] — Anthropic has no
 *    `response_format` / `json_schema` field. D-22-10 confirms existing
 *    prompts already embed schema requirements in the system prompt; the
 *    schema *body* lives in the system prompt itself, not in the wire DTO.
 *  - **systemPrompt → top-level `system` field**, NEVER a role="system"
 *    message. Anthropic rejects role="system" entries in `messages[]`.
 *
 * Behaviour preserved from [OpenAiCompatibleAiClient]:
 *  - temperature 0.7, max_tokens 4096
 *  - SSE streaming with onProgress
 *  - Pure `AiError` propagation from underlying client
 */
class AnthropicAiClient(
    private val client: AnthropicClient
) : AiClient {

    override suspend fun completeJsonSchema(
        baseUrl: String,                       // ignored — Anthropic endpoint fixed
        model: String,
        systemPrompt: String,
        userPrompt: String,
        schema: AiJsonSchema,                  // ignored — schema lives in systemPrompt
        onProgress: (content: String, reasoning: String) -> Unit
    ): String {
        val request = AnthropicMessagesRequest(
            model = model,
            system = systemPrompt,
            messages = listOf(AnthropicMessage(role = "user", content = userPrompt)),
            maxTokens = 4096,
            temperature = 0.7,
            stream = false  // overridden to true by AnthropicClient.chatCompletionStreaming
        )
        return client.chatCompletionStreaming(request, onProgress)
    }

    override suspend fun completeJsonObject(
        baseUrl: String,                       // ignored — see completeJsonSchema
        model: String,
        systemPrompt: String,
        userPrompt: String,
        onProgress: (content: String, reasoning: String) -> Unit
    ): String {
        // Same body as completeJsonSchema — Anthropic doesn't distinguish
        // json_schema vs json_object at the wire level; both paths inject the
        // schema requirement through the system prompt (D-22-10).
        val request = AnthropicMessagesRequest(
            model = model,
            system = systemPrompt,
            messages = listOf(AnthropicMessage(role = "user", content = userPrompt)),
            maxTokens = 4096,
            temperature = 0.7,
            stream = false
        )
        return client.chatCompletionStreaming(request, onProgress)
    }
}
```
  </action>
  <verify>
    <automated>./gradlew :shared:compileCommonMainKotlinMetadata --quiet 2>&1 | tail -10 && grep -q "class AnthropicAiClient" shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/AnthropicAiClient.kt && grep -q ": AiClient" shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/AnthropicAiClient.kt && grep -q "system = systemPrompt" shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/AnthropicAiClient.kt && grep -q "AnthropicMessage(role = \"user\", content = userPrompt)" shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/AnthropicAiClient.kt && ! grep -q "role = \"system\"" shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/AnthropicAiClient.kt</automated>
  </verify>
  <acceptance_criteria>
- AnthropicAiClient implementiert AiClient (override both methods)
- systemPrompt geht in AnthropicMessagesRequest.system (top-level)
- userPrompt geht in messages=[AnthropicMessage(role="user", ...)]
- KEINE role="system"-Message im messages[]
- maxTokens = 4096, temperature = 0.7
- baseUrl + schema werden in completeJsonSchema ignoriert (Kommentare im Code)
- compile: `:shared:compileCommonMainKotlinMetadata` BUILD SUCCESSFUL
  </acceptance_criteria>
  <done>AnthropicAiClient committet; compile grün; ready for DI-Wiring in Plan 06.</done>
</task>

<task type="auto">
  <name>Task 2: DispatchingAiClient facade + WorkoutAiUseCase + RecipeAiUseCase migration</name>
  <files>
    shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/DispatchingAiClient.kt,
    shared/src/commonMain/kotlin/com/pumpernickel/domain/ai/WorkoutAiUseCase.kt,
    shared/src/commonMain/kotlin/com/pumpernickel/domain/ai/RecipeAiUseCase.kt
  </files>
  <read_first>
    shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/AiClient.kt,
    shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/OpenAiCompatibleAiClient.kt,
    shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/AnthropicAiClient.kt,
    shared/src/commonMain/kotlin/com/pumpernickel/domain/repository/SettingsRepository.kt,
    shared/src/commonMain/kotlin/com/pumpernickel/domain/ai/WorkoutAiUseCase.kt,
    shared/src/commonMain/kotlin/com/pumpernickel/domain/ai/RecipeAiUseCase.kt,
    shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/ProviderId.kt
  </read_first>
  <action>
(1) Erstelle `shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/DispatchingAiClient.kt`:

```kotlin
package com.pumpernickel.infrastructure.ai

import com.pumpernickel.domain.repository.SettingsRepository
import kotlinx.coroutines.flow.first

/**
 * D-22-04 — runtime dispatcher across provider-specific [AiClient] adapters.
 * Reads [SettingsRepository.activeProvider] per call (cheap — single DataStore
 * snapshot) and delegates to the matching adapter. This is the schlankste
 * provider-switch strategy from PATTERNS.md "Option C":
 *  - no Koin module reload
 *  - no runBlocking
 *  - no Flow-observable factory plumbing
 *  - re-evaluates per call so a Settings UI provider-switch is visible to
 *    the very next AI call, no app restart required
 *
 * Use-cases continue to inject `AiClient` — they remain provider-agnostic.
 *
 * Note: `OpenAi` and `Together` BOTH dispatch to [openAiAdapter] because they
 * share the same wire format (OpenAI-compatible /chat/completions). The
 * caller-supplied `baseUrl` argument is what distinguishes them — see
 * [WorkoutAiUseCase] / [RecipeAiUseCase] callers passing the per-provider
 * base URL.
 */
class DispatchingAiClient(
    private val settings: SettingsRepository,
    private val openAiAdapter: OpenAiCompatibleAiClient,
    private val anthropicAdapter: AnthropicAiClient
) : AiClient {

    override suspend fun completeJsonSchema(
        baseUrl: String,
        model: String,
        systemPrompt: String,
        userPrompt: String,
        schema: AiJsonSchema,
        onProgress: (content: String, reasoning: String) -> Unit
    ): String = resolve().completeJsonSchema(baseUrl, model, systemPrompt, userPrompt, schema, onProgress)

    override suspend fun completeJsonObject(
        baseUrl: String,
        model: String,
        systemPrompt: String,
        userPrompt: String,
        onProgress: (content: String, reasoning: String) -> Unit
    ): String = resolve().completeJsonObject(baseUrl, model, systemPrompt, userPrompt, onProgress)

    private suspend fun resolve(): AiClient = when (settings.activeProvider.first()) {
        ProviderId.OpenAI, ProviderId.Together -> openAiAdapter
        ProviderId.Anthropic -> anthropicAdapter
    }
}
```

(2) Modifiziere `shared/src/commonMain/kotlin/com/pumpernickel/domain/ai/WorkoutAiUseCase.kt` — ersetze die beiden Zeilen in `invoke(...)`:

```kotlin
// VORHER (Zeilen 41-42):
val baseUrl = settingsRepository.aiBaseUrl.first()
val model = settingsRepository.aiModel.first()

// NACHHER:
val activeProvider = settingsRepository.activeProvider.first()
val baseUrl = settingsRepository.baseUrlByProvider.first()[activeProvider]
    ?: throw AiError.SchemaInvalid("No base URL configured for provider $activeProvider")
val model = settingsRepository.modelByProvider.first()[activeProvider]
    ?: throw AiError.SchemaInvalid("No model configured for provider $activeProvider")
```

Imports ergänzen am Anfang der Datei (falls noch nicht da):
```kotlin
import com.pumpernickel.infrastructure.ai.ProviderId
```
(ProviderId wird durch die `activeProvider`-Variable transitiv genutzt — Compiler braucht den Import aber direkt nicht; Import bleibt optional aber empfohlen für künftige Provider-spezifische Logik.)

(3) Modifiziere `shared/src/commonMain/kotlin/com/pumpernickel/domain/ai/RecipeAiUseCase.kt` — identische Änderung in `invoke(...)` (Zeilen 65-66 nach aktuellem Stand):

```kotlin
// VORHER:
val baseUrl = settingsRepository.aiBaseUrl.first()
val model = settingsRepository.aiModel.first()

// NACHHER:
val activeProvider = settingsRepository.activeProvider.first()
val baseUrl = settingsRepository.baseUrlByProvider.first()[activeProvider]
    ?: throw AiError.SchemaInvalid("No base URL configured for provider $activeProvider")
val model = settingsRepository.modelByProvider.first()[activeProvider]
    ?: throw AiError.SchemaInvalid("No model configured for provider $activeProvider")
```

NICHT entfernen: die legacy `aiBaseUrl/aiModel`-Flows in SettingsRepository bleiben — werden in Plan 06 (Migration) gelesen. Andere Konsumenten (z.B. `AiSettingsViewModel`) bleiben unverändert bis Plan 07.
  </action>
  <verify>
    <automated>./gradlew :shared:compileCommonMainKotlinMetadata --quiet 2>&1 | tail -10 && grep -q "class DispatchingAiClient" shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/DispatchingAiClient.kt && grep -q "settings.activeProvider.first()" shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/DispatchingAiClient.kt && grep -q "ProviderId.Anthropic -> anthropicAdapter" shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/DispatchingAiClient.kt && grep -q "settingsRepository.baseUrlByProvider.first()\[activeProvider\]" shared/src/commonMain/kotlin/com/pumpernickel/domain/ai/WorkoutAiUseCase.kt && grep -q "settingsRepository.baseUrlByProvider.first()\[activeProvider\]" shared/src/commonMain/kotlin/com/pumpernickel/domain/ai/RecipeAiUseCase.kt && ! grep -q "settingsRepository.aiBaseUrl.first()" shared/src/commonMain/kotlin/com/pumpernickel/domain/ai/WorkoutAiUseCase.kt && ! grep -q "settingsRepository.aiBaseUrl.first()" shared/src/commonMain/kotlin/com/pumpernickel/domain/ai/RecipeAiUseCase.kt</automated>
  </verify>
  <acceptance_criteria>
- DispatchingAiClient implementiert AiClient mit beiden override-Methoden
- DispatchingAiClient.resolve() liest `settings.activeProvider.first()` und dispatched per `when`
- OpenAI + Together delegieren an `openAiAdapter`; Anthropic an `anthropicAdapter`
- WorkoutAiUseCase liest baseUrl und model via `baseUrlByProvider.first()[activeProvider]` + `modelByProvider.first()[activeProvider]`
- RecipeAiUseCase macht das Gleiche
- Beide Use-Cases werfen AiError.SchemaInvalid wenn Provider-Mapping null liefert (defensive — sollte mit Plan-02-Defaults nie passieren)
- KEIN Aufruf mehr von `settingsRepository.aiBaseUrl.first()` oder `settingsRepository.aiModel.first()` in den beiden Use-Cases (grep-Gate)
- `:shared:compileCommonMainKotlinMetadata` BUILD SUCCESSFUL
  </acceptance_criteria>
  <done>DispatchingAiClient + Use-Cases-Migration committet; Provider-Switch funktional aus Use-Case-Sicht; Wiring in Plan 06 schließt den Kreis.</done>
</task>

</tasks>

<verification>
- `grep -rn "settingsRepository.aiBaseUrl\|settingsRepository.aiModel" shared/src/commonMain/kotlin/com/pumpernickel/domain/ai/` zeigt KEINE Treffer mehr (außer evtl. in AiGenerationManager, der nicht in scope ist — AiGenerationManager delegiert nur an die Use-Cases)
- `grep -rn "baseUrlByProvider\|modelByProvider" shared/src/commonMain/kotlin/com/pumpernickel/domain/ai/` zeigt mindestens 4 Treffer (je 2 pro Use-Case)
- DispatchingAiClient delegiert korrekt: OpenAI→openAiAdapter, Together→openAiAdapter, Anthropic→anthropicAdapter
- commonMain compile grün
</verification>

<success_criteria>
- AiClient-Port bleibt unverändert (D-20-Refactor respektiert)
- Anthropic-Adapter ist ein dünner Wrapper über AnthropicClient
- Provider-Switch ist via DispatchingAiClient zur Laufzeit ohne Restart möglich
- Use-Cases sind provider-agnostisch (kein einziger `ProviderId`-Branch in Use-Case-Code)
- Bestehende Prompts (D-22-10) werden 1:1 ans top-level `system`-Field gepasst
</success_criteria>

<output>
After completion, create `.planning/phases/22-anthropic-ai-provider-3-provider-neben-openai-together-mit-o/22-04-SUMMARY.md` documenting:
- AnthropicAiClient: baseUrl + schema ignored, system → top-level system field, userPrompt → user message
- DispatchingAiClient: `settings.activeProvider.first()` pro Aufruf, OpenAI+Together delegieren beide an openAiAdapter
- Use-Case-Migration: legacy aiBaseUrl/aiModel-Reads ersetzt durch per-provider Maps
- Hinweis: AiGenerationManager bleibt unverändert (delegiert nur an Use-Cases, kennt keinen Provider)
</output>
