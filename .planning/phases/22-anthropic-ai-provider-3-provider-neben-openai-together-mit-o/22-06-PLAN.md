---
phase: 22-anthropic-ai-provider-3-provider-neben-openai-together-mit-o
plan: 06
type: execute
wave: 3
depends_on: [1, 2, 3, 4, 5]
files_modified:
  - shared/src/commonMain/kotlin/com/pumpernickel/data/repository/SettingsMigration.kt
  - shared/src/commonMain/kotlin/com/pumpernickel/di/AiModule.kt
  - shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/MigratingAiClient.kt
  - shared/src/iosMain/kotlin/com/pumpernickel/di/PlatformModule.ios.kt
  - shared/src/androidMain/kotlin/com/pumpernickel/di/PlatformModule.android.kt
autonomous: true
requirements:
  - D-22-04
  - D-22-08
must_haves:
  truths:
    - "SettingsMigration ist idempotent — zweiter Run no-op"
    - "Legacy openai.api.key wird zu ProviderId.OpenAI-Slot migriert"
    - "Legacy aiBaseUrl=together.ai → activeProvider=Together"
    - "Koin liefert für AiClient den DispatchingAiClient; OpenAiCompatibleAiClient + AnthropicAiClient bleiben als Sub-Singletons verfügbar"
    - "OAuthBrowserLauncher + AnthropicClient + AnthropicOAuthClient sind in Koin registriert"
    - "Migration läuft beim ersten AI-Use-Case-Aufruf (lazy via AiClient init-Path) — KEINE Application/AppDelegate-Modifikation"
  artifacts:
    - path: shared/src/commonMain/kotlin/com/pumpernickel/data/repository/SettingsMigration.kt
      provides: "Klasse SettingsMigration mit suspend run() — liest legacy api.key + aiBaseUrl, schreibt activeProvider + Credential, setzt sentinel"
    - path: shared/src/commonMain/kotlin/com/pumpernickel/di/AiModule.kt
      provides: "Koin bindings für AnthropicClient + AnthropicOAuthClient + AnthropicAiClient + DispatchingAiClient + AnthropicOAuthFlow + SettingsMigration"
  key_links:
    - from: "AiModule.single<AiClient>"
      to: "DispatchingAiClient(settings, openAiAdapter, anthropicAdapter)"
      via: "Koin DSL single with explicit get() chain"
      pattern: "single<AiClient>.*DispatchingAiClient"
    - from: "SettingsMigration.run"
      to: "SecureKeyStore.writeCredential + SettingsRepository.setActiveProvider + setMigratedToMultiProvider"
      via: "if !migratedToMultiProvider then readLegacyApiKey + aiBaseUrl + infer provider + persist"
      pattern: "if .*migratedToMultiProvider.*first\\(\\)"
---

<objective>
Verdrahte Wave 1+2 in Koin und implementiere die one-time Migration. Nach diesem Plan ist Phase 22 funktional: Use-Cases bekommen via Koin den `DispatchingAiClient`, der zur Laufzeit zwischen OpenAI/Together/Anthropic dispatched; der OAuth-Browser-Flow ist provider-side ready; legacy User-Settings werden beim ersten AI-Aufruf auf das Multi-Provider-Schema migriert.

Purpose: Verbindung aller Bausteine. Migration läuft lazy beim ersten `AiClient`-Konsum: Wir wickeln `DispatchingAiClient.resolve()` in einen "migration-once-first"-Adapter. Vorteil: keine Application/AppDelegate-Modifikation, keine Startup-Verzögerung wenn der User AI nicht benutzt.

Output: SettingsMigration-Klasse, AiModule-Erweiterung, Platform-Module-Bindings für OAuthBrowserLauncher.

Migration-Logik (D-22-08):
```
if migratedToMultiProvider.first() == true → no-op (idempotent)
else:
    legacyKey = secureKeyStore.readLegacyApiKey()
    if legacyKey != null:
        oldBaseUrl = settingsRepository.aiBaseUrl.first()  // legacy field
        provider = when {
            oldBaseUrl.contains("together.ai") → Together
            oldBaseUrl.contains("openrouter.ai") → OpenAI (custom baseUrl, scope decision)
            oldBaseUrl.contains("groq.com") → OpenAI (custom baseUrl)
            else → OpenAI
        }
        secureKeyStore.writeCredential(provider, Credential.ApiKey(legacyKey))
        secureKeyStore.clearLegacyApiKey()  // only if provider != OpenAI (else same slot!)
        settingsRepository.setActiveProvider(provider)
        if oldBaseUrl was non-default:
            settingsRepository.setBaseUrl(provider, oldBaseUrl)
        // Also migrate aiModel.first() if non-default:
        oldModel = settingsRepository.aiModel.first()
        if oldModel.isNotBlank():
            settingsRepository.setModel(provider, oldModel)
    settingsRepository.setMigratedToMultiProvider(true)
```

**SUBTILER PUNKT:** `secureKeyStore.readLegacyApiKey()` und `secureKeyStore.readCredential(ProviderId.OpenAI)` lesen denselben Slot (`openai.api.key`). Migration "kopiert" deshalb nicht physisch — sie schreibt explicit denselben Wert im neuen Format. Wenn `provider == OpenAI` ist es ein Read-Write-Replace; wenn `provider == Together` müssen wir den Wert vom OpenAI-Slot zum Together-Slot verschieben UND den OpenAI-Slot löschen (= clearLegacyApiKey). Die Migration-Code muss dieses Edge-Case behandeln.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-anthropic-ai-provider-3-provider-neben-openai-together-mit-o/22-CONTEXT.md
@shared/src/commonMain/kotlin/com/pumpernickel/di/AiModule.kt
@shared/src/commonMain/kotlin/com/pumpernickel/di/SharedModule.kt
@shared/src/iosMain/kotlin/com/pumpernickel/di/PlatformModule.ios.kt
@shared/src/androidMain/kotlin/com/pumpernickel/di/PlatformModule.android.kt
@shared/src/commonMain/kotlin/com/pumpernickel/data/repository/SettingsRepositoryImpl.kt
@shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/SecureKeyStore.kt
@shared/src/commonMain/kotlin/com/pumpernickel/data/api/AnthropicClient.kt
@shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/AnthropicAiClient.kt
@shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/DispatchingAiClient.kt

<interfaces>
<!-- Current AiModule wiring (Plan 18 / Phase 20 final). -->
```kotlin
val aiModule = module {
    single { AiPromptCatalog() }
    single { OpenAICompatibleClient(client = get(), keyProvider = { get<SecureKeyStore>().readApiKey() }) }
    single<AiClient> { OpenAiCompatibleAiClient(get()) }
    viewModel { AiSettingsViewModel(get(), get()) }
    single { WorkoutAiUseCase(get(), get(), get(), get(), get()) }
    single { RecipeAiUseCase(get(), get(), get(), get(), get(), get(), get()) }
    single { AiGenerationManager(get(), get(), get(), get()) }
    viewModel { WorkoutAiViewModel(get(), get(), get()) }
    viewModel { RecipeAiViewModel(get(), get(), get()) }
}
```

NOTE: `readApiKey()` no longer exists (Plan 01 replaced it). The keyProvider lambda for `OpenAICompatibleClient` must be rewritten to use the new API.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SettingsMigration class</name>
  <files>shared/src/commonMain/kotlin/com/pumpernickel/data/repository/SettingsMigration.kt</files>
  <read_first>
    shared/src/commonMain/kotlin/com/pumpernickel/data/repository/SettingsRepositoryImpl.kt,
    shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/SecureKeyStore.kt,
    shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/ProviderId.kt,
    shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/Credential.kt,
    shared/src/commonMain/kotlin/com/pumpernickel/domain/repository/SettingsRepository.kt
  </read_first>
  <action>
Erstelle `shared/src/commonMain/kotlin/com/pumpernickel/data/repository/SettingsMigration.kt`:

```kotlin
package com.pumpernickel.data.repository

import com.pumpernickel.domain.repository.SettingsRepository
import com.pumpernickel.infrastructure.ai.Credential
import com.pumpernickel.infrastructure.ai.ProviderId
import com.pumpernickel.infrastructure.ai.SecureKeyStore
import kotlinx.coroutines.flow.first

/**
 * D-22-08 — one-time migration from Phase 18 single-key/single-baseUrl AI
 * settings to Phase 22 Multi-Provider schema. Idempotent: guarded by the
 * `migratedToMultiProvider` sentinel in DataStore.
 *
 * Triggered lazily by [com.pumpernickel.infrastructure.ai.MigratingAiClient]
 * before each AI call — that way users who never use AI never pay startup
 * cost, and the migration runs on background coroutine context (the
 * Use-Case-Caller's scope).
 *
 * Mapping rules:
 *  - Legacy `openai.api.key` slot exists → write as Credential.ApiKey under
 *    inferred provider's slot. Inferred provider is derived from the legacy
 *    `aiBaseUrl` value:
 *      - contains "together.ai"    → ProviderId.Together
 *      - contains "openrouter.ai"  → ProviderId.OpenAI (preserve baseUrl)
 *      - contains "groq.com"       → ProviderId.OpenAI (preserve baseUrl)
 *      - otherwise                 → ProviderId.OpenAI
 *  - Sets `activeProvider = inferredProvider`
 *  - If inferred provider != OpenAI, the legacy slot is cleaned up (otherwise
 *    the legacy and target slots are the same physical key, no cleanup needed)
 *  - Migrates non-blank legacy `aiModel` into per-provider model slot
 *  - Migrates non-default legacy `aiBaseUrl` into per-provider base-URL slot
 *    (Anthropic skipped — baseUrl is fixed)
 *  - Sets sentinel `migratedToMultiProvider = true` LAST so a crash mid-flight
 *    re-runs the migration on next boot
 *
 * If no legacy key is present: still sets the sentinel so we don't keep
 * probing the Keychain every AI call.
 */
class SettingsMigration(
    private val settingsRepository: SettingsRepository,
    private val secureKeyStore: SecureKeyStore
) {
    suspend fun run() {
        if (settingsRepository.migratedToMultiProvider.first()) return

        try {
            val legacyKey = secureKeyStore.readLegacyApiKey()
            if (legacyKey != null) {
                val oldBaseUrl = settingsRepository.aiBaseUrl.first()
                val oldModel = settingsRepository.aiModel.first()
                val inferredProvider = inferProvider(oldBaseUrl)

                // Write to the new credential slot. For inferredProvider == OpenAI, the
                // legacy and target slots are identical (`openai.api.key`); we still call
                // writeCredential to ensure ApiKeyState sentinel is set correctly. For
                // Together, we move the value and clear the legacy slot below.
                secureKeyStore.writeCredential(inferredProvider, Credential.ApiKey(legacyKey))

                if (inferredProvider != ProviderId.OpenAI) {
                    // Legacy slot is `openai.api.key` == ProviderId.OpenAI's apiKeyAccount.
                    // Together-inferred users had their key sitting in the OpenAI slot pre-migration.
                    // Move it to Together and clear the OpenAI slot.
                    secureKeyStore.clearLegacyApiKey()
                }

                settingsRepository.setActiveProvider(inferredProvider)

                // Preserve user's custom baseUrl / model unless they're the legacy defaults.
                if (oldBaseUrl.isNotBlank() && oldBaseUrl != "https://api.openai.com/v1") {
                    if (inferredProvider != ProviderId.Anthropic) {
                        // Anthropic baseUrl is fixed (setBaseUrl is a no-op there)
                        settingsRepository.setBaseUrl(inferredProvider, oldBaseUrl)
                    }
                }
                if (oldModel.isNotBlank() && oldModel != "gpt-4o-mini") {
                    settingsRepository.setModel(inferredProvider, oldModel)
                }

                println("[SettingsMigration] migrated legacy key → provider=$inferredProvider model=$oldModel baseUrl=$oldBaseUrl")
            } else {
                println("[SettingsMigration] no legacy key present — sentinel-only migration")
            }
        } catch (t: Throwable) {
            // Don't crash the app — but also don't set the sentinel. Next call retries.
            println("[SettingsMigration] migration failed: ${t::class.simpleName}: ${t.message}")
            return
        }

        settingsRepository.setMigratedToMultiProvider(true)
    }

    private fun inferProvider(baseUrl: String): ProviderId = when {
        baseUrl.contains("together.ai") -> ProviderId.Together
        // OpenRouter / Groq users stay on the OpenAI-compatible adapter; their custom
        // baseUrl is preserved via setBaseUrl(OpenAI, ...). This intentionally folds
        // multiple legacy presets into a single new provider slot — full per-preset
        // representation is a deferred idea in CONTEXT.md.
        else -> ProviderId.OpenAI
    }
}
```
  </action>
  <verify>
    <automated>./gradlew :shared:compileCommonMainKotlinMetadata --quiet 2>&1 | tail -10 && grep -q "class SettingsMigration" shared/src/commonMain/kotlin/com/pumpernickel/data/repository/SettingsMigration.kt && grep -q "if (settingsRepository.migratedToMultiProvider.first()) return" shared/src/commonMain/kotlin/com/pumpernickel/data/repository/SettingsMigration.kt && grep -q "secureKeyStore.readLegacyApiKey()" shared/src/commonMain/kotlin/com/pumpernickel/data/repository/SettingsMigration.kt && grep -q "secureKeyStore.writeCredential(inferredProvider, Credential.ApiKey(legacyKey))" shared/src/commonMain/kotlin/com/pumpernickel/data/repository/SettingsMigration.kt && grep -q "setMigratedToMultiProvider(true)" shared/src/commonMain/kotlin/com/pumpernickel/data/repository/SettingsMigration.kt</automated>
  </verify>
  <acceptance_criteria>
- Early-return wenn migratedToMultiProvider.first() == true (idempotent)
- Liest legacyKey via secureKeyStore.readLegacyApiKey()
- inferProvider(baseUrl): together.ai → Together; sonst OpenAI
- writeCredential(provider, ApiKey(legacyKey))
- clearLegacyApiKey() NUR wenn inferredProvider != OpenAI
- setActiveProvider(inferredProvider)
- setBaseUrl(provider, oldBaseUrl) wenn baseUrl non-default UND provider != Anthropic
- setModel(provider, oldModel) wenn model non-default
- setMigratedToMultiProvider(true) als letzter Schritt (Crash-Safety)
- compile grün
  </acceptance_criteria>
  <done>SettingsMigration committet; commonMain grün.</done>
</task>

<task type="auto">
  <name>Task 2: AiModule + PlatformModule wiring (Anthropic stack + OAuth launcher + migration)</name>
  <files>
    shared/src/commonMain/kotlin/com/pumpernickel/di/AiModule.kt,
    shared/src/iosMain/kotlin/com/pumpernickel/di/PlatformModule.ios.kt,
    shared/src/androidMain/kotlin/com/pumpernickel/di/PlatformModule.android.kt
  </files>
  <read_first>
    shared/src/commonMain/kotlin/com/pumpernickel/di/AiModule.kt,
    shared/src/iosMain/kotlin/com/pumpernickel/di/PlatformModule.ios.kt,
    shared/src/androidMain/kotlin/com/pumpernickel/di/PlatformModule.android.kt,
    shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/SecureKeyStore.kt,
    shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/DispatchingAiClient.kt,
    shared/src/commonMain/kotlin/com/pumpernickel/data/api/AnthropicClient.kt,
    shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/AnthropicOAuthFlow.kt
  </read_first>
  <action>
(1) Ersetze den AiModule-Inhalt vollständig in `shared/src/commonMain/kotlin/com/pumpernickel/di/AiModule.kt`:

```kotlin
package com.pumpernickel.di

import com.pumpernickel.data.api.AnthropicClient
import com.pumpernickel.data.api.AnthropicOAuthClient
import com.pumpernickel.data.api.OpenAICompatibleClient
import com.pumpernickel.data.repository.SettingsMigration
import com.pumpernickel.domain.ai.AiPromptCatalog
import com.pumpernickel.domain.ai.RecipeAiUseCase
import com.pumpernickel.domain.ai.WorkoutAiUseCase
import com.pumpernickel.domain.repository.SettingsRepository
import com.pumpernickel.infrastructure.ai.AiClient
import com.pumpernickel.infrastructure.ai.AnthropicAiClient
import com.pumpernickel.infrastructure.ai.AnthropicOAuthFlow
import com.pumpernickel.infrastructure.ai.Credential
import com.pumpernickel.infrastructure.ai.DispatchingAiClient
import com.pumpernickel.infrastructure.ai.MigratingAiClient
import com.pumpernickel.infrastructure.ai.OpenAiCompatibleAiClient
import com.pumpernickel.infrastructure.ai.ProviderId
import com.pumpernickel.infrastructure.ai.SecureKeyStore
import com.pumpernickel.presentation.ai.AiSettingsViewModel
import com.pumpernickel.presentation.ai.RecipeAiViewModel
import com.pumpernickel.presentation.ai.WorkoutAiViewModel
import kotlinx.coroutines.flow.first
import org.koin.core.module.dsl.viewModel
import org.koin.dsl.module

/**
 * Phase 22 — Multi-Provider DI wiring.
 *
 * Layer cake (call order from a Use-Case perspective):
 *   Use-Case → AiClient (= MigratingAiClient) → DispatchingAiClient → {OpenAiCompatibleAiClient, AnthropicAiClient}
 *
 * MigratingAiClient is the outermost layer: it runs SettingsMigration.run()
 * lazily on first call (idempotent via DataStore sentinel) before delegating
 * to the dispatcher. This avoids any Application/AppDelegate-level
 * initialization hook (CONTEXT.md Discretion: migration trigger).
 */
val aiModule = module {
    single { AiPromptCatalog() }

    // === Provider-1: OpenAI / Together (OpenAI-compatible) ===
    // Key resolution: active provider's ApiKey credential. Used for both
    // OpenAI- and Together-active states (both dispatch to this adapter via
    // DispatchingAiClient).
    single {
        OpenAICompatibleClient(
            client = get(),
            keyProvider = {
                val active = get<SettingsRepository>().activeProvider.first()
                val provider = if (active == ProviderId.Anthropic) ProviderId.OpenAI else active
                when (val cred = get<SecureKeyStore>().readCredential(provider)) {
                    is Credential.ApiKey -> cred.value
                    else -> null
                }
            }
        )
    }
    single { OpenAiCompatibleAiClient(get()) }

    // === Provider-2: Anthropic ===
    // AnthropicClient owns its own credential lookup (OAuth + API-key dispatch
    // in ensureFreshCredential) so no keyProvider lambda needed at Koin level.
    single { AnthropicOAuthClient(client = get()) }
    single {
        AnthropicClient(
            client = get(),
            secureKeyStore = get(),
            oauthClient = get(),
            oauthClientId = AnthropicOAuthFlow.CLIENT_ID
        )
    }
    single { AnthropicAiClient(get()) }

    // === OAuth flow orchestrator (consumed by AiSettingsViewModel in Plan 08) ===
    single {
        AnthropicOAuthFlow(
            httpClient = get(),
            browserLauncher = get()
        )
    }

    // === Migration (lazy, idempotent) ===
    single { SettingsMigration(settingsRepository = get(), secureKeyStore = get()) }

    // === Provider switch + lazy migration wrapper ===
    // The AiClient binding is wrapped so the very first AI call triggers migration.
    // Use-cases inject `AiClient` — they get the migrating wrapper transparently.
    single {
        DispatchingAiClient(
            settings = get(),
            openAiAdapter = get(),
            anthropicAdapter = get()
        )
    }
    single<AiClient> {
        MigratingAiClient(delegate = get<DispatchingAiClient>(), migration = get())
    }

    // === Use-cases + VMs (unchanged from Phase 18 — they inject AiClient) ===
    viewModel { AiSettingsViewModel(get(), get()) }
    single { WorkoutAiUseCase(get(), get(), get(), get(), get()) }
    single { RecipeAiUseCase(get(), get(), get(), get(), get(), get(), get()) }
    single { com.pumpernickel.domain.ai.AiGenerationManager(get(), get(), get(), get()) }
    viewModel { WorkoutAiViewModel(get(), get(), get()) }
    viewModel { RecipeAiViewModel(get(), get(), get()) }
}
```

(2) Erstelle `shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/MigratingAiClient.kt`:

```kotlin
package com.pumpernickel.infrastructure.ai

import com.pumpernickel.data.repository.SettingsMigration

/**
 * D-22-08 — outermost [AiClient] wrapper that runs [SettingsMigration.run] on
 * every call. The migration is idempotent (sentinel guarded), so post-first-call
 * the overhead is one DataStore read.
 *
 * Rationale (PATTERNS.md "no application/AppDelegate trigger"): the migration
 * lives in the AiClient call path because anyone who needs an AiClient
 * implicitly also needs the migration to have run. Users who never trigger
 * an AI call have nothing to migrate — and pay no cost.
 */
class MigratingAiClient(
    private val delegate: AiClient,
    private val migration: SettingsMigration
) : AiClient {

    override suspend fun completeJsonSchema(
        baseUrl: String,
        model: String,
        systemPrompt: String,
        userPrompt: String,
        schema: AiJsonSchema,
        onProgress: (content: String, reasoning: String) -> Unit
    ): String {
        migration.run()
        return delegate.completeJsonSchema(baseUrl, model, systemPrompt, userPrompt, schema, onProgress)
    }

    override suspend fun completeJsonObject(
        baseUrl: String,
        model: String,
        systemPrompt: String,
        userPrompt: String,
        onProgress: (content: String, reasoning: String) -> Unit
    ): String {
        migration.run()
        return delegate.completeJsonObject(baseUrl, model, systemPrompt, userPrompt, onProgress)
    }
}
```

(3) Erweitere `shared/src/iosMain/kotlin/com/pumpernickel/di/PlatformModule.ios.kt`. Direkt nach dem bestehenden SecureKeyStore-Eintrag:

```kotlin
    // Phase 22 — OAuth browser bridge for Anthropic Pro/Max auth flow (D-22-01).
    single<com.pumpernickel.infrastructure.ai.OAuthBrowserLauncher> {
        com.pumpernickel.infrastructure.ai.OAuthBrowserLauncher()
    }
```

(4) Erweitere `shared/src/androidMain/kotlin/com/pumpernickel/di/PlatformModule.android.kt`. Direkt nach dem bestehenden SecureKeyStore-Eintrag:

```kotlin
    // Phase 22 — OAuth browser bridge for Anthropic Pro/Max auth flow (D-22-01).
    single<com.pumpernickel.infrastructure.ai.OAuthBrowserLauncher> {
        com.pumpernickel.infrastructure.ai.OAuthBrowserLauncher(androidContext())
    }
```
  </action>
  <verify>
    <automated>./gradlew :shared:compileCommonMainKotlinMetadata :shared:compileKotlinIosSimulatorArm64 :shared:compileKotlinAndroid --quiet 2>&1 | tail -20 && grep -q "single<AiClient>" shared/src/commonMain/kotlin/com/pumpernickel/di/AiModule.kt && grep -q "MigratingAiClient(delegate = get<DispatchingAiClient>(), migration = get())" shared/src/commonMain/kotlin/com/pumpernickel/di/AiModule.kt && grep -q "AnthropicClient(" shared/src/commonMain/kotlin/com/pumpernickel/di/AiModule.kt && grep -q "AnthropicOAuthFlow.CLIENT_ID" shared/src/commonMain/kotlin/com/pumpernickel/di/AiModule.kt && grep -q "class MigratingAiClient" shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/MigratingAiClient.kt && grep -q "OAuthBrowserLauncher()" shared/src/iosMain/kotlin/com/pumpernickel/di/PlatformModule.ios.kt && grep -q "OAuthBrowserLauncher(androidContext())" shared/src/androidMain/kotlin/com/pumpernickel/di/PlatformModule.android.kt</automated>
  </verify>
  <acceptance_criteria>
- AiModule.kt vollständig refaktoriert: OpenAICompatibleClient.keyProvider liest activeProvider, Anthropic-Stack registriert (Client + OAuthClient + AiClient + OAuthFlow), DispatchingAiClient + MigratingAiClient + SettingsMigration registriert
- `single<AiClient>` bindet auf MigratingAiClient (outermost layer)
- OpenAICompatibleClient.keyProvider: bei activeProvider=Anthropic fallback auf OpenAI-Slot (defensive — Use-Case sollte gar nicht erst zum OpenAI-Path delegieren wenn Anthropic aktiv; aber für Crash-Safety)
- MigratingAiClient.kt existiert und ruft migration.run() vor jedem delegate-Call
- PlatformModule.ios.kt + .android.kt registrieren OAuthBrowserLauncher (ctor-Variante per Platform)
- compile alle drei targets (commonMain, iosSimulatorArm64, android) BUILD SUCCESSFUL — bestätigt dass Plan 01's "Konsumenten broken" jetzt finally repariert sind (`AiSettingsViewModel` braucht noch Anpassung in Plan 08, aber Module bauen)
  </acceptance_criteria>
  <done>DI komplett verdrahtet; Use-Cases bekommen den richtigen AiClient transparent; Migration läuft lazy auf erstem AI-Call; Wave 3 done.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration (one-shot) | Reads legacy key + writes new credential — runs in user-process, no privilege boundary |
| Koin singleton graph | Provider-switch via DataStore — race between activeProvider write and next AI call is harmless (next call re-resolves) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-06 | T | SettingsMigration partial-failure | mitigate | Sentinel set LAST; mid-flight crash re-runs migration on next AI call (idempotent reads, defensive write order: writeCredential before clearLegacyApiKey) |
| T-22-07 | I | OpenAICompatibleClient.keyProvider lambda logs | mitigate | Existing pattern logs `keyLen` only, never the value — verified in plan-03 mitigation; AiModule's lambda inherits this property since the underlying client does the logging |
| T-22-08 | E | Provider-switch + in-flight request | accept | DispatchingAiClient.resolve() reads activeProvider per CALL, not per session. A user who switches provider mid-stream sees the next call hit the new provider — acceptable UX (rare race) |
</threat_model>

<verification>
- `:shared:compileCommonMainKotlinMetadata` BUILD SUCCESSFUL
- `:shared:compileKotlinIosSimulatorArm64` BUILD SUCCESSFUL
- `:shared:compileKotlinAndroid` BUILD SUCCESSFUL
- grep `single<AiClient>` in AiModule.kt liefert genau 1 Match — der MigratingAiClient binding
- grep `MigratingAiClient(` zeigt: ein Konstruktor-Call in AiModule + Klassen-Definition in MigratingAiClient.kt
</verification>

<success_criteria>
- Use-Cases bleiben unverändert in ihrer Injection: `private val aiClient: AiClient` — sie bekommen jetzt transparent den MigratingAiClient → DispatchingAiClient → konkrete Adapter
- Migration läuft beim ersten AI-Aufruf eines bestehenden Phase-18-Users, transparent, idempotent
- Provider-Switch ist im Settings nur eine `setActiveProvider`-Aktion entfernt (Plan 08 implementiert die UI)
- OAuth-Flow ist von der UI aus aufrufbar via `koinInject<AnthropicOAuthFlow>().authorize()`
</success_criteria>

<output>
After completion, create `.planning/phases/22-anthropic-ai-provider-3-provider-neben-openai-together-mit-o/22-06-SUMMARY.md` documenting:
- Layer-cake: Use-Case → AiClient (MigratingAiClient) → DispatchingAiClient → {OpenAi, Anthropic} adapters
- Migration trigger: lazy auf erstem AI-Call (kein Application/AppDelegate hook nötig)
- Migration-Edge-Case: Provider=OpenAI → legacy slot == target slot; Provider=Together → move + clear
- Koin-Bindings-Übersicht (was ist single vs. viewModel)
- Hinweis: AiSettingsViewModel ist noch broken in Plan 06 (referenziert old readApiKey-API) — wird in Plan 08 finalisiert
</output>
