---
phase: 22-anthropic-ai-provider-3-provider-neben-openai-together-mit-o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/ProviderId.kt
  - shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/Credential.kt
  - shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/SecureKeyStore.kt
  - shared/src/iosMain/kotlin/com/pumpernickel/infrastructure/ai/SecureKeyStore.ios.kt
  - shared/src/androidMain/kotlin/com/pumpernickel/infrastructure/ai/SecureKeyStore.android.kt
autonomous: true
requirements:
  - D-22-07
  - D-22-08
must_haves:
  truths:
    - "Credentials für mehrere Provider (openai/together/anthropic) können nebeneinander gespeichert werden"
    - "OAuth-Token und API-Key werden typsicher unterschieden (sealed Credential)"
    - "Legacy-Read-Pfad für alten openai.api.key-Slot bleibt verfügbar (Migration in Plan 06)"
  artifacts:
    - path: shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/ProviderId.kt
      provides: "Enum ProviderId { OpenAI, Together, Anthropic }"
    - path: shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/Credential.kt
      provides: "sealed class Credential mit ApiKey + OAuthToken"
    - path: shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/SecureKeyStore.kt
      provides: "expect class SecureKeyStore mit writeCredential/readCredential/clearCredential pro ProviderId + legacy readApiKey/clearApiKey für Migration"
    - path: shared/src/iosMain/kotlin/com/pumpernickel/infrastructure/ai/SecureKeyStore.ios.kt
      provides: "iOS Keychain actual mit per-Provider kSecAttrAccount-Slots"
    - path: shared/src/androidMain/kotlin/com/pumpernickel/infrastructure/ai/SecureKeyStore.android.kt
      provides: "Android EncryptedSharedPreferences actual mit Multi-Key in ai_secrets"
  key_links:
    - from: "SecureKeyStore.ios.kt"
      to: "iOS Keychain (kSecClassGenericPassword + service PumpernickelApp.AI)"
      via: "SecItemAdd/Update/CopyMatching/Delete mit baseQueryDict(account = provider.keychainAccount)"
      pattern: "kSecAttrAccount to .*provider"
    - from: "SecureKeyStore.android.kt"
      to: "EncryptedSharedPreferences (file ai_secrets)"
      via: "prefs.edit().putString(provider.prefsKey, jsonEncodedCredential)"
      pattern: "prefs.edit\\(\\).putString\\(provider"
---

<objective>
Erweitere `SecureKeyStore` zu Multi-Slot-Storage mit typsicherer `Credential`-Sealed-Class und `ProviderId`-Enum, damit OpenAI/Together/Anthropic-Credentials nebeneinander platform-secure persistiert werden können (D-22-07). Legacy-Read-Pfad bleibt vorhanden für die in Plan 06 folgende einmalige Migration (D-22-08).

Purpose: Foundation für Multi-Provider-Koexistenz. Anthropic braucht OAuth-Token-Storage (mit Refresh-Token + Expiry), OpenAI/Together brauchen API-Key-Slots. Sealed-Credential macht die Auth-Mode-Dispatch im AnthropicClient typsicher (D-22-01/D-22-02).

Output: Ein expect/actual-Triple plus zwei neue commonMain-Files (ProviderId + Credential). Bestehende `writeApiKey/readApiKey/clearApiKey`-Methoden bleiben temporär als `readLegacyApiKey/clearLegacyApiKey` umbenannt für die Migration; neue `*Credential`-Methoden sind die kanonischen Pfade ab Phase 22.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/22-anthropic-ai-provider-3-provider-neben-openai-together-mit-o/22-CONTEXT.md
@.planning/phases/22-anthropic-ai-provider-3-provider-neben-openai-together-mit-o/22-PATTERNS.md
@shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/SecureKeyStore.kt
@shared/src/iosMain/kotlin/com/pumpernickel/infrastructure/ai/SecureKeyStore.ios.kt
@shared/src/androidMain/kotlin/com/pumpernickel/infrastructure/ai/SecureKeyStore.android.kt

<interfaces>
<!-- Existing SecureKeyStore — current single-slot expect (must be REPLACED, not extended). -->
```kotlin
expect class SecureKeyStore {
    suspend fun writeApiKey(value: String)
    suspend fun readApiKey(): String?
    suspend fun clearApiKey()
}
```

<!-- Existing iOS account constant (must become per-provider). -->
```kotlin
private const val SERVICE = "PumpernickelApp.AI"
private const val ACCOUNT = "openai.api.key"  // legacy fixed
```

<!-- Existing Android key constants (must become per-provider). -->
```kotlin
private const val PREFS_NAME = "ai_secrets"
private const val KEY_API_KEY = "openai.api.key"
```

<!-- ApiKeyState sentinel — keep updating from the new path so existing UI keeps working. -->
```kotlin
// com.pumpernickel.domain.ai.ApiKeyState
object ApiKeyState {
    val configured: StateFlow<Boolean>
    fun set(value: Boolean)
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ProviderId + Credential sealed class (commonMain)</name>
  <files>
    shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/ProviderId.kt,
    shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/Credential.kt
  </files>
  <read_first>
    .planning/phases/22-anthropic-ai-provider-3-provider-neben-openai-together-mit-o/22-CONTEXT.md,
    .planning/phases/22-anthropic-ai-provider-3-provider-neben-openai-together-mit-o/22-PATTERNS.md,
    shared/src/commonMain/kotlin/com/pumpernickel/domain/ai/AiError.kt
  </read_first>
  <action>
Erstelle zwei neue Files in commonMain:

(1) `ProviderId.kt`:
```kotlin
package com.pumpernickel.infrastructure.ai

/**
 * D-22-07 — IDs der drei unterstützten AI-Provider. Enum-Name wird als
 * DataStore-String (SettingsRepository.activeProvider) und Keychain/Prefs-
 * Slot-Suffix verwendet (`openai.api.key`, `together.api.key`,
 * `anthropic.api.key`, `anthropic.oauth.token`).
 */
enum class ProviderId(
    /** Persistenz-Form für DataStore-String und Keychain-account. */
    val wireName: String
) {
    OpenAI("openai"),
    Together("together"),
    Anthropic("anthropic");

    /** Keychain-account / EncryptedSharedPrefs-Key für API-Key-Slot. */
    val apiKeyAccount: String get() = "$wireName.api.key"

    /** Keychain-account / EncryptedSharedPrefs-Key für OAuth-Token-Slot (nur Anthropic). */
    val oauthTokenAccount: String get() = "$wireName.oauth.token"

    companion object {
        fun fromWireNameOrNull(s: String?): ProviderId? =
            entries.firstOrNull { it.wireName == s }
    }
}
```

(2) `Credential.kt`:
```kotlin
package com.pumpernickel.infrastructure.ai

import kotlinx.serialization.Serializable

/**
 * D-22-07 — Typsichere Repräsentation eines gespeicherten Credentials.
 *
 * - [ApiKey] wird als roher String in Keychain/EncryptedSharedPrefs gelegt
 *   (Slot: `${provider}.api.key`).
 * - [OAuthToken] wird als JSON-encoded String gespeichert (Slot:
 *   `${provider}.oauth.token`). `expiresAtEpochSeconds` ist UNIX-time, NICHT
 *   relative TTL — Token-Refresh-Logik im AnthropicClient (Plan 03) prüft
 *   `expiresAtEpochSeconds < now + 60` für Pre-Refresh (D-22-12).
 *
 * Diese sealed class lebt in `infrastructure/ai/` (nicht `domain/ai/`) weil sie
 * Storage-Wire-Format ist, kein Domain-Concept — analog zu Phase 20's
 * SecureKeyStore-Location.
 */
sealed class Credential {
    @Serializable
    data class ApiKey(val value: String) : Credential()

    @Serializable
    data class OAuthToken(
        val accessToken: String,
        val refreshToken: String?,
        val expiresAtEpochSeconds: Long
    ) : Credential()
}
```

KEINE `@Serializable` auf der sealed class selbst — die Sub-Klassen sind individuell serializable, weil sie in unterschiedlichen Storage-Slots landen (api.key Slot rohes String, oauth.token Slot JSON-encoded).
  </action>
  <verify>
    <automated>test -f shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/ProviderId.kt && test -f shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/Credential.kt && grep -q "enum class ProviderId" shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/ProviderId.kt && grep -q "sealed class Credential" shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/Credential.kt && grep -q "data class OAuthToken" shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/Credential.kt</automated>
  </verify>
  <acceptance_criteria>
- ProviderId enum mit drei Konstanten (OpenAI, Together, Anthropic) und wireName-Property existiert
- ProviderId.apiKeyAccount liefert `<wireName>.api.key`
- ProviderId.oauthTokenAccount liefert `<wireName>.oauth.token`
- ProviderId.fromWireNameOrNull("anthropic") == ProviderId.Anthropic
- Credential ist sealed class mit ApiKey(value) und OAuthToken(accessToken, refreshToken, expiresAtEpochSeconds)
- Beide Sub-Klassen sind @Serializable
- Build: `./gradlew :shared:compileCommonMainKotlinMetadata` succeeds
  </acceptance_criteria>
  <done>ProviderId.kt + Credential.kt committet; commonMain kompiliert; keine bestehenden Konsumenten gebrochen (Files sind neu).</done>
</task>

<task type="auto">
  <name>Task 2: SecureKeyStore Multi-Slot expect + iOS + Android actuals</name>
  <files>
    shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/SecureKeyStore.kt,
    shared/src/iosMain/kotlin/com/pumpernickel/infrastructure/ai/SecureKeyStore.ios.kt,
    shared/src/androidMain/kotlin/com/pumpernickel/infrastructure/ai/SecureKeyStore.android.kt
  </files>
  <read_first>
    shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/SecureKeyStore.kt,
    shared/src/iosMain/kotlin/com/pumpernickel/infrastructure/ai/SecureKeyStore.ios.kt,
    shared/src/androidMain/kotlin/com/pumpernickel/infrastructure/ai/SecureKeyStore.android.kt,
    shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/ProviderId.kt,
    shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/Credential.kt
  </read_first>
  <action>
Ersetze die single-slot SecureKeyStore-API (`writeApiKey/readApiKey/clearApiKey`) durch eine Multi-Slot-API. Der ApiKeyState-Sentinel (`com.pumpernickel.domain.ai.ApiKeyState`) muss weiterhin gepflegt werden (UI verläßt sich noch drauf via `AiSettingsViewModel.apiKeyConfigured`).

(1) `shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/SecureKeyStore.kt` — neue expect-Surface (vollständiger Datei-Inhalt ersetzen):
```kotlin
package com.pumpernickel.infrastructure.ai

/**
 * D-22-07 — Multi-Slot platform-secure credential storage. Phase 22 evolution
 * of the single-key SecureKeyStore from Phase 18.
 *
 * Implementations:
 * - Android: EncryptedSharedPreferences (file `ai_secrets`,
 *   AES256_SIV keys / AES256_GCM values, MasterKey.DEFAULT_MASTER_KEY_ALIAS).
 * - iOS: Keychain (kSecClass=GenericPassword,
 *   kSecAttrService=PumpernickelApp.AI,
 *   kSecAttrAccount=<provider>.<api.key|oauth.token>,
 *   kSecAttrAccessible=kSecAttrAccessibleAfterFirstUnlockThisDeviceOnly).
 *
 * Slot layout per [ProviderId]:
 * - `${provider.wireName}.api.key` (raw String) — used by all three providers'
 *   API-Key auth path.
 * - `${provider.wireName}.oauth.token` (JSON-encoded `Credential.OAuthToken`)
 *   — currently only Anthropic uses this slot (D-22-01).
 *
 * Read-back contract: [readCredential] returns whichever variant is present
 * for the given provider. If both api.key and oauth.token are present
 * (theoretically possible after a user switches between auth modes),
 * OAuth-Token wins (D-22-01 OAuth-Primary).
 *
 * Legacy migration helpers ([readLegacyApiKey] / [clearLegacyApiKey]) read /
 * delete the Phase-18 fixed-slot `openai.api.key` regardless of any
 * [ProviderId] mapping; consumed exclusively by `SettingsMigration` (Plan 06).
 *
 * Credentials are NEVER returned to logs, NEVER persisted to DataStore, NEVER
 * held as a long-lived field. Callers read them just-in-time per request and
 * let them fall out of scope after the HTTPS call.
 */
expect class SecureKeyStore {
    suspend fun writeCredential(provider: ProviderId, credential: Credential)
    suspend fun readCredential(provider: ProviderId): Credential?
    suspend fun clearCredential(provider: ProviderId)
    /** Returns the set of providers that currently have any credential stored. */
    suspend fun listProviders(): Set<ProviderId>

    /** Phase-18 legacy slot read; for Plan-06 migration only. */
    suspend fun readLegacyApiKey(): String?
    /** Phase-18 legacy slot delete; for Plan-06 migration only (idempotent). */
    suspend fun clearLegacyApiKey()
}
```

(2) `shared/src/iosMain/kotlin/com/pumpernickel/infrastructure/ai/SecureKeyStore.ios.kt` — vollständig ersetzen. Behalte SERVICE + die `MemScope.makeDict`/`baseQueryDict`-Helpers, aber parametriere `account`. Pseudo-Skelett:

```kotlin
@file:OptIn(kotlinx.cinterop.ExperimentalForeignApi::class, kotlinx.cinterop.BetaInteropApi::class)

package com.pumpernickel.infrastructure.ai

import com.pumpernickel.domain.ai.ApiKeyState
// ... bestehende CFDictionary / SecItem imports ...
import kotlinx.serialization.json.Json

private const val SERVICE = "PumpernickelApp.AI"
private const val LEGACY_ACCOUNT = "openai.api.key"
private val json = Json { ignoreUnknownKeys = true; encodeDefaults = true }

actual class SecureKeyStore {

    actual suspend fun writeCredential(provider: ProviderId, credential: Credential) = withContext(Dispatchers.Default) {
        val (account, payload) = when (credential) {
            is Credential.ApiKey -> provider.apiKeyAccount to credential.value
            is Credential.OAuthToken -> provider.oauthTokenAccount to json.encodeToString(Credential.OAuthToken.serializer(), credential)
        }
        writeRaw(account, payload)
        // Update ApiKeyState only when the currently-active provider's slot changes; for now
        // mirror Phase-18 semantics — ANY write flips the sentinel to true. AiSettingsViewModel
        // will eventually replace this with per-provider state (Plan 07).
        ApiKeyState.set(true)
    }

    actual suspend fun readCredential(provider: ProviderId): Credential? = withContext(Dispatchers.Default) {
        // D-22-01 OAuth-Primary: try OAuth slot first, fall back to API-Key slot.
        val oauthRaw = readRaw(provider.oauthTokenAccount)
        if (oauthRaw != null) {
            return@withContext try {
                json.decodeFromString(Credential.OAuthToken.serializer(), oauthRaw)
            } catch (_: Throwable) { null }
        }
        val apiKey = readRaw(provider.apiKeyAccount)
        if (apiKey != null) Credential.ApiKey(apiKey) else null
    }

    actual suspend fun clearCredential(provider: ProviderId) = withContext(Dispatchers.Default) {
        deleteRaw(provider.apiKeyAccount)
        deleteRaw(provider.oauthTokenAccount)
        // Recompute ApiKeyState after clear.
        ApiKeyState.set(listProvidersBlocking().isNotEmpty())
    }

    actual suspend fun listProviders(): Set<ProviderId> = withContext(Dispatchers.Default) {
        ProviderId.entries.filter { p ->
            readRaw(p.apiKeyAccount) != null || readRaw(p.oauthTokenAccount) != null
        }.toSet()
    }

    actual suspend fun readLegacyApiKey(): String? = withContext(Dispatchers.Default) {
        readRaw(LEGACY_ACCOUNT)
    }

    actual suspend fun clearLegacyApiKey() = withContext(Dispatchers.Default) {
        deleteRaw(LEGACY_ACCOUNT)
        Unit
    }

    // --- internal helpers ---
    private fun writeRaw(account: String, value: String) {
        // SecItemUpdate first, SecItemAdd if errSecItemNotFound — bestehender Pattern aus
        // SecureKeyStore.ios.kt:60-92, aber mit `account`-Parameter durch baseQueryDict.
        // ...
    }
    private fun readRaw(account: String): String? {
        // SecItemCopyMatching mit kSecReturnData=true — bestehender Pattern aus
        // SecureKeyStore.ios.kt:98-140, aber account parametriert.
        // ...
    }
    private fun deleteRaw(account: String) {
        // SecItemDelete mit account — bestehender Pattern aus SecureKeyStore.ios.kt:142-151.
        // ...
    }
    private fun listProvidersBlocking(): Set<ProviderId> { /* sync-Variante für ApiKeyState-Update */ }

    private fun MemScope.baseQueryDict(account: String, extras: Array<Pair<CPointer<*>?, CPointer<*>?>> = emptyArray()): CFDictionaryRef {
        // Selbe Mechanik wie bestehend (SecureKeyStore.ios.kt:162-174), aber `account` Param
        // statt fixed ACCOUNT-Konstante.
    }
}

// Helpers (objcPointer, ByteArray.toNSData) bleiben unverändert.
```

Wichtig: Die Keychain-Mechanik aus dem bestehenden File (`baseQueryDict`, `makeDict`, `objcPointer`, `ByteArray.toNSData`) bleibt 1:1 — nur `ACCOUNT` wird Parameter. Behalte alle bestehenden `try/catch (t: Throwable) { println(...); ... }` Defensive-Logs.

(3) `shared/src/androidMain/kotlin/com/pumpernickel/infrastructure/ai/SecureKeyStore.android.kt` — vollständig ersetzen:

```kotlin
package com.pumpernickel.infrastructure.ai

import android.content.Context
import androidx.security.crypto.EncryptedSharedPreferences
import androidx.security.crypto.MasterKeys
import com.pumpernickel.domain.ai.ApiKeyState
import kotlinx.coroutines.Dispatchers
import kotlinx.coroutines.withContext
import kotlinx.serialization.json.Json

actual class SecureKeyStore(private val context: Context) {

    private val json = Json { ignoreUnknownKeys = true; encodeDefaults = true }

    private val prefs by lazy {
        val masterKeyAlias = MasterKeys.getOrCreate(MasterKeys.AES256_GCM_SPEC)
        EncryptedSharedPreferences.create(
            PREFS_NAME, masterKeyAlias, context,
            EncryptedSharedPreferences.PrefKeyEncryptionScheme.AES256_SIV,
            EncryptedSharedPreferences.PrefValueEncryptionScheme.AES256_GCM
        )
    }

    actual suspend fun writeCredential(provider: ProviderId, credential: Credential) = withContext(Dispatchers.IO) {
        val (key, value) = when (credential) {
            is Credential.ApiKey -> provider.apiKeyAccount to credential.value
            is Credential.OAuthToken -> provider.oauthTokenAccount to json.encodeToString(Credential.OAuthToken.serializer(), credential)
        }
        prefs.edit().putString(key, value).apply()
        ApiKeyState.set(true)
    }

    actual suspend fun readCredential(provider: ProviderId): Credential? = withContext(Dispatchers.IO) {
        val oauthRaw = prefs.getString(provider.oauthTokenAccount, null)
        if (oauthRaw != null) {
            return@withContext try {
                json.decodeFromString(Credential.OAuthToken.serializer(), oauthRaw)
            } catch (_: Throwable) { null }
        }
        prefs.getString(provider.apiKeyAccount, null)?.let { Credential.ApiKey(it) }
    }

    actual suspend fun clearCredential(provider: ProviderId) = withContext(Dispatchers.IO) {
        prefs.edit()
            .remove(provider.apiKeyAccount)
            .remove(provider.oauthTokenAccount)
            .apply()
        ApiKeyState.set(hasAnyCredentialSync())
    }

    actual suspend fun listProviders(): Set<ProviderId> = withContext(Dispatchers.IO) {
        ProviderId.entries.filter { p ->
            prefs.contains(p.apiKeyAccount) || prefs.contains(p.oauthTokenAccount)
        }.toSet()
    }

    actual suspend fun readLegacyApiKey(): String? = withContext(Dispatchers.IO) {
        prefs.getString(LEGACY_KEY, null)
    }

    actual suspend fun clearLegacyApiKey() = withContext(Dispatchers.IO) {
        prefs.edit().remove(LEGACY_KEY).apply()
    }

    private fun hasAnyCredentialSync(): Boolean =
        ProviderId.entries.any { p ->
            prefs.contains(p.apiKeyAccount) || prefs.contains(p.oauthTokenAccount)
        }

    companion object {
        private const val PREFS_NAME = "ai_secrets"
        private const val LEGACY_KEY = "openai.api.key"
    }
}
```

Achtung: `LEGACY_KEY` == `ProviderId.OpenAI.apiKeyAccount`. Das ist Absicht: vor der Migration existiert nur dieser eine Slot — er IST der OpenAI-Slot. `readLegacyApiKey()` und `readCredential(ProviderId.OpenAI)` lesen den GLEICHEN Slot. Die Migration in Plan 06 stellt sicher, dass nach Migration der `activeProvider`-DataStore-Wert gesetzt ist; danach wechseln Aufrufer auf den neuen API-Pfad.

KEIN Aufruf von `writeApiKey/readApiKey/clearApiKey` darf nach dieser Task übrig sein — verifiziere mit grep. Bestehende Konsumenten (`AiSettingsViewModel.kt`, `AiModule.kt`) werden in Plan 04 und Plan 07 angepasst; sie sind solange COMPILE-broken — das ist erwartet und wird durch das Acceptance-Criterion `compileCommonMainKotlinMetadata` ABSICHTLICH NICHT abgedeckt; siehe Hinweis unten.

HINWEIS: Diese Task bricht commonMain-Compile bewusst (AiSettingsViewModel + AiModule referenzieren noch `readApiKey`). Plan 04 (AnthropicAiClient + Wiring) und Plan 07 (Migration + Wiring + AiSettingsViewModel) reparieren diese Konsumenten. Wave-1-Tasks 02 und 03 kompilieren unabhängig — kein Block. Wave 2 (Plan 03) referenziert NUR die neuen Methoden, daher kompiliert dort der Anthropic-Pfad isoliert. Die finale grüne Compile-Stufe für commonMain ist Plan 07 (Wave 3).
  </action>
  <verify>
    <automated>grep -q "expect class SecureKeyStore" shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/SecureKeyStore.kt && grep -q "writeCredential(provider: ProviderId, credential: Credential)" shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/SecureKeyStore.kt && grep -q "readLegacyApiKey" shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/SecureKeyStore.kt && ! grep -q "fun writeApiKey\|fun readApiKey()\|fun clearApiKey()" shared/src/commonMain/kotlin/com/pumpernickel/infrastructure/ai/SecureKeyStore.kt && grep -q "actual suspend fun writeCredential" shared/src/iosMain/kotlin/com/pumpernickel/infrastructure/ai/SecureKeyStore.ios.kt && grep -q "actual suspend fun writeCredential" shared/src/androidMain/kotlin/com/pumpernickel/infrastructure/ai/SecureKeyStore.android.kt && grep -q "provider.apiKeyAccount" shared/src/androidMain/kotlin/com/pumpernickel/infrastructure/ai/SecureKeyStore.android.kt</automated>
  </verify>
  <acceptance_criteria>
- `expect class SecureKeyStore` exponiert NUR: writeCredential / readCredential / clearCredential / listProviders / readLegacyApiKey / clearLegacyApiKey (keine writeApiKey/readApiKey/clearApiKey mehr)
- iOS actual nutzt `provider.apiKeyAccount` und `provider.oauthTokenAccount` für `kSecAttrAccount`
- Android actual nutzt `provider.apiKeyAccount` und `provider.oauthTokenAccount` als EncryptedSharedPreferences-Keys
- iOS legacy: `readLegacyApiKey()` liest `kSecAttrAccount = "openai.api.key"` (== `ProviderId.OpenAI.apiKeyAccount`)
- Android legacy: `readLegacyApiKey()` liest `prefs.getString("openai.api.key", null)`
- OAuth-Slot wird mit `Json.encodeToString(Credential.OAuthToken.serializer(), cred)` serialisiert
- `readCredential(p)`: OAuth-Slot wins über api.key-Slot wenn beide gesetzt
- ApiKeyState.set(true) wird in writeCredential aufgerufen
- ApiKeyState.set(...) in clearCredential reflektiert den Stand nach Delete
- iosMain + androidMain compile (commonMain bewusst broken — Konsumenten werden in Plan 04+07 angepasst)
  </acceptance_criteria>
  <done>SecureKeyStore-API umgestellt auf Multi-Slot. iOS Keychain + Android EncryptedSharedPreferences-Slots pro ProviderId verfügbar. Legacy-Read-Pfad bereit für Migration (Plan 06).</done>
</task>

</tasks>

<verification>
- Frische Files: ProviderId.kt + Credential.kt existieren in commonMain
- SecureKeyStore.kt expect-Surface zeigt nur Multi-Slot-API + Legacy-Helpers
- `grep -rn "secureKeyStore.readApiKey\|secureKeyStore.writeApiKey\|secureKeyStore.clearApiKey" shared/ androidApp/ iosApp/` listet exakt die Konsumenten die Plan 04 + Plan 07 anpassen werden (AiSettingsViewModel, AiModule) — KEIN bekannter Konsument darf sich ergänzend einschleichen
- iOS + Android actuals compile isoliert (commonMain compile broken bis Plan 07 — erwartete Phase-22-Wave-Übergangs-State)
</verification>

<success_criteria>
- Drei platform-secure Slot-Familien (`<provider>.api.key`, `<provider>.oauth.token`) sind über die expect-API erreichbar
- OAuth-Tokens für Anthropic können mit Refresh-Token + Expiry rund-trip-fähig persistiert werden
- Legacy-Read-Pfad bereit für die one-time Migration in Plan 06 (D-22-08)
</success_criteria>

<output>
After completion, create `.planning/phases/22-anthropic-ai-provider-3-provider-neben-openai-together-mit-o/22-01-SUMMARY.md` documenting:
- Final expect-Surface
- Keychain-account vs EncryptedSharedPreferences-Key-Naming entry (`<provider>.api.key`, `<provider>.oauth.token`)
- Backward-compat note: `LEGACY_KEY == ProviderId.OpenAI.apiKeyAccount` (Migration nutzt dies)
- Liste der commonMain-Konsumenten die JETZT broken sind (AiSettingsViewModel, AiModule) und in welchem Plan sie repariert werden (04 + 07)
</output>
