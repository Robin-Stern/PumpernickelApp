---
phase: 10-minimal-set-screen-ui-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - iosApp/iosApp/Extensions/Color+App.swift
  - iosApp/iosApp/Views/Workout/WorkoutSessionView.swift
  - iosApp/iosApp/Views/Workout/RestTimerView.swift
  - iosApp/iosApp/Views/Workout/WorkoutSetRow.swift
  - iosApp/iosApp/Views/Workout/WorkoutFinishedView.swift
autonomous: true
requirements: [UX-04]
must_haves:
  truths:
    - "All workout view accent colors reference a single shared Color.appAccent constant"
    - "Horizontal padding on buttons and cards is consistently 32pt across workout screens"
    - "Typography follows the hierarchy: .title2.weight(.bold) for exercise name, .headline for set info, .subheadline for metadata, .caption for picker labels"
  artifacts:
    - path: "iosApp/iosApp/Extensions/Color+App.swift"
      provides: "Shared Color.appAccent constant"
      contains: "static let appAccent"
    - path: "iosApp/iosApp/Views/Workout/WorkoutSessionView.swift"
      provides: "Color.appAccent usage replacing hardcoded RGB"
    - path: "iosApp/iosApp/Views/Workout/RestTimerView.swift"
      provides: "Color.appAccent usage replacing hardcoded RGB"
    - path: "iosApp/iosApp/Views/Workout/WorkoutSetRow.swift"
      provides: "Color.appAccent usage replacing hardcoded RGB"
    - path: "iosApp/iosApp/Views/Workout/WorkoutFinishedView.swift"
      provides: "Color.appAccent and consistent padding"
  key_links:
    - from: "iosApp/iosApp/Extensions/Color+App.swift"
      to: "All workout views"
      via: "Color.appAccent static constant"
      pattern: "Color\\.appAccent"
---

<objective>
Extract the hardcoded accent color into a shared constant and standardize padding/typography across all workout views.

Purpose: Establishes the visual foundation (Color.appAccent) that all subsequent polish work builds on, and fixes the padding/typography inconsistencies identified in the UX audit (per D-09, D-10, D-11, D-12).

Output: New Color+App.swift extension file, all 9 hardcoded color occurrences in workout views replaced, padding standardized, typography verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-minimal-set-screen-ui-polish/10-CONTEXT.md
@.planning/phases/10-minimal-set-screen-ui-polish/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Color.appAccent extension and replace hardcoded colors in workout views</name>
  <files>
    iosApp/iosApp/Extensions/Color+App.swift,
    iosApp/iosApp/Views/Workout/WorkoutSessionView.swift,
    iosApp/iosApp/Views/Workout/RestTimerView.swift,
    iosApp/iosApp/Views/Workout/WorkoutSetRow.swift,
    iosApp/iosApp/Views/Workout/WorkoutFinishedView.swift
  </files>
  <read_first>
    iosApp/iosApp/Views/Workout/WorkoutSessionView.swift,
    iosApp/iosApp/Views/Workout/RestTimerView.swift,
    iosApp/iosApp/Views/Workout/WorkoutSetRow.swift,
    iosApp/iosApp/Views/Workout/WorkoutFinishedView.swift
  </read_first>
  <action>
Per D-09: Create `iosApp/iosApp/Extensions/Color+App.swift` with:

```swift
import SwiftUI

extension Color {
    static let appAccent = Color(red: 0.4, green: 0.733, blue: 0.416)
}
```

Create the `Extensions/` directory first (`mkdir -p iosApp/iosApp/Extensions/`).

Then replace all 9 occurrences of `Color(red: 0.4, green: 0.733, blue: 0.416)` with `Color.appAccent` in workout views only (per D-12 scope):

**WorkoutSessionView.swift** (5 occurrences):
- Line 153: `.foregroundColor(Color(red: 0.4, green: 0.733, blue: 0.416))` -> `.foregroundColor(.appAccent)` (Rest Complete text)
- Line 164: `.background(Color(red: 0.4, green: 0.733, blue: 0.416))` -> `.background(Color.appAccent)` (Continue button)
- Line 346: `Color(red: 0.4, green: 0.733, blue: 0.416)` -> `Color.appAccent` (Complete Set button ternary)
- Line 442: `.background(Color(red: 0.4, green: 0.733, blue: 0.416))` -> `.background(Color.appAccent)` (Edit sheet Save)
- Line 562: `.background(Color(red: 0.4, green: 0.733, blue: 0.416))` -> `.background(Color.appAccent)` (Recap Save Workout)

**RestTimerView.swift** (1 occurrence):
- Line 26: `.fill(Color(red: 0.4, green: 0.733, blue: 0.416))` -> `.fill(Color.appAccent)` (Progress bar)

**WorkoutSetRow.swift** (1 occurrence):
- Line 18: `.foregroundColor(isCompleted ? Color(red: 0.4, green: 0.733, blue: 0.416) : .secondary)` -> `.foregroundColor(isCompleted ? .appAccent : .secondary)` (Checkmark)

**WorkoutFinishedView.swift** (2 occurrences):
- Line 16: `.foregroundColor(Color(red: 0.4, green: 0.733, blue: 0.416))` -> `.foregroundColor(.appAccent)` (Large checkmark)
- Line 41: `.background(Color(red: 0.4, green: 0.733, blue: 0.416))` -> `.background(Color.appAccent)` (Done button)

Note: Use `.appAccent` (shorthand) in modifier chains where type inference works (foregroundColor, fill), and `Color.appAccent` where explicit Color type is needed (ternary expressions, background).
  </action>
  <verify>
    <automated>cd /Users/olli/Studium/semester_6/mobile_app_dev/PumpernickelApp && grep -r "Color(red: 0.4, green: 0.733, blue: 0.416)" iosApp/iosApp/Views/Workout/ | wc -l | tr -d ' ' && echo "should be 0" && grep -c "appAccent" iosApp/iosApp/Views/Workout/WorkoutSessionView.swift && echo "should be 5" && cat iosApp/iosApp/Extensions/Color+App.swift</automated>
  </verify>
  <acceptance_criteria>
    - iosApp/iosApp/Extensions/Color+App.swift exists and contains `static let appAccent = Color(red: 0.4, green: 0.733, blue: 0.416)`
    - Zero occurrences of `Color(red: 0.4, green: 0.733, blue: 0.416)` remain in iosApp/iosApp/Views/Workout/ directory
    - WorkoutSessionView.swift contains 5 references to `appAccent`
    - RestTimerView.swift contains 1 reference to `appAccent`
    - WorkoutSetRow.swift contains 1 reference to `appAccent`
    - WorkoutFinishedView.swift contains 2 references to `appAccent`
  </acceptance_criteria>
  <done>Color.appAccent extension created, all 9 workout view color references migrated, zero hardcoded RGB values remain in workout views</done>
</task>

<task type="auto">
  <name>Task 2: Standardize padding and verify typography hierarchy in workout views</name>
  <files>
    iosApp/iosApp/Views/Workout/WorkoutFinishedView.swift
  </files>
  <read_first>
    iosApp/iosApp/Views/Workout/WorkoutFinishedView.swift,
    iosApp/iosApp/Views/Workout/WorkoutSessionView.swift,
    iosApp/iosApp/Views/Workout/RestTimerView.swift,
    iosApp/iosApp/Views/Workout/WorkoutSetRow.swift
  </read_first>
  <action>
Per D-10: Standardize horizontal padding on buttons and cards to 32pt across workout screens.

The research audit found one inconsistency:

**WorkoutFinishedView.swift line 30:** Change `.padding(.horizontal, 24)` to `.padding(.horizontal, 32)` on the summary card (the VStack containing SummaryRow items). This aligns with the dominant 32pt horizontal padding used by all buttons (Complete Set, Continue, Save, Done) and the rest timer progress bar.

All other padding values are already consistent or intentionally different:
- `.padding(.horizontal, 32)` on buttons: already correct (WorkoutSessionView lines 166/348/444/564, RestTimerView line 37, WorkoutFinishedView line 43)
- `.padding(.horizontal, 12)` in WorkoutSetRow: correct (row-internal padding, not screen-level)
- `.padding()` (default 16): correct for container padding
- `.padding(.vertical, *)`: vertical padding varies intentionally by context

Per D-11: Verify typography hierarchy. The current codebase already follows the prescribed hierarchy:
- `.title2.weight(.bold)` for exercise name (WorkoutSessionView line 264) -- correct
- `.headline` for set info (WorkoutSessionView line 268, ExerciseOverviewSheet line 100 for current) -- correct
- `.subheadline` for metadata (WorkoutSessionView lines 255/259/277, ExerciseOverviewSheet line 105) -- correct
- `.caption` for picker labels (WorkoutSessionView lines 302/317) -- correct

No typography changes needed -- all values already match the prescribed hierarchy.
  </action>
  <verify>
    <automated>cd /Users/olli/Studium/semester_6/mobile_app_dev/PumpernickelApp && grep "padding(.horizontal, 24)" iosApp/iosApp/Views/Workout/WorkoutFinishedView.swift | wc -l | tr -d ' ' && echo "should be 0" && grep "padding(.horizontal, 32)" iosApp/iosApp/Views/Workout/WorkoutFinishedView.swift | wc -l | tr -d ' ' && echo "should be 2"</automated>
  </verify>
  <acceptance_criteria>
    - WorkoutFinishedView.swift contains zero occurrences of `.padding(.horizontal, 24)`
    - WorkoutFinishedView.swift contains 2 occurrences of `.padding(.horizontal, 32)` (summary card + Done button)
    - No other workout view files modified for padding (all already consistent)
  </acceptance_criteria>
  <done>Horizontal padding standardized to 32pt across all workout screens, typography hierarchy verified as already consistent</done>
</task>

</tasks>

<verification>
- `grep -r "Color(red: 0.4, green: 0.733, blue: 0.416)" iosApp/iosApp/Views/Workout/` returns 0 results
- `grep -r "appAccent" iosApp/iosApp/Views/Workout/` returns 9 results
- `cat iosApp/iosApp/Extensions/Color+App.swift` shows the extension
- `grep "padding(.horizontal, 24)" iosApp/iosApp/Views/Workout/WorkoutFinishedView.swift` returns 0 results
- Xcode build succeeds (no compilation errors from color references)
</verification>

<success_criteria>
- Color.appAccent extension exists and is the single source of truth for accent color
- All 9 hardcoded color occurrences in workout views replaced with Color.appAccent
- WorkoutFinishedView summary card padding standardized from 24 to 32
- Typography hierarchy matches D-11 prescription (verified, no changes needed)
- All changes scoped to workout views only per D-12
</success_criteria>

<output>
After completion, create `.planning/phases/10-minimal-set-screen-ui-polish/10-01-SUMMARY.md`
</output>
