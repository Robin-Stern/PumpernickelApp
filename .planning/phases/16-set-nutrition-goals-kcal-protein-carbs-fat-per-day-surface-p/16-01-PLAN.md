---
phase: 16-set-nutrition-goals-kcal-protein-carbs-fat-per-day-surface-p
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/src/commonMain/kotlin/com/pumpernickel/domain/model/UserPhysicalStats.kt
autonomous: true
requirements: []
decisions_addressed:
  - D-16-10
  - D-16-11
  - D-16-12

must_haves:
  truths:
    - "A `UserPhysicalStats` data class exists in commonMain that the calculator and SettingsRepository can both reference."
    - "Two enums — `Sex` (MALE / FEMALE) and `ActivityLevel` (5 tiers) — are declared so they can be persisted as `.name` strings and surfaced in UI."
    - "The data class has no defaults, so `Flow<UserPhysicalStats?>` semantics in the repo (null = never set) are preserved."
  artifacts:
    - path: "shared/src/commonMain/kotlin/com/pumpernickel/domain/model/UserPhysicalStats.kt"
      provides: "UserPhysicalStats data class + Sex enum + ActivityLevel enum"
      contains: "data class UserPhysicalStats"
  key_links: []
---

<objective>
Create the shared domain model for the user's physical stats — weight, height, age, sex, and activity level — that the TDEE calculator and SettingsRepository will both consume.

Purpose: Give every downstream consumer a single canonical representation of "who the user is for nutrition-goal purposes" before any of them are written. Per D-16-10/D-16-11, these stats live in `SettingsRepository` (DataStore, not Room) and are stored alongside the existing nutrition-goal keys. The model has no defaults — null at the repo level means "never set," and the editor opens with placeholder values from UI-SPEC.

Output: One file with two enums and one data class.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-set-nutrition-goals-kcal-protein-carbs-fat-per-day-surface-p/16-CONTEXT.md
@.planning/phases/16-set-nutrition-goals-kcal-protein-carbs-fat-per-day-surface-p/16-PATTERNS.md

@shared/src/commonMain/kotlin/com/pumpernickel/domain/model/NutritionGoals.kt

<interfaces>
<!-- Existing analog (full file) — copy structure verbatim -->
From shared/src/commonMain/kotlin/com/pumpernickel/domain/model/NutritionGoals.kt:
```kotlin
package com.pumpernickel.domain.model

data class NutritionGoals(
    val calorieGoal: Int = 2500,
    val proteinGoal: Int = 150,
    val fatGoal: Int = 80,
    val carbGoal: Int = 300,
    val sugarGoal: Int = 50
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create UserPhysicalStats.kt with Sex + ActivityLevel enums + data class</name>
  <files>shared/src/commonMain/kotlin/com/pumpernickel/domain/model/UserPhysicalStats.kt</files>
  <read_first>
    - shared/src/commonMain/kotlin/com/pumpernickel/domain/model/NutritionGoals.kt (analog — copy package + structure pattern)
    - .planning/phases/16-set-nutrition-goals-kcal-protein-carbs-fat-per-day-surface-p/16-PATTERNS.md (UserPhysicalStats section, lines 31–73)
    - .planning/phases/16-set-nutrition-goals-kcal-protein-carbs-fat-per-day-surface-p/16-CONTEXT.md (D-16-10, D-16-11, D-16-12)
  </read_first>
  <behavior>
    - File compiles in commonMain with no other source dependencies (no imports beyond stdlib).
    - `Sex` enum has exactly two entries: `MALE`, `FEMALE`, in that order.
    - `ActivityLevel` enum has exactly five entries: `SEDENTARY`, `LIGHTLY_ACTIVE`, `MODERATELY_ACTIVE`, `VERY_ACTIVE`, `EXTRA_ACTIVE`, in that order (D-16-05).
    - `UserPhysicalStats` has fields `weightKg: Double`, `heightCm: Int`, `age: Int`, `sex: Sex`, `activityLevel: ActivityLevel` — in that order, with NO default values (per D-16-11 — null at the repo level means "never set," not "use defaults").
  </behavior>
  <action>
    Create the file `shared/src/commonMain/kotlin/com/pumpernickel/domain/model/UserPhysicalStats.kt` with the exact contents below. Do NOT add KDoc, do NOT add companion objects, do NOT add `Serializable` annotations (this model is consumed by DataStore only, which serializes its fields individually as String preferences per D-16-10). Match the package and style of the analog file `NutritionGoals.kt` 1:1.

    File contents (verbatim):

    ```kotlin
    package com.pumpernickel.domain.model

    enum class Sex { MALE, FEMALE }

    enum class ActivityLevel {
        SEDENTARY,
        LIGHTLY_ACTIVE,
        MODERATELY_ACTIVE,
        VERY_ACTIVE,
        EXTRA_ACTIVE
    }

    data class UserPhysicalStats(
        val weightKg: Double,
        val heightCm: Int,
        val age: Int,
        val sex: Sex,
        val activityLevel: ActivityLevel
    )
    ```

    Per D-16-12: weight is in kg only (no lbs in the model — the app-wide `weightUnit` enum from existing `WeightUnit.kt` stays scoped to workout weights and is NOT referenced here). Height is in cm only.
  </action>
  <verify>
    <automated>cd /Users/olli/Studium/semester_6/mobile_app_dev/PumpernickelApp && ./gradlew :shared:compileKotlinIosSimulatorArm64 --no-daemon -q 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - File `shared/src/commonMain/kotlin/com/pumpernickel/domain/model/UserPhysicalStats.kt` exists.
    - `grep -c "^package com.pumpernickel.domain.model$" shared/src/commonMain/kotlin/com/pumpernickel/domain/model/UserPhysicalStats.kt` returns `1`.
    - `grep -c "^enum class Sex" shared/src/commonMain/kotlin/com/pumpernickel/domain/model/UserPhysicalStats.kt` returns `1`.
    - `grep -c "MALE, FEMALE" shared/src/commonMain/kotlin/com/pumpernickel/domain/model/UserPhysicalStats.kt` returns `1`.
    - `grep -c "^enum class ActivityLevel" shared/src/commonMain/kotlin/com/pumpernickel/domain/model/UserPhysicalStats.kt` returns `1`.
    - `grep -c "SEDENTARY\|LIGHTLY_ACTIVE\|MODERATELY_ACTIVE\|VERY_ACTIVE\|EXTRA_ACTIVE" shared/src/commonMain/kotlin/com/pumpernickel/domain/model/UserPhysicalStats.kt` returns `5`.
    - `grep -c "^data class UserPhysicalStats" shared/src/commonMain/kotlin/com/pumpernickel/domain/model/UserPhysicalStats.kt` returns `1`.
    - `grep -c "weightKg: Double" shared/src/commonMain/kotlin/com/pumpernickel/domain/model/UserPhysicalStats.kt` returns `1`.
    - `grep -c "heightCm: Int" shared/src/commonMain/kotlin/com/pumpernickel/domain/model/UserPhysicalStats.kt` returns `1`.
    - `grep -c "= " shared/src/commonMain/kotlin/com/pumpernickel/domain/model/UserPhysicalStats.kt` returns `0` (no defaults anywhere — per D-16-11).
    - `grep -cE "import\s+" shared/src/commonMain/kotlin/com/pumpernickel/domain/model/UserPhysicalStats.kt` returns `0` (no imports beyond stdlib).
    - `./gradlew :shared:compileKotlinIosSimulatorArm64 -q` exits 0.
  </acceptance_criteria>
  <done>UserPhysicalStats.kt is created with `Sex`, `ActivityLevel`, and the data class as specified; shared module compiles cleanly on iOS simulator and Android targets.</done>
</task>

</tasks>

<verification>
Phase-level checks for this plan:
1. `./gradlew :shared:compileKotlinIosSimulatorArm64 -q` succeeds (model is reachable from iOS).
2. `./gradlew :shared:compileDebugKotlinAndroid -q` succeeds (model is reachable from Android).
3. The file contains zero defaults on `UserPhysicalStats` constructor parameters (verifies the null-vs-default contract from D-16-11).
4. Both enums are declared in this file (no separate Sex.kt / ActivityLevel.kt files — keeps the model self-contained for downstream consumers).
</verification>

<success_criteria>
- New file exists at `shared/src/commonMain/kotlin/com/pumpernickel/domain/model/UserPhysicalStats.kt`.
- Compiles on Android + iOS targets.
- All decisions D-16-10, D-16-11, D-16-12 are honored: DataStore-style storage means no model-level defaults; kg/cm only; data class is plain (no `Serializable`).
- Downstream plans (02 TdeeCalculator, 03 SettingsRepository) can `import com.pumpernickel.domain.model.{UserPhysicalStats, Sex, ActivityLevel}` directly.
</success_criteria>

<output>
After completion, create `.planning/phases/16-set-nutrition-goals-kcal-protein-carbs-fat-per-day-surface-p/16-01-SUMMARY.md` recording: file path, exact contents, compile-targets verified, and any deviations from the planned shape (none expected).
</output>
