---
phase: 16
slug: set-nutrition-goals-kcal-protein-carbs-fat-per-day-surface-p
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-28
---

# Phase 16 — UI Design Contract

> Visual and interaction contract for Phase 16: Set nutrition goals (kcal/protein/carbs/fat per day).
> Generated by gsd-ui-researcher. Verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (no shadcn — KMP + Compose Material 3 + SwiftUI project) |
| Preset | not applicable |
| Component library (Android) | Jetpack Compose Material 3 (dynamic accent via `PumpernickelTheme`) |
| Component library (iOS) | SwiftUI native components |
| Icon library (Android) | `androidx.compose.material.icons` (filled + outlined) |
| Icon library (iOS) | SF Symbols (system images) |
| Font | System default — Material 3 type scale on Android; San Francisco on iOS |

**Design system note:** No new design system tooling is introduced. This phase reuses every established token, component, and pattern from the existing codebase. `PumpernickelTheme` already provides dynamic color per D-post-v1.5 (8 accent presets + light/dark mode). All color references below use M3 semantic roles, not hard-coded hex values, so they adapt automatically.

---

## Spacing Scale

Declared values (multiples of 4, matching the existing codebase):

| Token | Value | Usage in this phase |
|-------|-------|---------------------|
| xs | 4dp/pt | Icon gaps, label-to-picker gap, legend dot-to-label gap |
| sm | 8dp/pt | Between banner icon and text; between stat field and its label; top padding after TopAppBar |
| smPlus | 12dp/pt | Overview banner internal vertical padding (OS notification banner conventions); suggestion card internal padding (tighter than full card to pack three cards side-by-side) |
| md | 16dp/pt | Section card internal padding; horizontal screen padding; stat row padding |
| lg | 24dp/pt | Vertical spacing between cards in Overview; vertical spacing between sections inside editor |
| xl | 32dp/pt | Save button horizontal padding; bottom scroll padding |
| 2xl | 48dp/pt | Not used directly; reserved |
| 3xl | 64dp/pt | Not used |

Exceptions:
- DrumPicker item height: **40dp** (existing `DrumPicker.kt` constant — do not change)
- Touch target minimum: **44pt** (iOS) / **48dp** (Android) for all interactive controls (banner dismiss "×", stat picker controls, Save button)

---

## Typography

All type roles map to the M3 type scale on Android and the equivalent SwiftUI `.font()` modifier on iOS.

| Role | M3 Android token | iOS SwiftUI font | Weight | Line Height | Usage in this phase |
|------|-----------------|-----------------|--------|-------------|---------------------|
| Screen title | `titleLarge` (~22sp) | `.title2.weight(.semibold)` | SemiBold (600) | 1.2 | "Ernährungsziele" editor NavigationBar title; editor section headers |
| Card / section title | `titleMedium` (~16sp) | `.headline` | SemiBold (600) | 1.3 | "Meine Stats" / "Vorschläge" / "Zielwerte" section headers inside editor; existing "Ernährung · Heute" card title |
| Body / field label | `bodyMedium` (~14sp) | `.body` | Regular (400) | 1.5 | Activity tier labels (German text), stat field helper text, suggestion card macro breakdown lines |
| Caption / supporting | `labelSmall` (~11sp) | `.caption2` | Regular (400) | 1.4 | Drum picker unit labels ("kcal", "g"); banner supporting text; suggestion-card kcal number display |

**Constraint:** Maximum 4 type sizes, 2 weights (Regular 400 + SemiBold 600). Bold (700) is reserved for the centered DrumPicker selected-item value only (matches existing `DrumPicker.kt` `FontWeight.Bold` on distance==0 row) — this counts as a display variant of the existing weight scale, not a third weight.

---

## Color

The app uses a dynamic accent system. All roles below are M3 semantic tokens; hard-coded hex values come from established codebase constants for data-visualization only.

| Role | Android (M3 token) | iOS (SwiftUI) | 60/30/10 | Usage in this phase |
|------|--------------------|---------------|----------|---------------------|
| Dominant surface (60%) | `colorScheme.surface` / `colorScheme.background` | `Color(.systemBackground)` | 60% | Screen background; editor scroll area; banner background |
| Secondary / card surface (30%) | `colorScheme.surfaceVariant.copy(alpha = 0.5f)` | `Color(.secondarySystemGroupedBackground)` | 30% | Existing NutritionRingsCard; new editor section groupings; suggestion cards (unselected) |
| Accent (10%) | `colorScheme.primary` (user-chosen accent) | `Color.appAccent` | 10% | Selected suggestion card border + filled background tint; Save button fill; banner CTA chevron; DrumPicker selection divider lines (existing) |
| Destructive | `colorScheme.error` / `Color(0xFFB3261E)` (light) | `.red` / `Color.red` | — | Not used in this phase (no delete actions) |

**Accent reserved for:**
1. Selected suggestion card — 2dp accent-colored border + `colorScheme.primary.copy(alpha = 0.08f)` background fill
2. Save button — filled `colorScheme.primary` background with `colorScheme.onPrimary` label
3. Banner chevron "→" icon — `colorScheme.primary` tint
4. DrumPicker center-row divider lines — `colorScheme.primary.copy(alpha = 0.3f)` (existing, unchanged)

**Macro ring colors** (data-visualization, not accent — these are established in `OverviewScreen.kt` and `OverviewView.swift`; do not change):

| Macro | Android hex | iOS approx |
|-------|-------------|------------|
| Kcal / Calories | `0xFFFF6B6B` (red) | `Color.red` |
| Protein | `0xFF4FC3F7` (light blue) | `Color(red: 0.31, green: 0.76, blue: 0.97)` |
| Carbs | `0xFFFFD54F` (yellow) | `Color(red: 1.0, green: 0.84, blue: 0.31)` |
| Fat | `0xFFFF8A65` (orange) | `Color(red: 1.0, green: 0.54, blue: 0.40)` |
| Sugar | `0xFFBA68C8` (purple) | `Color(red: 0.73, green: 0.41, blue: 0.78)` |

These same colors are used as visual cues in suggestion cards (small colored dots next to each macro value) so users immediately associate them with the corresponding Overview rings.

---

## Drum Picker Ranges (Claude's Discretion — D-16-03)

Per-macro ranges for the five `DrumPicker` instances in the editor. Values are whole integers.

| Macro | Min | Max | Step | Default (NutritionGoals) |
|-------|-----|-----|------|--------------------------|
| kcal | 800 | 6 000 | 50 | 2 500 |
| Protein (g) | 20 | 400 | 5 | 150 |
| Carbs (g) | 20 | 700 | 5 | 300 |
| Fat (g) | 10 | 250 | 5 | 80 |
| Sugar (g) | 0 | 200 | 5 | 50 |

**Step rationale:** 50 kcal steps keep the kcal list length at 105 items (manageable scroll). 5 g steps for macros keep lists at max 76 items. Both match the snappy iOS Picker(.wheel) feel.

**Gram rounding for suggestion cards (Claude's Discretion):** Macro splits derived from TDEE are rounded to the nearest 5 g before being pushed into the drum pickers. Exception: values < 20 g are rounded to the nearest 1 g to avoid rounding to 0. This prevents the picker from jumping to a non-existent step.

---

## Calculator First-Time Placeholder Stats (Claude's Discretion — D-16-11)

When `userPhysicalStats` is null (no stats ever saved), the editor pre-fills these values as non-persisted placeholder defaults:

| Field | Default |
|-------|---------|
| Weight (kg) | 80 |
| Height (cm) | 180 |
| Age | 30 |
| Sex | Male |
| Activity tier | Moderately active (Mäßig aktiv 3–5×/Woche) |

These are displayed in the input fields immediately so users see realistic output from the suggestion cards without entering anything first.

---

## Sex Selector Idiom (Claude's Discretion — D-16-03)

| Platform | Component | Values |
|----------|-----------|--------|
| Android | `SegmentedButton` (M3 `MultiChoiceSegmentedButtonRow` with 2 options) | "Männlich" / "Weiblich" |
| iOS | `Picker(.segmented)` with 2 segments | "Männlich" / "Weiblich" |

Segmented control is the canonical cross-platform idiom for a binary choice with both options visible. Labels in German to match the rest of the stats section.

---

## Suggestion Card "Selected" Visual Treatment (Claude's Discretion)

Three cards: Cut / Beibehalten / Bulk (displayed in a horizontal row on wide screens; vertical stack on narrow).

| State | Android | iOS |
|-------|---------|-----|
| Unselected | `ElevatedCard` or `OutlinedCard` with `surfaceVariant` background, no border | `RoundedRectangle` with `secondarySystemGroupedBackground` fill, 1pt `Color(.separator)` stroke |
| Selected | `OutlinedCard` with `colorScheme.primary` 2dp border + `colorScheme.primary.copy(alpha = 0.08f)` background | `RoundedRectangle` with `Color.appAccent` 2pt stroke + `Color.appAccent.opacity(0.08)` fill |
| Hover / pressed | M3 ripple on the card surface | SwiftUI `.buttonStyle(.plain)` with opacity press animation |

Card content layout (same on both platforms):
1. Title badge: "Cut" / "Beibehalten" / "Bulk" — `titleMedium` weight SemiBold
2. Kcal value: e.g. "2 100 kcal" — `titleMedium` weight Regular, colored with `CalorieRingColor`
3. Macro breakdown: three lines: "Protein 156 g", "Kohlenhydrate 210 g", "Fett 58 g" — `labelSmall` + macro ring color dot (8dp circle) per line
4. Sugar line: "Zucker 50 g" — `labelSmall` + sugar ring color dot

Card internal padding uses the `smPlus` token (12dp/pt) to pack three cards side-by-side.

Tapping a card selects it (only one can be selected at a time), fills the pickers below, and visually highlights. Tapping the same card again has no effect (does not deselect — the user must pick one of the three).

---

## Overview Banner (Claude's Discretion — D-16-13 / D-16-14)

**Placement:** Above the NutritionRingsCard in the Overview scroll column, inserted before it in the `Column`/`VStack`.

**Visibility:** Only shown while `nutritionGoalsBannerVisible == true` in the ViewModel.

**Copy:**
- Leading icon: SF Symbol `target` (iOS) / `Icons.Outlined.TrackChanges` (Android) — in `colorScheme.primary` / `Color.appAccent`
- Main text: **"Persönliche Ziele setzen"** — `bodyMedium` / `.body` weight Regular
- Sub-text: **"Berechne deinen Tagesbedarf und passe deine Makros an."** — `labelSmall` / `.caption` color `onSurfaceVariant` / `.secondary`
- Trailing: chevron "→" icon in accent color + dismiss "×" button

**Dismiss animation:**
- Android: `AnimatedVisibility` with `slideOutVertically` + `fadeOut`, duration 300ms
- iOS: `withAnimation(.easeOut(duration: 0.3))` + `transition(.move(edge: .top).combined(with: .opacity))`

**Banner card style:**
- Android: `Card` with `RoundedCornerShape(12.dp)`, `colorScheme.surfaceVariant.copy(alpha = 0.6f)` background; internal vertical padding uses `smPlus` token (12dp)
- iOS: `RoundedRectangle(cornerRadius: 12)` with `Color(.secondarySystemGroupedBackground)` fill; internal vertical padding uses `smPlus` token (12pt)

---

## Screen Layout: NutritionGoalsEditorScreen / NutritionGoalsEditorView

### Entry

- **Android:** `NutritionGoalsEditorScreen` pushed onto the Overview-tab NavHost via `navController.navigate(NutritionGoalsEditorRoute)`. Back navigation = standard back button in TopAppBar.
- **iOS:** `NutritionGoalsEditorView` presented as a `.sheet` from the edit button on `OverviewView`. Dismiss via `.presentationDragIndicator(.visible)` + swipe down or "Abbrechen" button.

### Structure (single scrollable screen with 3 sections — D-16-02)

```
Screen
├── TopAppBar / NavigationBar title: "Ernährungsziele"
│   └── [iOS] "Abbrechen" leading button (dismisses sheet without saving)
│
├── ScrollView / LazyColumn
│   │
│   ├── Section 1: "Meine Stats" (collapsible — D-16-09)
│   │   ├── Disclosure group header / ExpandableCard toggle: "Meine Stats"
│   │   └── [when expanded]
│   │       ├── Weight field: NumericField or DrumPicker-style row — "Gewicht (kg)"
│   │       ├── Height field: "Körpergröße (cm)"
│   │       ├── Age field: "Alter"
│   │       ├── Sex selector: SegmentedControl "Männlich / Weiblich"
│   │       └── Activity tier: DropdownMenu (Android ExposedDropdownMenuBox) /
│   │                           Picker(.menu) (iOS) — German labels from D-16-05
│   │
│   ├── Section 2: "Vorschlag berechnen" (always visible)
│   │   └── HStack / Row of 3 tappable SuggestionCards: Cut · Beibehalten · Bulk
│   │       (live-updating as stats change; tapping pre-fills Section 3 pickers)
│   │
│   └── Section 3: "Zielwerte anpassen"
│       ├── DrumPickerRow: "Kalorien" — range 800–6000, step 50, unit "kcal"
│       ├── DrumPickerRow: "Protein" — range 20–400, step 5, unit "g"
│       ├── DrumPickerRow: "Kohlenhydrate" — range 20–700, step 5, unit "g"
│       ├── DrumPickerRow: "Fett" — range 10–250, step 5, unit "g"
│       └── DrumPickerRow: "Zucker" — range 0–200, step 5, unit "g"
│
└── Sticky bottom bar (or last item in scroll)
    └── Save button: "Ziele speichern" — full-width, accent-filled
```

### Stat input fields

- **Android:** `OutlinedTextField` with numeric keyboard (`keyboardType = KeyboardType.Number`). One field per stat (weight, height, age). No drum picker for stats inputs — text fields are more ergonomic for arbitrary numeric entry.
- **iOS:** SwiftUI `TextField` with `.keyboardType(.numberPad)`. Same layout.
- Weight field shows "kg" suffix inline (not as a separate label).
- Height field shows "cm" suffix inline.

### Section collapse behavior (D-16-09)

- Stats section starts **expanded** on first open (when `userPhysicalStats == null`); starts **collapsed** on subsequent opens (when stats already stored).
- Android: `AnimatedVisibility` wrapping the stats content, toggled by a header `Row` with a `chevron_up / chevron_down` icon.
- iOS: `DisclosureGroup` with the section title as the label.

---

## Interaction States

| State | Trigger | Visual Response |
|-------|---------|----------------|
| Stats not yet entered | `userPhysicalStats == null` | Stats section expanded; suggestion cards show dashes "—" for macro values |
| Stats changing (any field edit) | User types in stat field | Suggestion cards update live via ViewModel recomputation; no loading indicator needed (pure math) |
| Suggestion card tapped | User taps Cut / Beibehalten / Bulk | Card highlights (selected style); drum pickers animate to new values via `LaunchedEffect(selectedItem)` scroll in DrumPicker |
| Picker scrolling | User flings DrumPicker | Existing snap behavior (unchanged); selected suggestion card visually deselects if picker value diverges from card values by more than ±1 step |
| Save tapped | User taps "Ziele speichern" | Button shows `CircularProgressIndicator` inset for 300ms (matches DataStore write latency); screen dismisses on success |
| Banner tapped | User taps the banner body | Navigate to editor (same as edit button); banner persists until Save or explicit dismiss |
| Banner dismissed | User taps "×" | Slide-out animation (see above); `dismissBanner()` call; never shown again |
| Goals saved | `setNutritionGoals()` completes | Banner dismissed (D-16-14); screen popped; no toast/snack needed (Overview rings update immediately via Flow) |

---

## Copywriting Contract

| Element | German copy | Notes |
|---------|-------------|-------|
| Editor screen title | "Ernährungsziele" | TopAppBar / NavigationBar title |
| iOS cancel button | "Abbrechen" | Leading nav button, dismisses sheet without saving |
| Section 1 header | "Meine Stats" | Collapsible disclosure header |
| Section 2 header | "Vorschlag berechnen" | Always-visible heading above suggestion cards |
| Section 3 header | "Zielwerte anpassen" | Above drum pickers |
| Suggestion card — Cut | "Defizit" / subtitle "−500 kcal" | "Cut" is too English for a German-first app; "Defizit" is natural |
| Suggestion card — Maintain | "Erhalt" / subtitle "TDEE" | Short label; "Erhalt" = maintain in German |
| Suggestion card — Bulk | "Aufbau" / subtitle "+300 kcal" | "Aufbau" = bulk / building phase |
| Drum picker label — kcal | "Kalorien" | Above picker, unit label "kcal" |
| Drum picker label — protein | "Protein" | Unit "g" |
| Drum picker label — carbs | "Kohlenhydrate" | Unit "g" |
| Drum picker label — fat | "Fett" | Unit "g" |
| Drum picker label — sugar | "Zucker" | Unit "g" |
| Save button | "Ziele speichern" | Full-width, accent-filled |
| Banner main text | "Persönliche Ziele setzen" | Body weight Regular |
| Banner sub-text | "Berechne deinen Tagesbedarf und passe deine Makros an." | Caption / labelSmall, secondary color |
| Banner dismiss button a11y label | "Banner ausblenden" | VoiceOver / TalkBack label for the "×" button |
| Edit button on rings card (Android) | `Icons.Default.Edit` icon, contentDescription = "Ziele bearbeiten" | IconButton in card header row |
| Edit button on rings card (iOS) | SF Symbol `pencil`, a11y label "Ziele bearbeiten" | Placed top-trailing of the nutritionRingsSection card header |
| Empty suggestion card state | "—" for each macro value | When stats not yet entered (null UserPhysicalStats) |
| Stats section — weight field placeholder | "80" | Placeholder (not pre-filled text) when null |
| Stats section — height field placeholder | "180" | |
| Stats section — age field placeholder | "30" | |
| Activity tier — Sedentary label | "Bürojob / kaum Bewegung" | D-16-05 |
| Activity tier — Lightly active | "Leicht aktiv (1–3×/Woche)" | D-16-05 |
| Activity tier — Moderately active | "Mäßig aktiv (3–5×/Woche)" | D-16-05 |
| Activity tier — Very active | "Sehr aktiv (6–7×/Woche)" | D-16-05 |
| Activity tier — Extra active | "Extrem aktiv / körperlicher Beruf" | D-16-05 |
| Empty state (no goals ever set, rings all at 0) | Not applicable — default NutritionGoals (2500/150/80/300/50) populate from SettingsRepository from first launch. There is no "no goals" state. | |
| Error state — save failed | "Ziele konnten nicht gespeichert werden. Bitte nochmal versuchen." | Snackbar (Android) / `.alert` (iOS); only if DataStore write throws. Rare edge case. |

**No destructive actions in this phase.** There are no delete buttons, no confirmation dialogs required.

---

## Component Inventory

### New components

| Component | Platform | File | Description |
|-----------|----------|------|-------------|
| `NutritionGoalsEditorScreen` | Android (Compose) | `androidApp/.../screens/NutritionGoalsEditorScreen.kt` | Full editor screen (3 sections, pickers, save button) |
| `NutritionGoalsEditorView` | iOS (SwiftUI) | `iosApp/.../Views/Overview/NutritionGoalsEditorView.swift` | SwiftUI equivalent, presented as a sheet |
| `SuggestionCard` | Android (Compose) | Nested composable inside editor screen | Cut/Maintain/Bulk card |
| `SuggestionCardView` | iOS (SwiftUI) | Nested view inside editor view | Cut/Maintain/Bulk card |
| `NutritionGoalsBanner` | Android (Compose) | `androidApp/.../screens/OverviewScreen.kt` (added composable) | Dismissable banner in Overview |
| `NutritionGoalsBannerView` | iOS (SwiftUI) | Inline in `OverviewView.swift` | Same banner in iOS Overview |

### Reused components (do NOT re-implement)

| Component | Platform | Existing location |
|-----------|----------|------------------|
| `DrumPicker` | Android | `androidApp/.../components/DrumPicker.kt` |
| `Picker(.wheel)` | iOS | SwiftUI native — used verbatim, no wrapper needed |
| `NutritionRingsCard` | Android | `OverviewScreen.kt` — add edit button row to existing card header |
| `nutritionRingsSection` | iOS | `OverviewView.swift` — add edit button to existing section header HStack |
| `OutlinedTextField` | Android | M3 built-in |
| `TextField` / `Form` / `Section` | iOS | SwiftUI built-in |
| `ExposedDropdownMenuBox` | Android | M3 built-in — for activity tier selector |
| `DisclosureGroup` | iOS | SwiftUI built-in — for collapsible stats section |

---

## Accessibility

| Requirement | Android | iOS |
|-------------|---------|-----|
| DrumPicker accessible label | `contentDescription = "Kalorien Picker, aktueller Wert {value} kcal"` on each item | `Picker` native accessibility; add `.accessibilityLabel("Kalorien")` on container |
| Suggestion card selected state | `Modifier.semantics { selected = true }` on selected card | `.accessibilityAddTraits(.isSelected)` |
| Banner dismiss button | `contentDescription = "Banner ausblenden"` | `.accessibilityLabel("Banner ausblenden")` |
| Edit button on rings card | `contentDescription = "Ziele bearbeiten"` | `.accessibilityLabel("Ziele bearbeiten")` |
| Stats section toggle | `contentDescription = "Meine Stats \${if expanded "zuklappen" else "ausklappen"}"` | DisclosureGroup handles this natively |
| Minimum touch targets | 48dp on all interactive elements | 44pt — same as existing VoiceOver-compliant views |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none (not applicable — KMP/Compose project) | not required |
| Third-party | none | not required |

No registry tooling is involved. All components are either native M3/SwiftUI or existing project components.

---

## Platform-Specific Notes

### Android
- Editor screen is a regular Compose `Screen` navigated via the Overview-tab NavHost (`navController.navigate(NutritionGoalsEditorRoute)`).
- Stats section collapse uses `AnimatedVisibility(visible = isStatsExpanded)`.
- Drum pickers are laid out in a `LazyColumn` (scrollable editor) — each picker needs a fixed height of `200.dp` (5 visible items × 40dp) to prevent LazyColumn measurement conflicts. Set `modifier = Modifier.height(200.dp)` on each `DrumPicker`.
- The three suggestion cards use `Row(horizontalArrangement = Arrangement.spacedBy(8.dp))` — each card gets `Modifier.weight(1f)`.

### iOS
- Editor presented as `.sheet(isPresented: $showEditor)` from a `@State` variable on `OverviewView`.
- Stats section collapse uses `DisclosureGroup("Meine Stats", isExpanded: $statsExpanded)`.
- Drum pickers: native SwiftUI `Picker(.wheel)` — five pickers in a `Form` / `Section` or a custom `HStack` layout. Use `MultiComponentPicker` pattern from `WorkoutSessionView.swift` if side-by-side pickers are needed; otherwise stack them vertically in a `Form`.
- Suggestion cards: `HStack(spacing: 8)` with `frame(maxWidth: .infinity)` on each card.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
