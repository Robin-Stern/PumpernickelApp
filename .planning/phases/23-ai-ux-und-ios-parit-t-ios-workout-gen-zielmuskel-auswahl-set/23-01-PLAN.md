---
phase: 23-ai-ux-und-ios-parit-t-ios-workout-gen-zielmuskel-auswahl-set
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/src/commonMain/kotlin/com/pumpernickel/domain/ai/WorkoutAiPreview.kt
  - shared/src/commonMain/kotlin/com/pumpernickel/presentation/ai/WorkoutAiViewModel.kt
  - shared/src/commonMain/kotlin/com/pumpernickel/domain/ai/WorkoutAiUseCase.kt
  - shared/src/commonMain/resources/workout-system-prompt.md
autonomous: true
requirements: []
tags:
  - kmp
  - ai-workout
  - shared-domain

must_haves:
  truths:
    - "`WorkoutAiForm` data class enthält Feld `setsPerExercise: Int` mit Default 3 (per D-23-01)"
    - "`WorkoutAiUiState.Form` sealed-subclass enthält `setsPerExercise: Int = 3` und wird vom ViewModel durch alle `copy()`-Pfade getragen"
    - "`WorkoutAiViewModel.onSetsPerExerciseChanged(count: Int)` existiert, klemmt Eingaben via `coerceIn(1, 6)` (per D-23-01) und aktualisiert `_uiState`"
    - "`WorkoutAiViewModel.defaultForm` setzt `setsPerExercise = 3` explizit (kein Fallback auf Default-Wert nötig)"
    - "`WorkoutAiViewModel.generate()` propagiert `form.setsPerExercise` in den `WorkoutAiForm(...)`-Konstruktor-Aufruf an `generationManager.startWorkoutGeneration()`"
    - "`WorkoutAiUseCase.buildUserMessage()` interpoliert die Zeile `setsPerExercise: ${form.setsPerExercise}` zwischen `exerciseCount` und `splitStyle` (per D-23-02)"
    - "`workout-system-prompt.md` Hard-Rule `targetSets ∈ 3..4` wurde durch `targetSets: use setsPerExercise from user message exactly` ersetzt — damit Range 1–6 nicht vom System-Prompt überschrieben wird"
  artifacts:
    - path: "shared/src/commonMain/kotlin/com/pumpernickel/domain/ai/WorkoutAiPreview.kt"
      provides: "WorkoutAiForm data class mit setsPerExercise"
      contains: "val setsPerExercise: Int"
    - path: "shared/src/commonMain/kotlin/com/pumpernickel/presentation/ai/WorkoutAiViewModel.kt"
      provides: "ViewModel handler + Form-State + defaultForm-Update + generate()-Propagation"
      contains: "fun onSetsPerExerciseChanged"
    - path: "shared/src/commonMain/kotlin/com/pumpernickel/domain/ai/WorkoutAiUseCase.kt"
      provides: "Prompt-Interpolation für setsPerExercise"
      contains: "setsPerExercise:"
    - path: "shared/src/commonMain/resources/workout-system-prompt.md"
      provides: "System-Prompt mit korrigierter targetSets-Hard-Rule"
      contains: "use setsPerExercise from user message"
  key_links:
    - from: "WorkoutAiViewModel.onSetsPerExerciseChanged"
      to: "WorkoutAiUiState.Form.setsPerExercise"
      via: "_uiState.value = current.copy(setsPerExercise = count.coerceIn(1, 6))"
      pattern: "copy\\(setsPerExercise"
    - from: "WorkoutAiViewModel.generate"
      to: "AiGenerationManager.startWorkoutGeneration"
      via: "WorkoutAiForm(...).setsPerExercise propagation"
      pattern: "setsPerExercise = form.setsPerExercise"
    - from: "WorkoutAiUseCase.buildUserMessage"
      to: "LLM prompt body"
      via: "string interpolation"
      pattern: "setsPerExercise:\\s*\\$\\{form.setsPerExercise\\}"
---

<objective>
Shared-KMP-Schicht für Phase 23 Sets-Stepper: `setsPerExercise: Int` (Range 1–6, Default 3) wird in `WorkoutAiForm`, `WorkoutAiUiState.Form`, im ViewModel-Handler und in der Prompt-Interpolation eingeführt. Die Hard-Rule im System-Prompt wird ein-zeilig korrigiert, damit das LLM Werte außerhalb 3..4 nicht überschreibt.

Purpose: Erfüllt D-23-01 / D-23-02 (CONTEXT.md) — die einzige Datengrundlage, die anschließend Android-UI (Plan 02) + iOS-UI (per Plan 03 Handoff) konsumieren.
Output: 4 modifizierte Dateien im `shared`-Modul; commonTest bleibt grün; `compileCommonMainKotlinMetadata` + `compileKotlinIosSimulatorArm64` bauen ohne Fehler.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-ai-ux-und-ios-parit-t-ios-workout-gen-zielmuskel-auswahl-set/23-CONTEXT.md
@.planning/phases/23-ai-ux-und-ios-parit-t-ios-workout-gen-zielmuskel-auswahl-set/23-RESEARCH.md
@.planning/phases/23-ai-ux-und-ios-parit-t-ios-workout-gen-zielmuskel-auswahl-set/23-PATTERNS.md

<interfaces>
<!-- Pre-existing contracts the executor will modify. Verified against codebase 2026-05-19. -->

From shared/src/commonMain/kotlin/com/pumpernickel/domain/ai/WorkoutAiPreview.kt (Zeilen 46-50):
```kotlin
data class WorkoutAiForm(
    val targetMuscles: List<MuscleGroup>,
    val exerciseCount: Int,                  // 3..8 typical
    val splitStyle: WorkoutAiSplit
)
```

From shared/src/commonMain/kotlin/com/pumpernickel/presentation/ai/WorkoutAiViewModel.kt:
- Zeilen 97-101: `private val defaultForm = WorkoutAiUiState.Form(targetMuscles = emptyList(), exerciseCount = 5, splitStyle = WorkoutAiSplit.NONE)`
- Zeilen 183-185: `fun onExerciseCountChanged(count: Int) { val current = _uiState.value as? WorkoutAiUiState.Form ?: return; _uiState.value = current.copy(exerciseCount = count.coerceIn(1, 12)) }`
- Zeilen 197-202: `val started = generationManager.startWorkoutGeneration(WorkoutAiForm(targetMuscles = form.targetMuscles, exerciseCount = form.exerciseCount, splitStyle = form.splitStyle))`
- Zeilen 292-296: `data class Form(val targetMuscles: List<MuscleGroup>, val exerciseCount: Int, val splitStyle: WorkoutAiSplit) : WorkoutAiUiState()`

From shared/src/commonMain/kotlin/com/pumpernickel/domain/ai/WorkoutAiUseCase.kt (Zeilen 165-173 in `buildUserMessage()`):
```kotlin
return """
    targetMuscles: $targets
    exerciseCount: ${form.exerciseCount}
    splitStyle: ${form.splitStyle.name}
    templatesExpected: ${form.splitStyle.templateCount}

    existingExercises:
    $available
""".trimIndent()
```

From shared/src/commonMain/resources/workout-system-prompt.md (Zeile 46):
```
targetSets ∈ 3..4
```
(Diese Zeile MUSS ersetzt werden — siehe Pitfall #5 in RESEARCH.md.)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add setsPerExercise to WorkoutAiForm + WorkoutAiUiState.Form + ViewModel handler + generate() propagation</name>
  <files>shared/src/commonMain/kotlin/com/pumpernickel/domain/ai/WorkoutAiPreview.kt, shared/src/commonMain/kotlin/com/pumpernickel/presentation/ai/WorkoutAiViewModel.kt</files>

  <read_first>
    - shared/src/commonMain/kotlin/com/pumpernickel/domain/ai/WorkoutAiPreview.kt — aktuelle WorkoutAiForm-Definition (Zeilen 46-50)
    - shared/src/commonMain/kotlin/com/pumpernickel/presentation/ai/WorkoutAiViewModel.kt — gesamte Datei lesen für defaultForm (~Z97), onExerciseCountChanged (~Z183), generate() (~Z197), WorkoutAiUiState.Form (~Z292)
    - .planning/phases/23-ai-ux-und-ios-parit-t-ios-workout-gen-zielmuskel-auswahl-set/23-PATTERNS.md — Abschnitt "WorkoutAiPreview.kt" + "WorkoutAiViewModel.kt" (Zeilen 24-115)
    - .planning/phases/23-ai-ux-und-ios-parit-t-ios-workout-gen-zielmuskel-auswahl-set/23-CONTEXT.md — D-23-01 (Range 1–6, Default 3)
  </read_first>

  <behavior>
    - WorkoutAiForm wird mit setsPerExercise: Int = 3 erweitert (Default sichert Source-Kompatibilität für alle Callsites)
    - WorkoutAiUiState.Form bekommt setsPerExercise: Int = 3
    - defaultForm in WorkoutAiViewModel setzt setsPerExercise = 3 explizit (auch wenn Default greifen würde — Lesbarkeit)
    - Neuer Handler onSetsPerExerciseChanged(count: Int): klemmt via count.coerceIn(1, 6) und updated _uiState analog onExerciseCountChanged
    - generate()-Methode propagiert form.setsPerExercise in den neuen WorkoutAiForm()-Konstruktor-Aufruf
  </behavior>

  <action>
**Datei 1 — `shared/src/commonMain/kotlin/com/pumpernickel/domain/ai/WorkoutAiPreview.kt`**

Ändere die `WorkoutAiForm`-Definition (Zeilen 46–50) von:

```kotlin
data class WorkoutAiForm(
    val targetMuscles: List<MuscleGroup>,
    val exerciseCount: Int,                  // 3..8 typical
    val splitStyle: WorkoutAiSplit
)
```

zu:

```kotlin
data class WorkoutAiForm(
    val targetMuscles: List<MuscleGroup>,
    val exerciseCount: Int,                  // 3..8 typical
    val splitStyle: WorkoutAiSplit,
    val setsPerExercise: Int = 3             // D-23-01: Range 1–6, Default 3
)
```

**Datei 2 — `shared/src/commonMain/kotlin/com/pumpernickel/presentation/ai/WorkoutAiViewModel.kt`**

Drei chirurgische Änderungen:

(a) `defaultForm` (~Zeilen 97–101) — ergänze `setsPerExercise = 3`:

```kotlin
private val defaultForm = WorkoutAiUiState.Form(
    targetMuscles = emptyList(),
    exerciseCount = 5,
    splitStyle = WorkoutAiSplit.NONE,
    setsPerExercise = 3
)
```

(b) Neuer Handler `onSetsPerExerciseChanged(count: Int)` — direkt nach `onExerciseCountChanged()` einfügen (~nach Zeile 185):

```kotlin
fun onSetsPerExerciseChanged(count: Int) {
    val current = _uiState.value as? WorkoutAiUiState.Form ?: return
    _uiState.value = current.copy(setsPerExercise = count.coerceIn(1, 6))
}
```

(c) `generate()` (~Zeilen 197–202) — Propagation in `WorkoutAiForm(...)`-Konstruktor:

```kotlin
val started = generationManager.startWorkoutGeneration(
    WorkoutAiForm(
        targetMuscles = form.targetMuscles,
        exerciseCount = form.exerciseCount,
        splitStyle = form.splitStyle,
        setsPerExercise = form.setsPerExercise
    )
)
```

(d) `WorkoutAiUiState.Form` sealed-subclass (~Zeilen 292–296) — Feld ergänzen:

```kotlin
data class Form(
    val targetMuscles: List<MuscleGroup>,
    val exerciseCount: Int,
    val splitStyle: WorkoutAiSplit,
    val setsPerExercise: Int = 3
) : WorkoutAiUiState()
```

**Wenn weitere `copy(...)`-Aufrufe oder Konstruktor-Aufrufe von `WorkoutAiUiState.Form` / `WorkoutAiForm` im File existieren, die alle Felder explizit setzen:** Compile-Error-getrieben den Feldnamen `setsPerExercise` ergänzen (Default `3`). Lass den Compiler die fehlenden Argumente listen — nicht raten, grep nicht erforderlich, da Defaults greifen.

Keine weiteren Imports nötig.
  </action>

  <acceptance_criteria>
    - `grep -n "val setsPerExercise: Int" shared/src/commonMain/kotlin/com/pumpernickel/domain/ai/WorkoutAiPreview.kt` returns at least 1 match
    - `grep -n "val setsPerExercise: Int" shared/src/commonMain/kotlin/com/pumpernickel/presentation/ai/WorkoutAiViewModel.kt` returns at least 1 match (im `Form`-data-class)
    - `grep -n "fun onSetsPerExerciseChanged" shared/src/commonMain/kotlin/com/pumpernickel/presentation/ai/WorkoutAiViewModel.kt` returns 1 match
    - `grep -n "count.coerceIn(1, 6)" shared/src/commonMain/kotlin/com/pumpernickel/presentation/ai/WorkoutAiViewModel.kt` returns 1 match
    - `grep -n "setsPerExercise = 3" shared/src/commonMain/kotlin/com/pumpernickel/presentation/ai/WorkoutAiViewModel.kt` returns at least 1 match (defaultForm)
    - `grep -n "setsPerExercise = form.setsPerExercise" shared/src/commonMain/kotlin/com/pumpernickel/presentation/ai/WorkoutAiViewModel.kt` returns 1 match (generate-Propagation)
    - `./gradlew :shared:compileCommonMainKotlinMetadata` exits 0
  </acceptance_criteria>

  <verify>
    <automated>./gradlew :shared:compileCommonMainKotlinMetadata --no-daemon</automated>
  </verify>

  <done>WorkoutAiForm und WorkoutAiUiState.Form tragen das Feld setsPerExercise; ViewModel klemmt Range 1–6 in onSetsPerExerciseChanged; defaultForm initialisiert mit 3; generate() propagiert in startWorkoutGeneration; commonMain kompiliert grün.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Inject setsPerExercise into WorkoutAiUseCase.buildUserMessage() + fix workout-system-prompt.md targetSets hard-rule</name>
  <files>shared/src/commonMain/kotlin/com/pumpernickel/domain/ai/WorkoutAiUseCase.kt, shared/src/commonMain/resources/workout-system-prompt.md</files>

  <read_first>
    - shared/src/commonMain/kotlin/com/pumpernickel/domain/ai/WorkoutAiUseCase.kt — `buildUserMessage()` Zeilen 156–174 (gesamte Methode lesen, damit Triple-Quote-Block-Position klar ist)
    - shared/src/commonMain/resources/workout-system-prompt.md — vollständig lesen; Zeile mit `targetSets ∈ 3..4` identifizieren (laut RESEARCH.md Zeile ~46)
    - .planning/phases/23-ai-ux-und-ios-parit-t-ios-workout-gen-zielmuskel-auswahl-set/23-PATTERNS.md — Abschnitt "WorkoutAiUseCase.kt" + "System-Prompt-Änderung" (Zeilen 117–149, 577–587)
    - .planning/phases/23-ai-ux-und-ios-parit-t-ios-workout-gen-zielmuskel-auswahl-set/23-RESEARCH.md — Pitfall #5 (Zeilen 378–385)
  </read_first>

  <behavior>
    - Nach Plan-Ausführung steht zwischen `exerciseCount` und `splitStyle` im buildUserMessage-Triple-Quote-Block die Zeile `setsPerExercise: ${form.setsPerExercise}`
    - Im System-Prompt ist die Hard-Rule-Zeile `targetSets ∈ 3..4` durch `targetSets: use setsPerExercise from user message exactly` ersetzt
    - Keine weiteren strukturellen Prompt-Änderungen (D-23-02: "kein Rewrite")
  </behavior>

  <action>
**Datei 1 — `shared/src/commonMain/kotlin/com/pumpernickel/domain/ai/WorkoutAiUseCase.kt`**

In `buildUserMessage()` (~Zeilen 165–173), ersetze:

```kotlin
return """
    targetMuscles: $targets
    exerciseCount: ${form.exerciseCount}
    splitStyle: ${form.splitStyle.name}
    templatesExpected: ${form.splitStyle.templateCount}

    existingExercises:
    $available
""".trimIndent()
```

durch:

```kotlin
return """
    targetMuscles: $targets
    exerciseCount: ${form.exerciseCount}
    setsPerExercise: ${form.setsPerExercise}
    splitStyle: ${form.splitStyle.name}
    templatesExpected: ${form.splitStyle.templateCount}

    existingExercises:
    $available
""".trimIndent()
```

**Position:** `setsPerExercise`-Zeile direkt nach `exerciseCount`, vor `splitStyle` (per D-23-02 / PATTERNS.md Zeile 139–148).

**Datei 2 — `shared/src/commonMain/resources/workout-system-prompt.md`**

Suche die Zeile mit Inhalt `targetSets ∈ 3..4` (laut RESEARCH.md Zeile 46; falls Zeilennummer abweicht, durch exakten String identifizieren — sie ist eindeutig im File).

Ersetze sie durch:

```
targetSets: use setsPerExercise from user message exactly
```

(Whitespace/Indent der Original-Zeile beibehalten.)

Keine weiteren Änderungen am System-Prompt. Wenn die Zeile in einem Bullet-Block steht (`- targetSets ∈ 3..4`), den Bullet-Marker erhalten: `- targetSets: use setsPerExercise from user message exactly`.
  </action>

  <acceptance_criteria>
    - `grep -n "setsPerExercise: \${form.setsPerExercise}" shared/src/commonMain/kotlin/com/pumpernickel/domain/ai/WorkoutAiUseCase.kt` returns 1 match
    - `grep -c "targetSets ∈ 3..4" shared/src/commonMain/resources/workout-system-prompt.md` returns 0 (alte Hard-Rule ist weg)
    - `grep -n "use setsPerExercise from user message exactly" shared/src/commonMain/resources/workout-system-prompt.md` returns 1 match
    - `./gradlew :shared:compileCommonMainKotlinMetadata` exits 0
    - Reihenfolge in `buildUserMessage`: `exerciseCount` steht vor `setsPerExercise` steht vor `splitStyle` — verifizierbar via `grep -n "exerciseCount\|setsPerExercise\|splitStyle:" shared/src/commonMain/kotlin/com/pumpernickel/domain/ai/WorkoutAiUseCase.kt` (Zeilenzahlen aufsteigend)
  </acceptance_criteria>

  <verify>
    <automated>./gradlew :shared:compileCommonMainKotlinMetadata --no-daemon && grep -n "setsPerExercise" shared/src/commonMain/kotlin/com/pumpernickel/domain/ai/WorkoutAiUseCase.kt && grep -c "targetSets ∈" shared/src/commonMain/resources/workout-system-prompt.md</automated>
  </verify>

  <done>buildUserMessage interpoliert setsPerExercise; System-Prompt hat die Hard-Rule-Korrektur; Shared-Modul kompiliert grün.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User UI → ViewModel | User-typed step button presses; bound via Compose callback |
| ViewModel → LLM HTTPS | `setsPerExercise` Int passes via shared client; transport already hardened (Phase 18/22) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | T (Tampering) | `WorkoutAiViewModel.onSetsPerExerciseChanged` — out-of-range values | mitigate | `count.coerceIn(1, 6)` clamps any caller input; no manual text entry path exists in this phase |
| T-23-02 | I (Information Disclosure) | `workout-system-prompt.md` — prompt content | accept | Prompt is application-internal markdown bundled in resources; not user-controlled; no PII included |
| T-23-03 | E (Elevation of Privilege) | LLM ignores `setsPerExercise` from user message and uses hard-rule | mitigate | System-prompt hard-rule changed to `targetSets: use setsPerExercise from user message exactly` (D-23-02 enforcement) |
| T-23-04 | D (Denial of Service) | extreme `setsPerExercise` (e.g. negative or 1e9) | mitigate | Range-clamp `coerceIn(1, 6)` enforces both upper and lower bounds; IconButton-`enabled` UI also gates extremes |
</threat_model>

<verification>
- `./gradlew :shared:compileCommonMainKotlinMetadata` BUILD SUCCESSFUL
- `./gradlew :shared:compileKotlinIosSimulatorArm64` BUILD SUCCESSFUL (sichert KMP-NativeCoroutines-Export für iOS)
- `./gradlew :shared:allTests` weiterhin grün (Plan 23-01 ändert keine Test-Verträge — bestehende Tests müssen unverändert passen)
- Grep-Gates der `<acceptance_criteria>` bestehen für beide Tasks
</verification>

<success_criteria>
- WorkoutAiForm + WorkoutAiUiState.Form + ViewModel-defaultForm enthalten `setsPerExercise` (Default 3)
- `onSetsPerExerciseChanged` klemmt auf 1..6
- `generate()` propagiert `setsPerExercise` an `AiGenerationManager.startWorkoutGeneration`
- `buildUserMessage()` enthält `setsPerExercise:` zwischen `exerciseCount` und `splitStyle`
- `workout-system-prompt.md` Hard-Rule `targetSets ∈ 3..4` ist ersetzt durch `targetSets: use setsPerExercise from user message exactly`
- :shared kompiliert für common + iOS-Simulator
- D-23-01 + D-23-02 vollständig in Shared-Schicht umgesetzt — Android-UI (Plan 02) kann das Feld konsumieren
</success_criteria>

<output>
After completion, create `.planning/phases/23-ai-ux-und-ios-parit-t-ios-workout-gen-zielmuskel-auswahl-set/23-01-SUMMARY.md`
</output>
